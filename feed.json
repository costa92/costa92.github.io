{
    "version": "https://jsonfeed.org/version/1",
    "title": "翼舞成梦",
    "subtitle": "翼舞成梦",
    "icon": "https://costalong.com/images/favicon.ico",
    "description": "翼舞成梦的个人博客，专注于后端开发、K8s 容器化技术、Linux 运维以及人工智能学习笔记分享。",
    "home_page_url": "https://costalong.com",
    "items": [
        {
            "id": "https://costalong.com/2026/06/27/llama/llama-install/",
            "url": "https://costalong.com/2026/06/27/llama/llama-install/",
            "title": "llama 安装",
            "date_published": "2026-06-27T16:34:53.026Z",
            "content_html": "<h2 id=\"安装环境\"><a href=\"#安装环境\" class=\"headerlink\" title=\"安装环境\"></a>安装环境</h2><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">sudo add-apt-repository ppa:deadsnakes/ppa\nsudo apt update\nsudo apt install python3.11 python3.11-venv -y\n</code></pre>\n\n<p>安装虚拟环境</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">/usr/bin/python3.11 -m venv venv</code></pre>\n\n<p>使用虚拟</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">source venv/bin/activate\n# 如果使用的 fish\nsource venv/bin/activate.fish\n</code></pre>\n\n<p>安装 Install PyTorch:</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">pip install --pre torch torchvision --extra-index-url https://download.pytorch.org/whl/nightly/cpu</code></pre>\n\n<h2 id=\"下载\"><a href=\"#下载\" class=\"headerlink\" title=\"下载\"></a>下载</h2><p>1. 下载 llama.cp</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git clone https://github.com/ggerganov/llama.cpp.git </code></pre>\n\n<ol>\n<li>下载通义千问1.5-7B模型 或 1.5-32B 模型 </li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 使用 huggingface 仓库\ngit clone https://huggingface.co/Qwen/Qwen1.5-7B-Chat\n# 或\ngit clone https://huggingface.co/qwen/Qwen1.5-32B-Chat.git\n\n\n# 使用 modelscope 仓库\ngit clone https://www.modelscope.cn/Qwen/Qwen1.5-7B-Chat\n# 或\ngit clone https://www.modelscope.cn/qwen/Qwen1.5-32B-Chat.git</code></pre>\n\n<h2 id=\"编译-llama-cp\"><a href=\"#编译-llama-cp\" class=\"headerlink\" title=\"编译 llama.cp\"></a>编译 llama.cp</h2><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">cd llama.cpp \nmake</code></pre>\n\n<h3 id=\"安装llama-依赖\"><a href=\"#安装llama-依赖\" class=\"headerlink\" title=\"安装llama 依赖\"></a>安装llama 依赖</h3><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">python3 -m  pip install -r requirements.txt</code></pre>\n\n<h3 id=\"转换-Qwen-模型为-GGUF\"><a href=\"#转换-Qwen-模型为-GGUF\" class=\"headerlink\" title=\"转换 Qwen 模型为 GGUF\"></a>转换 Qwen 模型为 GGUF</h3><p>什么是 GGUF? GGUF是一种用于存储用于GGML推断和基于GGML的执行器的模型的文件格式。GGUF是一种二进制格式，旨在快速加载和保存模型，并易于阅读。传统上，模型是使用PyTorch或其他框架开发的，然后转换为GGUF以在GGML中使用。</p>\n<p>GGUF是GGML、GGMF和GGJT的后继文件格式，旨在通过包含加载模型所需的所有信息来消除歧义。它还设计为可扩展的，因此可以向模型添加新信息而不会破坏兼容性，更多信息访问<a href=\"https://github.com/ggerganov/ggml/blob/master/docs/gguf.md\" title=\"官方说明文档\">官方说明文档</a>。</p>\n<p>查看文件结构</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">$ tree -L 1\n.\n├── llama.cpp  \n├── Qwen1.5-32B-Chat\n└── venv</code></pre>\n\n<p>执行这转换命令</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">python3 convert-hf-to-gguf.py  ../Qwen1.5-32B-Chat\n\n=&gt; .....\nINFO:hf-to-gguf:Model successfully exported to '../Qwen1.5-32B-Chat/ggml-model-f16.gguf'\n</code></pre>\n\n<p>最后的结果表示已经转为 F16 的 gguf 格式的模型了</p>\n<h3 id=\"量化模型\"><a href=\"#量化模型\" class=\"headerlink\" title=\"量化模型\"></a>量化模型</h3><p>将gguf 的模型量化到INT4 </p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">./llama-quantize  ../Qwen1.5-32B-Chat/ggml-model-f16.gguf  ./models/qwen1.5-chat-ggml-model-Q4_K_M.gguf Q4_K_M\n=&gt;  ....\n[ 767/ 771]                   blk.63.attn_q.bias - [ 5120,     1,     1,     1], type =    f32, size =    0.020 MB\n[ 768/ 771]                 blk.63.attn_q.weight - [ 5120,  5120,     1,     1], type =    f16, converting to q4_K .. size =    50.00 MiB -&gt;    14.06 MiB\n[ 769/ 771]                   blk.63.attn_v.bias - [ 1024,     1,     1,     1], type =    f32, size =    0.004 MB\n[ 770/ 771]                 blk.63.attn_v.weight - [ 5120,  1024,     1,     1], type =    f16, converting to q6_K .. size =    10.00 MiB -&gt;     4.10 MiB\n[ 771/ 771]                   output_norm.weight - [ 5120,     1,     1,     1], type =    f32, size =    0.020 MB\nllama_model_quantize_internal: model size  = 62014.27 MB\nllama_model_quantize_internal: quant size  = 18780.70 MB\n\nmain: quantize time = 421046.36 ms\nmain:    total time = 421046.36 ms\n\n\n</code></pre>\n\n<p>../Qwen1.5-32B-Chat/ggml-model-f16.gguf   是转换的源文件路径</p>\n<p>./models/qwen1.5-chat-ggml-model-Q4_K_M.gguf   是转换成功生成的文件路径</p>\n<p>Q4_K_M 是量化方法</p>\n<p>新版量化方法包括：</p>\n<ul>\n<li>Q2_K</li>\n<li>Q3_K_S, Q3_K_M, Q3_K_L</li>\n<li>Q4_K_S, Q4_K_M</li>\n<li>Q5_K_S, Q5_K_M</li>\n<li>Q6_K</li>\n</ul>\n<h3 id=\"运行\"><a href=\"#运行\" class=\"headerlink\" title=\"运行\"></a>运行</h3><h3 id=\"运行测试\"><a href=\"#运行测试\" class=\"headerlink\" title=\"运行测试\"></a>运行测试</h3><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">./llama-cli -m ./models/qwen1.5-chat-ggml-model-Q4_K_M.gguf -n 128\n=&gt; \n\n1) 甲乙丙丁四地中，最可能出现水土流失的是\nA. 甲\nB0 乙\nC0 丙\nD0 丁\n答案：C\n关键点：中国分区地理，水土流失及治理，区域农业生产的条件、布局特点、问题\n\n\nllama_print_timings:        load time =   17027.80 ms\nllama_print_timings:      sample time =      22.98 ms /   128 runs   (    0.18 ms per token,  5571.27 tokens per second)\nllama_print_timings: prompt eval time =       0.00 ms /     0 tokens (    -nan ms per token,     -nan tokens per second)\nllama_print_timings:        eval time =  867086.12 ms /   128 runs   ( 6774.11 ms per token,     0.15 tokens per second)\nllama_print_timings:       total time =  867381.32 ms /   128 tokens\nLog end</code></pre>\n\n<p>注意： 这个运行等待的时间比较长</p>\n<p>接下来我们进入对话模型。如何启动呢？我们这里查看 examples/chat.sh的启动方式，来编写启动 Qwen 模型命令。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">./llama-cli -m ./models/qwen1.5-chat-ggml-model-Q4_K_M.gguf  -n 256 --grammar-file grammars/json.gbnf -p 'Request: schedule a call at 8pm; Command:'\n</code></pre>\n\n<h2 id=\"错误\"><a href=\"#错误\" class=\"headerlink\" title=\"错误\"></a>错误</h2><ol>\n<li>通义千问safetensors_rust.SafetensorError: Error while deserializing header: HeaderTooLarge解决方法</li>\n</ol>\n<p>安装 gi-lfs</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">sudo apt-get install git-lfs\n</code></pre>\n\n<p>进入 Qwen1.5-32B-Chat  执行 git lfs pull</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">cd Qwen1.5-32B-Chat &amp;&amp; git lfs pull</code></pre>\n\n",
            "tags": [
                "llama",
                "ggml"
            ]
        },
        {
            "id": "https://costalong.com/2026/06/26/314/",
            "url": "https://costalong.com/2026/06/26/314/",
            "title": "打破 API 围墙：基于 Claude Code 与 Ollama 的混合云 AI 编程助手架构深度解析",
            "date_published": "2026-06-26T16:00:00.000Z",
            "content_html": "<blockquote>\n<p><strong>📄 Abstract：核心摘要</strong><br>本文将从架构师视角深度拆解 <strong>Claude Code</strong> (Anthropic 的下一代 CLI Agent) 与 <strong>Ollama</strong> (本地大模型运行时) 的集成方案。我们将探讨如何通过协议劫持与环境注入，让 Claude Code 运行在本地 <code>Qwen2.5-Coder</code> 或 DeepSeek 模型之上，同时利用 Ollama 的 <code>:cloud</code> 混合云能力实现算力弹性。文章包含完整的通信链路分析、环境配置脚本、模型选型矩阵以及 TCO (总拥有成本) 对比分析。</p>\n</blockquote>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/314_fc2c5dff76576c45023c54a00bf95291.jpg\" alt=\"封面图\"></p>\n<h2 id=\"1-引言：Agent-时代的算力主权\"><a href=\"#1-引言：Agent-时代的算力主权\" class=\"headerlink\" title=\"1. 引言：Agent 时代的算力主权\"></a>1. 引言：Agent 时代的算力主权</h2><p>Claude Code 的发布标志着 AI 编程助手从 “Copilot (副驾驶)” 向 “Agent (智能体)” 的进化。与简单的代码补全不同，Claude Code 具备阅读文件、执行终端命令、管理 Git 版本控制的<strong>自主权</strong>。然而，默认配置下它强依赖 Anthropic 的 SaaS 服务 (Sonnet 3.5)，这对于拥有严格数据隐私合规要求（如金融、军工、核心研发）的企业架构师来说，是一个无法忽视的阻碍。</p>\n<p>Ollama 的出现以及其近期对 OpenAI/Anthropic 协议的兼容，为我们提供了一个完美的 “B 计划”：<strong>Bring Your Own Model (BYOM)</strong>。</p>\n<p>通过将 Claude Code 强大的 Agent 逻辑与 Ollama 灵活的推理后端结合，我们可以构建一个既拥有顶级交互体验，又完全掌握数据主权的 AI 编程系统。更进一步，Ollama 新推出的 <code>:cloud</code> 混合云模式，允许我们在本地算力不足时，无缝切换到云端高性能模型，形成了完美的 “端云协同” 架构。</p>\n<h2 id=\"2-架构原理：协议劫持与接口适配\"><a href=\"#2-架构原理：协议劫持与接口适配\" class=\"headerlink\" title=\"2. 架构原理：协议劫持与接口适配\"></a>2. 架构原理：协议劫持与接口适配</h2><p>要理解这一方案的可行性，首先需要剖析 Claude Code 的通信机制。</p>\n<h3 id=\"2-1-通信链路分析\"><a href=\"#2-1-通信链路分析\" class=\"headerlink\" title=\"2.1 通信链路分析\"></a>2.1 通信链路分析</h3><p>Claude Code 本质上是一个运行在 Node.js 环境中的 CLI 客户端。它通过标准 HTTP 请求与远端推理 API 进行交互。其核心工作流如下：</p>\n<ol>\n<li><strong>System Prompt 注入</strong>：内置了大量的工具定义（读写文件、Shell 执行等）和安全围栏。</li>\n<li><strong>Context Management</strong>：自动管理会话上下文，截断过长的历史记录。</li>\n<li><strong>Tool Execution</strong>：解析 LLM 返回的 Function Call，在本地执行后将结果回传。</li>\n</ol>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/314_f8d265a65ad30cf64e707166ad3e12de.jpg\" alt=\"架构示意图\"></p>\n<h3 id=\"2-2-兼容性层-The-Compatibility-Layer\"><a href=\"#2-2-兼容性层-The-Compatibility-Layer\" class=\"headerlink\" title=\"2.2 兼容性层 (The Compatibility Layer)\"></a>2.2 兼容性层 (The Compatibility Layer)</h3><p>Ollama 在 v0.14+ 版本中引入了对 Anthropic Messages API 的部分兼容性支持，或者更准确地说，是社区通过中间件或 Ollama 原生支持实现了接口对齐。</p>\n<p>当我们配置 <code>ANTHROPIC_BASE_URL</code> 时，我们实际上是在告诉 Claude Code：”不要去访问 <code>api.anthropic.com</code>，去访问我指定的地址”。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 标准请求路径\nhttps://api.anthropic.com/v1/messages\n\n# 重定向后的路径\nhttp://localhost:11434/v1/messages  (Ollama 提供的兼容接口)</code></pre>\n\n<p><strong>架构师视角的挑战：</strong><br>虽然接口通了，但<strong>模型能力</strong>是最大的瓶颈。Claude Code 严重依赖<strong>Function Calling (工具调用)</strong> 能力。如果后端模型（如 Llama 3 8B）无法准确理解 XML 标签或 JSON Schema 的工具定义，Agent 就会陷入 “幻觉循环” 或无法执行任何操作。因此，模型选型至关重要（详见第 4 节）。</p>\n<h2 id=\"3-实战部署：构建本地-Agent-运行时\"><a href=\"#3-实战部署：构建本地-Agent-运行时\" class=\"headerlink\" title=\"3. 实战部署：构建本地 Agent 运行时\"></a>3. 实战部署：构建本地 Agent 运行时</h2><p>本节将详细演示如何在 macOS/Linux 环境下搭建该系统。</p>\n<h3 id=\"3-1-环境准备\"><a href=\"#3-1-环境准备\" class=\"headerlink\" title=\"3.1 环境准备\"></a>3.1 环境准备</h3><p>确保你已安装以下组件：</p>\n<ul>\n<li><strong>Node.js v18+</strong>: 运行 Claude Code。</li>\n<li><strong>Ollama v0.14+</strong>: 必须支持兼容 API。</li>\n<li><strong>Claude Code</strong>: <code>npm install -g @anthropic-ai/claude-code</code></li>\n</ul>\n<h3 id=\"3-2-核心配置-The-Magic-Variables\"><a href=\"#3-2-核心配置-The-Magic-Variables\" class=\"headerlink\" title=\"3.2 核心配置 (The Magic Variables)\"></a>3.2 核心配置 (The Magic Variables)</h3><p>我们需要在 Shell 配置文件（<code>.zshrc</code> 或 <code>.bashrc</code>）中注入环境变量。这里有两种策略：<strong>纯本地模式</strong> 和 <strong>混合云模式</strong>。</p>\n<blockquote>\n<p><strong>💡 Tip：架构师建议</strong><br>建议使用 <code>alias</code> 来区分不同模式，而不是全局污染环境变量，这样你可以灵活切换 “满血版 Claude” (官方 API) 和 “隐私版 Claude” (本地 Ollama)。</p>\n</blockquote>\n<h4 id=\"策略-A：纯本地模式-Privacy-First\"><a href=\"#策略-A：纯本地模式-Privacy-First\" class=\"headerlink\" title=\"策略 A：纯本地模式 (Privacy First)\"></a>策略 A：纯本地模式 (Privacy First)</h4><p>适用于离线环境或极高隐私敏感度场景。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 添加到 ~/.zshrc\nalias claude-local='env \\\n  ANTHROPIC_API_KEY=\"ollama\" \\\n  ANTHROPIC_BASE_URL=\"http://localhost:11434/v1\" \\\n  claude'</code></pre>\n\n<ul>\n<li><code>ANTHROPIC_API_KEY=\"ollama\"</code>: 这是一个占位符，Ollama 本地模式不需要真实 Key，但不传会报错。</li>\n<li><code>ANTHROPIC_BASE_URL</code>: 指向 Ollama 的本地服务端口。</li>\n</ul>\n<h4 id=\"策略-B：Ollama-混合云模式-Hybrid-Power\"><a href=\"#策略-B：Ollama-混合云模式-Hybrid-Power\" class=\"headerlink\" title=\"策略 B：Ollama 混合云模式 (Hybrid Power)\"></a>策略 B：Ollama 混合云模式 (Hybrid Power)</h4><p>Ollama 现在支持 <code>:cloud</code> 后缀的模型，这些模型运行在 Ollama 托管的云端（通常是高性能模型，如 Llama 3.1 405B）。</p>\n<blockquote>\n<p><strong>ℹ️ Info：为什么需要混合云？</strong><br>本地运行 70B 参数以上的模型通常需要 48GB+ VRAM（双路 RTX 3090/4090）。对于普通 MacBook Pro 用户，<code>:cloud</code> 提供了按需访问顶级算力的能力，且无需复杂的 AWS/Azure 部署。</p>\n</blockquote>\n<ol>\n<li>注册 Ollama 账号并获取 Public Key。</li>\n<li>在本地登录：<code>ollama login</code></li>\n<li>配置别名：</li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">alias claude-cloud='env \\\n  ANTHROPIC_API_KEY=\"your-ollama-cloud-key\" \\\n  ANTHROPIC_BASE_URL=\"https://api.ollama.com/v1\" \\\n  claude'</code></pre>\n\n<h3 id=\"3-3-模型启动与验证\"><a href=\"#3-3-模型启动与验证\" class=\"headerlink\" title=\"3.3 模型启动与验证\"></a>3.3 模型启动与验证</h3><p>在运行 Claude Code 之前，你必须确保 Ollama 后台已有模型在运行。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 拉取对代码和工具支持较好的模型\nollama pull qwen2.5-coder:32b\nollama pull deepseek-r1:32b\n\n# 启动 Claude Code (本地模式)\nclaude --model qwen2.5-coder:32b</code></pre>\n\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/314_b5ed60273bfde52e92ec0a7a58c7d8be.jpg\" alt=\"配置流程图\"></p>\n<h2 id=\"4-模型选型矩阵：谁能驾驭-Agent？\"><a href=\"#4-模型选型矩阵：谁能驾驭-Agent？\" class=\"headerlink\" title=\"4. 模型选型矩阵：谁能驾驭 Agent？\"></a>4. 模型选型矩阵：谁能驾驭 Agent？</h2><p>这是架构设计的核心难点。Claude Code 的 System Prompt 非常复杂，包含大量的工具定义。大多数小参数模型（&lt;10B）会直接崩溃或输出乱码。</p>\n<p>以下是基于实测的推荐矩阵（截至 2026 年初）：</p>\n<table>\n<thead>\n<tr>\n<th align=\"left\">模型系列</th>\n<th align=\"left\">推荐版本</th>\n<th align=\"left\">最小 VRAM</th>\n<th align=\"left\">Agent 能力评分</th>\n<th align=\"left\">适用场景</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"left\"><strong>Qwen 2.5 Coder</strong></td>\n<td align=\"left\"><strong>32B</strong></td>\n<td align=\"left\">24GB</td>\n<td align=\"left\">⭐⭐⭐⭐⭐</td>\n<td align=\"left\"><strong>最佳本地替代品</strong>。工具调用极其精准，几乎能达到 Sonnet 3.5 80% 的水平。</td>\n</tr>\n<tr>\n<td align=\"left\"><strong>DeepSeek R1</strong></td>\n<td align=\"left\">Distill Llama 70B</td>\n<td align=\"left\">48GB</td>\n<td align=\"left\">⭐⭐⭐⭐</td>\n<td align=\"left\">逻辑推理极强，但输出格式有时难以被 Claude Code 解析（需 Prompt 引导）。</td>\n</tr>\n<tr>\n<td align=\"left\"><strong>Llama 3.1</strong></td>\n<td align=\"left\">70B</td>\n<td align=\"left\">48GB</td>\n<td align=\"left\">⭐⭐⭐⭐</td>\n<td align=\"left\">稳健，通用性强，但代码特化能力不如 Qwen Coder。</td>\n</tr>\n<tr>\n<td align=\"left\"><strong>Mistral Large</strong></td>\n<td align=\"left\">(Cloud Only)</td>\n<td align=\"left\">-</td>\n<td align=\"left\">⭐⭐⭐⭐</td>\n<td align=\"left\">欧洲合规首选，工具调用能力强。</td>\n</tr>\n<tr>\n<td align=\"left\"><strong>Qwen 2.5 Coder</strong></td>\n<td align=\"left\">7B / 14B</td>\n<td align=\"left\">8GB / 16GB</td>\n<td align=\"left\">⭐⭐</td>\n<td align=\"left\"><strong>不推荐</strong>。仅能处理简单对话，执行复杂文件修改任务时极易出错。</td>\n</tr>\n</tbody></table>\n<blockquote>\n<p><strong>⚠️ Warning：警告：小模型的陷阱</strong><br>千万不要尝试用 <code>llama3.2:3b</code> 或 <code>qwen:7b</code> 去驱动 Claude Code 进行复杂工程任务。它们往往会忽略工具定义的 XML 结构，直接把“我要修改文件”这句话打印出来，而不是真正调用 <code>Edit</code> 工具。这会导致 Agent 卡死。</p>\n</blockquote>\n<h2 id=\"5-性能与-TCO-分析\"><a href=\"#5-性能与-TCO-分析\" class=\"headerlink\" title=\"5. 性能与 TCO 分析\"></a>5. 性能与 TCO 分析</h2><p>对于企业架构师，成本和性能是决策的关键。</p>\n<h3 id=\"5-1-延迟-Latency-对比\"><a href=\"#5-1-延迟-Latency-对比\" class=\"headerlink\" title=\"5.1 延迟 (Latency) 对比\"></a>5.1 延迟 (Latency) 对比</h3><ul>\n<li><strong>Claude Sonnet 3.5 (SaaS)</strong>: 平均首字延迟 (TTFT) 约 0.5s - 1.2s。生成速度极快 (~80 tokens/s)。</li>\n<li><strong>Local Qwen 32B (M2 Ultra)</strong>: TTFT 约 2s - 4s（取决于 Prompt 长度）。生成速度 ~20-30 tokens/s。</li>\n<li><strong>Ollama Cloud</strong>: 介于两者之间，取决于网络状况。</li>\n</ul>\n<p><strong>结论</strong>：本地部署的体验明显慢于 SaaS，但对于代码生成这种“思考型”任务，稍长的等待通常可以接受。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/314_d097a3cd7c09504ac70b9670dcda58d6.jpg\" alt=\"性能对比图\"></p>\n<h3 id=\"5-2-成本-TCO-计算\"><a href=\"#5-2-成本-TCO-计算\" class=\"headerlink\" title=\"5.2 成本 (TCO) 计算\"></a>5.2 成本 (TCO) 计算</h3><p>假设一个 10 人的研发团队，每人每天消耗 500k tokens (输入+输出)。</p>\n<ul>\n<li><p><strong>SaaS 方案 (Anthropic)</strong>:</p>\n<ul>\n<li>Input: $3.00 / M * 0.4 * 10 = $12/day</li>\n<li>Output: $15.00 / M * 0.1 * 10 = $15/day</li>\n<li><strong>年成本</strong>: ~$7,000 (按 260 工作日)</li>\n<li><em>风险</em>: 随使用量线性增长，不可控。</li>\n</ul>\n</li>\n<li><p><strong>本地方案 (Qwen 32B)</strong>:</p>\n<ul>\n<li>硬件: 10 台配备 64GB 统一内存的 Mac mini (M4 Pro) 或 消费级 GPU 工作站。</li>\n<li>一次性投入: ~$20,000。</li>\n<li><strong>年成本</strong>: 电费微乎其微。</li>\n<li><em>优势</em>: 3 年摊销后，成本远低于 SaaS，且资产归公司所有。</li>\n</ul>\n</li>\n</ul>\n<h2 id=\"6-进阶调优：解决常见问题\"><a href=\"#6-进阶调优：解决常见问题\" class=\"headerlink\" title=\"6. 进阶调优：解决常见问题\"></a>6. 进阶调优：解决常见问题</h2><h3 id=\"6-1-上下文窗口溢出-Context-Window\"><a href=\"#6-1-上下文窗口溢出-Context-Window\" class=\"headerlink\" title=\"6.1 上下文窗口溢出 (Context Window)\"></a>6.1 上下文窗口溢出 (Context Window)</h3><p>本地模型通常显存受限。Claude Code 默认假设有 200k context。<br><strong>解决方案</strong>：<br>在启动 Ollama 模型时，显式指定上下文窗口大小（根据你的显存量力而行）：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># Modelfile 配置\nPARAMETER num_ctx 32768</code></pre>\n\n<p>如果显存不足，Ollama 会自动 offload 到 CPU，导致速度骤降（从 30 t/s 降至 2 t/s）。请务必监控 <code>ollama ps</code>。</p>\n<h3 id=\"6-2-“幻觉”-修复\"><a href=\"#6-2-“幻觉”-修复\" class=\"headerlink\" title=\"6.2 “幻觉” 修复\"></a>6.2 “幻觉” 修复</h3><p>如果发现 Agent 总是说 “I will read the file” 但不执行动作。<br><strong>解决方案</strong>：<br>通常是因为 System Prompt 对本地模型来说太复杂。尝试在启动 Claude Code 时通过 <code>--prompt</code> 参数注入更明确的指令，或者（更硬核地）修改 Claude Code 源码中的 Prompt 模板，简化 XML 结构要求。</p>\n<h2 id=\"7-结语：混合-AI-的未来\"><a href=\"#7-结语：混合-AI-的未来\" class=\"headerlink\" title=\"7. 结语：混合 AI 的未来\"></a>7. 结语：混合 AI 的未来</h2><p>通过 Claude Code + Ollama，我们不再是被动的 API 消费者，而是 AI 系统的<strong>架构师</strong>。</p>\n<p>这种架构不仅解决了隐私之痛，更为企业内部的 “Domain-Specific Agent”（领域专用智能体）开辟了道路。你完全可以在企业私有代码库上微调一个 <code>CodeLlama</code>，然后通过 Ollama 挂载给 Claude Code 使用，实现既懂公司业务逻辑，又具备 Agent 执行能力的终极编程助手。</p>\n<p>未来已来，它运行在你的 <code>localhost</code> 上。</p>\n<hr>\n<blockquote>\n<p><strong>✅ Success：下一步行动</strong></p>\n<ol>\n<li>检查你的硬件显存（推荐 24GB+）。</li>\n<li>安装 Ollama 并 pull <code>qwen2.5-coder:32b</code>。</li>\n<li>配置 <code>alias</code> 并开始你的第一次本地 Agent 之旅。</li>\n</ol>\n</blockquote>\n<h2 id=\"参考链接\"><a href=\"#参考链接\" class=\"headerlink\" title=\"参考链接\"></a>参考链接</h2><ul>\n<li><strong>Ollama GitHub</strong>: <a href=\"https://github.com/ollama/ollama\">https://github.com/ollama/ollama</a></li>\n<li><strong>Claude Code CLI</strong>: <a href=\"https://github.com/anthropics/claude-code\">https://github.com/anthropics/claude-code</a></li>\n<li><strong>Qwen 2.5 Coder</strong>: <a href=\"https://huggingface.co/Qwen/Qwen2.5-Coder-32B-Instruct\">https://huggingface.co/Qwen/Qwen2.5-Coder-32B-Instruct</a></li>\n<li><strong>Freedium Mirror</strong>: <a href=\"https://freedium-mirror.cfd/https://medium.com/@joe.njenga/i-tried-new-claude-code-ollama-workflow-its-wild-free-cb7a12b733b5\">https://freedium-mirror.cfd/https://medium.com/@joe.njenga/i-tried-new-claude-code-ollama-workflow-its-wild-free-cb7a12b733b5</a></li>\n</ul>\n",
            "tags": [
                "AI Engineering",
                "Ollama",
                "Claude Code",
                "AI Agent",
                "Local LLM",
                "Software Architecture",
                "DevOps"
            ]
        },
        {
            "id": "https://costalong.com/2026/06/26/moment-2026-06-26-5/",
            "url": "https://costalong.com/2026/06/26/moment-2026-06-26-5/",
            "title": "",
            "date_published": "2026-06-26T04:55:01.000Z",
            "content_html": "<p>雨，总是那么突如其来，让人措手不及！</p>\n",
            "tags": [
                "moments"
            ]
        },
        {
            "id": "https://costalong.com/2026/06/25/313/",
            "url": "https://costalong.com/2026/06/25/313/",
            "title": "vLLM vs Ollama vs llama.cpp：DevOps 怎么选",
            "date_published": "2026-06-25T16:00:00.000Z",
            "content_html": "<h1 id=\"vLLM-vs-Ollama-vs-llama-cpp：DevOps-怎么选\"><a href=\"#vLLM-vs-Ollama-vs-llama-cpp：DevOps-怎么选\" class=\"headerlink\" title=\"vLLM vs Ollama vs llama.cpp：DevOps 怎么选\"></a>vLLM vs Ollama vs llama.cpp：DevOps 怎么选</h1><p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/313_8707ae18a549044a36763c2b108b1877.jpg\" alt=\"三种不同体量的推理引擎并排，象征三种运行时\"></p>\n<p>上周又有同事在群里问：自建大模型服务，到底是上 vLLM 还是 Ollama？要不要试试 llama.cpp？</p>\n<p>这个问题我自己踩过坑——一开始拿 Ollama 怼生产，并发一上来延迟直接飙红；后来全栈换 vLLM，结果 CI 机器上根本没 GPU，编译都跑不起来。折腾完我才明白：<strong>这三个东西压根不是竞品，是一条流水线上的三个工位。</strong></p>\n<p>很多选型文章喜欢搞个评分表，最后给个”综合最强”的冠军。我觉得这思路从一开始就错了。它们各自优化的目标根本不在一个维度上：vLLM 优化的是生产吞吐（一个服务能扛 50+ 并发），Ollama 优化的是开发者上手体验（一条 <code>ollama run</code> 就开干），llama.cpp 优化的是 CPU 效率和可移植性（4.5GB 内存跑 7B）。拿吞吐去比 Ollama、拿易用性去比 vLLM，就像拿货车跟跑车比谁省油——问题本身就问歪了。</p>\n<p>先把结论摆这儿，不绕弯子：本地开发拿 Ollama，CI 和边缘设备用 llama.cpp，生产高并发上 vLLM。模型文件三者基本通用，换运行时不是重写架构，更像换个发动机。</p>\n<p>下面我把三个发动机一个个拆开看，顺便纠正两个流传很广的错误说法。</p>\n<h2 id=\"vLLM：为高并发而生的吞吐怪兽\"><a href=\"#vLLM：为高并发而生的吞吐怪兽\" class=\"headerlink\" title=\"vLLM：为高并发而生的吞吐怪兽\"></a>vLLM：为高并发而生的吞吐怪兽</h2><p>如果你的场景是”一个服务，几十上百号人同时调”，那 vLLM 几乎没有对手。</p>\n<p>它快的核心是 <strong>PagedAttention</strong>。官方文档原话是 “Efficient management of attention key and value memory with PagedAttention”——说白了，传统推理给每个请求的 KV 缓存预留一整块连续显存，请求一多显存就碎成渣、白白浪费。PagedAttention 把 KV 缓存当成操作系统的”虚拟内存页”来管，按需分配，碎片几乎归零。</p>\n<p>显存省下来了，就能塞更多请求一起算，这就是第二招 <strong>连续批处理（continuous batching）</strong>。官方功能列表里写的是 “Continuous batching of incoming requests, chunked prefill, prefix caching”。</p>\n<p>这里值得多说两句，因为它是 vLLM 吞吐高的真正原因。传统的”静态批处理”是一批请求必须凑齐、一起进、一起出——其中一个请求生成的 token 特别长，整批都得等它，GPU 大部分时间在干等。连续批处理则是”谁生成完谁就走，空出来的位置立刻补新请求进来”，GPU 利用率几乎拉满。</p>\n<p>配套的还有两个细节：<strong>prefix caching</strong> 会把相同的前缀（比如一段几百 token 的 system prompt）算一次就缓存住，后面命中的请求直接复用；<strong>chunked prefill</strong> 把长输入切块处理，避免一个超长 prompt 把整批的解码都卡死。这三招叠在一起，才是那个夸张吞吐数字的来源——后面 <code>docker run</code> 起服务时这些默认就开着，你不用调。</p>\n<blockquote>\n<p>这里要纠正一个常见说法：很多文章引用 vLLM “吞吐提升 23 倍”。这个 23x 数字其实来自 Anyscale 的一篇外部博客（讲连续批处理的），不是 vLLM 文档自己的实测，引用时最好把出处带上。</p>\n</blockquote>\n<p>部署上 vLLM 走 Docker 最省事，起来就是一个 OpenAI 兼容的 API：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">docker run --runtime nvidia --gpus all \\\n  -v ~/.cache/huggingface:/root/.cache/huggingface \\\n  -p 8000:8000 \\\n  vllm/vllm-openai:latest \\\n  --model mistralai/Mistral-7B-Instruct-v0.2</code></pre>\n\n<p>起来之后，客户端代码跟调 OpenAI 没区别，迁移成本几乎为零：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">curl http://localhost:8000/v1/chat/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"mistralai/Mistral-7B-Instruct-v0.2\",\n    \"messages\": [{\"role\": \"user\", \"content\": \"你好\"}]\n  }'</code></pre>\n\n<p>官方文档还提到，除了 OpenAI 接口，它同时支持 “Anthropic Messages API and gRPC support”，接入选择很宽。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/313_79b96ffdcb1e9bf9fa3d54c6416eb9b1.png\" alt=\"vLLM 官方文档：PagedAttention 与连续批处理特性\"></p>\n<p>性能数字我引一下源文章的实测（注意：<strong>这是第三方博客数据，我没有独立复现</strong>）：A10G 显卡跑 Mistral 7B、batch 16，吞吐约 1800 tokens/秒，p50 延迟 820ms、p99 1450ms，50+ 并发不掉速。数量级可信，具体数字以你自己的硬件为准。</p>\n<p>最后纠正第二个误区——<strong>vLLM 不是只能用 NVIDIA</strong>。这是流传最广的错误。官方明确写支持 “NVIDIA GPUs, AMD GPUs, and x86/ARM/PowerPC CPUs”，还有 TPU、Apple Silicon 等一堆插件。当然生产主流仍是 N 卡，但”必须 N 卡”这话不成立。</p>\n<p>运维上有几个坑我提前说一下。第一，显存要算清楚——模型权重之外，KV 缓存会随并发和上下文长度涨，<code>--max-model-len</code> 设太大很容易 OOM，上线前务必按峰值并发压一遍。第二，单卡放不下的大模型用张量并行 <code>--tensor-parallel-size 2</code> 拆到多卡，但跨卡通信会吃掉一部分收益，不是卡越多越线性。第三，它是常驻显存的重型服务，别指望像 Ollama 那样用完即走——这是生产服务该有的样子，但也意味着你得为它专门留机器。</p>\n<p>我的判断很直接：<strong>只要是面向多人的线上推理，我一律上 vLLM，不会用别的。</strong> 它上手确实比另外两个陡，但生产环境要的就是吞吐和稳定性，这块它做到了。</p>\n<p>反过来，<strong>单人用、低流量的场景别上 vLLM，纯属杀鸡用牛刀。</strong> 它的所有设计——分页显存、连续批处理——都是为”很多请求挤在一起”准备的。你就一个人偶尔调一下，这些机制全是空转，还得伺候 GPU 和一堆启动参数，性价比极低。这种场景 Ollama 一条命令的事。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/313_e511dbde5841c2447e8161a63a04d216.jpg\" alt=\"KV 缓存从碎片化到分页管理的抽象示意\"></p>\n<h2 id=\"llama-cpp：没有-GPU-也能把模型扛起来\"><a href=\"#llama-cpp：没有-GPU-也能把模型扛起来\" class=\"headerlink\" title=\"llama.cpp：没有 GPU 也能把模型扛起来\"></a>llama.cpp：没有 GPU 也能把模型扛起来</h2><p>vLLM 再强，遇到”机器上没 GPU”就歇菜。这时候 llama.cpp 是我唯一的选择。</p>\n<p>它的定位官方一句话：”LLM inference in C/C++”。纯 C/C++ 写的，没有那堆 Python 依赖，CPU 上就能跑。这让它特别适合两个场景：CI 流水线里跑模型冒烟测试，以及边缘设备/Mac 上做本地推理。</p>\n<p>能在 CPU 上跑大模型，靠的是 <strong>GGUF 量化</strong>。官方支持从 “1.5-bit, 2-bit, 3-bit, 4-bit, 5-bit, 6-bit, and 8-bit” 的整数量化——把原本 FP16 的权重压成低比特整数，体积和内存占用断崖式下降。源文章给的例子是 Mistral 7B 的 Q4_K_M 格式只占 4.5GB 内存（这个具体档位官方页没单列，属 GGUF 常见量化档）。</p>\n<p>量化档位怎么选，是用 llama.cpp 绕不开的决策。规律很简单：<strong>比特越低，体积越小、速度越快，但模型质量损失越大。</strong> 我的经验是 Q4_K_M 是甜点档——7B 模型压到 4-5GB，质量损失肉眼基本看不出，普通笔记本就能跑。要更高质量上 Q5、Q6，机器内存够就 Q8（接近原始精度）；2-bit、3-bit 那种极限压缩除非内存实在紧张，否则别碰，模型会开始说胡话。</p>\n<p>如果机器上有一块不大的显卡，还能用 <code>-ngl</code> 参数把一部分层 offload 到 GPU，剩下的留在 CPU，混合着跑：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 把 20 层放到 GPU，其余在 CPU，适合显存不够装下整个模型的情况\n./build/bin/llama-server -m model.gguf -ngl 20</code></pre>\n\n<p>编译和起服务都很直接：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git clone https://github.com/ggml-org/llama.cpp\ncd llama.cpp\ncmake -B build &amp;&amp; cmake --build build --config Release</code></pre>\n\n<p>llama.cpp 自带一个 OpenAI 兼容的 HTTP 服务，官方描述是 “A lightweight, OpenAI API compatible, HTTP server for serving LLMs”：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 直接从 HuggingFace 拉 GGUF 模型起服务\n./build/bin/llama-server -hf ggml-org/gemma-3-1b-it-GGUF</code></pre>\n\n<p>想跑一次性推理不开服务，用 CLI 就够：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">./build/bin/llama-cli -m my_model.gguf -p \"解释一下什么是量化\"</code></pre>\n\n<p><img src=\"/medias/loading.gif\" data-original=\"https://cdn.jsdelivr.net/gh/costa92/article-images/clawdbot/shot_llamacpp.png\" alt=\"llama.cpp GitHub 仓库：LLM inference in C/C++\"></p>\n<p>性能方面，源文章说现代 CPU 上 15-25 tokens/秒，Apple Silicon（M2 Pro，走 Metal）能到 50 tokens/秒。我得提醒一句：llama.cpp 官方 README 里有 <code>llama-bench</code> 的示例跑分，但<strong>没有针对”现代 CPU / Apple Silicon 通用速度”的官方实测数字</strong>，文中这些 15-25 / 50 tokens/秒 都来自第三方，自己的机器请用 <code>llama-bench</code> 实测。不过 Apple Silicon 的优化是官方实锤的——文档里写 “Apple silicon is a first-class citizen - optimized via ARM NEON, Accelerate and Metal frameworks”。在 Mac 上跑本地模型，它确实是第一梯队。</p>\n<p>放进 CI 的价值我特别想强调。模型上线前最怕”换了个量化版本，结果输出全乱了”这种事。在 CI 里用 llama.cpp 加载目标 GGUF、跑一条固定 prompt、断言输出非空且格式正确，这一步不需要 GPU、几秒钟跑完，能挡住一大半”模型文件本身坏了”的低级事故。我现在的项目都把这步当成模型版本的冒烟测试。</p>\n<p>我的判断：llama.cpp 不追求并发吞吐，它追求的是”在任何破机器上都能把模型跑起来”。CI 没 GPU？边缘盒子只有 ARM？数据敏感不能出本机？这活儿基本只有它和 Ollama 干得了，而要塞进自动化流水线、不带多余依赖，一个 <code>llama-cli -m model.gguf</code> 就能跑的它更纯粹。</p>\n<h2 id=\"Ollama：本地开发，五分钟跑起来\"><a href=\"#Ollama：本地开发，五分钟跑起来\" class=\"headerlink\" title=\"Ollama：本地开发，五分钟跑起来\"></a>Ollama：本地开发，五分钟跑起来</h2><p>如果说 vLLM 是给运维的、llama.cpp 是给嵌入式的，那 Ollama 就是给开发者图省事的。</p>\n<p>我自己日常本地调模型，第一反应永远是 Ollama，因为它把所有麻烦事都藏起来了。一条命令拉模型、起服务、进对话：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">ollama run gemma3</code></pre>\n\n<p>模型从 ollama.com/library 拉，名字一报就行。它现在的明星阵容（官方 About 原话）是 “Kimi-K2.6, GLM-5.1, MiniMax, DeepSeek, gpt-oss, Qwen, Gemma and other models”，主流开源模型基本都在。</p>\n<p>更关键的是它内置 REST API，官方原话 “Ollama has a REST API for running and managing models”，而且带 OpenAI 兼容端点（路径就在 <code>/v1</code>）。本地写的代码，将来切到 vLLM 接口不用动——直接用这个兼容端点，跟前面 vLLM 的请求体长得一模一样：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">curl http://localhost:11434/v1/chat/completions \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"model\": \"gemma3\",\n    \"messages\": [{\"role\": \"user\", \"content\": \"写一个快速排序\"}]\n  }'</code></pre>\n\n<p>想自定义模型行为，用 Modelfile（类似 Dockerfile 的思路）：</p>\n<pre class=\"language-docker\" data-language=\"docker\"><code class=\"language-docker\">FROM gemma3\nPARAMETER temperature 0.7\nSYSTEM \"你是一个简洁的代码助手\"</code></pre>\n\n<p>顺带说个很多人不知道的点：<strong>Ollama 底层其实就是封装了 llama.cpp</strong>。所以它跑的也是 GGUF 量化模型，能在 CPU 上跑、Apple Silicon 上快，都是继承自 llama.cpp 的能力。Ollama 在它之上加了模型管理、自动显存调度、热加载这些”开发者体验糖”——你只管 <code>ollama run</code>，模型拉取、量化档选择、显存分配它全替你办了。</p>\n<p>它还有几个让本地开发特别舒服的小设计：模型默认会在内存里驻留一段时间（<code>keep_alive</code>），短时间内反复调不用重新加载；多个模型可以并存，按需换出。这些对本地反复试验太友好了。</p>\n<p>还有一点对很多团队是刚需——<strong>全程本地、数据不出门</strong>。代码、内部文档这些敏感东西喂给云端 API 总让人心里没底，用 Ollama 在自己机器上跑，数据一个字节都不外传。对合规要求严的场景，这个特性比性能更重要。175k stars 的社区也意味着新模型出来基本第二天就能 <code>ollama pull</code> 到，跟进速度极快。</p>\n<p>至于它的短板，前面说过了，这里再钉一下：它不是为并发设计的。我那次线上事故的根因就是把”单人爽”误当成”能扛量”。记住它的角色——开发期的加速器，到此为止。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"https://cdn.jsdelivr.net/gh/costa92/article-images/clawdbot/shot_ollama.png\" alt=\"Ollama GitHub 仓库：内置 REST 与 OpenAI 兼容 API\"></p>\n<p>截至写稿，Ollama 最新版是 v0.30.10（2026-06-17 发布），仓库 175k stars——社区热度它是三者里最高的。</p>\n<p>但别被热度骗了，<strong>我不推荐拿 Ollama 直接上生产</strong>。它优化的是”上手快、单人爽”，不是高并发吞吐。我那次拿它扛线上流量、并发一冲延迟就崩，就是没认清它的定位。它是开发期的瑞士军刀，不是生产期的发动机。</p>\n<h2 id=\"一张表看清三者差异\"><a href=\"#一张表看清三者差异\" class=\"headerlink\" title=\"一张表看清三者差异\"></a>一张表看清三者差异</h2><p>口说无凭，把关键维度拍成表（其中性能数字来自源文章第三方实测，仅供量级参考）：</p>\n<table>\n<thead>\n<tr>\n<th>维度</th>\n<th>vLLM</th>\n<th>llama.cpp</th>\n<th>Ollama</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>定位</td>\n<td>生产高并发</td>\n<td>CPU/边缘/CI</td>\n<td>本地开发</td>\n</tr>\n<tr>\n<td>硬件</td>\n<td>N卡/A卡/CPU 都支持</td>\n<td>CPU 优先，Apple Silicon 一流</td>\n<td>GPU + CPU</td>\n</tr>\n<tr>\n<td>核心技术</td>\n<td>PagedAttention + 连续批处理</td>\n<td>GGUF 量化</td>\n<td>封装 + 一键体验</td>\n</tr>\n<tr>\n<td>API</td>\n<td>OpenAI / Anthropic / gRPC</td>\n<td>OpenAI 兼容</td>\n<td>REST + OpenAI 兼容</td>\n</tr>\n<tr>\n<td>上手难度</td>\n<td>高（要懂 GPU/K8s）</td>\n<td>中（要编译）</td>\n<td>极低（一条命令）</td>\n</tr>\n<tr>\n<td>并发能力</td>\n<td>极强（50+ 不掉速*）</td>\n<td>弱</td>\n<td>弱</td>\n</tr>\n<tr>\n<td>GitHub stars</td>\n<td>——</td>\n<td>118k</td>\n<td>175k</td>\n</tr>\n</tbody></table>\n<blockquote>\n<p>*并发数字来自源文章实测，未独立复现。</p>\n</blockquote>\n<p>表里我特意把 API 那行标出来，因为这是三者能组成流水线的关键——<strong>它们都提供 OpenAI 兼容接口</strong>。这意味着你的业务代码只认一套接口，改个 <code>OPENAI_BASE_URL</code> 就换了底层运行时，代码一行不动。对 DevOps 来说这是天大的好事：不用为换运行时改业务逻辑，也不会被某一家锁死。</p>\n<p>我自己有个真实的演进路线可以参考。最早做一个内部文档问答，本地用 Ollama 把流程跑通，三天就有了能演示的 demo。要进 CI 做回归测试时，CI 机器没 GPU，换 llama.cpp 加载同一个模型的 GGUF 版本，跑个最小推理验证模型没崩。最后上线给全公司用，并发压力上来了，换 vLLM 部署到 K8s。整个过程模型选型没变，业务代码因为都是 OpenAI 接口几乎没动，每一步只是换了个运行时——这就是把它们当流水线用的实际收益。</p>\n<p>三者最妙的地方是模型可以平滑流转。你在 Ollama 上选定的模型，导出成 GGUF 就能喂给 llama.cpp，HuggingFace 上的原始权重又能直接喂给 vLLM。换运行时，配置改几行就行，比如生产的 K8s 里 vLLM 作为主服务、Ollama 作为开发 sidecar：</p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\"># 开发环境：Ollama 作为轻量 sidecar\ncontainers:\n  - name: ollama\n    image: ollama/ollama:latest\n    ports:\n      - containerPort: 11434</code></pre>\n\n<p>而 CI 里则用 llama.cpp 做一次无 GPU 的推理冒烟测试：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># CI 步骤：无需 GPU，验证模型能正常出 token\n./build/bin/llama-cli -m model.gguf -p \"ping\" -n 16 || exit 1</code></pre>\n\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/313_fda592b966c431c36869eb0bed3f63e2.jpg\" alt=\"从本地开发到 CI 再到生产的三段式流水线抽象图\"></p>\n<h2 id=\"最后：我会怎么选\"><a href=\"#最后：我会怎么选\" class=\"headerlink\" title=\"最后：我会怎么选\"></a>最后：我会怎么选</h2><p>不绕了，直接给场景化推荐：</p>\n<table>\n<thead>\n<tr>\n<th>场景</th>\n<th>我的选择</th>\n<th>原因</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>本地写代码、调 prompt</td>\n<td>Ollama</td>\n<td>一条命令起步，省心</td>\n</tr>\n<tr>\n<td>CI 流水线、边缘设备、无 GPU</td>\n<td>llama.cpp</td>\n<td>纯 C/C++，哪都能跑</td>\n</tr>\n<tr>\n<td>团队共享、生产高并发</td>\n<td>vLLM</td>\n<td>PagedAttention 吞吐碾压</td>\n</tr>\n<tr>\n<td>Mac 上跑本地模型</td>\n<td>llama.cpp</td>\n<td>Apple Silicon 一流优化</td>\n</tr>\n</tbody></table>\n<p>判断逻辑其实可以压成三行：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">有没有高并发生产需求？ 有 → vLLM\n机器上有 GPU 吗？      没有 → llama.cpp\n就想本地快速试试？     是  → Ollama</code></pre>\n\n<p>再补一个很多人忽略的成本维度。vLLM 要常驻 GPU，机器成本最高，但摊到每个请求上反而最便宜——因为它把 GPU 榨得最干（高并发时 <code>nvidia-smi</code> 看显存利用率基本贴满），单 token 成本最低。Ollama 和 llama.cpp 单机轻量、几乎零运维成本，但它们不为吞吐设计，硬要扛大流量等于用算力换便利，反而不划算。所以”省钱”在不同规模下答案是反的：小流量用轻量方案省钱，大流量用 vLLM 才省钱。</p>\n<p>还有人会问：能不能只用一个、全场景通吃？技术上当然行，但我不建议。拿 vLLM 在本地开发，每次改代码都要重启一个吃满显存的重服务，体验很差；拿 Ollama 上生产，并发一冲就现原形。每个工具在它不擅长的场景都会让你难受——这正是”三个都装、按阶段用”比”All in 一个”更省心的原因。</p>\n<p>迁移成本低到什么程度？同一个模型，开发期在 Ollama，上线时换 vLLM，客户端代码因为都是 OpenAI 兼容接口，基本一行不用改：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 开发：指向本地 Ollama\nexport OPENAI_BASE_URL=http://localhost:11434/v1\n# 生产：指向 vLLM，代码不变\nexport OPENAI_BASE_URL=http://vllm-svc:8000/v1</code></pre>\n\n<p>最后说句公道话，这三个工具也有它们都不管的事，别指望换个运行时就万事大吉。<strong>模型本身的质量它们一个都不负责</strong>——选错了基座模型，跑得再快也是快速地输出废话。鉴权、限流（超额了得自己返 <code>429</code>）、可观测性、灰度这些生产必备的东西，三者也都只给你一个裸接口，得你自己在外面套一层。运行时只解决”怎么把模型跑起来、跑多快”，”跑得对不对、稳不稳”还是你的活。</p>\n<p>所以别再纠结”三选一”了。我的建议是三个都装上，按阶段用——这才是把它们当成一个家族的正确姿势。</p>\n<p>把这篇转给那个还在纠结自建大模型用哪个运行时的同事，能省他不少试错时间。</p>\n<p>下一步：先在本机 <code>ollama run</code> 一个小模型跑起来，找回手感，再考虑后面两个。</p>\n",
            "tags": [
                "技术",
                "Ollama",
                "vLLM",
                "llama.cpp",
                "大模型部署",
                "推理运行时",
                "DevOps实战"
            ]
        },
        {
            "id": "https://costalong.com/2026/06/24/311/",
            "url": "https://costalong.com/2026/06/24/311/",
            "title": "claude-code-setup：让 Claude 扫描你的项目，自动给出最优配置方案",
            "date_published": "2026-06-24T16:00:00.000Z",
            "content_html": "<h1 id=\"claude-code-setup：让-Claude-扫描你的项目，自动给出最优配置方案\"><a href=\"#claude-code-setup：让-Claude-扫描你的项目，自动给出最优配置方案\" class=\"headerlink\" title=\"claude-code-setup：让 Claude 扫描你的项目，自动给出最优配置方案\"></a>claude-code-setup：让 Claude 扫描你的项目，自动给出最优配置方案</h1><p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/311_0beabc4479f728a520a26c27d32fde09.jpg\" alt=\"Claude 扫描代码库并生成配置卡片的场景\"></p>\n<p>用了 Claude Code 一段时间之后，你可能遇到过这个问题：明明知道可以配置很多东西，但不知道从哪开始，也不确定什么配置对当前这个项目最有价值。</p>\n<p>钩子、MCP 服务器、子代理、技能包——这些概念散落在文档的不同角落，每个都要单独去研究。加上不同项目的技术栈不一样，”什么配置对我这个项目有用”这个问题没有一个通用的回答。对于刚接触 Claude Code 的人来说，这段摸索的成本比预期高很多。</p>\n<p><strong>claude-code-setup 做的事情很具体</strong>：扫描你的代码库，根据项目实际用到的语言、框架和依赖，给出针对性的配置建议。不是给你一份通用清单，是真的看过你的项目再说话。</p>\n<p>这是 Anthropic 官方出品的插件（<code>claude-plugins-official</code> 市场中的 <code>claude-code-setup</code>），作者是 Anthropic 内部团队，当前版本 1.0.0。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"https://cdn.jsdelivr.net/gh/costa92/article-images/images/screenshot_readme.png\" alt=\"claude-code-setup GitHub README\"></p>\n<h2 id=\"安装只需一条命令\"><a href=\"#安装只需一条命令\" class=\"headerlink\" title=\"安装只需一条命令\"></a>安装只需一条命令</h2><p>Claude Code 启动时会自动加载官方市场，不需要额外添加源。在 Claude Code 中运行：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">/plugin install claude-code-setup@claude-plugins-official</code></pre>\n\n<p>安装后激活插件：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">/reload-plugins</code></pre>\n\n<p>如果提示找不到插件，多半是市场没加载或过期了。先刷新一下市场再重试：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">/plugin marketplace update claude-plugins-official</code></pre>\n\n<p>刷新后还找不到，可能是这台机器从没加过这个市场，手动加一次：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">/plugin marketplace add anthropics/claude-plugins-official</code></pre>\n\n<p>要是插件装上了、技能却死活不出现，通常是缓存坏了——清掉缓存目录，重启 Claude Code 再装一遍就好：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">rm -rf ~/.claude/plugins/cache</code></pre>\n\n<p>我在 MacBook 上测试全程不超过 30 秒。安装支持三种范围：<strong>用户级</strong>（跨所有项目生效）、<strong>项目级</strong>（写入 <code>.claude/settings.json</code>，可以共享给团队协作者）、<strong>本地级</strong>（只在当前仓库生效，不共享）。我一般选用户级，让它在所有项目里都能随时调用。</p>\n<p>如果你习惯用图形界面，也可以在 Claude Code 里运行 <code>/plugin</code> 打开插件管理器，切到 <strong>Discover</strong> 标签页，搜索 <code>claude-code-setup</code> 然后点安装。两种方式结果一样，图形界面里还会提前显示这个插件会加载哪些内容和每轮对话大概增加多少上下文成本（Claude Code 2.1.143 以后版本开始显示这个数字），可以提前评估。</p>\n<p>安装完之后不需要做任何额外配置，切到你的项目目录就能直接用。值得一提的是，这个插件是只读模式——它只分析，不修改任何文件，不写 <code>.claude/settings.json</code>，不动 <code>CLAUDE.md</code>，所有建议都以文本形式输出给你看，装不装完全由你决定。这对于还不太熟悉 Claude Code 配置机制的人来说，安全边界很清晰。</p>\n<p>反过来，装了之后想撤也简单：运行 <code>/plugin</code> 切到 <strong>Installed</strong> 标签页就能启用、停用或卸载，命令行直接 <code>/plugin uninstall claude-code-setup@claude-plugins-official</code> 也行。试错成本很低，不合用随时移除。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/311_2f2988cf78186628506f9f48b4747f4e.png\" alt=\"Claude Code 插件发现页面\"></p>\n<h2 id=\"它扫了什么、推了什么\"><a href=\"#它扫了什么、推了什么\" class=\"headerlink\" title=\"它扫了什么、推了什么\"></a>它扫了什么、推了什么</h2><p>我装好后直接用自然语言触发：</p>\n<pre class=\"language-text\" data-language=\"text\"><code class=\"language-text\">recommend automations for this project</code></pre>\n\n<p>或者更简短的：</p>\n<pre class=\"language-text\" data-language=\"text\"><code class=\"language-text\">help me set up Claude Code</code></pre>\n\n<p>插件会先读取项目结构，分析用到了什么语言、有没有前端框架、依赖里有没有数据库 SDK 或外部 API 客户端，然后给出 5 类推荐，每类最多 2 条。</p>\n<p><strong>MCP 服务器</strong>：外部工具集成，把 Claude 连接到数据库、API、文档系统等。比如项目里有大量调用外部文档的需求，会推 context7（实时查官方文档，避免 Claude 凭记忆讲过时 API）；有前端 UI 测试，会推 Playwright MCP（让 Claude 直接操控浏览器做验证）。</p>\n<p><strong>技能包</strong>：预打包的专项能力，一条命令装好就能用。Plan agent 可以让 Claude 在动手之前先输出完整的修改计划，适合改动涉及多个文件的任务；frontend-design 技能提供了一套针对 UI 层的代码约定和审查标准。</p>\n<p><strong>钩子</strong>：绑定到 Claude 工具事件上的自动动作，不需要每次手动触发。比如每次 Claude 编辑完文件后自动跑格式化，或者在 Claude 试图写入某些敏感路径时主动阻断并提示风险——这类保护在多人协作的项目里特别有用。</p>\n<p><strong>子代理</strong>：专项检查的后台代理，可以和主任务并行跑。安全子代理会在每次改动之后扫描常见漏洞类型；性能子代理关注资源占用；无障碍子代理专门检查前端输出是否符合 WCAG 规范，省掉手动审查的时间。</p>\n<p><strong>斜杠命令</strong>：预定义的快捷工作流，比如 <code>/pr-review</code> 自动审查当前改动的质量和风险，<code>/test</code> 快速生成测试用例，<code>/explain</code> 对当前文件生成解释性注释。这些工作流在不同项目里重复用，打包成命令比每次重新描述需求效率高很多。</p>\n<p>每类推荐最多 2 条是有意为之，不想在你第一次用的时候就堆太多选项。如果想深入某一类，可以针对具体类型再追问，插件会在那个类别里给出更多选项。</p>\n<table>\n<thead>\n<tr>\n<th>类型</th>\n<th>解决的问题</th>\n<th>典型例子</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>MCP 服务器</td>\n<td>Claude 需要访问外部资源</td>\n<td>context7、Playwright</td>\n</tr>\n<tr>\n<td>技能包</td>\n<td>特定任务需要专项能力</td>\n<td>Plan agent、frontend-design</td>\n</tr>\n<tr>\n<td>钩子</td>\n<td>重复动作自动执行</td>\n<td>自动格式化、拦截敏感路径</td>\n</tr>\n<tr>\n<td>子代理</td>\n<td>需要专项检查</td>\n<td>安全、性能、无障碍审查</td>\n</tr>\n<tr>\n<td>斜杠命令</td>\n<td>常用工作流一键触发</td>\n<td>/pr-review、/test</td>\n</tr>\n</tbody></table>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/311_a90a30ee11de8314660a68a63b5d5287.jpg\" alt=\"五类自动化推荐类型示意图\"></p>\n<h2 id=\"实际效果和一个重要前提\"><a href=\"#实际效果和一个重要前提\" class=\"headerlink\" title=\"实际效果和一个重要前提\"></a>实际效果和一个重要前提</h2><p>我在几个不同的项目上试了一遍，推荐结果和项目实际情况的对应关系都挺准的。</p>\n<p>我那个 Next.js 项目里有 Playwright 测试用例，推荐里就真冒出了 Playwright MCP；package.json 里声明了 TypeScript，它就给了 typescript-lsp（代码跳转和类型检查）。我又换到一个 Go 微服务仓库，推荐立马变成了 gopls-lsp 和 git commit 工作流技能。并没有给出什么通用的”适合所有项目”的结果，确实是在看你的代码再说的。</p>\n<p>拿到建议之后，按需安装对应的插件：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 例：安装 context7（官方文档 MCP 服务器）\n/plugin install context7@claude-plugins-official</code></pre>\n\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 例：安装 gopls-lsp（Go 语言服务器）\n/plugin install gopls-lsp@claude-plugins-official</code></pre>\n\n<p><strong>装 LSP 类插件有个前提</strong>：像 gopls-lsp、typescript-lsp 这类代码智能插件，本身只配置 Claude Code 的 LSP 连接，真正干活的语言服务器二进制得你自己装好——gopls-lsp 依赖 <code>gopls</code>，typescript-lsp 依赖 <code>typescript-language-server</code>。装完插件如果在 <code>/plugin</code> 的 Errors 标签页看到 <code>Executable not found in $PATH</code>，就是少了对应二进制，补上即可。装好之后 Claude 每次改完文件会自动拿到类型错误和缺失 import，不用手动跑编译器（按 Ctrl+O 可内联查看诊断）。</p>\n<p><strong>有一点值得说清楚</strong>：claude-code-setup 只给建议，不帮你执行。扫完出一份清单，装不装、装哪个，全由你自己决定。这个设计让你可以逐条评估，也可以直接追问 Claude “为什么推荐这个”来了解背后的判断依据，比直接帮你全装上要透明很多。我试过追问几次，解释通常都很清楚——比如”你的 package.json 里有 @playwright/test，所以推了 Playwright MCP”，而不是模糊地说”因为你有测试需求”。</p>\n<p>不太适合的情况我也得说清楚：如果你已经系统用过 Claude Code 的配置体系，这个插件提供的信息密度有限——推荐范围主要基于已知的常见模式，不会去搜索市场上所有可用插件，也不会针对特别小众的框架给出定制建议。对于想要”分析完直接帮我全装上”的需求，还是得自己再操作一步。</p>\n<p>适合的场景：刚开始用 Claude Code 不知道从哪里配置；接手了别人的项目或换了新的技术栈，想快速了解有没有现成的插件可以用；团队里来了新人，需要给出配置起点，省掉每个人重新摸索的时间。</p>\n<h2 id=\"配置之后的下一步\"><a href=\"#配置之后的下一步\" class=\"headerlink\" title=\"配置之后的下一步\"></a>配置之后的下一步</h2><p>切到你在用的项目目录，对 Claude 说：</p>\n<pre class=\"language-text\" data-language=\"text\"><code class=\"language-text\">what hooks should I use?</code></pre>\n\n<p>看看它对这个项目的具体建议。有用的装进来，不相关的跳过，比从零开始猜配置效率高很多。</p>\n<p>如果你想集中了解某一类，也可以直接问得更具体，比如”给我推荐适合这个项目的 MCP 服务器”或者”有没有合适的安全检查子代理”——插件支持这类聚焦请求，会在单一类别里给出更多选项（3-5 条而不是 2 条上限）。</p>\n<p>对某条推荐不确定要不要装，直接追问”为什么推荐这个”。它会给出具体依据，比如”你的 package.json 里有 @playwright/test，所以推了 Playwright MCP”，而不是模糊地说”因为你有测试需求”。根据这个逻辑判断值不值得装，比盲目试要靠谱很多。</p>\n<p>插件官方文档在 Anthropic 的 <a href=\"https://github.com/anthropics/claude-plugins-official/tree/main/plugins/claude-code-setup\">claude-plugins-official 仓库</a>，README 里有每个推荐类型的解释。更多可用的官方插件可以在 Claude Code 的 <a href=\"https://code.claude.com/docs/en/discover-plugins\">discover-plugins 文档</a> 里按类别浏览——代码智能（各语言 LSP）、外部集成（GitHub、Figma、Slack 等）、自动安全审查（<code>security-guidance</code> 插件）都在里面。官方市场由 Anthropic 策展、是否收录由官方把关，来源比第三方市场可靠；不过官方也明确提醒：插件会以你的权限执行代码，Anthropic 并不保证每个插件都按预期工作，所以装前最好在详情页扫一眼它”会装哪些东西”（v2.1.145 起详情页会列出插件包含的命令、技能、MCP/LSP）再决定。</p>\n<p>配置这种事情，实际跑一遍才知道哪些真正有用。claude-code-setup 省掉的是”我都不知道有这些可以配”的那段时间，先让 Claude 给出一份可以执行的候选清单，再按需决策。</p>\n<blockquote>\n<p>收藏起来，下次换项目配置 Claude Code 时按图索骥。</p>\n</blockquote>\n",
            "tags": [
                "AI工具",
                "Claude Code",
                "实战教程",
                "开发工具",
                "AI插件",
                "自动化配置"
            ]
        },
        {
            "id": "https://costalong.com/2026/06/24/xun-huan-gong-cheng-cong-xie-ti-shi-ci-dao-she-ji-rang-agent-zi-ji-pao-de-xun-huan/",
            "url": "https://costalong.com/2026/06/24/xun-huan-gong-cheng-cong-xie-ti-shi-ci-dao-she-ji-rang-agent-zi-ji-pao-de-xun-huan/",
            "title": "循环工程：从写提示词，到设计让 Agent 自己跑的循环",
            "date_published": "2026-06-24T09:03:58.000Z",
            "content_html": "<h1 id=\"循环工程：从写提示词，到设计让-Agent-自己跑的循环\"><a href=\"#循环工程：从写提示词，到设计让-Agent-自己跑的循环\" class=\"headerlink\" title=\"循环工程：从写提示词，到设计让 Agent 自己跑的循环\"></a>循环工程：从写提示词，到设计让 Agent 自己跑的循环</h1><p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/20_9638448bcd3e50d5dbc2aa08585c968d.jpg\" alt=\"循环工程封面：工程师退后一步，循环自动运行\"></p>\n<p>Anthropic 负责 Claude Code 的 Boris Cherny 说过一句话：「我不再给 Claude 写提示词了。我让一堆循环替我写、替我判断该干什么。」造出最常用编码 Agent 的人都不再手动敲提示词，这事值得认真看。</p>\n<p><strong>一句话讲清楚：循环工程（Loop Engineering）就是把你自己从「按提示词的人」，换成「设计那套按提示词的系统的人」。</strong> Addy Osmani 在 2026 年 6 月 7 日的<a href=\"https://addyosmani.com/blog/loop-engineering/\">这篇博客</a>里把这个说法系统化了——你写一小段程序，可能是个 shell 循环，可能是个定时任务，由它去跟模型对话，而不是你本人。</p>\n<p>Boris Cherny 的完整说法是：「我不再给 Claude 写提示词了。我有一堆循环跑着，替我给 Claude 写提示词、替我想清楚该干什么。」当造出最常用编码 Agent 的那批人都说自己已经停手不再手动敲了，这件事就从小圈子的玩法变成了值得正经对待的范式。</p>\n<p>下面拆三件事：这套范式到底变了什么、一个循环由哪些零件拼成、以及它有哪些你绕不开的代价。</p>\n<h2 id=\"提示词工程为什么走到了头\"><a href=\"#提示词工程为什么走到了头\" class=\"headerlink\" title=\"提示词工程为什么走到了头\"></a>提示词工程为什么走到了头</h2><p>过去两年，从编码 Agent 里拿东西的方式很固定：写个好提示词，喂够上下文，读它的回复，再写下一句。你打一行，看返回，再打下一行。Addy 的判断是，这部分基本结束了。</p>\n<pre class=\"language-text\" data-language=\"text\"><code class=\"language-text\">你:   实现用户登录接口，用 JWT\nAgent: [给出代码]\n你:   加上 refresh token\nAgent: [改]\n你:   测试没过，错误码 401\nAgent: [再改]\n... 你全程在场，一句一句喂</code></pre>\n\n<p>问题不是 Agent 不够强，而是你成了瓶颈——它每迭代一步都要停下来等你拍板。模型一年比一年能扛长任务，可你的注意力没变多。当一个任务要二十轮来回，你就被钉在椅子上当那根串行的导线：复述约定、贴上下文、确认下一步。我自己最烦的就是每开一个新会话，又要把项目的目录结构、命名习惯、测试怎么跑重新讲一遍。</p>\n<p>Peter Steinberger 把替代方案讲得很直接：你不该再给编码 Agent 写提示词了，你该设计那些给 Agent 写提示词的循环。这句话的重量在于，说它的人正是天天用这些工具的人，而不是旁观者。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 最朴素的循环：把\"判断该干什么\"也交出去\n# pick_next_task 是你自己写的挑活函数（从 issue / CI / backlog 里取一件）\nwhile true; do\n  task=$(pick_next_task)\n  claude -p \"完成这件事：$task\"      # 让 Agent 干\n  sleep 300\ndone</code></pre>\n\n<p>这段循环里，你已经不在每一步出现了。Claude Code 干脆把这件事做成了内建命令。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">/loop 30m 检查 CI 失败并尝试修复    # 每 30 分钟跑一轮\n/goal 把测试覆盖率提到 80%           # 给一个目标，自己迭代到达成</code></pre>\n\n<p>两个命令的差别值得停一下：<code>/loop</code> 给的是节奏（每隔多久跑一次），<code>/goal</code> 给的是终点（达成某个条件才停）。前者适合”持续盯着某类问题”，后者适合”一件有明确完成标准的事”。</p>\n<p>Addy 把循环定义成「一个递归的目标：你定义一个目的，AI 反复迭代直到完成」。活没变轻松，变的是杠杆点的位置——从”敲提示词”挪到了”设计系统”。提示词工程没有消失，它被降级成了循环内部的一个零件：你仍然要写好那段触发提示词，只是不再亲手把它喂进去。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/20_a6a3a6cfbf218d979fd0d6bdf4480c6d.png\" alt=\"Addy Osmani《Loop Engineering》原文（2026-06-07）\"></p>\n<h2 id=\"循环的骨架：自动化、worktree-和-skill\"><a href=\"#循环的骨架：自动化、worktree-和-skill\" class=\"headerlink\" title=\"循环的骨架：自动化、worktree 和 skill\"></a>循环的骨架：自动化、worktree 和 skill</h2><p>一个像样的循环，Addy 拆成五个构件外加一层记忆。先看撑起骨架的三个，加上那层缺不得的记忆。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/20_fd599dd8d4da4474b656289d9d84280b.jpg\" alt=\"循环五构件加记忆层结构图\"></p>\n<p><strong>自动化是循环的心跳。</strong> 它在固定节奏上做发现和分诊——定时触发、git 事件触发、或者 CI 信号触发。Addy 强调过：正是这个会自己醒来的触发器，才让一个循环真的成其为循环，而不是一次手动运行。没有心跳，你写得再漂亮的 prompt 也只是个一次性脚本。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 自动化 = 循环的心跳：定时触发发现和分诊\n# crontab：每天早上 9 点跑一次\n0 9 * * * cd /repo &amp;&amp; claude -p \"$(cat triage-prompt.md)\" &gt;&gt; loop.log</code></pre>\n\n<p><strong>worktree 解决并行打架。</strong> 同时跑多个 Agent 时，最怕它们改同一批文件互相覆盖。<code>git worktree</code> 给每个任务一份隔离的工作目录。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 每个并行任务一个独立 worktree，互不踩踏\ngit worktree add ../fix-issue-142 -b fix/issue-142\ngit worktree add ../fix-issue-153 -b fix/issue-153\ngit worktree list</code></pre>\n\n<p><strong>skill 把项目知识固化下来。</strong> Addy 有个说法叫”意图债”——你每次开新会话重新解释一遍约定，就是在反复还这笔债。skill 是一次性还清的方式：一个 <code>SKILL.md</code> 就是一段会被反复调用的说明书，外加可选的脚本和参考文件，让循环每次跑都不必重新教一遍。</p>\n<pre class=\"language-markdown\" data-language=\"markdown\"><code class=\"language-markdown\">---\nname: triage\ndescription: 读取昨天的 CI 失败和新开 issue，分类并写入待办\n---\n# 分诊流程\n1. 拉取过去 24h 的 CI 失败记录\n2. 按「能自动修 / 需人介入」分两类\n3. 把结论写进 backlog.md</code></pre>\n\n<p>还有一层不算构件、却缺不得的东西：外部记忆。对话窗口会断、会被压缩，进度不能跟着断——循环把状态写进一个 markdown 文件或 Linear 看板，下一轮醒来先读它，知道昨天干到哪、哪些还没碰。这层记忆活在对话之外，所以换模型、清上下文都不影响它。</p>\n<pre class=\"language-markdown\" data-language=\"markdown\"><code class=\"language-markdown\"># loop-state.md（循环的记忆）\n### 进行中\n- [ ] #142 修复登录 401（worktree: fix-issue-142）\n### 已完成\n- [x] #138 补全 README 安装步骤</code></pre>\n\n<h2 id=\"让循环能动手、会自查：连接器和子-Agent\"><a href=\"#让循环能动手、会自查：连接器和子-Agent\" class=\"headerlink\" title=\"让循环能动手、会自查：连接器和子 Agent\"></a>让循环能动手、会自查：连接器和子 Agent</h2><p>剩下两个构件最关键——一个让循环碰得到真实世界，一个让它别自己批改自己的作业。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/20_d8e8188b3eee335e3eaf88a67990478f.jpg\" alt=\"写代码的 Agent 与审查的 Agent 互相校验的循环\"></p>\n<p><strong>只能读文件系统的循环，只能提建议。</strong> 要让它真正动手——开 PR、更新工单、发 Slack、查数据库——靠的是 MCP（Model Context Protocol）连接器。按 Anthropic 的定义，MCP 是「连接 AI Agent 与外部系统的开放标准」。</p>\n<pre class=\"language-json\" data-language=\"json\"><code class=\"language-json\">{\n  \"mcpServers\": {\n    \"github\": { \"command\": \"npx\", \"args\": [\"-y\", \"@modelcontextprotocol/server-github\"] },\n    \"linear\": { \"command\": \"npx\", \"args\": [\"-y\", \"mcp-linear\"] }\n  }\n}</code></pre>\n\n<p>MCP 自 2024 年 11 月发布以来铺得很快，按 Anthropic 的说法，社区已经造了上千个 MCP 服务器，主流语言都有 SDK，行业把它当成了事实标准。一个 Agent 实现一次 MCP，就接上了一整个工具生态。</p>\n<p>Anthropic 给的一个数字很能说明问题：用代码方式调用 MCP 工具，能把一次任务的 token 从 15 万压到 2000，省掉 98.7%（见 <a href=\"https://www.anthropic.com/engineering/code-execution-with-mcp\">code execution with MCP</a>）。这对长期跑着的循环不是小数——省下的是真金白银。Codex 和 Claude Code 都讲 MCP，你为一个写的连接器，换到另一个上大多直接能用，不用为换工具重写一遍集成。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># claude mcp 只管配置：list 看已接入的 server\nclaude mcp list\n# github / linear ... 具体工具（open_pr、create_issue）由模型在会话内调用</code></pre>\n\n<p><strong>子 Agent 把”写”和”查”分开。</strong> 这是循环里最有用的一个结构性动作。Addy 的原话是：写代码的那个模型，给自己的作业打分时太手软了。让同一个 Agent 既写又审，它会本能地替自己辩护。第二个 Agent 换一套指令、有时换一个模型、调高推理档位，专门挑刺，就能抓住第一个说服自己糊弄过去的东西。</p>\n<p>更细一点的分工是按角色拆：一个负责探索（读代码、搞清现状），一个负责动手（写改动），一个负责验证（只找问题）。每个角色给不同的指令，甚至不同的模型，谁也别替谁背书。</p>\n<pre class=\"language-toml\" data-language=\"toml\"><code class=\"language-toml\"># .codex/agents/reviewer.toml —— 一个只负责挑刺的子 Agent\nname = \"reviewer\"\nmodel = \"gpt-5-codex\"\ninstructions = \"\"\"\n你是代码审查员。只找问题，不写新功能。\n重点看：边界条件、错误处理、有没有测试。\n\"\"\"</code></pre>\n\n<p>我自己把审查交给第二个 Agent 之后，第一个 Agent 那些”自我感觉良好”的改动才露馅——它会把一个没处理空值的函数写得理直气壮。社区已经有人把这类现成模式整理成了工具集（<a href=\"https://github.com/cobusgreyling/loop-engineering\">loop-engineering</a>，含 loop-audit / loop-init / loop-cost）。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 让审查子 Agent 过一遍刚写的改动\nclaude --agent reviewer -p \"审查 fix/issue-142 分支的 diff\"</code></pre>\n\n<h2 id=\"一天的循环长什么样\"><a href=\"#一天的循环长什么样\" class=\"headerlink\" title=\"一天的循环长什么样\"></a>一天的循环长什么样</h2><p>五个零件单看都不稀奇，拼起来才有意思。Addy 给的样板是一个每天早上在仓库上跑的自动化，我把它顺着走一遍：闹钟一响，自动化醒来，它的 prompt 调一个分诊 skill，读昨天的 CI 失败、新开 issue、最近的 commit，把结论写进一个 markdown 文件或 Linear 看板。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/20_6d80896526f99155cc4e5fcb17e9537e.jpg\" alt=\"一天的循环流水线：触发到开PR\"></p>\n<p>心跳可以是定时器，也可以挂在 CI 事件上。用 GitHub Actions 当触发器，循环就跟着仓库的节奏走。</p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\"># .github/workflows/morning-loop.yml —— 用 CI 事件当心跳\non:\n  schedule:\n    - cron: \"0 1 * * *\"   # 每天 UTC 01:00\njobs:\n  loop:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: claude -p \"$(cat .loop/triage.md)\"</code></pre>\n\n<p>这个状态文件就是循环的工作记忆。它读昨天的烂摊子，写下今天要处理的清单，下一轮自己再接上。整套流程里最值得玩味的，是它把”发现工作”也自动化了——你不再需要每天早上自己翻 issue 列表决定先修哪个。</p>\n<p>分诊完，循环对着每件待办开隔离 worktree、让 fixer 子 Agent 起草修复、让 reviewer 子 Agent 对着项目 skill 审一遍，最后连接器开 PR、更新工单。每件事在自己的 worktree 里跑，互不干扰，跑挂了也只脏一个分支。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 分诊 → 隔离 → 修 → 审 → 开 PR，全程无人值守\n# read_triaged 是你自己的解析函数，从 backlog.md 读出已分诊的 issue 号\nfor issue in $(read_triaged backlog.md); do\n  git worktree add \"../wt-$issue\" -b \"fix/$issue\"\n  ( cd \"../wt-$issue\" \\\n      &amp;&amp; claude --agent fixer    -p \"修复 $issue\" \\\n      &amp;&amp; claude --agent reviewer -p \"审查这次改动\" \\\n      &amp;&amp; gh pr create --fill --label \"loop-generated\" --draft )  # 连接器/gh 开 PR\ndone</code></pre>\n\n<p>跑完一轮，状态文件被改写成次日早上你能一眼看懂的样子——哪些进了 PR、哪些被标记成需要人介入。</p>\n<pre class=\"language-markdown\" data-language=\"markdown\"><code class=\"language-markdown\"># loop-state.md（次日早上自动更新）\n### 今早处理\n- [x] #142 → PR #210 已开，待人审\n- [ ] #155 改动过大，已标记需人介入</code></pre>\n\n<p>你早上真正要做的，是看循环昨晚干了什么、决定哪些值得合并。这一步永远在你手上。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 你的活：看循环昨晚干了什么，决定哪些合并\ngh pr list --label loop-generated\ngit log --oneline fix/issue-142</code></pre>\n\n<h2 id=\"第一个循环该怎么起步\"><a href=\"#第一个循环该怎么起步\" class=\"headerlink\" title=\"第一个循环该怎么起步\"></a>第一个循环该怎么起步</h2><p>看完五个构件，别急着上 GitHub Actions 全自动。循环最稳的起法是先做小、先做得能随时叫停，让它在你看得见的地方把行为暴露出来。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 第一步：只让它分诊和提建议，先别动代码\nclaude -p \"读 backlog.md，列出今天该修的 3 件事，但不要改任何文件\"</code></pre>\n\n<p>先看它挑的活靠不靠谱，确认判断力没问题，再给它动手的权限。起步阶段我推荐用 <code>/goal</code> 而不是 <code>/loop</code>，因为前者有明确终点、错了有边界，后者一旦放久最容易在你没盯着的时候跑偏。下一步是把作用域钉死，别让它碰整个仓库。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 用 label 把循环的作用域圈住，它只碰带这个标签的 issue\ngh issue list --label \"loop-safe\" --json number,title</code></pre>\n\n<p>挑一类边界清楚的活先试——补测试、修 lint、更新文档，这些错了也好回滚。等行为稳定，再加一道人审闸门：循环只开 PR，合并永远留给你。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 循环只开草稿 PR，是否合并由人决定\nclaude --agent fixer -p \"修复 #142\" &amp;&amp; \\\n  gh pr create --label \"loop-generated\" --draft</code></pre>\n\n<p>draft 加人审，循环再勤快也越不过你这一关。手动跑顺几天、确认它不闯祸，才值得把它升级成定时循环。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 跑稳几天后，才挂上定时器变成真正的循环\ncrontab -l | grep loop || \\\n  echo \"0 9 * * * cd /repo &amp;&amp; ./run-loop.sh\" | crontab -</code></pre>\n\n<p>Addy 那套每天早上自动跑的循环，是终点而不是起点。一上来就全自动，等于把一个你还没摸清脾气的系统放出去替你改代码。</p>\n<h2 id=\"循环的三个局限，每个都得你扛\"><a href=\"#循环的三个局限，每个都得你扛\" class=\"headerlink\" title=\"循环的三个局限，每个都得你扛\"></a>循环的三个局限，每个都得你扛</h2><p>循环替你做不了的事，恰恰是最要紧的那几件。Addy 把它们说得很白。</p>\n<p><strong>验证还是你的活。</strong> 原话是：无人值守跑着的循环，也是无人值守犯错的循环。循环开的 PR 别盲合，至少过一道自动门。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 循环开的 PR 别盲合，至少过一道自动门\ngh pr checks 210                          # CI 全绿？\ngit diff main...fix/issue-142 | wc -l     # 改了多少行，心里有数</code></pre>\n\n<p><strong>理解会欠债。</strong> 循环越快地交付你没写的代码，「存在的东西」和「你真正搞懂的东西」之间的缺口就越大。我自己的体会是：连着合并几个循环生成的 PR 后，再去 debug 一个线上问题，会发现有些模块根本没读过，得现学自己仓库里的代码。这笔债平时不响，出事那天利滚利。对付它没有巧办法，只能定期逼自己回读循环写的关键路径。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 对循环生成的代码，强制自己回读关键路径\ngit log --author=\"loop\" --since=\"1 week\" --stat</code></pre>\n\n<p><strong>判断会交出去，钱也会烧掉。</strong> Addy 说，循环自己跑起来后，很容易就不再有自己的看法，它给什么你就收什么——这是比代码出错更隐蔽的塌方，因为你连”该不该这么做”都不再问了。再加上一句他反复强调的话：你绝对得小心 token 成本。一个无人盯着的循环，半夜可以替你把一个月的预算烧光，所以单轮要设上限，每天要回头看花了多少。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 给循环装个成本刹车：每轮设美元上限，超了自动停\nclaude -p --max-budget-usd 5.00 \"$(cat .loop/triage.md)\"\n# 会话里随时用 /usage 看本轮花了多少；月度封顶在 Claude Console 设 workspace spend limit</code></pre>\n\n<h2 id=\"总结：把循环建起来，但人得留在工程师的位置上\"><a href=\"#总结：把循环建起来，但人得留在工程师的位置上\" class=\"headerlink\" title=\"总结：把循环建起来，但人得留在工程师的位置上\"></a>总结：把循环建起来，但人得留在工程师的位置上</h2><p>Addy 的收尾很准——把循环建起来，但要像一个打算继续当工程师的人那样去建。循环把杠杆点从”敲提示词”挪到了”设计系统”，可验证、犯错、判断该不该合并这些事，没有一样能交出去。</p>\n<p>循环适合边界清楚、能自动验证的活：修 CI、补测试、跑迁移、清 issue 队列。不适合需求还没想清、或者错一次代价很大的改动——那种你还是得一句一句盯着。想往深里读，Addy 的<a href=\"https://addyosmani.com/blog/loop-engineering/\">原文</a>和 Anthropic 那篇<a href=\"https://www.anthropic.com/engineering/code-execution-with-mcp\">用代码调用 MCP</a>都值得看。</p>\n<blockquote>\n<p>觉得有用，<strong>点个「在看」</strong>，让还在一句句敲提示词的同行也看到。</p>\n</blockquote>\n",
            "tags": [
                "AI工程",
                "Claude Code",
                "Loop Engineering",
                "循环工程",
                "AI编程",
                "软件研发"
            ]
        },
        {
            "id": "https://costalong.com/2026/06/22/16gb-bie-peng-24gb-cou-he-32gb-cai-shi-sheng-chan-li-ben-di-bian-cheng-llm-de-2026-zhen-xiang/",
            "url": "https://costalong.com/2026/06/22/16gb-bie-peng-24gb-cou-he-32gb-cai-shi-sheng-chan-li-ben-di-bian-cheng-llm-de-2026-zhen-xiang/",
            "title": "16GB 别碰、24GB 凑合、32GB+ 才是生产力：本地编程 LLM 的 2026 真相",
            "date_published": "2026-06-22T06:56:20.000Z",
            "content_html": "<h1 id=\"16GB-别碰、24GB-凑合、32GB-才是生产力：本地编程-LLM-的-2026-真相\"><a href=\"#16GB-别碰、24GB-凑合、32GB-才是生产力：本地编程-LLM-的-2026-真相\" class=\"headerlink\" title=\"16GB 别碰、24GB 凑合、32GB+ 才是生产力：本地编程 LLM 的 2026 真相\"></a>16GB 别碰、24GB 凑合、32GB+ 才是生产力：本地编程 LLM 的 2026 真相</h1><p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/19_39102f06a7b410b3a8daafcd40c907e6.jpg\" alt=\"一台笔记本电脑本地运行代码模型，旁边三档内存分级的概念封面\"></p>\n<p>先说结论，省得你往下翻：2026 年能在自己电脑上认真写代码的本地模型，只有一个——<strong>Qwen3.6-27B 跑 Q4_K_M 量化</strong>。不是之一，是唯一。</p>\n<p>这话不是我拍脑袋。一位叫 Andrus 的开发者写了篇<a href=\"https://freedium-mirror.cfd/https://ai.gopubby.com/i-went-looking-for-the-best-local-llm-for-coding-in-2026-7fa97efb34f1\">本地编程模型实测</a>，把手头三个真实任务甩给四款模型跑了一遍。我读完后把里面的数据跟我自己这两年折腾本地模型的判断捋了一遍，写成这篇。数据归他，态度归我。</p>\n<p>本地大模型这条路，我看着它坑了所有人好几年。要么是小模型——能装进内存，但写出来的代码是垃圾；要么是大模型——聪明是聪明，可在 MacBook 上每秒吐 4 个字，等它写完一个函数你咖啡都凉了。两头不靠岸，这就是过去几年本地编程的真实处境。直到 2026 年 4 月这一拨模型出来，局面才第一次变了。</p>\n<p><strong>一句话的选型光谱</strong>：16GB 内存别碰本地编程模型，纯属浪费时间；24GB 能跑但要忍；32GB 起步，本地模型才从”玩具”变成”干活的工具”。下面我把怎么测的、为什么是它、你该怎么配，一条条说清楚。</p>\n<h2 id=\"三个真实任务，四款模型现原形\"><a href=\"#三个真实任务，四款模型现原形\" class=\"headerlink\" title=\"三个真实任务，四款模型现原形\"></a>三个真实任务，四款模型现原形</h2><p>跑分榜我一向不太信——刷榜和干活是两码事。Andrus 这次的测法我服：他没用合成基准，直接从自己的待办里拎出三个真实任务，全是会出现在你我 PR 里的活儿。</p>\n<pre class=\"language-text\" data-language=\"text\"><code class=\"language-text\">任务一：跨 4 个 Laravel 文件，重构一个支付通知服务\n任务二：调一条三表 join、莫名返回重复行的 SQL\n任务三：给一个完全没测试的模块补单元测试</code></pre>\n\n<p>复现环境很简单，一条命令拉模型就能跟着测：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 拉 Qwen3.6-27B 的 Q4_K_M 量化版，文件 16.8GB\n# 占 ~17GB 磁盘，按带宽算下载时间，断了能续传\nollama pull qwen3.6:27b\n\n# 直接对话跑\nollama run qwen3.6:27b</code></pre>\n\n<p>先说主角 <strong>Qwen3.6-27B</strong> 的真实表现，三个任务三种脸色。重构那道，它第一次漏了一个调用点，我指出来之后第二次才补全——Andrus 的原话是”正是那种让你忍不住全程复查的差一点”。SQL 那道最惊艳，它一眼看穿是 join 条件导致行重复，给的解释比作者自己原来的方案还清楚。单测那道则暴露了短板：代码能编译、能跑，但只覆盖 happy path，明明让它写失败用例，时区边界还是漏了。</p>\n<p>四款模型的真实表现，我整理成一张表：</p>\n<table>\n<thead>\n<tr>\n<th>模型</th>\n<th>参数/架构</th>\n<th>SWE-bench Verified</th>\n<th>真实任务表现</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Qwen3.6-27B</strong></td>\n<td>27B 稠密</td>\n<td><strong>77.2%</strong></td>\n<td>重构第二次过；SQL 一眼看穿重复行根因；单测只覆盖 happy path</td>\n</tr>\n<tr>\n<td>Qwen3.6-35B-A3B</td>\n<td>35B/3B 激活 MoE</td>\n<td>—</td>\n<td>更快但更莽，语法改对了却踩坏两文件外的类型契约</td>\n</tr>\n<tr>\n<td>Devstral Small 2</td>\n<td>24B agentic</td>\n<td>68.0%</td>\n<td>agent 循环里能读报错改对文件，纯对话就一般</td>\n</tr>\n<tr>\n<td>Codestral</td>\n<td>补全专用</td>\n<td>—</td>\n<td>本地补全能用，但跨文件感知不靠谱，作者两天后关了</td>\n</tr>\n</tbody></table>\n<p>Qwen3.6-27B 的 <a href=\"https://www.swebench.com/\">SWE-bench Verified</a> 拿到 <strong>77.2%</strong>，距 Claude Opus 4.6 的 80.8% 只差 3.6 分。这是什么概念？一个能塞进笔记本、Apache 2.0 授权、没人能涨价没人能封号的开源模型，干到了一线云端模型不久前坐的位置。它原生支持 256K 上下文、能吃文本图像视频，配 vLLM、SGLang、KTransformers 这些框架都顺。我敢说，这是第一个让”本地写代码”不再是行为艺术的模型。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/19_1d8a84cfae6ecaf91f2d9fffa8086797.png\" alt=\"Qwen3.6-27B 在 Hugging Face 的模型卡：Apache 2.0 授权、月下载近 600 万\"></p>\n<p>剩下三款各有各的定位，别指望一个模型通吃。<strong>Devstral Small 2</strong>（24B、Apache 2.0）是个 agentic 选手，跟 All Hands AI 一起调出来的，在 edit-test-fix 这种 agent 循环里它能可靠地读懂报错、动对的那个文件，而不是动你”刚才提过的”那个文件——这点很关键，但你纯拿它聊天就没那么灵。<strong>Codestral</strong> 是补全专用，native FIM、号称 HumanEval 86.6%，接进 Continue 当本地 Copilot 体验”大概落后云端 Copilot 一年”，跨文件感知不靠谱，Andrus 嫌它费电、有延迟，两天就关了。</p>\n<p>那个 MoE 变体 <strong>Qwen3.6-35B-A3B</strong> 我得单独多说两句。35B 总参但每个 token 只激活 3B。据 Simon Willison 公开实测（二手转引），稠密版在 MacBook 上约 25.57 tokens/s；而 MoE 版社区实测能冲到 35–55 tokens/s——这两个数都不是我亲测的，照搬来给你个量级感。快是真快。但 Andrus 那句吐槽精准：<strong>“错得更快，也改得更快”</strong>——同一个重构任务它输出得漂漂亮亮，却违反了两个文件之外的类型契约；你把报错丢回去，它又秒改。我的判断很简单：MoE 适合探索性瞎试，不适合你打算直接 commit 的活儿。提速的代价是少了那份谨慎，这笔账你自己算。</p>\n<h2 id=\"瓶颈从来不是算力，是内存带宽\"><a href=\"#瓶颈从来不是算力，是内存带宽\" class=\"headerlink\" title=\"瓶颈从来不是算力，是内存带宽\"></a>瓶颈从来不是算力，是内存带宽</h2><p>这是最反直觉的一点，也是为什么过去几年本地大模型一直”能装下但跑不动”。很多人以为本地推理慢是显卡不够猛，错了。</p>\n<p>本地生成 token 的速度，瓶颈不在算力，在内存带宽。Apple Silicon 的统一内存带宽约 270GB/s，而一张 RTX 3090 是 936GB/s——差了三倍多。所以同一个模型，塞进显卡和塞进 Mac，吐字速度完全是两回事。这也解释了为什么以前的大稠密模型在 Mac 上慢成 PPT：每生成一个 token 都要把全部参数从内存里捞一遍，带宽就是天花板。</p>\n<p>MoE 架构这次真正解决的就是这件事——每个 token 只激活一小部分参数（35B 里只动 3B），要搬运的数据量骤降，带宽瓶颈一下就松了。2026 年 4 月这拨模型让大模型在 Apple Silicon 上跑得动，靠的不是芯片变快，是架构绕开了带宽墙。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/19_a86af3246d07d8b57916e1d71a935fc6.jpg\" alt=\"内存带宽对比示意，Apple Silicon 与独立显卡的数据吞吐差异\"></p>\n<p>还有条铁律得刻进脑子：<strong>模型文件大小要压在总内存的三分之二以内</strong>。macOS 自己先吃掉 3–4GB，上下文缓存还会随 token 数往上涨——你开的上下文越大，缓存越占内存。算一下就懂为什么 16GB 是绝路：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 看模型实际占用，跑起来后查（SIZE 含 KV 缓存，随 num_ctx 浮动）\nollama ps\n# NAME            SIZE     PROCESSOR\n# qwen3.6:27b     18 GB    100% GPU</code></pre>\n\n<p>16.8GB 的模型文件，加上系统和上下文缓存，实际吃到 18GB 往上，16GB 内存连塞都塞不下。想知道你这台机器能跑多大的模型，用这个粗算，别凭感觉：</p>\n<pre class=\"language-python\" data-language=\"python\"><code class=\"language-python\"># 可用预算 = 总内存 × 2/3 - 系统占用\ntotal_ram = 36        # 你的总内存 GB\nbudget = total_ram * 2 / 3 - 4   # 减掉 macOS 的 3-4GB\nprint(f\"模型文件别超过 {budget:.0f}GB\")   # 20GB，刚好够 27B Q4</code></pre>\n\n<p>顺带说下 <strong>Q4_K_M</strong> 是什么——4-bit 的 K 系列中档量化。为什么是它而不是更高精度？因为它在体积和质量之间卡得最准：16.8GB 的体积让 24GB 内存也摸得到门槛，质量损失又小到日常写代码基本无感。更高精度的版本质量提升有限，体积却把你直接顶出内存预算。至于 DeepSeek V4 这种动辄要 140GB 的，本地这条路上直接 pass，别惦记，那是给服务器集群准备的。</p>\n<h2 id=\"num-ctx-默认-2048，这是-Ollama-欠了两年的债\"><a href=\"#num-ctx-默认-2048，这是-Ollama-欠了两年的债\" class=\"headerlink\" title=\"num_ctx 默认 2048，这是 Ollama 欠了两年的债\"></a>num_ctx 默认 2048，这是 Ollama 欠了两年的债</h2><p>模型选对了，工具不对照样白搭。这里有个坑，几乎每个用 Ollama 写代码的人都踩过，而且踩了还不一定知道：<a href=\"https://docs.ollama.com/faq\">Ollama 的默认上下文窗口 num_ctx 只有 2048 token</a>。</p>\n<p>2048 是什么水平？你贴一个稍长点的源文件进去就溢出了。最坑的是它不报错——模型不是变笨了，是它压根没看全你的代码，却照样一本正经给你回答。你以为是模型不行，其实是工具默认值在坑你。这个问题被用户吐槽了两年，至今没改默认。写代码至少拉到 32768：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 运行时临时改\nollama run qwen3.6:27b\n&gt;&gt;&gt; /set parameter num_ctx 32768</code></pre>\n\n<p>想一劳永逸，写进 Modelfile，以后这个模型永远带着大上下文起：</p>\n<pre class=\"language-docker\" data-language=\"docker\"><code class=\"language-docker\">FROM qwen3.6:27b\nPARAMETER num_ctx 32768</code></pre>\n\n<p>走 API 的话，就在每个请求里带上 options：</p>\n<pre class=\"language-json\" data-language=\"json\"><code class=\"language-json\">{\n  \"model\": \"qwen3.6:27b\",\n  \"prompt\": \"重构这个支付服务\",\n  \"options\": { \"num_ctx\": 32768 }\n}</code></pre>\n\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/19_5acc81bdb9684257f5c95c19f1d9f5d1.png\" alt=\"Ollama 官方 FAQ 里专门有一条「如何设置上下文窗口大小」\"></p>\n<p>还有个白捡的提速，别只盯着 Ollama 一棵树。Ollama 的后端是 llama.cpp，而 <a href=\"https://lmstudio.ai/\">LM Studio</a> 用的是 Apple 的 MLX 框架，同样的模型在 Apple Silicon 上社区实测快 10–30%。同样一块芯片，换个推理后端就白得三成速度，这便宜不占白不占。我的建议很直接：纯对话场景用 LM Studio 吃 MLX 的速度红利，要嵌进脚本和 agent 流程再用 Ollama 的接口和生态。Ollama 截至 2026 年 6 月也出了 MLX 预览支持，但成熟度还差点意思，我暂时不押它。</p>\n<h2 id=\"光跑起来不算用，得接进编辑器\"><a href=\"#光跑起来不算用，得接进编辑器\" class=\"headerlink\" title=\"光跑起来不算用，得接进编辑器\"></a>光跑起来不算用，得接进编辑器</h2><p>终端里 <code>ollama run</code> 复制粘贴算什么生产力？要真拿它写代码，得把本地端点接进你天天用的编辑器。两条路我都走过，分开配反而清爽。</p>\n<p>补全和对话用 <a href=\"https://continue.dev/\">Continue</a> 插件接 Ollama 的本地端点，VS Code 和 JetBrains 都吃，配置就三行：</p>\n<pre class=\"language-json\" data-language=\"json\"><code class=\"language-json\">{\n  \"models\": [\n    { \"title\": \"Qwen3.6-27B\", \"provider\": \"ollama\", \"model\": \"qwen3.6:27b\" }\n  ]\n}</code></pre>\n\n<p>要的是 edit-test-fix 那种 agent 流程，就换 Devstral Small 2 配它自带的 Mistral Vibe CLI，让模型在终端里自己读报错、改对文件、再跑一遍：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 把 Devstral 拉下来，交给 agent 流程驱动\nollama pull devstral</code></pre>\n\n<p>别指望一个插件全搞定——补全归补全，agent 归 agent，混在一起只会互相拖累。</p>\n<h2 id=\"按你的内存对号入座，别幻想\"><a href=\"#按你的内存对号入座，别幻想\" class=\"headerlink\" title=\"按你的内存对号入座，别幻想\"></a>按你的内存对号入座，别幻想</h2><p>讲了半天原理，落到你这台机器上到底能不能用，就看一个数：内存。我把三档直接说死，不跟你打太极。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/19_70ec53e8de22e8a072909048046f4618.jpg\" alt=\"三档内存对应的本地编程能力分级示意\"></p>\n<table>\n<thead>\n<tr>\n<th>内存</th>\n<th>结论</th>\n<th>我的话</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>16GB</td>\n<td>别碰</td>\n<td>没有一款跑得舒服，装上只会失望，省下这两小时去喝咖啡</td>\n</tr>\n<tr>\n<td>24GB</td>\n<td>凑合</td>\n<td>能跑 27B，但上下文要收紧、生成要忍，算半个工具</td>\n</tr>\n<tr>\n<td>32GB+</td>\n<td>生产力</td>\n<td>从”做实验”变成”真干活”的分水岭，Andrus 那台 36GB 正好踩在中间偏上</td>\n</tr>\n</tbody></table>\n<p>24GB 的机器想跑，得把上下文压回去，拿可用性换余量——别一上来就拉满，缓存会把你顶爆：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 24GB 内存：上下文收到 16K，给系统和缓存留余地\nollama run qwen3.6:27b\n&gt;&gt;&gt; /set parameter num_ctx 16384</code></pre>\n\n<p>32GB 以上就放开手脚，该多大上下文给多大，还能用 q8 缓存再省点：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 32GB+：直接拉满 32K 上下文写代码，KV 缓存用 q8 压一压\nOLLAMA_KV_CACHE_TYPE=q8_0 ollama run qwen3.6:27b\n&gt;&gt;&gt; /set parameter num_ctx 32768</code></pre>\n\n<p>如果你内存不够又非要本地，那条路不是换更小的模型——小模型能装下，但写出来的代码是垃圾，我宁可不用也不会拿那种东西糊弄自己。要么加内存，要么老老实实掏钱用云端 API。在 16GB 上硬撑本地编程模型，是我见过最典型的”为了省钱反而最费时间”。</p>\n<p>补一句独显党：上面三档说的是 Mac 的统一内存，<strong>独显看的是显存，不是总内存</strong>。一张 24GB 显存的 4090 跑 27B Q4 余量足，带宽还是 Apple Silicon 的三倍多（936 对 270 GB/s），吐字明显更快。代价是显存比内存”硬”——一旦塞不下溢出到内存/CPU，掉速比 Mac 还狠。所以预算够上独显，本地编程体验其实优于同价位的 Mac。Windows / Linux 用户这套显存账和 num_ctx 设置完全照搬；唯独 Intel Mac 别折腾，跟 16GB 同列，直接 pass。</p>\n<p>我自己最认同 Andrus 的一点，是他的诚实：他没吹”本地替代了云端”。<strong>复杂调试、需要跨整个代码库做假设分析的活儿，一线云端模型依然明显更强。</strong> 本地模型不是来取代云端的，是来把 80% 的日常杂活接走的——常规生成、客户代码、补样板，这些根本不值得动用你最贵的那个 API key。</p>\n<p>他实测后的真实变化很说明问题：日常活全交给本地 Qwen3.6-27B，只在真卡死时才升级到付费 API，一个月 API 开销降了 50% 以上。他还顺手在飞行模式下让模型审了自己那篇文章——这才是本地模型最爽的地方：没人能断你网，没人能半夜给你涨价，16.8GB 的权重落在你硬盘上，谁也收不回去。</p>\n<p>所以别问”哪个本地模型最好”，要问”我这个场景配哪个”。我按用途把这四款拆开，对号入座：</p>\n<table>\n<thead>\n<tr>\n<th>场景</th>\n<th>我的推荐</th>\n<th>原因</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>日常写代码 / 重构 / 客户活</td>\n<td><strong>Qwen3.6-27B (Q4_K_M)</strong></td>\n<td>唯一能扛主力的，77.2% SWE-bench 排同体量第一档</td>\n</tr>\n<tr>\n<td>agent 流程（edit-test-fix 循环）</td>\n<td>Devstral Small 2</td>\n<td>读报错、动对文件这件事它做得最稳</td>\n</tr>\n<tr>\n<td>编辑器里的实时补全</td>\n<td>Codestral，但要忍</td>\n<td>能用，跨文件感知弱，费电，按需开</td>\n</tr>\n<tr>\n<td>探索性瞎试、不打算 commit</td>\n<td>Qwen3.6-35B-A3B</td>\n<td>快，但别信它一遍过，错得也快</td>\n</tr>\n<tr>\n<td>16GB 机器</td>\n<td>哪个都别装</td>\n<td>加内存或用云端，这是省时间不是省钱</td>\n</tr>\n</tbody></table>\n<p>说白了，主力位只有 Qwen3.6-27B 一个，其余三款是配角，各补一块短板。我推荐 Qwen3.6-27B 当主力，因为它是唯一一个在重构、SQL、单测三类任务上都没掉链子的（77.2% 的 SWE-bench 也排在同体量第一档）；我不用 35B-A3B 当主力，因为它快归快，提交前那种”两文件外类型契约被它悄悄改坏”的提心吊胆，我受不了。你要是只装一个，别犹豫，就 27B。</p>\n<p>它也不是没有让我皱眉的地方。单测只写 happy path 这事我很介意——明确要了失败用例还漏时区边界，意味着关键路径的测试你还得自己兜底，不能甩手。重构那次的”差一个调用点”也是同理：能用，但你得养成全程复查的习惯，把它当一个聪明但偶尔马虎的初级工程师使，而不是当甩手掌柜。这不是黑它，是用它的正确姿势。</p>\n<p>本地编程模型熬了这么多年，2026 年 4 月这一拨才算真正跨过”能用”那道线。Qwen3.6-27B 不完美，单测还偷懒、复杂活还得求云端，但它是第一个让”在自己机器上写代码”这件事配得上”生产力”三个字的模型。我的判断是：今年年底之前，本地编程会从极客的玩具变成不少人 PR 里的默认起手式。</p>\n<p>把这篇转给那个还在为月底 API 账单肉疼、或者还在 16GB 机器上跟本地模型死磕的同事——先让他把内存这一关搞明白，别的都是后话。</p>\n<p>下一步很简单，一条命令开始：<code>ollama pull qwen3.6:27b</code>，跑起来第一件事，记得把 num_ctx 拉到 32768。</p>\n",
            "tags": [
                "模型与评测",
                "本地LLM",
                "Qwen3.6",
                "Ollama",
                "编程模型",
                "模型选型",
                "量化部署"
            ]
        },
        {
            "id": "https://costalong.com/2026/06/08/292/",
            "url": "https://costalong.com/2026/06/08/292/",
            "title": "把 Claude Code 接到 Prometheus + Grafana：14 个变量讲清楚",
            "date_published": "2026-06-08T16:00:00.000Z",
            "content_html": "<h1 id=\"把-Claude-Code-接到-Prometheus-Grafana：14-个变量讲清楚\"><a href=\"#把-Claude-Code-接到-Prometheus-Grafana：14-个变量讲清楚\" class=\"headerlink\" title=\"把 Claude Code 接到 Prometheus + Grafana：14 个变量讲清楚\"></a>把 Claude Code 接到 Prometheus + Grafana：14 个变量讲清楚</h1><p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/292_8ffec82c409c857806865aac71012e58.jpg\" alt=\"Claude Code → OTLP → Prometheus → Grafana 流水线封面\"></p>\n<p>我前两天帮一个团队 debug 他们的 Claude Code telemetry，把 alias 拷过去之后 Prometheus 一条数据都没收到。一查发现：默认 protocol 是 gRPC 不是 http/protobuf，端口是 4317 不是 4318，而对方的 Prometheus 只开了 HTTP receiver。一个变量错位，整条数据流哑火。</p>\n<p>Claude Code 的 OpenTelemetry 配置一共有 14 个常用环境变量，分四层：开关、传输、隐私、基数。每一层都有”默认值跟你想要的不一样”的坑。</p>\n<p>这篇分两半。前半把 14 个变量按层讲清楚（含完整对照表）。后半给你一份贴上就能跑的 Prometheus 2.47+ 原生 OTLP receive + Grafana 的 docker-compose——不需要 OTel Collector 中转，最少零件。</p>\n<h2 id=\"14-个-env-vars-分四层看\"><a href=\"#14-个-env-vars-分四层看\" class=\"headerlink\" title=\"14 个 env vars 分四层看\"></a>14 个 env vars 分四层看</h2><p>按功能分组，记住每层只解决一个问题，记起来就简单：</p>\n<table>\n<thead>\n<tr>\n<th>层</th>\n<th>变量</th>\n<th>默认值</th>\n<th>决定</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>开关</strong></td>\n<td><code>CLAUDE_CODE_ENABLE_TELEMETRY</code></td>\n<td>unset (= 关)</td>\n<td>总开关，没有它后面全失效</td>\n</tr>\n<tr>\n<td></td>\n<td><code>CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC</code></td>\n<td>unset</td>\n<td>关 Anthropic 自己的遥测（与你的 OTel 无关）</td>\n</tr>\n<tr>\n<td></td>\n<td><code>OTEL_METRICS_EXPORTER</code></td>\n<td>unset</td>\n<td><code>otlp</code> / <code>prometheus</code> / <code>console</code> / <code>none</code></td>\n</tr>\n<tr>\n<td></td>\n<td><code>OTEL_LOGS_EXPORTER</code></td>\n<td>unset</td>\n<td><code>otlp</code> / <code>console</code> / <code>none</code>（注意没有 prometheus）</td>\n</tr>\n<tr>\n<td><strong>传输</strong></td>\n<td><code>OTEL_EXPORTER_OTLP_PROTOCOL</code></td>\n<td><code>grpc</code> ⚠️</td>\n<td><code>grpc</code> / <code>http/protobuf</code> / <code>http/json</code></td>\n</tr>\n<tr>\n<td></td>\n<td><code>OTEL_EXPORTER_OTLP_ENDPOINT</code></td>\n<td><code>localhost:4317</code></td>\n<td>gRPC 用 4317，HTTP 用 4318</td>\n</tr>\n<tr>\n<td></td>\n<td><code>OTEL_EXPORTER_OTLP_HEADERS</code></td>\n<td>unset</td>\n<td><code>Authorization=Bearer xxx</code></td>\n</tr>\n<tr>\n<td><strong>标签</strong></td>\n<td><code>OTEL_RESOURCE_ATTRIBUTES</code></td>\n<td>unset</td>\n<td><code>team=platform,user.email=x@y.com</code></td>\n</tr>\n<tr>\n<td><strong>隐私</strong></td>\n<td><code>OTEL_LOG_USER_PROMPTS</code></td>\n<td><code>0</code></td>\n<td>prompt 正文上不上报</td>\n</tr>\n<tr>\n<td></td>\n<td><code>OTEL_LOG_TOOL_CONTENT</code></td>\n<td><code>0</code></td>\n<td>tool 输入输出正文</td>\n</tr>\n<tr>\n<td></td>\n<td><code>OTEL_LOG_TOOL_DETAILS</code></td>\n<td><code>0</code></td>\n<td>bash 命令字符串、MCP server 名等元信息</td>\n</tr>\n<tr>\n<td><strong>基数</strong></td>\n<td><code>OTEL_METRICS_INCLUDE_SESSION_ID</code></td>\n<td><code>true</code> ⚠️</td>\n<td>不关掉会导致指标基数爆炸</td>\n</tr>\n<tr>\n<td></td>\n<td><code>OTEL_METRICS_INCLUDE_ACCOUNT_UUID</code></td>\n<td><code>true</code></td>\n<td>多用户场景下也要</td>\n</tr>\n<tr>\n<td></td>\n<td>时间间隔</td>\n<td><code>60000</code> / <code>5000</code> ms</td>\n<td>metrics 60s、logs 5s，debug 期调小</td>\n</tr>\n</tbody></table>\n<p>标⚠️的两个最坑：默认 protocol 是 gRPC，但很多公司 HTTPS 反代只允许 HTTP；默认带 <code>session.id</code>，每次 Claude 重启都是新 series，几天 Prometheus 内存就崩。</p>\n<p>最小启用模板（本地 dev，推到本地 4317）：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export CLAUDE_CODE_ENABLE_TELEMETRY=1\nexport OTEL_METRICS_EXPORTER=otlp\nexport OTEL_LOGS_EXPORTER=otlp\n# 默认就是 grpc + localhost:4317，可以不写</code></pre>\n\n<p>跑一下 <code>claude --version</code> 之后用 <code>tcpdump -i lo port 4317</code> 抓包，如果有流量，说明开关层 OK。</p>\n<p>debug 阶段不想搭后端时，最方便的是 console exporter——直接把 metric 打到 stderr：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export CLAUDE_CODE_ENABLE_TELEMETRY=1\nexport OTEL_METRICS_EXPORTER=console\nexport OTEL_METRIC_EXPORT_INTERVAL=5000   # 5 秒一次，看得见\nclaude\n# 然后看 stderr，会有 JSON 形式的 metric 流出来</code></pre>\n\n<h2 id=\"三种-exporter：gRPC-HTTP-protobuf-内置-Prometheus\"><a href=\"#三种-exporter：gRPC-HTTP-protobuf-内置-Prometheus\" class=\"headerlink\" title=\"三种 exporter：gRPC / HTTP/protobuf / 内置 Prometheus\"></a>三种 exporter：gRPC / HTTP/protobuf / 内置 Prometheus</h2><p>我推荐先想清楚走哪条路再配，不然变量改来改去会乱：</p>\n<p><strong>Path A：gRPC OTLP</strong>（默认，最快）</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export OTEL_EXPORTER_OTLP_PROTOCOL=grpc\nexport OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317</code></pre>\n\n<p>适合本地 / VPC 内部、能直连的场景。性能最好，但很多企业反代不放 gRPC。</p>\n<p><strong>Path B：HTTP/protobuf OTLP</strong>（跨网络、跨反代最稳）</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf\nexport OTEL_EXPORTER_OTLP_ENDPOINT=https://usage.example.com/api/v1/otlp\n# Prometheus 2.47+ 直接收，无需 OTel Collector</code></pre>\n\n<p>这是 HelloTalk 那种远程聚合常用的姿势——HTTP/2 走 443，反代不挑食。我自己生产也用这条。</p>\n<p><strong>Path C：内置 Prometheus exporter</strong>（最简单）</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export OTEL_METRICS_EXPORTER=prometheus\n# Claude Code 会在本地起一个 /metrics 端点，Prometheus 主动 scrape</code></pre>\n\n<p>零依赖，本地用最快。缺点：只能采 metrics，logs 没法走这条；多用户聚合也不方便。</p>\n<p>我自己的选型经验：单机本地用 Path C，团队聚合用 Path B，重度场景（要做 trace 关联）才上 Path A + OTel Collector。</p>\n<h2 id=\"传输进阶：per-signal-拆分-4-种认证姿势\"><a href=\"#传输进阶：per-signal-拆分-4-种认证姿势\" class=\"headerlink\" title=\"传输进阶：per-signal 拆分 + 4 种认证姿势\"></a>传输进阶：per-signal 拆分 + 4 种认证姿势</h2><p>走到生产环境，基本会遇到两个需求：metrics 和 logs 想推到不同后端、跨网络要带认证。Claude Code 都支持，只是参数容易写错。</p>\n<p><strong>Metrics 和 logs 走不同后端</strong>——比如 metrics 进 Prometheus、logs 进 Loki：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 通用 endpoint 不写，用 per-signal override\nexport OTEL_EXPORTER_OTLP_METRICS_PROTOCOL=http/protobuf\nexport OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://prometheus:9090/api/v1/otlp\nexport OTEL_EXPORTER_OTLP_LOGS_PROTOCOL=grpc\nexport OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=http://loki:4317</code></pre>\n\n<p>注意 metrics endpoint 走 HTTP（带 <code>/api/v1/otlp</code> 路径），logs 走 gRPC（只到端口）。两边都各自的协议参数，不会互相覆盖。</p>\n<blockquote>\n<p>⚠️ <strong>Claude 2.1 实测坑</strong>：单独设 <code>OTEL_EXPORTER_OTLP_METRICS_ENDPOINT</code> + <code>OTEL_EXPORTER_OTLP_LOGS_ENDPOINT</code>，<strong>实际只有 metrics endpoint 生效</strong>，logs 信号仍走通用 endpoint。我自己的环境（2.1.153）跑下来就是这个行为——Prom 拿到了、loki 静默落空。最稳的做法是<strong>只设 <code>OTEL_EXPORTER_OTLP_ENDPOINT</code> 指向一个 OTel Collector</strong>,让 Collector 内部按 pipeline 分流给 Prom 和 Loki。Collector 这层配置 12 行就够，下面”Docker 容器”章节会贴完整 yaml。</p>\n</blockquote>\n<p><strong>认证姿势 1：静态 Bearer token</strong>——最简单，token 写在环境变量里：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export OTEL_EXPORTER_OTLP_HEADERS=\"Authorization=Bearer $(cat /etc/secrets/otel-token)\"\n# 多个 header 用逗号分隔\nexport OTEL_EXPORTER_OTLP_HEADERS=\"Authorization=Bearer xxx,X-Tenant-ID=team-platform\"</code></pre>\n\n<p><strong>认证姿势 2：mTLS</strong>——企业内 zero-trust 网络常用，OTLP exporter 直接吃 cert 文件路径：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 客户端证书三件套（与 OTLP_PROTOCOL 配合）\nexport OTEL_EXPORTER_OTLP_CLIENT_CERTIFICATE=/etc/ssl/claude-client.crt\nexport OTEL_EXPORTER_OTLP_CLIENT_KEY=/etc/ssl/claude-client.key\nexport OTEL_EXPORTER_OTLP_CERTIFICATE=/etc/ssl/ca-bundle.crt   # 服务端 CA</code></pre>\n\n<p><strong>认证姿势 3：动态 headers</strong>——token 会过期、要轮换，写个脚本输出 JSON：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 准备一个 helper 脚本，stdout 输出 JSON\ncat &gt; /usr/local/bin/get-otel-headers.sh &lt;&lt;'EOF'\n#!/bin/bash\nTOKEN=$(aws sts assume-role-with-web-identity ... | jq -r '.Credentials.SessionToken')\necho \"{\\\"Authorization\\\": \\\"Bearer $TOKEN\\\"}\"\nEOF\nchmod +x /usr/local/bin/get-otel-headers.sh\n\n# 告诉 Claude Code 用这个脚本（仅 http/protobuf 和 http/json 支持，gRPC 不行）\nexport OTEL_EXPORTER_OTLP_HEADERS_HELPER=/usr/local/bin/get-otel-headers.sh\nexport CLAUDE_CODE_OTEL_HEADERS_HELPER_DEBOUNCE_MS=900000   # 15 分钟刷新一次</code></pre>\n\n<p><strong>认证姿势 4：管理员配置文件</strong>——团队不想让每个开发者自己配 env vars，用 settings 文件集中管理：</p>\n<pre class=\"language-json\" data-language=\"json\"><code class=\"language-json\">// /etc/claude-code/managed-settings.json（管理员级，优先级最高）\n{\n  \"env\": {\n    \"CLAUDE_CODE_ENABLE_TELEMETRY\": \"1\",\n    \"OTEL_METRICS_EXPORTER\": \"otlp\",\n    \"OTEL_EXPORTER_OTLP_PROTOCOL\": \"http/protobuf\",\n    \"OTEL_EXPORTER_OTLP_ENDPOINT\": \"https://usage.example.com/api/v1/otlp\",\n    \"OTEL_EXPORTER_OTLP_HEADERS\": \"Authorization=Bearer $(cat /etc/secrets/otel)\",\n    \"OTEL_RESOURCE_ATTRIBUTES\": \"deployment.environment=prod\"\n  }\n}</code></pre>\n\n<p>放好之后，开发者 <code>claude</code> 一启动就自动拿到这些 env vars，不需要自己 export，也不能覆盖。多团队场景下 <code>OTEL_RESOURCE_ATTRIBUTES</code> 可以叠加打标签：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 用户 shell 里追加 team / user 维度（managed.json 里 prod 不变）\nexport OTEL_RESOURCE_ATTRIBUTES=\"${OTEL_RESOURCE_ATTRIBUTES},team=infra,user.email=$(git config user.email),cost_center=eng-123\"</code></pre>\n\n<p>后端拿到的 resource attributes 是合并集，Prometheus 那边在 <code>promote_resource_attributes</code> 里挑要 promote 成 label 的字段就行。</p>\n<h2 id=\"隐私-基数：你不想全推的两类参数\"><a href=\"#隐私-基数：你不想全推的两类参数\" class=\"headerlink\" title=\"隐私 + 基数：你不想全推的两类参数\"></a>隐私 + 基数：你不想全推的两类参数</h2><p>这块是踩坑重灾区。默认值在 <em>最保守</em> 一端（什么都不上报），但很多人为了”看清楚一点”把开关全打开，结果一周后日志里塞满了源码片段被合规扫到。</p>\n<p><strong>隐私三件套</strong>（默认全是关，按需开）：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 我个人推荐的\"够用不越界\"组合\nexport OTEL_LOG_USER_PROMPTS=0     # 用户 prompt 正文：永远关\nexport OTEL_LOG_TOOL_CONTENT=0     # tool 输入输出正文：永远关（会包含文件内容！）\nexport OTEL_LOG_TOOL_DETAILS=1     # tool 元信息：可以开，对审计有用</code></pre>\n\n<p><code>OTEL_LOG_TOOL_DETAILS=1</code> 会上报 Bash 命令字符串、MCP server / tool 名、skill 名。这部分对审计很有用（知道用户在调哪些工具），但本身不含正文，比 <code>TOOL_CONTENT</code> 安全得多。</p>\n<p>如果你要在 Grafana 的 prompt 二级页里看到 <strong>Tool Calls 的请求参数正文</strong>，还必须额外打开：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export OTEL_LOG_TOOL_CONTENT=1</code></pre>\n\n<p>注意这只对<strong>新启动的 Claude Code 进程</strong>生效，历史 Loki 数据补不回来。它会把 tool 输入/输出内容一起上报，可能包含文件路径、命令参数、代码片段甚至文件内容，只建议在明确的审计环境里打开。</p>\n<p><strong>还有一个高危隐私变量必须知道</strong>：<code>OTEL_LOG_RAW_API_BODIES</code>。它会上报完整的 Anthropic Messages API 请求 / 响应 JSON，包含整段对话历史和生成内容。开启它等于把所有上面三个开关一次性打开，并且体量翻倍——单次 prompt 体量可以到 60KB。除非你在做 LLM 审计平台，不然永远别开：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 危险，除非你知道自己在做什么\n# export OTEL_LOG_RAW_API_BODIES=1            # 内联，单条 60KB 截断\n# export OTEL_LOG_RAW_API_BODIES=file:/var/log/claude-bodies   # 落盘，无截断</code></pre>\n\n<p><strong>基数控制四件套</strong>（全部默认开的两个一定要关，另外两个按需）：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 高基数自杀风险：必须关\nexport OTEL_METRICS_INCLUDE_SESSION_ID=false       # 默认 true，每次重启换值\nexport OTEL_METRICS_INCLUDE_ACCOUNT_UUID=false     # 默认 true，多用户场景按 team 聚合时不需要\n\n# 默认关的两个：按需开\nexport OTEL_METRICS_INCLUDE_VERSION=true           # 默认 false。Claude Code 升级监控想看版本分布时打开\nexport OTEL_METRICS_INCLUDE_ENTRYPOINT=true        # 默认 false。区分 `claude` / `claude -p` / Agent SDK 调用源</code></pre>\n\n<p><code>session.id</code> 每次 <code>claude</code> 命令重启都换，加进 metric 标签就是 cardinality 自杀。我之前没关，跑了 4 天 Prometheus 内存从 200MB 涨到 8GB，回查才发现是这个。<code>VERSION</code> 和 <code>ENTRYPOINT</code> 默认是关的——只在你要做”哪些用户还卡在旧版本””脚本调用占比多少”这类分析时才打开。</p>\n<h2 id=\"Prometheus-原生-OTLP-接收：一条-flag-搞定\"><a href=\"#Prometheus-原生-OTLP-接收：一条-flag-搞定\" class=\"headerlink\" title=\"Prometheus 原生 OTLP 接收：一条 flag 搞定\"></a>Prometheus 原生 OTLP 接收：一条 flag 搞定</h2><p>Prometheus 从 2.47 开始原生支持 OTLP HTTP receive，不需要 OTel Collector 当中间件。这是目前最短的接收路径。</p>\n<p>启动时加一个 flag：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">prometheus \\\n  --config.file=/etc/prometheus/prometheus.yml \\\n  --web.enable-otlp-receiver</code></pre>\n\n<p>然后 prometheus.yml 里加一个 <code>otlp:</code> 段，告诉它把哪些 OTel resource attribute 提升成 label：</p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\"># prometheus.yml\notlp:\n  # 把这些 OTel 资源属性 promote 成 Prometheus label，方便查询\n  promote_resource_attributes:\n    - service.name\n    - service.instance.id\n    - team\n    - user.email\n  keep_identifying_resource_attributes: true\n  # translation_strategy 用默认（UnderscoreEscapingWithSuffixes）\n  #   - OTel 名里 . → _（claude_code.session.count → claude_code_session_count）\n  #   - counter 自动加 _total 后缀\n  #   - 带单位的 metric 加单位后缀（详见下文\"验证\"段）\n\nstorage:\n  tsdb:\n    # OTLP 数据偶尔会乱序到达，给一点容忍\n    out_of_order_time_window: 30m</code></pre>\n\n<p>Claude Code 端指过去：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export CLAUDE_CODE_ENABLE_TELEMETRY=1\nexport OTEL_METRICS_EXPORTER=otlp\nexport OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf\nexport OTEL_EXPORTER_OTLP_ENDPOINT=http://prometheus:9090/api/v1/otlp\nexport OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=cumulative\nexport OTEL_METRICS_INCLUDE_SESSION_ID=false\nclaude</code></pre>\n\n<p>注意三个细节：endpoint 是 <code>/api/v1/otlp</code>（Prometheus 内部自动补 <code>/v1/metrics</code>）；temporality 一定要 <code>cumulative</code>，Prometheus 不擅长 delta；session_id 关掉。</p>\n<p>验证：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">curl -s http://localhost:9090/api/v1/query?query=claude_code_session_count_total | jq .</code></pre>\n\n<p>如果返回里有 <code>result</code> 数组，数据流就通了。</p>\n<p><strong>实测过的命名规则</strong>（默认 <code>UnderscoreEscapingWithSuffixes</code> 策略）：</p>\n<table>\n<thead>\n<tr>\n<th>OTel 原名</th>\n<th>Prometheus 名</th>\n<th>备注</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>claude_code.session.count</code></td>\n<td><code>claude_code_session_count_total</code></td>\n<td>unit=count 时不加单位后缀</td>\n</tr>\n<tr>\n<td><code>claude_code.token.usage</code></td>\n<td><code>claude_code_token_usage_**tokens**_total</code></td>\n<td>⚠️ unit=tokens,加单位</td>\n</tr>\n<tr>\n<td><code>claude_code.cost.usage</code></td>\n<td><code>claude_code_cost_usage_**USD**_total</code></td>\n<td>⚠️ unit=USD,加单位</td>\n</tr>\n<tr>\n<td><code>claude_code.active_time.total</code></td>\n<td><code>claude_code_active_time_**seconds**_total</code></td>\n<td>⚠️ unit=s → seconds</td>\n</tr>\n<tr>\n<td><code>claude_code.lines_of_code.count</code></td>\n<td><code>claude_code_lines_of_code_count_total</code></td>\n<td>unit=count</td>\n</tr>\n<tr>\n<td><code>claude_code.code_edit_tool.decision</code></td>\n<td><code>claude_code_code_edit_tool_decision_total</code></td>\n<td>unit=count</td>\n</tr>\n</tbody></table>\n<p>带单位的那 3 个最容易踩坑——按”原 OTel 名替换点”猜会查不到。我第一次跑就栽在 <code>claude_code_cost_usage_total</code> 上,debug 半天才发现要加 <code>_USD</code>。下面的 PromQL 都用了实测过的正确命名。</p>\n<h2 id=\"加-Grafana，跑一个-demo-dashboard\"><a href=\"#加-Grafana，跑一个-demo-dashboard\" class=\"headerlink\" title=\"加 Grafana，跑一个 demo dashboard\"></a>加 Grafana，跑一个 demo dashboard</h2><p>Grafana 用 provisioning 模式自动加 data source + dashboard，比手点省心。</p>\n<p><code>grafana/provisioning/datasources/prometheus.yml</code>：</p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\">apiVersion: 1\ndatasources:\n  - name: Prometheus\n    type: prometheus\n    access: proxy\n    url: http://prometheus:9090\n    isDefault: true\n    editable: false\n    jsonData:\n      httpMethod: POST\n      timeInterval: 15s</code></pre>\n\n<p>几条我每天都会看的 PromQL，覆盖”成本 / 产出 / 谁在用”三个维度：</p>\n<pre class=\"language-promql\" data-language=\"promql\"><code class=\"language-promql\"># 1. 各模型每小时 token 消耗（成本归因第一选）\nsum by (model, type) (increase(claude_code_token_usage_tokens_total[1h]))\n\n# 2. 各团队每天的 cost（USD）— 需要前面配了 team resource attribute\nsum by (team) (increase(claude_code_cost_usage_USD_total[1d]))\n\n# 3. 每天有多少独立 session 启动（adoption 跟进）\nsum(increase(claude_code_session_count_total[1d]))\n\n# 4. lines_of_code 增减（产出维度）\nsum(rate(claude_code_lines_of_code_count_total{type=\"added\"}[1h]))\n\n# 5. 工具调用决策分布（被拒绝率，看权限模型健康度）\nsum by (decision) (rate(claude_code_code_edit_tool_decision_total[5m]))</code></pre>\n\n<p>我自己 dashboard 上最常盯着的是第 2 条 + 第 5 条。前者管钱，后者看 Claude 是不是经常被人按 deny——按 deny 太多说明工具描述写得不好，或者用户对 Claude 不信任，都是要 fix 的信号。</p>\n<blockquote>\n<p>⚠️ <strong>OTLP 累计 counter + <code>increase()</code> 的坑（实测）</strong>：Claude Code 走 OTLP 推的是<strong>累计 counter</strong>，而且只在值<strong>变化时</strong>才推一个样本（稀疏推送）。当某指标在你选的时间窗内只有 1 个样本时，<code>increase(metric[$__range])</code> 因为”两点才能算增量”会<strong>返回空</strong>——dashboard 上就是一片 <code>No data</code>，空闲几分钟后打开尤其明显。两个稳妥替代：</p>\n<ul>\n<li>看”区间累计值”（如总成本、总 token、commit 数）用 <code>sum(last_over_time(metric[$__range]))</code>——取窗口内最后一个样本，顺便绕开 Prometheus 5 分钟 staleness 导致的 instant 查询变空；</li>\n<li>看”窗口增量趋势”，要么把范围拉到足够长让 <code>increase()</code> 有 ≥2 个样本，要么干脆把趋势计算挪到 <strong>Loki 端</strong> <code>sum_over_time({...} | unwrap &lt;字段&gt; [$__interval])</code>——日志侧每条事件都是独立样本，不受稀疏 counter 影响，趋势更准。本文后面 Loki 章节的成本/token 趋势都用后一种。</li>\n</ul>\n</blockquote>\n<p>如果你想直接拿一份现成 dashboard，Anthropic 自己开源了 <code>anthropics/claude-code-monitoring-guide</code> 仓库，里面有 docker-compose + dashboard JSON。我下面这份 docker-compose 是简化版（去掉了它的 Loki 部分，只留 Prometheus + Grafana）：</p>\n<h2 id=\"Dashboard-自动-provisioning-告警规则\"><a href=\"#Dashboard-自动-provisioning-告警规则\" class=\"headerlink\" title=\"Dashboard 自动 provisioning + 告警规则\"></a>Dashboard 自动 provisioning + 告警规则</h2><p>PromQL 写完，下一步是让 Grafana 启动时自动加载 dashboard 和 alert，开发机和生产环境出一份代码就够。</p>\n<p><code>grafana/provisioning/dashboards/dashboards.yml</code>：</p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\">apiVersion: 1\nproviders:\n  - name: claude-code\n    type: file\n    disableDeletion: true\n    updateIntervalSeconds: 30\n    allowUiUpdates: false\n    options:\n      path: /etc/grafana/provisioning/dashboards/json\n      foldersFromFilesStructure: true</code></pre>\n\n<p>然后把 dashboard JSON 放到 <code>grafana/provisioning/dashboards/json/cost.json</code>——下面是一个最小可用的成本归因面板骨架（一个 stat + 一个 timeseries + 两个变量 <code>$team</code> 和 <code>$model</code>）：</p>\n<pre class=\"language-json\" data-language=\"json\"><code class=\"language-json\">{\n  \"title\": \"Claude Code Cost Attribution\",\n  \"templating\": {\n    \"list\": [\n      {\"name\": \"team\", \"type\": \"query\", \"datasource\": \"Prometheus\",\n       \"query\": \"label_values(claude_code_cost_usage_USD_total, team)\", \"multi\": true, \"includeAll\": true},\n      {\"name\": \"model\", \"type\": \"query\", \"datasource\": \"Prometheus\",\n       \"query\": \"label_values(claude_code_cost_usage_USD_total, model)\", \"multi\": true, \"includeAll\": true}\n    ]\n  },\n  \"panels\": [\n    {\"id\": 1, \"type\": \"stat\", \"title\": \"Today's Total Cost (USD)\",\n     \"targets\": [{\"expr\": \"sum(increase(claude_code_cost_usage_USD_total{team=~\\\"$team\\\",model=~\\\"$model\\\"}[1d]))\"}]},\n    {\"id\": 2, \"type\": \"timeseries\", \"title\": \"Hourly Cost by Team\",\n     \"targets\": [{\"expr\": \"sum by (team) (increase(claude_code_cost_usage_USD_total{model=~\\\"$model\\\"}[1h]))\",\n                  \"legendFormat\": \"{{team}}\"}]}\n  ]\n}</code></pre>\n\n<p><strong>告警规则</strong>——团队成本超阈值时报警，写在 Prometheus 端（也可以用 Grafana 9+ 的 unified alerting）：</p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\"># prometheus-rules.yml，挂到 prometheus.yml 的 rule_files: 段\ngroups:\n  - name: claude_code_cost\n    interval: 1m\n    rules:\n      - alert: ClaudeCodeTeamCostOverBudget\n        expr: |\n          sum by (team) (increase(claude_code_cost_usage_USD_total[1d])) &gt; 50\n        for: 5m\n        labels:\n          severity: warning\n        annotations:\n          summary: \"Team {{ $labels.team }} 今日成本 ${{ $value | printf \\\"%.2f\\\" }} 超 $50 阈值\"\n      - alert: ClaudeCodeDenyRateHigh\n        expr: |\n          sum by (team) (rate(claude_code_code_edit_tool_decision_total{decision=\"deny\"}[15m]))\n            / sum by (team) (rate(claude_code_code_edit_tool_decision_total[15m])) &gt; 0.3\n        for: 10m\n        labels:\n          severity: info\n        annotations:\n          summary: \"Team {{ $labels.team }} 工具拒绝率 &gt; 30%，工具描述或权限模型可能要 fix\"</code></pre>\n\n<p>第二条告警是我自己生产里很有价值的一条——deny 率高说明 Claude 推荐的命令经常被否决，要么是 prompt 写得不准，要么是工具描述不清楚，能直接反推 fix 方向。</p>\n<h2 id=\"Skill-Agent-MCP-使用分布：数据驱动砍-推哪些\"><a href=\"#Skill-Agent-MCP-使用分布：数据驱动砍-推哪些\" class=\"headerlink\" title=\"Skill / Agent / MCP 使用分布：数据驱动砍/推哪些\"></a>Skill / Agent / MCP 使用分布：数据驱动砍/推哪些</h2><p>装了 30 个 skill 装了 8 个 MCP server，半年后没人记得到底哪些在跑活，凭印象砍最容易砍错。Claude Code 的 OTel 数据其实把每次调用都打了标签——同一个 <code>claude_code.token.usage</code> metric 上额外带 <code>skill.name</code> / <code>plugin.name</code> / <code>agent.name</code> 维度，按它们 group by 就是一张”谁在烧 token”的天然 dashboard。</p>\n<p><strong>前提是 <code>OTEL_LOG_TOOL_DETAILS=1</code> 必须打开</strong>——不然下面所有 skill/plugin/agent 标签全是空字符串，dashboard 永远看不到：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export OTEL_LOG_TOOL_DETAILS=1\n# 这会上报：Bash 命令字符串、MCP server/tool 名、skill 名、subagent 类型\n# 不含：prompt 正文、tool 输入输出 — 见前面\"隐私 + 基数\"章节的权衡</code></pre>\n\n<p>打开之后，Prometheus 端会看到 <code>claude_code_token_usage_tokens_total</code> 上多出三个标签：<code>skill_name</code> / <code>plugin_name</code> / <code>agent_name</code>（OTel 名里的 <code>.</code> 在 Prom 端转成 <code>_</code>）。</p>\n<blockquote>\n<p>⚠️ <strong>重要实测发现</strong>：<code>skill_name</code> 和 <code>plugin_name</code> 标签的<strong>值不是 skill 的具体名</strong>(如 <code>using-superpowers</code>)，而是<strong>分类</strong>(<code>'third-party'</code> / 推测还有 <code>'built-in'</code> 等)。Claude Code 应该不想把私有 skill 名上报到 metric。所以下面这条 <code>topk(10, sum by skill_name)</code> 拿到的是”分类型的 token 分布”(third-party vs built-in)而不是”哪 10 个 skill 最烧 token”——别被 PromQL 表达式骗了。<code>agent_name</code> 倒是具体的（如 <code>general-purpose</code>、自定义 subagent 名）。<strong>想要具体 skill 名（全名）别在 Prometheus 这边死磕——走后面 Loki 章节的 <code>skill_activated</code> 事件，那里 <code>skill_name</code> 是真名。</strong></p>\n</blockquote>\n<p><strong>按 skill 分类看 token 分布</strong>——分清楚是 third-party 插件还是内置 skill 在烧钱：</p>\n<pre class=\"language-promql\" data-language=\"promql\"><code class=\"language-promql\"># 实测值通常只有 1-2 类:third-party / built-in\nsum by (skill_name) (claude_code_token_usage_tokens_total{skill_name!=\"\"})\n\n# 如果只想看本周新增\nsum by (skill_name) (increase(claude_code_token_usage_tokens_total{skill_name!=\"\"}[7d]))</code></pre>\n\n<p><strong>每个 plugin 分类的成本归因（USD）</strong>——类似上,跟 skill_name 通常一致:</p>\n<pre class=\"language-promql\" data-language=\"promql\"><code class=\"language-promql\">sum by (plugin_name) (claude_code_cost_usage_USD_total{plugin_name!=\"\"})\n\n# 团队 × plugin 分类的二维视图\nsum by (team, plugin_name) (claude_code_cost_usage_USD_total{plugin_name!=\"\"})</code></pre>\n\n<p><strong>Subagent 调用密度</strong>——<code>agent_name</code> 是真正的具体名（<code>general-purpose</code> / <code>code-reviewer</code> / 自定义 subagent 名），这条最有用：</p>\n<pre class=\"language-promql\" data-language=\"promql\"><code class=\"language-promql\"># Top 10 烧 token 的 subagent\ntopk(10,\n  sum by (agent_name) (claude_code_token_usage_tokens_total{agent_name!=\"\"})\n)\n\n# 配合 user_email 看是哪个开发者疯狂调 subagent\ntopk(5,\n  sum by (user_email, agent_name) (rate(claude_code_token_usage_tokens_total{agent_name!=\"\"}[1h]))\n)</code></pre>\n\n<p><strong>要看具体 skill / MCP server / 工具名,走 logs 后端</strong>(Loki/Elastic)最直接。这里有一组<strong>第一版踩过坑、必须讲清楚的事实</strong>：Claude Code 走 OTel 上报时，所有 attribute 经 OTel Collector 写成 Loki 3.x 的 <strong>structured metadata</strong>（既不是 stream label，也不是 JSON body），并且<strong>属性名里的 <code>.</code> 全部转成 <code>_</code></strong>——<code>skill.name</code>→<code>skill_name</code>、<code>process.pid</code>→<code>process_pid</code>、<code>event.name</code>→<code>event_name</code>、<code>server.name</code>→<code>server_name</code>。LogQL 查询一律用下划线名。</p>\n<blockquote>\n<p>🔑 <strong>structured metadata 怎么过滤（关键，纠正一个广为流传的误解）</strong>：</p>\n<ul>\n<li>❌ <strong>不能</strong>放进 stream selector 大括号——<code>{service_name=\"claude-code\", tool_name=\"Skill\"}</code> 直接返 <strong>0</strong>（大括号只认真正的 stream label，本环境只有 <code>service_name</code> 一个）；</li>\n<li>✅ <strong>能</strong>用 <strong>pipeline label filter</strong>（管道符）<code>{service_name=\"claude-code\"} | event_name=\"tool_result\" | tool_name=\"Skill\"</code> 精确过滤，实测稳定返回数据，也能 <code>sum by(tool_name)</code> 聚合、<code>| unwrap duration_ms</code> 取数值。</li>\n</ul>\n<p>一句话：<strong>过滤 structured metadata 用管道符 <code>| key=\"val\"</code>，不是大括号 <code>{key=\"val\"}</code></strong>。记牢这条，下面所有按 skill / agent / server 精确过滤的查询都能跑通——不必退回 <code>|=</code> 子串匹配那种容易误伤的写法。</p>\n</blockquote>\n<p>按 <code>event_name</code> 区分事件类型后，最有价值的几类：</p>\n<ul>\n<li><strong><code>skill_activated</code></strong> — 每次 skill 激活打一条，<strong>直接带具体 skill 名</strong>：<code>skill_name</code>（<code>superpowers:using-superpowers</code> / <code>feishu-docs</code> 这种全名）、<code>skill_source</code>（plugin / userSettings / project）、<code>invocation_trigger</code>（user-slash / nested-skill / claude-proactive）。<strong>这是拿”具体哪个 skill 在跑”的正解</strong>——不用像旧版那样绕 <code>tool_result</code> 按行人工翻（详见下面「找具体 skill 名」小节）。</li>\n<li><strong><code>subagent_completed</code></strong> — 每个子代理跑完打一条，带 per-run 明细：<code>agent_type</code>（general-purpose / Explore / 自定义名）、<code>total_tokens</code>、<code>total_tool_uses</code>、<code>duration_ms</code>、<code>model</code>、<code>agent_source</code>（built-in / 自定义）。比 Prometheus 的 <code>agent_name</code> 聚合多了”每一次跑了多久、用了多少工具/token”的粒度。</li>\n<li><strong><code>mcp_server_connection</code></strong> — 每次 MCP server 连接握手打一条。<code>server_name</code> 是具体名（context7 / sequential-thinking / n8n-mcp 等）；<strong><code>status</code> 是连接结果（connected / failed / disconnected）——这是 MCP 健康度的关键信号</strong>（实测 failed 常常 ≥ connected，多发生在 sse/远程 server）；还有 <code>transport_type</code>（stdio 本地 / sse·claudeai-proxy 远程）、<code>server_scope</code>、<code>duration_ms</code>（握手耗时）。</li>\n<li><strong><code>tool_result</code></strong> — 每次工具调用完打一条。<strong>注意（2.1.x 实际行为）</strong>：<code>tool_name</code> 对 MCP 工具<strong>始终是字符串 <code>mcp_tool</code></strong>（不区分 server），真实 server+tool 名在另一个 structured metadata 字段 <code>tool_parameters</code> 里，是 JSON 字符串，例如 <code>{\"mcp_server_name\":\"context7\",\"mcp_tool_name\":\"resolve-library-id\"}</code>。Loki 的 <code>| json</code> 只能解 log body、解不了 structured metadata 里的 JSON value，要拆 server 出来得在客户端（Python / jq）解，或用 <code>line_format</code> 把整串打出来人工看。</li>\n</ul>\n<blockquote>\n<p>📌 <strong>更正一则旧版误解</strong>:本文早期版本写过”Loki 3.3 下用精确 match 偶尔返空、得退回 line filter <code>|=</code> 子串匹配”——复盘下来，那是把 stream selector <code>{key=\"val\"}</code>（structured metadata 放这里确实返 0）和 pipeline filter <code>| key=\"val\"</code>（完全可行）搞混了。实测 <code>| event_name=\"mcp_server_connection\"</code>、<code>| tool_name=\"Skill\"</code>、<code>| status=\"failed\"</code> 这类<strong>管道符</strong>过滤都稳定生效。下面的例子统一用管道符精确过滤；<code>|=</code> 子串匹配只在”搜某个关键词出现在哪”时才用，不再拿来当 label 过滤的替代。</p>\n</blockquote>\n<p><strong>列出本会话连接过的所有 MCP server</strong>(实测 12 个:context7 / chrome-devtools / mixsearch / sequential-thinking-1 / zread / web-search-prime / web-reader / vibe_kanban / playwright / n8n-mcp / plugin:superpowers-chrome:chrome / sequential-thinking):</p>\n<pre class=\"language-logql\" data-language=\"logql\"><code class=\"language-logql\"># 区间内连接过的 MCP server（管道符精确过滤 event_name）\nsum by (server_name) (\n  count_over_time({service_name=\"claude-code\"} | event_name=\"mcp_server_connection\" [$__range])\n)</code></pre>\n\n<p><strong>MCP 连接健康度（这是最该上 dashboard 的一张）</strong>——<code>status</code> 维度此前完全被”连接次数”掩盖，实测把它拆出来才发现 <code>failed</code> 往往比 <code>connected</code> 还多：</p>\n<pre class=\"language-logql\" data-language=\"logql\"><code class=\"language-logql\"># 连接状态分布（connected / failed / disconnected）\nsum by (status) (\n  count_over_time({service_name=\"claude-code\"} | event_name=\"mcp_server_connection\" [$__range])\n)\n\n# 哪些 server 在掉线 —— 直接点名排障入口（实测集中在远程 sse server）\nsum by (server_name) (\n  count_over_time({service_name=\"claude-code\"} | event_name=\"mcp_server_connection\" | status=\"failed\" | server_name!=\"\" [$__range])\n)\n\n# 握手延迟 avg by server（unwrap 数值字段，慢连接拖慢启动）\navg by (server_name) (\n  avg_over_time({service_name=\"claude-code\"} | event_name=\"mcp_server_connection\" | server_name!=\"\" | unwrap duration_ms [$__range])\n)</code></pre>\n\n<p><strong>每次 MCP 工具调用的延迟、谁调的、参数</strong>(把 <code>tool_parameters</code> 这个 JSON label 解出 <code>mcp_server_name</code> 字段):</p>\n<pre class=\"language-logql\" data-language=\"logql\"><code class=\"language-logql\"># 关键:Loki 3.3 的 | json 只能解 log body,解不了 structured metadata label 里的 JSON value\n# (tool_parameters 是 OTel Collector 写到 Loki structured metadata 的 label)\n# 所以 LogQL 只做 line filter 圈定范围,JSON 解在客户端做\n{service_name=\"claude-code\"} |= \"tool_result\"\n  | line_format `{{.user_email}} → {{.tool_parameters}} | ok={{.success}} dur={{.duration_ms}}ms | in={{.tool_input_size_bytes}}B out={{.tool_result_size_bytes}}B`</code></pre>\n\n<p>实测一行: <code>costa9293@gmail.com → {\"mcp_server_name\":\"context7\",\"mcp_tool_name\":\"resolve-library-id\"} | ok=true dur=4763ms | in=43B out=1679B</code> —— 哪个用户在哪个时刻调了哪个 MCP server 的哪个 tool、参数多大、返回多大,白纸黑字。要按 server 拆开看,客户端用 jq / Python 解 <code>tool_parameters</code> 这串 JSON 即可(Loki WebUI 有自带 JSON inspector)。</p>\n<p><strong>按 MCP server 分组看调用次数</strong>(1h,降序):</p>\n<pre class=\"language-logql\" data-language=\"logql\"><code class=\"language-logql\">topk(20,\n  sum by (server_name) (\n    count_over_time({service_name=\"claude-code\"} |= \"mcp_server_connection\" [1h])\n  )\n)</code></pre>\n\n<p><strong>只看某个 server 的 tool_result</strong>(如 <code>context7</code>):</p>\n<pre class=\"language-logql\" data-language=\"logql\"><code class=\"language-logql\">{service_name=\"claude-code\"} |= \"context7\" |= \"tool_result\"</code></pre>\n\n<p><strong>每次调用的输入参数</strong>(<code>tool_input</code> 也是 JSON label,审计时重要):LogQL 不解它,直接 <code>|= \"query\":\"&lt;keyword&gt;\"</code> 做 substring 搜索最实际:</p>\n<pre class=\"language-logql\" data-language=\"logql\"><code class=\"language-logql\"># 找所有传了 query=\"fastapi\" 的 MCP tool 调用\n{service_name=\"claude-code\"} |= \"tool_result\" |= \"fastapi\"</code></pre>\n\n<p>如果团队关心某个 MCP server 的健康度,直接在 Grafana 里把 <code>server_name</code> 设成变量,配上 <code>rate(count_over_time(...[5m]))</code> 就能看到连接成功率/失败率时间序列。这套组合实测在 Loki 3.3 + OTel Collector 下稳定工作。</p>\n<p><strong>找具体 skill 名：直接查 <code>skill_activated</code>（正解，别再绕 <code>tool_result</code>）</strong></p>\n<p>旧版这里花了很大篇幅教人”用 <code>sum by(tool_name)</code> 拿分布、再到 logs panel 按行人工找 Skill 那一行”——那是当时误以为 structured metadata 不能精确过滤、又没发现有专门事件记这件事。正解是：</p>\n<pre class=\"language-logql\" data-language=\"logql\"><code class=\"language-logql\"># 用了哪些 skill、各多少次（具体全名，不是 third-party/built-in 分类）\nsum by (skill_name) (\n  count_over_time({service_name=\"claude-code\"} | event_name=\"skill_activated\" [$__range])\n)\n\n# skill 来源 / 触发方式分布\nsum by (skill_source)       (count_over_time({service_name=\"claude-code\"} | event_name=\"skill_activated\" [$__range]))\nsum by (invocation_trigger) (count_over_time({service_name=\"claude-code\"} | event_name=\"skill_activated\" [$__range]))</code></pre>\n\n<p>实测直接拿到 <code>superpowers:using-superpowers</code> / <code>superpowers:systematic-debugging</code> / <code>feishu-docs</code> 这种具体名，外加 <code>skill_source=plugin/userSettings</code>、<code>invocation_trigger=user-slash/nested-skill/claude-proactive</code>——比在 <code>tool_result</code> 里人工翻精确得多。</p>\n<p><strong>子代理 per-run 明细：查 <code>subagent_completed</code></strong></p>\n<p>Prometheus 的 <code>agent_name</code> 只能给”某 agent 累计烧了多少 token”。要看”每一次跑了多久、用了多少工具”，查事件：</p>\n<pre class=\"language-logql\" data-language=\"logql\"><code class=\"language-logql\"># 各子代理类型运行次数\nsum by (agent_type) (\n  count_over_time({service_name=\"claude-code\"} | event_name=\"subagent_completed\" [$__range])\n)\n\n# 每次运行的明细：类型 / 模型 / token / 工具数 / 耗时\n{service_name=\"claude-code\"} | event_name=\"subagent_completed\"\n  | line_format `{{.agent_type}} | {{.model}} | tok={{.total_tokens}} tools={{.total_tool_uses}} dur={{.duration_ms}}ms`</code></pre>\n\n<p><strong>通用范式：structured metadata 一律用管道符精确过滤</strong>。下面这些旧版以为”Loki 返 0”的需求，换成管道符全部能跑：</p>\n<table>\n<thead>\n<tr>\n<th>想做的事</th>\n<th>❌ 旧版（返 0 / 容易误伤）</th>\n<th>✅ 正确写法</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>只看 Skill 工具调用</td>\n<td><code>{service_name=\"claude-code\", tool_name=\"Skill\"}</code></td>\n<td><code>{service_name=\"claude-code\"} | event_name=\"tool_result\" | tool_name=\"Skill\"</code></td>\n</tr>\n<tr>\n<td>只看某 server 连接</td>\n<td><code>|= \"context7\"</code> 子串匹配</td>\n<td><code>| event_name=\"mcp_server_connection\" | server_name=\"context7\"</code></td>\n</tr>\n<tr>\n<td>工具失败率</td>\n<td>（旧版做不了）</td>\n<td><code>| event_name=\"tool_result\" | success=\"false\"</code> 占总数</td>\n</tr>\n</tbody></table>\n<blockquote>\n<p>旧版列的那张”❌ 多 label match 返 0 / ✅ 只能 sum by”的表，根因就是<strong>没区分 stream selector <code>{}</code> 和 pipeline filter <code>\\|</code></strong>。<code>{key=\"val\"}</code> 对 structured metadata 确实返 0，但那不是 Loki 的限制，是写法放错了位置——挪到管道符后就好。</p>\n</blockquote>\n<p><strong>仍然有效的 <code>tool_result</code> logs panel</strong>（看每次调用的工具/参数/耗时，MCP 真实 server 在 <code>tool_parameters</code> JSON 里，客户端解）：</p>\n<pre class=\"language-logql\" data-language=\"logql\"><code class=\"language-logql\">{service_name=\"claude-code\"} | event_name=\"tool_result\"\n  | line_format `{{.tool_name}} | {{.tool_parameters}} | ok={{.success}} {{.duration_ms}}ms`</code></pre>\n<p>结合 <code>duration_ms</code>、<code>tool_input_size_bytes</code> / <code>tool_result_size_bytes</code> 能快速定位”哪些调用最重”。</p>\n<p>所以”找出 Skill 调用”这种需求,在 Loki 3.3 + OTel Collector 这套组合下,<strong>唯一可靠的方式是 sum by (tool_name) 拿到分布,再用 logs panel 按行人工找 Skill 那一行</strong>。</p>\n<p>也可以简化:只看 metrics 的话,<strong>最实用的是 <code>agent_name</code></strong>——subagent 的具体名能直接 group by 出来,半年下来能看出”哪些自定义 agent 真用了,哪些是装着好看”。skill/plugin 那两个分类标签只能给你”第三方 vs 内置”的粗粒度判断,而且实测在 2.1.153 <strong>永远只有 <code>third-party</code> 一个值</strong>,dashboard 画出来是单色,信息量约等于 0。</p>\n<p>我自己 dashboard 上常驻这三面板(都是确认有真实多样性的 label,不再用 skill/plugin 分类):</p>\n<ul>\n<li><strong>Tokens by Subagent</strong> — <code>sum by (agent_name) (claude_code_token_usage_tokens_total{agent_name!=\"\"})</code>。实测 <code>Explore</code> 和 <code>general-purpose</code> 各 50K+ tokens,两条柱,信息密度高。</li>\n<li><strong>Cost by Model (USD)</strong> — <code>sum by (model) (claude_code_cost_usage_USD_total)</code>。不同模型价格差几倍,这条直接告诉你钱烧在哪类模型上。</li>\n<li><strong>Top 10 Users by Cost (1h)</strong> — <code>topk(10, sum by (user_email) (claude_code_cost_usage_USD_total))</code>。需要 <code>OTEL_METRICS_INCLUDE_USER_EMAIL=true</code> 才能让 <code>user_email</code> 标签有值(默认是 <code>user_id</code> hash)。这条帮我跟 manager 解释钱花在哪——数据胜过 vibe。</li>\n</ul>\n<p>Prometheus 的 <code>skill_name</code> / <code>plugin_name</code> 标签确实只有”第三方 vs 内置”这种粗分类（实测常只有 <code>third-party</code> 一个值，画出来单色、信息量≈0），但<strong>想看具体 skill 名不必退回 <code>tool_result</code> 人工翻——走上面的 <code>skill_activated</code> 事件就好</strong>。MCP 真实 server/tool 仍在 <code>tool_result</code> 的 <code>tool_parameters</code> JSON 里（客户端解）。</p>\n<p><strong>成本归因再进一步：按 <code>query_source</code> 拆”交互 vs 后台自动”</strong></p>\n<p><code>api_request</code> 事件带一个容易被忽略、却极有价值的字段 <code>query_source</code>——标明这次 LLM 调用是谁发起的。实测一拆才发现后台自动调用在悄悄烧钱：</p>\n<pre class=\"language-logql\" data-language=\"logql\"><code class=\"language-logql\"># 成本按来源拆（Loki 端 unwrap cost_usd，精确、不受稀疏 counter 影响）\nsum by (query_source) (\n  sum_over_time({service_name=\"claude-code\"} | event_name=\"api_request\" | unwrap cost_usd [$__range])\n)</code></pre>\n\n<p>实测分布：<code>repl_main_thread</code>（你手敲的交互）占大头，但 <code>sdk</code>、<code>away_summary</code>（你离开时 Claude 自动写的总结）、<code>agent:*</code>（子代理）、<code>prompt_suggestion</code>、<code>generate_session_title</code> 这些<strong>后台来源加起来能占三到四成</strong>——Prometheus 的 <code>cost_usage</code> 按 model/team 拆完全看不到这一层。想压成本，先看这张图里哪些后台来源能关。</p>\n<h2 id=\"把-Claude-Code-跑在-Docker-容器里\"><a href=\"#把-Claude-Code-跑在-Docker-容器里\" class=\"headerlink\" title=\"把 Claude Code 跑在 Docker 容器里\"></a>把 Claude Code 跑在 Docker 容器里</h2><p>很多人 dev 环境是 host 直跑 Claude Code，但越来越多的团队把它装进容器——一是隔离权限避免一不小心 <code>rm -rf</code> 到宿主机，二是多个项目环境互不污染，三是 OTel 配置可以跟容器走，不污染开发者本地 shell。</p>\n<p><strong>第一步：把 OTel 配置抽到 <code>.env</code> 文件</strong>——容器化的第一收益就是配置和镜像分离：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># otel.env（提交到 git 里也安全，没有 secret）\nCLAUDE_CODE_ENABLE_TELEMETRY=1\nOTEL_METRICS_EXPORTER=otlp\nOTEL_LOGS_EXPORTER=otlp\nOTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf\nOTEL_EXPORTER_OTLP_ENDPOINT=http://prometheus:9090/api/v1/otlp\nOTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=cumulative\nOTEL_METRICS_INCLUDE_SESSION_ID=false\nOTEL_METRICS_INCLUDE_ACCOUNT_UUID=false\nOTEL_LOG_TOOL_DETAILS=1\nOTEL_RESOURCE_ATTRIBUTES=team=infra,deployment.environment=dev</code></pre>\n\n<p>Secret 单独放，永远别和 <code>.env</code> 混在一起：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># otel.secret.env（gitignore 加上！）\nOTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer xxxxxxxxxxxxx</code></pre>\n\n<p><strong>第二步：<code>docker run</code> 一次性起一个 Claude Code 容器</strong>——适合临时调一下：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">docker run -it --rm \\\n  --name claude-dev \\\n  --network monitoring \\\n  --env-file ./otel.env \\\n  --env-file ./otel.secret.env \\\n  -v \"$HOME/.claude:/root/.claude\" \\\n  -v \"$(pwd):/workspace\" \\\n  -w /workspace \\\n  node:24-bookworm \\\n  bash -lc \"npm i -g @anthropic-ai/claude-code &gt;/dev/null &amp;&amp; claude\"</code></pre>\n\n<p>三个 <code>-v</code> 挂载点要看清：<code>~/.claude</code> 装登录态和 settings，挂进去之后容器里的 <code>claude</code> 一启动就是登录状态；<code>$(pwd)</code> 是当前项目工作区；<code>--network monitoring</code> 是为了能解析到前面 docker-compose 起的 <code>prometheus</code> 容器名。</p>\n<p><strong>第三步：把 Claude Code 加进 docker-compose</strong>——适合常驻 dev container 模式：</p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\"># 加到前面 docker-compose.yml 的 services: 段下\n  claude:\n    image: node:24-bookworm\n    container_name: claude-dev\n    networks: [default]\n    env_file:\n      - otel.env\n      - otel.secret.env\n    volumes:\n      - \"$HOME/.claude:/root/.claude\"\n      - \"$PWD:/workspace\"\n    working_dir: /workspace\n    tty: true\n    stdin_open: true\n    command: [\"bash\", \"-lc\", \"npm i -g @anthropic-ai/claude-code &amp;&amp; sleep infinity\"]</code></pre>\n\n<p>挂上去之后 <code>docker compose up -d claude</code>，再 <code>docker compose exec claude claude</code> 就进入交互式 Claude，OTel 数据自动推到同 compose 里的 Prometheus 容器。</p>\n<p><strong>Secret 管理</strong>：上面用 <code>--env-file otel.secret.env</code> 是最简单的姿势，但 secret 文件还是落盘的。要更严格可以用 Docker secrets（compose 里 <code>secrets:</code> 段）或者在容器里跑一个 <code>OTEL_EXPORTER_OTLP_HEADERS_HELPER</code> 脚本去调云厂商的 KMS 拿临时 token——参考前面”传输进阶”章节那条动态 headers 的写法，把脚本 COPY 进容器即可。</p>\n<h2 id=\"完整-docker-compose：贴上就能跑\"><a href=\"#完整-docker-compose：贴上就能跑\" class=\"headerlink\" title=\"完整 docker-compose：贴上就能跑\"></a>完整 docker-compose：贴上就能跑</h2><p>把这份 <code>docker-compose.yml</code> + 两个配置文件放一个目录，<code>docker compose up</code> 就能起：</p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\"># docker-compose.yml\nservices:\n  prometheus:\n    image: prom/prometheus:v3.5.0\n    command:\n      - --config.file=/etc/prometheus/prometheus.yml\n      - --web.enable-otlp-receiver\n      - --storage.tsdb.retention.time=30d\n    ports:\n      - \"9090:9090\"\n    volumes:\n      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro\n      - prom_data:/prometheus\n\n  grafana:\n    image: grafana/grafana:12.2.0\n    ports:\n      - \"3000:3000\"\n    environment:\n      GF_SECURITY_ADMIN_PASSWORD: admin\n      GF_AUTH_ANONYMOUS_ENABLED: \"true\"\n      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer\n    volumes:\n      - ./grafana/provisioning:/etc/grafana/provisioning:ro\n      - grafana_data:/var/lib/grafana\n    depends_on:\n      - prometheus\n\nvolumes:\n  prom_data:\n  grafana_data:</code></pre>\n\n<p><code>prometheus.yml</code> 和前一节那份一致，<code>grafana/provisioning/datasources/prometheus.yml</code> 也是前一节那份。目录长这样：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">.\n├── docker-compose.yml\n├── prometheus.yml\n└── grafana\n    └── provisioning\n        └── datasources\n            └── prometheus.yml</code></pre>\n\n<p>起服务 + 在你常用 Claude Code 的 shell 配 env vars 指过去：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">docker compose up -d\nexport CLAUDE_CODE_ENABLE_TELEMETRY=1\nexport OTEL_METRICS_EXPORTER=otlp\nexport OTEL_LOGS_EXPORTER=otlp\nexport OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf\nexport OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:9090/api/v1/otlp\nexport OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=cumulative\nexport OTEL_METRICS_INCLUDE_SESSION_ID=false\nexport OTEL_RESOURCE_ATTRIBUTES=\"team=infra,user.email=$(git config user.email)\"\n\nclaude  # 用一会儿</code></pre>\n\n<p>5 分钟后打开浏览器访问 <code>localhost:3000</code>（Grafana），data source 已经自动连好 Prometheus，在 Explore 里输入 <code>claude_code_session_count_total</code> 应该能看到至少 1 个 series。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/292_78490e829c18d0fde72b98e4b35572a7.png\" alt=\"anthropics/claude-code-monitoring-guide：官方 ROI 测量指南，覆盖 telemetry / 成本 / 产出 / 团队分析四个维度\"></p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/292_bc2779b26e950c7e5b19a531d6b7215b.jpg\" alt=\"Claude Code → Prometheus OTLP → Grafana 三阶段架构图\"></p>\n<hr>\n<p>启用 telemetry 容易，把数据通起来难。把 14 个变量按”开关 / 传输 / 隐私 / 基数”四层记，知道每层只解决一个问题，配错了也容易定位是哪一层。</p>\n<p>最大的两个默认值坑：protocol 默认是 gRPC（不是 http/protobuf）、<code>session.id</code> 默认带（基数会爆）。剩下的都是常识。</p>\n<blockquote>\n<p>先收藏再看，里面的 docker-compose 是真能直接 <code>docker compose up</code>、跑得起来的最小集——下次起 OTel 接收端就别再现搜配置了。</p>\n</blockquote>\n<h2 id=\"来源\"><a href=\"#来源\" class=\"headerlink\" title=\"来源\"></a>来源</h2><ul>\n<li>Claude Code 监控配置（含全部 env vars 列表 + metric 名）：在 Anthropic docs 搜「monitoring usage」，路径 <code>code.claude.com/docs/en/monitoring-usage</code></li>\n<li>Prometheus OTLP receiver 文档：搜「prometheus opentelemetry」，主页 <code>prometheus.io/docs/guides/opentelemetry/</code>，关键 flag <code>--web.enable-otlp-receiver</code></li>\n<li>官方监控示例仓库（含 Docker Compose + Grafana dashboard）：GitHub 搜「anthropics claude-code-monitoring-guide」</li>\n<li>Prometheus 仓库：GitHub 搜「prometheus prometheus」（64k stars，2026-06-05 push）</li>\n<li>Grafana 仓库：GitHub 搜「grafana grafana」（74k stars，2026-06-05 push）</li>\n<li>OTel Collector contrib（如果你需要 Path A 加中间件）：GitHub 搜「open-telemetry opentelemetry-collector-contrib」</li>\n</ul>\n",
            "tags": [
                "AI-生态",
                "Claude Code",
                "OpenTelemetry",
                "Prometheus",
                "Grafana",
                "可观测性",
                "监控运维",
                "实战教程"
            ]
        },
        {
            "id": "https://costalong.com/2026/06/04/hermes-web-ui-shi-yong-jiao-cheng/",
            "url": "https://costalong.com/2026/06/04/hermes-web-ui-shi-yong-jiao-cheng/",
            "title": "hermes-web-ui 使用教程",
            "date_published": "2026-06-04T07:50:37.000Z",
            "content_html": "<h1 id=\"hermes-web-ui-使用教程\"><a href=\"#hermes-web-ui-使用教程\" class=\"headerlink\" title=\"hermes-web-ui 使用教程\"></a>hermes-web-ui 使用教程</h1><p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/1_317f5e4a392d2718f6afd27f528610fa.jpg\" alt=\"封面图\"></p>\n<p>hermes-web-ui 是 Hermes Agent 的全功能 Web 管理面板，可以通过简洁、响应式的 Web 界面管理 AI 对话会话、监控使用情况与成本、配置平台渠道、安排定时任务、浏览技能——所有操作都在一个干净的界面中完成。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/1_12e83cfbabee0f43e534ed629a76d295.png\" alt=\"Hermes Web UI 主界面\"></p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/1_f2ec8dde085e879210630292c43c3b65.png\" alt=\"AI 对话界面\"></p>\n<h2 id=\"什么是-Hermes-Agent\"><a href=\"#什么是-Hermes-Agent\" class=\"headerlink\" title=\"什么是 Hermes Agent\"></a>什么是 Hermes Agent</h2><p>Hermes Agent 是一个多平台 AI 代理，支持 Telegram、Discord、Slack、WhatsApp 等多个平台。hermes-web-ui 为其提供了可视化的管理界面，无需命令行即可完成大多数配置操作。</p>\n<p><strong>项目信息：</strong></p>\n<ul>\n<li>GitHub: <a href=\"https://github.com/EKKOLearnAI/hermes-studio\">EKKOLearnAI/hermes-studio</a></li>\n<li>许可证: BSL-1.1</li>\n</ul>\n<h2 id=\"快速开始\"><a href=\"#快速开始\" class=\"headerlink\" title=\"快速开始\"></a>快速开始</h2><h3 id=\"方法一：npm-安装（推荐）\"><a href=\"#方法一：npm-安装（推荐）\" class=\"headerlink\" title=\"方法一：npm 安装（推荐）\"></a>方法一：npm 安装（推荐）</h3><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 安装\nnpm install -g hermes-web-ui\n\n# 启动\nhermes-web-ui start</code></pre>\n\n<p>启动后打开 <a href=\"http://localhost:8648/\">http://localhost:8648</a></p>\n<h3 id=\"方法二：一键安装脚本（自动检测系统）\"><a href=\"#方法二：一键安装脚本（自动检测系统）\" class=\"headerlink\" title=\"方法二：一键安装脚本（自动检测系统）\"></a>方法二：一键安装脚本（自动检测系统）</h3><p>支持 Debian/Ubuntu/macOS，自动安装 Node.js（如缺少）和 hermes-web-ui：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">bash &lt;(curl -fsSL https://cdn.jsdelivr.net/gh/EKKOLearnAI/hermes-web-ui@main/scripts/setup.sh)</code></pre>\n\n<h3 id=\"方法三：Docker-Compose\"><a href=\"#方法三：Docker-Compose\" class=\"headerlink\" title=\"方法三：Docker Compose\"></a>方法三：Docker Compose</h3><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 使用预构建镜像（推荐）\nWEBUI_IMAGE=ekkoye8888/hermes-web-ui:latest docker compose up -d hermes-agent hermes-webui\n\n# 或从源码构建\ndocker compose up -d --build hermes-agent hermes-webui\n\n# 查看日志\ndocker compose logs -f hermes-webui</code></pre>\n\n<p>打开 <a href=\"http://localhost:8648/\">http://localhost:8648</a></p>\n<p><strong>Docker 数据持久化：</strong></p>\n<ul>\n<li>Hermes 数据存储在 <code>./hermes_data</code></li>\n<li>Web UI 认证 token 存储在 <code>./hermes_data/hermes-web-ui/.token</code></li>\n<li>首次运行启用认证时，token 会打印在容器日志中</li>\n</ul>\n<h3 id=\"WSL-环境\"><a href=\"#WSL-环境\" class=\"headerlink\" title=\"WSL 环境\"></a>WSL 环境</h3><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 安装\nbash &lt;(curl -fsSL https://cdn.jsdelivr.net/gh/EKKOLearnAI/hermes-web-ui@main/scripts/setup.sh)\n\n# 启动\nhermes-web-ui start\n\n# WSL 会自动检测并使用 hermes gateway run 后台启动\nhermes gateway run</code></pre>\n\n<h2 id=\"主要功能\"><a href=\"#主要功能\" class=\"headerlink\" title=\"主要功能\"></a>主要功能</h2><h3 id=\"AI-对话\"><a href=\"#AI-对话\" class=\"headerlink\" title=\"AI 对话\"></a>AI 对话</h3><ul>\n<li><strong>实时流式响应</strong>：通过 SSE 实现，支持异步运行</li>\n<li><strong>多会话管理</strong>：创建、重命名、删除、切换会话</li>\n<li><strong>自建会话数据库</strong>：本地 SQLite 存储，首次启动时自动从 Hermes state.db 同步会话</li>\n<li><strong>会话分组</strong>：按来源（Telegram、Discord、Slack 等）分组，可折叠</li>\n<li><strong>实时指示器</strong>：活跃会话置顶显示，带旋转图标</li>\n<li><strong>Markdown 渲染</strong>：语法高亮 + 代码复制</li>\n<li><strong>工具调用详情</strong>：展开查看参数和结果</li>\n<li><strong>文件上传/下载</strong>：支持本地、Docker、SSH、Singularity 后端</li>\n<li><strong>全局搜索</strong>：Ctrl+K 跨所有对话搜索</li>\n<li><strong>全局模型选择器</strong>：从 <code>~/.hermes/auth.json</code> 凭据池发现模型</li>\n<li><strong>每会话模型标签</strong>：显示当前使用的模型和上下文 Token 用量</li>\n</ul>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 凭据池位置\n~/.hermes/auth.json</code></pre>\n\n<h3 id=\"平台渠道配置\"><a href=\"#平台渠道配置\" class=\"headerlink\" title=\"平台渠道配置\"></a>平台渠道配置</h3><p>统一管理 8 个平台，在一个页面完成所有配置：</p>\n<ul>\n<li><strong>凭据管理</strong>：写入 <code>~/.hermes/.env</code></li>\n<li><strong>渠道行为设置</strong>：写入 <code>~/.hermes/config.yaml</code></li>\n<li><strong>配置变更自动重启网关</strong></li>\n<li><strong>各平台配置状态检测</strong></li>\n</ul>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 配置文件位置\n~/.hermes/.env        # 凭据\n~/.hermes/config.yaml # 渠道行为配置</code></pre>\n\n<h3 id=\"使用分析\"><a href=\"#使用分析\" class=\"headerlink\" title=\"使用分析\"></a>使用分析</h3><ul>\n<li><strong>Token 使用量分解</strong>：输入 / 输出分别统计</li>\n<li><strong>会话数量与日均</strong></li>\n<li><strong>预估成本追踪 &amp; 缓存命中率</strong></li>\n<li><strong>模型使用分布图</strong></li>\n<li><strong>30 天每日趋势</strong>：柱状图 + 数据表</li>\n</ul>\n<h3 id=\"定时任务\"><a href=\"#定时任务\" class=\"headerlink\" title=\"定时任务\"></a>定时任务</h3><ul>\n<li><strong>完整的 Cron 管理</strong>：创建、编辑、暂停、恢复、删除</li>\n<li><strong>立即触发执行</strong></li>\n<li><strong>Cron 表达式快捷预设</strong></li>\n</ul>\n<h3 id=\"模型管理\"><a href=\"#模型管理\" class=\"headerlink\" title=\"模型管理\"></a>模型管理</h3><ul>\n<li><strong>自动发现模型</strong>：从凭据池 <code>~/.hermes/auth.json</code> 读取</li>\n<li><strong>获取提供商模型列表</strong>：调用 <code>/v1/models</code> 接口</li>\n<li><strong>添加、更新、删除提供商</strong>：预设 &amp; 自定义 OpenAI 兼容接口</li>\n<li><strong>OpenAI Codex &amp; Nous Portal OAuth 登录</strong></li>\n<li><strong>提供商 URL 自动检测</strong>：支持非 v1 版本（如 <code>/v4</code>）</li>\n<li><strong>提供商级别模型分组</strong>：可切换默认模型</li>\n</ul>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># API 版本示例\n/v1/models  # 标准\n/v4/models  # 非标准版本自动检测</code></pre>\n\n<h3 id=\"多配置文件-网关\"><a href=\"#多配置文件-网关\" class=\"headerlink\" title=\"多配置文件 &amp; 网关\"></a>多配置文件 &amp; 网关</h3><ul>\n<li><strong>配置文件管理</strong>：创建、重命名、删除、切换</li>\n<li><strong>克隆或导入</strong>：从归档文件（<code>.tar.gz</code>）导入</li>\n<li><strong>导出备份</strong>：分享或备份配置</li>\n<li><strong>多网关管理</strong>：每个配置文件独立启动、停止、监控网关</li>\n<li><strong>端口冲突自动解决</strong></li>\n<li><strong>配置文件作用域隔离</strong></li>\n</ul>\n<h3 id=\"文件浏览器\"><a href=\"#文件浏览器\" class=\"headerlink\" title=\"文件浏览器\"></a>文件浏览器</h3><ul>\n<li><strong>远程浏览</strong>：支持本地、Docker、SSH、Singularity 后端</li>\n<li><strong>完整文件操作</strong>：上传、下载、重命名、复制、移动、删除</li>\n<li><strong>创建目录</strong></li>\n<li><strong>文件预览</strong>：语法高亮</li>\n</ul>\n<h3 id=\"群聊\"><a href=\"#群聊\" class=\"headerlink\" title=\"群聊\"></a>群聊</h3><ul>\n<li><strong>多代理聊天室</strong>：通过 Socket.IO 实时消息</li>\n<li><strong>@提及路由</strong>：提及代理触发上下文回复</li>\n<li><strong>上下文压缩</strong>：历史超过 Token 阈值自动摘要</li>\n<li><strong>typing 状态和回复进度指示</strong></li>\n<li><strong>房间管理</strong>：创建、删除、邀请码</li>\n<li><strong>代理管理</strong>：添加/移除代理，带独立配置文件</li>\n<li><strong>SQLite 消息持久化</strong></li>\n<li><strong>移动端响应式</strong>：侧边栏可折叠</li>\n</ul>\n<h3 id=\"技能与记忆\"><a href=\"#技能与记忆\" class=\"headerlink\" title=\"技能与记忆\"></a>技能与记忆</h3><ul>\n<li><strong>浏览和搜索已安装技能</strong></li>\n<li><strong>查看技能详情和附加文件</strong></li>\n<li><strong>用户笔记和配置文件管理</strong></li>\n</ul>\n<h3 id=\"日志\"><a href=\"#日志\" class=\"headerlink\" title=\"日志\"></a>日志</h3><ul>\n<li><strong>查看代理 / 网关 / 错误日志</strong></li>\n<li><strong>多维度过滤</strong>：日志级别、文件、关键词</li>\n<li><strong>结构化日志解析</strong>：HTTP 访问日志高亮</li>\n</ul>\n<h3 id=\"认证\"><a href=\"#认证\" class=\"headerlink\" title=\"认证\"></a>认证</h3><ul>\n<li><strong>Token 认证</strong>：首次运行自动生成，或通过 <code>AUTH_TOKEN</code> 环境变量设置</li>\n<li><strong>可选用户名/密码登录</strong>：首次 Token 认证后通过设置页面设置</li>\n<li><strong>禁用认证</strong>：<code>AUTH_DISABLED=1</code></li>\n</ul>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 环境变量\nAUTH_TOKEN=your-token    # 设置认证 token\nAUTH_DISABLED=1         # 禁用认证（不推荐）</code></pre>\n\n<h3 id=\"设置\"><a href=\"#设置\" class=\"headerlink\" title=\"设置\"></a>设置</h3><ul>\n<li><strong>显示</strong>：流式输出、紧凑模式、推理、成本显示</li>\n<li><strong>代理</strong>：最大轮次、超时、工具强制</li>\n<li><strong>记忆</strong>：启用/禁用、字符限制</li>\n<li><strong>会话重置</strong>：空闲超时、计划重置</li>\n<li><strong>隐私</strong>：PII 去标识化</li>\n<li><strong>模型设置</strong>：默认模型 &amp; 提供商</li>\n<li><strong>API 服务器配置</strong></li>\n</ul>\n<h3 id=\"Web-Terminal\"><a href=\"#Web-Terminal\" class=\"headerlink\" title=\"Web Terminal\"></a>Web Terminal</h3><ul>\n<li><strong>集成终端</strong>：基于 node-pty 和 @xterm/xterm</li>\n<li><strong>多会话支持</strong>：创建、切换、关闭终端会话</li>\n<li><strong>实时键盘输入</strong>：通过 WebSocket 流式传输 PTY 输出</li>\n<li><strong>窗口大小调整支持</strong></li>\n</ul>\n<h2 id=\"CLI-命令\"><a href=\"#CLI-命令\" class=\"headerlink\" title=\"CLI 命令\"></a>CLI 命令</h2><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 启动\nhermes-web-ui start\n\n# 指定端口\nhermes-web-ui start --port 9000\n\n# 停止\nhermes-web-ui stop\n\n# 重启\nhermes-web-ui restart\n\n# 查看状态\nhermes-web-ui status\n\n# 更新\nhermes-web-ui update\n\n# 版本\nhermes-web-ui -v\n\n# 帮助\nhermes-web-ui -h</code></pre>\n\n<h2 id=\"自动配置\"><a href=\"#自动配置\" class=\"headerlink\" title=\"自动配置\"></a>自动配置</h2><p>启动时 BFF 服务器自动执行：</p>\n<ol>\n<li>验证 <code>~/.hermes/config.yaml</code> 并填充缺失的 <code>api_server</code> 字段</li>\n<li>如配置被修改，备份原文件为 <code>config.yaml.bak</code></li>\n<li>检测并启动网关（如需要）</li>\n<li>解决端口冲突（终止占用进程）</li>\n<li>启动成功后自动打开浏览器</li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 配置文件位置\n~/.hermes/config.yaml\n\n# 备份文件（如有修改）\nconfig.yaml.bak</code></pre>\n\n<h2 id=\"开发指南\"><a href=\"#开发指南\" class=\"headerlink\" title=\"开发指南\"></a>开发指南</h2><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 克隆源码\ngit clone https://github.com/EKKOLearnAI/hermes-studio.git\ncd hermes-studio\n\n# 安装依赖\nnpm install\n\n# 开发模式\nnpm run dev</code></pre>\n\n<ul>\n<li>前端开发服务器：<a href=\"http://localhost:5173/\">http://localhost:5173</a></li>\n<li>BFF 服务器：<a href=\"http://localhost:8648（代理到\">http://localhost:8648（代理到</a> Hermes 的 8642 端口）</li>\n</ul>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 生产构建\nnpm run build   # 输出到 dist/</code></pre>\n\n<h2 id=\"技术架构\"><a href=\"#技术架构\" class=\"headerlink\" title=\"技术架构\"></a>技术架构</h2><pre class=\"language-none\"><code class=\"language-none\">Browser → BFF (Koa, :8648) → Hermes Gateway (:8642)\n                ↓\n           Hermes CLI (sessions, logs, version)\n                ↓\n           ~/.hermes/config.yaml  (渠道行为)\n           ~/.hermes/auth.json    (凭据池)\n           Tencent iLink API      (微信扫码登录)</code></pre>\n\n<p><strong>架构说明：</strong></p>\n<ul>\n<li><strong>前端</strong>：Vue 3 + TypeScript + Vite + Naive UI + Pinia + Vue Router + vue-i18n + SCSS + markdown-it + highlight.js</li>\n<li><strong>BFF 层</strong>：Koa 2，处理 API 代理（路径重写）、SSE 流式响应、文件上传下载（多后端：本地/Docker/SSH/Singularity）、会话 CRUD、配置/凭据管理、微信扫码登录、模型发现、技能/记忆管理、日志读取、静态文件服务</li>\n<li><strong>Web Terminal</strong>：node-pty + @xterm/xterm</li>\n</ul>\n<p>前端采用多代理可扩展设计，所有 Hermes 相关代码都命名空间在 <code>hermes/</code> 目录下（API、组件、视图、存储），便于添加新的代理集成。</p>\n<h2 id=\"常见问题\"><a href=\"#常见问题\" class=\"headerlink\" title=\"常见问题\"></a>常见问题</h2><h3 id=\"首次启动需要认证？\"><a href=\"#首次启动需要认证？\" class=\"headerlink\" title=\"首次启动需要认证？\"></a>首次启动需要认证？</h3><p>首次运行时会自动生成认证 token，并在终端打印。按提示访问 <a href=\"http://localhost:8648/\">http://localhost:8648</a> 并输入 token 登录。</p>\n<h3 id=\"Docker-环境下无法使用-hermes-cli？\"><a href=\"#Docker-环境下无法使用-hermes-cli？\" class=\"headerlink\" title=\"Docker 环境下无法使用 hermes cli？\"></a>Docker 环境下无法使用 hermes cli？</h3><p>确保 docker-compose.yml 正确配置了 hermes-agent 服务，且网络连接正常。</p>\n<h3 id=\"端口被占用？\"><a href=\"#端口被占用？\" class=\"headerlink\" title=\"端口被占用？\"></a>端口被占用？</h3><p>hermes-web-ui 会自动检测并解决端口冲突，终止占用端口的旧进程。</p>\n<h3 id=\"如何查看详细日志？\"><a href=\"#如何查看详细日志？\" class=\"headerlink\" title=\"如何查看详细日志？\"></a>如何查看详细日志？</h3><p>在 Web UI 的「日志」页面可查看代理、网关、错误日志，支持按级别和关键词过滤。</p>\n<h2 id=\"总结\"><a href=\"#总结\" class=\"headerlink\" title=\"总结\"></a>总结</h2><p>hermes-web-ui 为 Hermes Agent 提供了一个功能完整的 Web 管理界面，覆盖了从日常对话管理到高级配置的所有场景。无论是个人使用还是团队协作，都能显著提升体验。</p>\n<p><strong>推荐安装方式：</strong></p>\n<ul>\n<li>本地开发：npm 安装</li>\n<li>生产环境：Docker Compose</li>\n</ul>\n<p>立即体验：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">npm install -g hermes-web-ui &amp;&amp; hermes-web-ui start</code></pre>\n\n",
            "tags": [
                "AI",
                "Hermes",
                "AI",
                "Web UI",
                "Vue3",
                "Dashboard"
            ]
        },
        {
            "id": "https://costalong.com/2026/03/01/moment-2026-03-01-book/",
            "url": "https://costalong.com/2026/03/01/moment-2026-03-01-book/",
            "title": "周末读了一本好书",
            "date_published": "2026-03-01T02:30:00.000Z",
            "content_html": "<p>周末花了一下午读完了《设计心理学》，诺曼对日常物品的洞察力太强了。好的设计应该是不需要说明书的。</p>\n",
            "tags": [
                "moments",
                "读书"
            ]
        },
        {
            "id": "https://costalong.com/2026/02/25/moment-2026-02-25-coffee/",
            "url": "https://costalong.com/2026/02/25/moment-2026-02-25-coffee/",
            "title": "尝试了新的咖啡豆",
            "date_published": "2026-02-25T00:15:00.000Z",
            "content_html": "<p>今天开了一包来自云南的日晒豆，风味很惊喜 – 有明显的蓝莓和巧克力调。国产精品豆越来越棒了。</p>\n",
            "tags": [
                "moments",
                "咖啡",
                "生活"
            ]
        },
        {
            "id": "https://costalong.com/2026/02/20/moment-2026-02-20-k8s/",
            "url": "https://costalong.com/2026/02/20/moment-2026-02-20-k8s/",
            "title": "终于搞定了 K8s 网络策略",
            "date_published": "2026-02-20T14:45:00.000Z",
            "content_html": "<p>折腾了两天的 NetworkPolicy 终于通了。关键是 <code>namespaceSelector</code> 和 <code>podSelector</code> 要同时匹配，不能只写一个。记个笔记以防下次再踩坑。</p>\n",
            "tags": [
                "moments",
                "k8s",
                "技术"
            ]
        },
        {
            "id": "https://costalong.com/2026/01/03/claude-code-cc-switch-antigravity-tools-zu-he-shi-yong-zhi-nan/",
            "url": "https://costalong.com/2026/01/03/claude-code-cc-switch-antigravity-tools-zu-he-shi-yong-zhi-nan/",
            "title": "Claude Code + CC-Switch + Antigravity Tools 组合使用指南",
            "date_published": "2026-01-03T15:42:42.000Z",
            "content_html": "<p><img src=\"/medias/loading.gif\" data-original=\"https://file.costalong.com/img/20260103224723137.png\" alt=\"Claude Code + CC-Switch + Antigravity Tools 组合示意图\"></p>\n<h2 id=\"概述\"><a href=\"#概述\" class=\"headerlink\" title=\"概述\"></a>概述</h2><p>结合了 Claude Code (强大的 AI 编程助手)、CC-Switch (假设为上下文/配置快速切换工具) 和 Antigravity Tools</p>\n<table>\n<thead>\n<tr>\n<th>工具</th>\n<th>功能</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Claude Code</strong></td>\n<td>Anthropic 官方 CLI 工具</td>\n<td>在终端中与 Claude 交互，进行代码编写、调试、重构等</td>\n</tr>\n<tr>\n<td><strong>CC-Switch</strong></td>\n<td>账号切换管理器</td>\n<td>管理多个 Claude 账号，一键切换，支持使用量统计</td>\n</tr>\n<tr>\n<td><strong>Antigravity Tools</strong></td>\n<td>API 反代 &amp; 账号管理</td>\n<td>提供本地 API 代理服务，支持 OAuth 授权管理多账号</td>\n</tr>\n</tbody></table>\n<h3 id=\"工作原理\"><a href=\"#工作原理\" class=\"headerlink\" title=\"工作原理\"></a>工作原理</h3><pre class=\"language-none\"><code class=\"language-none\">Claude Code CLI  →  Antigravity Tools (本地反代)  →  Claude API\n                            ↑\n                     CC-Switch (账号管理)</code></pre>\n\n<ul>\n<li><strong>Antigravity Tools</strong> 作为本地代理服务器，转发 Claude Code 的请求</li>\n<li><strong>CC-Switch</strong> 可以在 Antigravity 之上提供更便捷的账号切换功能</li>\n<li>三者配合使用，实现多账号管理和无缝切换</li>\n</ul>\n<hr>\n<h2 id=\"安装\"><a href=\"#安装\" class=\"headerlink\" title=\"安装\"></a>安装</h2><h3 id=\"1-Claude-Code\"><a href=\"#1-Claude-Code\" class=\"headerlink\" title=\"1. Claude Code\"></a>1. Claude Code</h3><p><a href=\"https://github.com/anthropics/claude-code\">GitHub 仓库</a></p>\n<p><strong>前提条件</strong>：</p>\n<ul>\n<li>Node.js 18+</li>\n<li>npm 或其他包管理器</li>\n</ul>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">npm install -g @anthropic-ai/claude-code</code></pre>\n\n<p>安装后验证：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">claude --version</code></pre>\n\n<h3 id=\"2-CC-Switch\"><a href=\"#2-CC-Switch\" class=\"headerlink\" title=\"2. CC-Switch\"></a>2. CC-Switch</h3><p><a href=\"https://github.com/farion1231/cc-switch\">GitHub 仓库</a></p>\n<p><strong>macOS 安装</strong>：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">brew tap farion1231/ccswitch\nbrew install --cask cc-switch</code></pre>\n\n<p><strong>Linux 安装</strong>：<br>前往 <a href=\"https://github.com/farion1231/cc-switch/releases\">Releases 页面</a> 下载适合的安装包：</p>\n<ul>\n<li><code>.deb</code> - Debian/Ubuntu 系</li>\n<li><code>.rpm</code> - RedHat/Fedora 系</li>\n<li><code>AppImage</code> - 通用格式</li>\n</ul>\n<h3 id=\"3-Antigravity-Tools\"><a href=\"#3-Antigravity-Tools\" class=\"headerlink\" title=\"3. Antigravity Tools\"></a>3. Antigravity Tools</h3><p><a href=\"https://github.com/lbjlaq/Antigravity-Manager\">GitHub 仓库</a></p>\n<p>前往 <a href=\"https://github.com/lbjlaq/Antigravity-Manager/releases\">GitHub Releases</a> 下载对应系统的包：</p>\n<table>\n<thead>\n<tr>\n<th>系统</th>\n<th>格式</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>macOS</strong></td>\n<td><code>.dmg</code></td>\n<td>支持 Apple Silicon &amp; Intel</td>\n</tr>\n<tr>\n<td><strong>Windows</strong></td>\n<td><code>.msi</code> 或 <code>.zip</code></td>\n<td>安装版或便携版</td>\n</tr>\n<tr>\n<td><strong>Linux</strong></td>\n<td><code>.deb</code> 或 <code>AppImage</code></td>\n<td>根据发行版选择</td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"配置\"><a href=\"#配置\" class=\"headerlink\" title=\"配置\"></a>配置</h2><h3 id=\"CC-Switch-配置\"><a href=\"#CC-Switch-配置\" class=\"headerlink\" title=\"CC-Switch 配置\"></a>CC-Switch 配置</h3><p><img src=\"/medias/loading.gif\" data-original=\"https://file.costalong.com/img/20260103205511054.png\" alt=\"CC-Switch 配置界面\"></p>\n<p><strong>主要功能</strong>：</p>\n<ul>\n<li>管理多个 Claude 账号</li>\n<li>快速切换当前使用的账号</li>\n<li>查看各账号的使用量统计</li>\n<li>支持快捷键切换</li>\n</ul>\n<h3 id=\"Antigravity-Tools-配置\"><a href=\"#Antigravity-Tools-配置\" class=\"headerlink\" title=\"Antigravity Tools 配置\"></a>Antigravity Tools 配置</h3><p><img src=\"/medias/loading.gif\" data-original=\"https://file.costalong.com/img/20260103205426774.png\" alt=\"Antigravity Tools 主界面\"></p>\n<p><img src=\"/medias/loading.gif\" data-original=\"https://file.costalong.com/img/20260103205632292.png\" alt=\"Antigravity Tools API 反代设置\"></p>\n<p><strong>关键配置项</strong>：</p>\n<ol>\n<li><strong>API 反代端口</strong>：默认 <code>8045</code>，可自定义</li>\n<li><strong>账号管理</strong>：通过 OAuth 添加多个 Claude 账号</li>\n<li><strong>负载均衡</strong>：可配置多账号轮询或指定账号</li>\n</ol>\n<hr>\n<h2 id=\"快速接入\"><a href=\"#快速接入\" class=\"headerlink\" title=\"快速接入\"></a>快速接入</h2><h3 id=\"步骤一：添加-Claude-账号（OAuth-授权）\"><a href=\"#步骤一：添加-Claude-账号（OAuth-授权）\" class=\"headerlink\" title=\"步骤一：添加 Claude 账号（OAuth 授权）\"></a>步骤一：添加 Claude 账号（OAuth 授权）</h3><ol>\n<li>打开 Antigravity Tools → “账号 / Accounts” → “添加账号” → “OAuth”</li>\n<li>点击生成的授权链接，链接会自动复制到剪贴板</li>\n<li>在浏览器中打开链接，完成 Claude 授权</li>\n<li>授权成功后浏览器显示 “✅ 授权成功!”</li>\n<li>应用自动完成后续流程；如未自动完成，点击”我已授权，继续”</li>\n</ol>\n<blockquote>\n<p><strong>注意</strong>：授权链接包含一次性回调端口，请使用弹窗里生成的最新链接。如果授权时应用未运行，浏览器可能会提示 <code>localhost refused connection</code>。</p>\n</blockquote>\n<h3 id=\"步骤二：启动-API-反代服务\"><a href=\"#步骤二：启动-API-反代服务\" class=\"headerlink\" title=\"步骤二：启动 API 反代服务\"></a>步骤二：启动 API 反代服务</h3><ol>\n<li>在 Antigravity Tools 中打开 “API 反代” 页面</li>\n<li>点击 “开启服务”</li>\n<li>确认服务状态为 “运行中”</li>\n</ol>\n<h3 id=\"步骤三：配置-Claude-Code-环境变量\"><a href=\"#步骤三：配置-Claude-Code-环境变量\" class=\"headerlink\" title=\"步骤三：配置 Claude Code 环境变量\"></a>步骤三：配置 Claude Code 环境变量</h3><p>在终端中设置环境变量：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export ANTHROPIC_API_KEY=\"sk-antigravity\"\nexport ANTHROPIC_BASE_URL=\"http://127.0.0.1:8045\"</code></pre>\n\n<p><strong>持久化配置</strong>（添加到 shell 配置文件）：</p>\n<p><strong>Zsh</strong> (~/.zshrc)：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">echo 'export ANTHROPIC_API_KEY=\"sk-antigravity\"' &gt;&gt; ~/.zshrc\necho 'export ANTHROPIC_BASE_URL=\"http://127.0.0.1:8045\"' &gt;&gt; ~/.zshrc\nsource ~/.zshrc</code></pre>\n\n<p><strong>Bash</strong> (~/.bashrc)：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">echo 'export ANTHROPIC_API_KEY=\"sk-antigravity\"' &gt;&gt; ~/.bashrc\necho 'export ANTHROPIC_BASE_URL=\"http://127.0.0.1:8045\"' &gt;&gt; ~/.bashrc\nsource ~/.bashrc</code></pre>\n\n<h3 id=\"步骤四：启动-Claude-Code\"><a href=\"#步骤四：启动-Claude-Code\" class=\"headerlink\" title=\"步骤四：启动 Claude Code\"></a>步骤四：启动 Claude Code</h3><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">claude</code></pre>\n\n<p>现在 Claude Code 会通过 Antigravity Tools 的本地代理服务连接到 Claude API。</p>\n<hr>\n<h2 id=\"工作流程示例\"><a href=\"#工作流程示例\" class=\"headerlink\" title=\"工作流程示例\"></a>工作流程示例</h2><h3 id=\"日常使用流程\"><a href=\"#日常使用流程\" class=\"headerlink\" title=\"日常使用流程\"></a>日常使用流程</h3><pre class=\"language-none\"><code class=\"language-none\">1. 启动 Antigravity Tools → 开启 API 反代\n2. (可选) 使用 CC-Switch 选择要使用的账号\n3. 打开终端 → 运行 claude\n4. 开始编程！</code></pre>\n\n<h3 id=\"切换账号\"><a href=\"#切换账号\" class=\"headerlink\" title=\"切换账号\"></a>切换账号</h3><p><strong>使用 CC-Switch</strong>：</p>\n<ul>\n<li>点击托盘图标，选择要切换的账号</li>\n<li>或使用配置的快捷键快速切换</li>\n</ul>\n<p><strong>使用 Antigravity Tools</strong>：</p>\n<ul>\n<li>在账号管理页面选择默认账号</li>\n<li>或配置负载均衡策略</li>\n</ul>\n<hr>\n<h2 id=\"常见问题排查\"><a href=\"#常见问题排查\" class=\"headerlink\" title=\"常见问题排查\"></a>常见问题排查</h2><h3 id=\"macOS-提示”应用已损坏，无法打开”\"><a href=\"#macOS-提示”应用已损坏，无法打开”\" class=\"headerlink\" title=\"macOS 提示”应用已损坏，无法打开”\"></a>macOS 提示”应用已损坏，无法打开”</h3><p>由于 macOS 的安全机制，非 App Store 下载的应用可能触发此提示。</p>\n<p><strong>解决方法 1 - 命令行修复</strong> (推荐)：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">sudo xattr -rd com.apple.quarantine \"/Applications/Antigravity Tools.app\"</code></pre>\n\n<p><strong>解决方法 2 - Homebrew 安装时规避</strong>：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">brew install --cask --no-quarantine antigravity-tools</code></pre>\n\n<h3 id=\"Claude-Code-连接失败\"><a href=\"#Claude-Code-连接失败\" class=\"headerlink\" title=\"Claude Code 连接失败\"></a>Claude Code 连接失败</h3><ol>\n<li><strong>检查 Antigravity 服务状态</strong>：确认 API 反代服务已启动</li>\n<li><strong>检查端口</strong>：确认 <code>ANTHROPIC_BASE_URL</code> 端口与 Antigravity 设置一致</li>\n<li><strong>检查环境变量</strong>：运行 <code>echo $ANTHROPIC_BASE_URL</code> 确认已设置</li>\n<li><strong>检查账号</strong>：确认至少有一个有效的 Claude 账号已授权</li>\n</ol>\n<h3 id=\"环境变量未生效\"><a href=\"#环境变量未生效\" class=\"headerlink\" title=\"环境变量未生效\"></a>环境变量未生效</h3><ul>\n<li>确认添加到正确的 shell 配置文件（<code>.zshrc</code> 或 <code>.bashrc</code>）</li>\n<li>运行 <code>source ~/.zshrc</code> 或重新打开终端</li>\n<li>使用 <code>env | grep ANTHROPIC</code> 验证设置</li>\n</ul>\n<hr>\n<h2 id=\"使用技巧\"><a href=\"#使用技巧\" class=\"headerlink\" title=\"使用技巧\"></a>使用技巧</h2><h3 id=\"1-开机自启\"><a href=\"#1-开机自启\" class=\"headerlink\" title=\"1. 开机自启\"></a>1. 开机自启</h3><ul>\n<li><strong>Antigravity Tools</strong>：在设置中开启”开机自启动”</li>\n<li><strong>CC-Switch</strong>：在设置中开启”登录时启动”</li>\n</ul>\n<h3 id=\"2-多账号负载均衡\"><a href=\"#2-多账号负载均衡\" class=\"headerlink\" title=\"2. 多账号负载均衡\"></a>2. 多账号负载均衡</h3><p>在 Antigravity Tools 中添加多个账号后，可配置：</p>\n<ul>\n<li><strong>轮询模式</strong>：请求自动分配到不同账号</li>\n<li><strong>指定模式</strong>：手动选择使用的账号</li>\n</ul>\n<h3 id=\"3-快捷操作\"><a href=\"#3-快捷操作\" class=\"headerlink\" title=\"3. 快捷操作\"></a>3. 快捷操作</h3><ul>\n<li>使用 CC-Switch 的托盘图标快速切换账号</li>\n<li>配置全局快捷键实现一键切换</li>\n</ul>\n<h3 id=\"4-使用量监控\"><a href=\"#4-使用量监控\" class=\"headerlink\" title=\"4. 使用量监控\"></a>4. 使用量监控</h3><p>通过 CC-Switch 或 Antigravity Tools 查看：</p>\n<ul>\n<li>各账号的 API 调用次数</li>\n<li>Token 使用量统计</li>\n<li>剩余配额</li>\n</ul>\n<hr>\n<h2 id=\"相关链接\"><a href=\"#相关链接\" class=\"headerlink\" title=\"相关链接\"></a>相关链接</h2><ul>\n<li><a href=\"https://docs.anthropic.com/en/docs/claude-code\">Claude Code 官方文档</a></li>\n<li><a href=\"https://github.com/farion1231/cc-switch\">CC-Switch GitHub</a></li>\n<li><a href=\"https://github.com/lbjlaq/Antigravity-Manager\">Antigravity Tools GitHub</a></li>\n<li><a href=\"https://docs.anthropic.com/en/api\">Anthropic API 文档</a></li>\n</ul>\n",
            "tags": [
                "ai"
            ]
        },
        {
            "id": "https://costalong.com/2025/12/31/moment-2026-01-01-newyear/",
            "url": "https://costalong.com/2025/12/31/moment-2026-01-01-newyear/",
            "title": "新年第一天",
            "date_published": "2025-12-31T16:05:00.000Z",
            "content_html": "<p>2026，新的开始。今年的目标：多写博客、坚持运动、学一门新语言。</p>\n",
            "tags": [
                "moments"
            ]
        },
        {
            "id": "https://costalong.com/2024/11/19/git/git-commit/",
            "url": "https://costalong.com/2024/11/19/git/git-commit/",
            "title": "Git Commit",
            "date_published": "2024-11-19T02:28:21.000Z",
            "content_html": "<h2 id=\"Git-Commit-介绍\"><a href=\"#Git-Commit-介绍\" class=\"headerlink\" title=\"Git Commit 介绍\"></a><strong>Git Commit 介绍</strong></h2><p><code>git commit</code> 命令用于将工作区或暂存区的更改提交到版本库，形成可追踪的历史记录点。<br>它是 Git 工作流的核心操作，常用来保存代码的进展或完成阶段性开发。</p>\n<hr>\n<h2 id=\"基本用法\"><a href=\"#基本用法\" class=\"headerlink\" title=\"基本用法\"></a><strong>基本用法</strong></h2><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 将暂存区内容提交到版本库，进入编辑器填写提交信息\ngit commit\n\n# 将特定文件提交到版本库（需已被跟踪）\ngit commit [file1] [file2] [...]\n\n# 将暂存区内容提交到版本库，同时直接添加提交信息（无需打开编辑器）\ngit commit -m \"&lt;message&gt;\"\n\n# 跳过 git add，将所有已跟踪文件的更改提交到版本库\ngit commit -am \"&lt;message&gt;\"\n\n# 替代上一次提交：\n# - 若代码无更改，用于修改提交信息\n# - 若代码有更改，将其合并到上一次提交\ngit commit --amend -m \"&lt;message&gt;\"</code></pre>\n\n<hr>\n<h2 id=\"常用选项\"><a href=\"#常用选项\" class=\"headerlink\" title=\"常用选项\"></a><strong>常用选项</strong></h2><table>\n<thead>\n<tr>\n<th>参数</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>-m &lt;message&gt;</code></td>\n<td>指定提交信息，避免打开编辑器</td>\n</tr>\n<tr>\n<td><code>-a</code></td>\n<td>提交所有已修改或删除的文件（限已被 Git 跟踪的文件）</td>\n</tr>\n<tr>\n<td><code>--amend</code></td>\n<td>修改最近一次提交（包括内容或提交信息）</td>\n</tr>\n<tr>\n<td><code>--no-edit</code></td>\n<td>不修改提交信息，直接沿用上次的提交消息</td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"修改历史提交\"><a href=\"#修改历史提交\" class=\"headerlink\" title=\"修改历史提交\"></a><strong>修改历史提交</strong></h2><h3 id=\"修改最近一次提交\"><a href=\"#修改最近一次提交\" class=\"headerlink\" title=\"修改最近一次提交\"></a><strong>修改最近一次提交</strong></h3><p>如果需要修改最近一次提交的内容或提交信息：</p>\n<ol>\n<li>编辑文件，完成更改后添加到暂存区：<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git add &lt;file-name&gt;</code></pre></li>\n<li>使用 <code>--amend</code> 更新提交：<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git commit --amend</code></pre></li>\n</ol>\n<h3 id=\"修改更早的提交\"><a href=\"#修改更早的提交\" class=\"headerlink\" title=\"修改更早的提交\"></a><strong>修改更早的提交</strong></h3><p>通过交互式变基（<code>git rebase -i</code>）修改历史提交：</p>\n<ol>\n<li><p>启动交互式变基<br>例如，修改最近 3 次提交：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git rebase -i HEAD~3</code></pre>\n</li>\n<li><p>编辑器中显示提交历史：</p>\n<pre class=\"language-none\"><code class=\"language-none\">pick 1234567 feat: 添加新功能\npick 89abcde fix: 修复启动问题\npick def7890 docs: 更新文档</code></pre>\n</li>\n<li><p>将需要修改的提交改为 <code>edit</code>：</p>\n<pre class=\"language-none\"><code class=\"language-none\">pick 1234567 feat: 添加新功能\nedit 89abcde fix: 修复启动问题\npick def7890 docs: 更新文档</code></pre>\n</li>\n<li><p>保存并退出。</p>\n</li>\n<li><p>修改文件并更新提交：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git add &lt;file-name&gt;\ngit commit --amend</code></pre>\n</li>\n<li><p>完成修改后继续变基：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git rebase --continue</code></pre></li>\n</ol>\n<hr>\n<h2 id=\"最佳实践\"><a href=\"#最佳实践\" class=\"headerlink\" title=\"最佳实践\"></a><strong>最佳实践</strong></h2><ol>\n<li><p><strong>编写清晰的提交信息</strong><br>遵循 <a href=\"https://www.conventionalcommits.org/\">Conventional Commits</a> 格式：</p>\n<pre class=\"language-none\"><code class=\"language-none\">&lt;type&gt;(&lt;scope&gt;): &lt;subject&gt;\n&lt;空行&gt;\n&lt;body&gt;\n&lt;空行&gt;\n&lt;footer&gt;</code></pre>\n\n<p><strong>示例：</strong></p>\n<pre class=\"language-none\"><code class=\"language-none\">feat(app): 添加用户登录功能\n\n实现用户登录和身份验证。解决了之前登录状态丢失的问题。\n\nCloses #123</code></pre>\n</li>\n<li><p><strong>保持提交单一职责</strong><br>每次提交只包含一种逻辑更改（例如，修复一个 bug 或实现一个功能），避免混合提交。</p>\n</li>\n<li><p><strong>修改已推送提交时的注意事项</strong></p>\n<ul>\n<li>若已推送到远程分支，需使用 <code>git push --force</code> 强制覆盖。</li>\n<li>与团队协作时应谨慎，确保其他人未基于该提交工作。</li>\n</ul>\n</li>\n</ol>\n<hr>\n<h2 id=\"总结\"><a href=\"#总结\" class=\"headerlink\" title=\"总结\"></a><strong>总结</strong></h2><ul>\n<li><strong><code>git commit</code> 是 Git 的核心命令</strong>，通过合适的参数选项可以更高效地管理提交。</li>\n<li>选择适合的用法（如 <code>--amend</code> 或 <code>rebase</code>）来优化历史提交，同时注意团队协作中的代码一致性。</li>\n</ul>\n",
            "tags": [
                "Tool",
                "git"
            ]
        },
        {
            "id": "https://costalong.com/2024/11/19/git/git-rollback/",
            "url": "https://costalong.com/2024/11/19/git/git-rollback/",
            "title": "git 回滚",
            "date_published": "2024-11-19T02:28:21.000Z",
            "content_html": "<p>在 Git 中，<code>revert</code>、<code>reset</code> 和 <code>rebase</code> 都是用来修改提交记录或状态的，但它们的使用场景和效果各不相同。</p>\n<hr>\n<h2 id=\"恢复提交：git-revert\"><a href=\"#恢复提交：git-revert\" class=\"headerlink\" title=\"恢复提交：git revert\"></a><strong>恢复提交：<code>git revert</code></strong></h2><p><code>git revert</code> 命令的实际结果类似于 <code>reset</code>，但它的方法不同。<code>reset</code> 是通过移动分支指针撤销更改，而 <code>revert</code> 是通过添加一个新的提交取消更改。</p>\n<h3 id=\"用法\"><a href=\"#用法\" class=\"headerlink\" title=\"用法\"></a><strong>用法</strong></h3><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git revert &lt;commit-id&gt;</code></pre>\n\n<p>例如，撤销最近一次提交：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git revert HEAD</code></pre>\n\n<h3 id=\"特点\"><a href=\"#特点\" class=\"headerlink\" title=\"特点\"></a><strong>特点</strong></h3><ul>\n<li>安全，不会修改历史记录。</li>\n<li>适合用于团队协作和已推送到远程的分支。</li>\n</ul>\n<hr>\n<h2 id=\"回退提交：git-reset\"><a href=\"#回退提交：git-reset\" class=\"headerlink\" title=\"回退提交：git reset\"></a><strong>回退提交：<code>git reset</code></strong></h2><p><code>git reset</code> 用于改变分支指针，并决定是否保留工作区和暂存区的更改。</p>\n<h3 id=\"步骤-1：查看提交历史\"><a href=\"#步骤-1：查看提交历史\" class=\"headerlink\" title=\"步骤 1：查看提交历史\"></a><strong>步骤 1：查看提交历史</strong></h3><p>使用 <code>git log</code> 查询需要退回的提交 ID：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git log</code></pre>\n\n<h3 id=\"步骤-2：选择回退模式\"><a href=\"#步骤-2：选择回退模式\" class=\"headerlink\" title=\"步骤 2：选择回退模式\"></a><strong>步骤 2：选择回退模式</strong></h3><ol>\n<li><p><strong>软回退 (<code>--soft</code>)</strong><br>仅回退分支指针，保留暂存区和工作区的更改。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git reset --soft &lt;commit-id&gt;</code></pre>\n</li>\n<li><p><strong>混合回退 (<code>--mixed</code>)</strong><br>回退分支指针，清除暂存区，保留工作区的更改。（这是默认模式）</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git reset --mixed &lt;commit-id&gt;</code></pre>\n</li>\n<li><p><strong>硬回退 (<code>--hard</code>)</strong><br>回退分支指针，同时清除暂存区和工作区的所有更改。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git reset --hard &lt;commit-id&gt;</code></pre></li>\n</ol>\n<h3 id=\"推送到远程\"><a href=\"#推送到远程\" class=\"headerlink\" title=\"推送到远程\"></a><strong>推送到远程</strong></h3><p>如果需要将本地的变更覆盖远程分支：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git push --force</code></pre>\n\n<hr>\n<h2 id=\"变基：git-rebase\"><a href=\"#变基：git-rebase\" class=\"headerlink\" title=\"变基：git rebase\"></a><strong>变基：<code>git rebase</code></strong></h2><p><code>git rebase</code> 用于重新整理提交历史，将当前分支的更改重新应用到另一个基础提交上。</p>\n<h3 id=\"场景：分支变基\"><a href=\"#场景：分支变基\" class=\"headerlink\" title=\"场景：分支变基\"></a><strong>场景：分支变基</strong></h3><p>假设有以下分支：</p>\n<ul>\n<li><strong>master</strong>: <code>C4 -&gt; C2 -&gt; C1 -&gt; C0</code></li>\n<li><strong>feature</strong>: <code>C5 -&gt; C3 -&gt; C2 -&gt; C1 -&gt; C0</code></li>\n</ul>\n<p>要将 <code>feature</code> 分支的提交重新基于 <code>master</code> 分支：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git checkout feature\ngit rebase master</code></pre>\n\n<p>变基后的结果：</p>\n<ul>\n<li><strong>master</strong>: <code>C4 -&gt; C2 -&gt; C1 -&gt; C0</code></li>\n<li><strong>feature</strong>: <code>C5' -&gt; C3' -&gt; C4 -&gt; C2 -&gt; C1 -&gt; C0</code></li>\n</ul>\n<h3 id=\"交互式变基\"><a href=\"#交互式变基\" class=\"headerlink\" title=\"交互式变基\"></a><strong>交互式变基</strong></h3><p>可以通过 <code>-i</code> 参数合并、修改或删除提交：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git rebase -i &lt;base-branch&gt;</code></pre>\n\n<p>编辑器中提供以下选项：</p>\n<ul>\n<li><code>pick</code>: 保留提交。</li>\n<li><code>squash</code>: 合并当前提交到上一个提交。</li>\n<li><code>edit</code>: 修改提交内容。</li>\n</ul>\n<hr>\n<h2 id=\"恢复历史：git-reflog\"><a href=\"#恢复历史：git-reflog\" class=\"headerlink\" title=\"恢复历史：git reflog\"></a><strong>恢复历史：<code>git reflog</code></strong></h2><p><code>git reflog</code> 用于查看分支操作历史。它记录了所有分支指针的变动，帮助找回丢失的提交。</p>\n<h3 id=\"用法-1\"><a href=\"#用法-1\" class=\"headerlink\" title=\"用法\"></a><strong>用法</strong></h3><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git reflog</code></pre>\n\n<p>示例输出：</p>\n<pre class=\"language-none\"><code class=\"language-none\">e309ee8 (HEAD -&gt; master) HEAD@{0}: pull: Fast-forward\n30c0e5e HEAD@{1}: commit: fix: 优化代码\n9429f03 HEAD@{2}: commit: fix: 优化代码\n1bdcafe HEAD@{3}: commit: fix: 修改配置</code></pre>\n\n<p>如果误操作（如 <code>git reset --hard</code>），可以通过 <code>git reflog</code> 找到目标提交并恢复：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git reset --hard &lt;commit-id&gt;</code></pre>\n\n<hr>\n<h2 id=\"对比总结\"><a href=\"#对比总结\" class=\"headerlink\" title=\"对比总结\"></a><strong>对比总结</strong></h2><table>\n<thead>\n<tr>\n<th>功能</th>\n<th><code>git revert</code></th>\n<th><code>git reset</code></th>\n<th><code>git rebase</code></th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>作用</strong></td>\n<td>撤销提交，创建新提交</td>\n<td>改变提交历史</td>\n<td>重组提交历史</td>\n</tr>\n<tr>\n<td><strong>安全性</strong></td>\n<td>安全，适合已推送分支</td>\n<td>高风险，慎用于远程分支</td>\n<td>高风险，慎用于远程分支</td>\n</tr>\n<tr>\n<td><strong>适用场景</strong></td>\n<td>团队协作，回滚指定提交</td>\n<td>修改本地提交</td>\n<td>清理历史、线性合并</td>\n</tr>\n</tbody></table>\n<p>选择合适的工具，确保团队协作无缝进行！</p>\n",
            "tags": [
                "Tool",
                "git"
            ]
        },
        {
            "id": "https://costalong.com/2024/07/16/go/tests/gomock/",
            "url": "https://costalong.com/2024/07/16/go/tests/gomock/",
            "title": "Gomock进行单元测试",
            "date_published": "2024-07-16T15:10:12.000Z",
            "content_html": "<h2 id=\"用法\"><a href=\"#用法\" class=\"headerlink\" title=\"用法\"></a>用法</h2><p>在 mockgen 命令中,支持两种生成模式：</p>\n<ol>\n<li>source：从源文件生成 mock 接口（通过 -source 启用）</li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">mockgen -source=foo.go [other options]</code></pre>\n\n<ol start=\"2\">\n<li>reflect：通过使用反射程序来生成 mock 接口.它通过传递两个非标志参数来启用：导入路径和逗号分隔的接口列表</li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">mockgen database/sql/driver Conn,Driver</code></pre>\n\n<p>两种方式生成的 mock 代码并没有什么区别.因此选择合适的就可以了</p>\n<h2 id=\"写测试用例\"><a href=\"#写测试用例\" class=\"headerlink\" title=\"写测试用例\"></a>写测试用例</h2><p><strong>1. 步骤:</strong></p>\n<ol>\n<li>想清楚整体逻辑</li>\n<li>定义想要（模拟）依赖项的 interface（接口）</li>\n<li>使用 mockgen 命令对所需 mock 的 interface 生成 mock 文件</li>\n<li>编写单元测试的逻辑,在测试中使用 mock</li>\n<li>进行单元测试的验证</li>\n</ol>\n<p><strong>2. 目录：</strong></p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\">├── mock\n├── person\n│   └── male.go\n└── user\n    ├── user.go\n    └── user_test.go</code></pre>\n\n<p><strong>2. 编写：</strong></p>\n<ol>\n<li><p>定义 interface </p>\n<p> 打开 person/male.go 文件,写入以下内容：</p>\n</li>\n</ol>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">package person\n\ntype Male interface {\n\tGet(id int64) error\n}</code></pre>\n\n<ol start=\"2\">\n<li><p>调用方法</p>\n<p>打开 user/user.go 文件,写入以下内容</p>\n</li>\n</ol>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">package user\n\nimport \"github.com/EDDYCJY/mockd/person\"\n\ntype User struct {\n\tPerson person.Male\n}\n\nfunc NewUser(p person.Male) *User {\n\treturn &amp;User{Person: p}\n}\n\nfunc (u *User) GetUserInfo(id int64) error {\n\treturn u.Person.Get(id)\n}</code></pre>\n\n<ol start=\"3\">\n<li>生成 mock 文件</li>\n</ol>\n<p>回到 mockd/ 的根目录下,执行以下命令</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">$ mockgen -source=./person/male.go -destination=./mock/male_mock.go -package=mock</code></pre>\n\n<p>在执行完毕后,可以发现 mock/ 目录下多出了 male_mock.go 文件,这就是 mock 文件.那么命令中的指令又分别有什么用呢？如下：</p>\n<pre><code>-source：设置需要模拟（mock）的接口文件\n\n-destination：设置 mock 文件输出的地方,若不设置则打印到标准输出中\n\n-package：设置 mock 文件的包名,若不设置则为 mock_ 前缀加上文件名（如本文的包名会为 mock_person）\n</code></pre>\n<p>想了解更多的指令符,可参见 官方文档: <a href=\"https://github.com/golang/mock#running-mockgen\">https://github.com/golang/mock#running-mockgen</a></p>\n<p><strong>输出的 mock 文件</strong></p>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">// Code generated by MockGen. DO NOT EDIT.\n// Source: ./person/male.go\n\n// Package mock is a generated GoMock package.\npackage mock\n\nimport (\n\tgomock \"github.com/golang/mock/gomock\"\n\treflect \"reflect\"\n)\n\n// MockMale is a mock of Male interface\ntype MockMale struct {\n\tctrl     *gomock.Controller\n\trecorder *MockMaleMockRecorder\n}\n\n// MockMaleMockRecorder is the mock recorder for MockMale\ntype MockMaleMockRecorder struct {\n\tmock *MockMale\n}\n\n// NewMockMale creates a new mock instance\nfunc NewMockMale(ctrl *gomock.Controller) *MockMale {\n\tmock := &amp;MockMale{ctrl: ctrl}\n\tmock.recorder = &amp;MockMaleMockRecorder{mock}\n\treturn mock\n}\n\n// EXPECT returns an object that allows the caller to indicate expected use\nfunc (m *MockMale) EXPECT() *MockMaleMockRecorder {\n\treturn m.recorder\n}\n\n// Get mocks base method\nfunc (m *MockMale) Get(id int64) error {\n\tret := m.ctrl.Call(m, \"Get\", id)\n\tret0, _ := ret[0].(error)\n\treturn ret0\n}\n\n// Get indicates an expected call of Get\nfunc (mr *MockMaleMockRecorder) Get(id interface{}) *gomock.Call {\n\treturn mr.mock.ctrl.RecordCallWithMethodType(mr.mock, \"Get\", reflect.TypeOf((*MockMale)(nil).Get), id)\n}</code></pre>\n\n<p><strong>4. 测试用例</strong></p>\n<p>打开 user/user_test.go 文件,写入以下内容：</p>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">package user\n\nimport (\n\t\"testing\"\n\n\t\"github.com/EDDYCJY/mockd/mock\"\n\n\t\"github.com/golang/mock/gomock\"\n)\n\nfunc TestUser_GetUserInfo(t *testing.T) {\n\tctl := gomock.NewController(t)\n\tdefer ctl.Finish()\n\n\tvar id int64 = 1\n\tmockMale := mock.NewMockMale(ctl)\n\tgomock.InOrder(\n\t\tmockMale.EXPECT().Get(id).Return(nil),\n\t)\n\n\tuser := NewUser(mockMale)\n\terr := user.GetUserInfo(id)\n\tif err != nil {\n\t\tt.Errorf(\"user.GetUserInfo err: %v\", err)\n\t}\n}</code></pre>\n\n<ol>\n<li><p>gomock.NewController：返回 gomock.Controller,它代表 mock 生态系统中的顶级控件.定义了 mock 对象的范围,生命周期和期待值.另外它在多个 goroutine 中是安全的</p>\n</li>\n<li><p>mock.NewMockMale：创建一个新的 mock 实例</p>\n</li>\n<li><p>gomock.InOrder：声明给定的调用应按顺序进行（是对 gomock.After 的二次封装）</p>\n</li>\n<li><p>mockMale.EXPECT().Get(id).Return(nil)：这里有三个步骤,EXPECT()返回一个允许调用者设置期望和返回值的对象.Get(id) 是设置入参并调用 mock 实例中的方法.Return(nil) 是设置先前调用的方法出参.简单来说,就是设置入参并调用,最后设置返回值</p>\n</li>\n<li><p>NewUser(mockMale)：创建 User 实例,值得注意的是,在这里注入了 mock 对象,因此实际在随后的 user.GetUserInfo(id) 调用（入参：id 为 1）中.它调用的是我们事先模拟好的 mock 方法</p>\n</li>\n<li><p>ctl.Finish()：进行 mock 用例的期望值断言,一般会使用 defer 延迟执行,以防止我们忘记这一操作</p>\n</li>\n</ol>\n<h2 id=\"测试\"><a href=\"#测试\" class=\"headerlink\" title=\"测试\"></a>测试</h2><p>回到 mockd/ 的根目录下,执行以下命令</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">$ go test ./user</code></pre>\n\n<p>看到这样的结果,就大功告成啦！您可以自己调整一下 Return() 的返回值,以此得到不一样的测试结果哦 </p>\n<ol>\n<li>测试覆盖率</li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">go test -cover ./user</code></pre>\n\n<p>可通过设置 -cover 标志符来开启覆盖率的统计,展示内容为 coverage: 100.0%.</p>\n<ol start=\"2\">\n<li>可视化界面</li>\n</ol>\n<p>生成测试覆盖率的 profile 文件</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">go test ./... -coverprofile=cover.out</code></pre>\n\n<p>利用 profile 文件生成可视化界面</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">go tool cover -html=cover.out</code></pre>\n\n<h2 id=\"常用-mock-方法\"><a href=\"#常用-mock-方法\" class=\"headerlink\" title=\"常用 mock 方法\"></a>常用 mock 方法</h2><ol>\n<li><p>调用方法<br> Call.Do()：声明在匹配时要运行的操作</p>\n<p> Call.DoAndReturn()：声明在匹配调用时要运行的操作,并且模拟返回该函数的返回值</p>\n<p> Call.MaxTimes()：设置最大的调用次数为 n 次</p>\n<p> Call.MinTimes()：设置最小的调用次数为 n 次</p>\n<p> Call.AnyTimes()：允许调用次数为 0 次或更多次</p>\n<p> Call.Times()：设置调用次数为 n 次</p>\n</li>\n<li><p>参数匹配</p>\n<p> gomock.Any()：匹配任意值</p>\n<p> gomock.Eq()：通过反射匹配到指定的类型值,而不需要手动设置<br> gomock.Nil()：返回 nil</p>\n</li>\n</ol>\n<h2 id=\"生成多个-mock-文件\"><a href=\"#生成多个-mock-文件\" class=\"headerlink\" title=\"生成多个 mock 文件\"></a>生成多个 mock 文件</h2><p> 可能会想一条条命令生成 mock 文件,岂不得崩溃？</p>\n<p>当然,官方提供了更方便的方式,我们可以利用 go:generate 来完成批量处理的功能</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">go generate [-run regexp] [-n] [-v] [-x] [build flags] [file.go... | packages]</code></pre>\n\n<ol>\n<li>修改 interface 方法</li>\n</ol>\n<p>打开 person/male.go 文件,修改为以下内容：</p>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">package person\n\n//go:generate mockgen -destination=../mock/male_mock.go -package=mock github.com/EDDYCJY/mockd/person Male\n\ntype Male interface {\n\tGet(id int64) error\n}</code></pre>\n\n<p>关注到 go:generate 这条语句,可分为以下部分：</p>\n<ol>\n<li>声明 //go:generate （注意不要留空格）</li>\n<li>使用 mockgen 命令</li>\n<li>定义 -destination</li>\n<li>定义 -package</li>\n<li>定义 source,此处为 person 的包路径</li>\n<li>定义 interfaces,此处为 Male</li>\n</ol>\n<p> <strong>注意:</strong> 上面注释中的mockgen命令因为在调用时其当前目录是person，所以在-destination参数中我们需要指定../mocks/作为mock生成代码的输出目录。</p>\n<p>有了上面的go:generate注释，我们要生成所有mock只需在项目的根目录下运行下面这条命令即可，是不是非常方便？</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">go generate ./...</code></pre>\n\n<p><strong>注意:</strong> 注释中的//和go:generate之间不能有空格，这样go generate才可以把注释当做一条命令来处理。</p>\n<p><strong>总结：</strong><br>在单元测试这一环,gomock 给我们提供了极大的便利.能够 mock 掉许许多多的依赖项,其中还有很多的使用方式和功能.您可以 mark 住后详细阅读下官方文档,记忆会更深刻 </p>\n",
            "tags": [
                "golang",
                "golang",
                "test"
            ]
        },
        {
            "id": "https://costalong.com/2024/07/16/go/package/semaphore/",
            "url": "https://costalong.com/2024/07/16/go/package/semaphore/",
            "title": "semaphore 信号量",
            "date_published": "2024-07-16T15:00:20.000Z",
            "content_html": "<p>源代码:  golang.org/x/sync/semaphore</p>\n<h1 id=\"semaphore-信号量\"><a href=\"#semaphore-信号量\" class=\"headerlink\" title=\"semaphore  信号量\"></a>semaphore  信号量</h1><p>源代码:  golang.org/x/sync/semaphore</p>\n<ol>\n<li>创建</li>\n</ol>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">// NewWeighted使用给定的值创建一个新的加权信号量\n// 并发访问的最大组合权重。\nfunc NewWeighted(n int64) *Weighted {\n   w := &amp;Weighted{size: n}\n   return w\n}</code></pre>\n\n<p>w 结构</p>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">// Weighted provides a way to bound concurrent access to a resource.\n// The callers can request access with a given weight.\n// NewWeighted使用给定的值创建一个新的加权信号量\n// 并发访问的最大组合权重。\ntype Weighted struct {\n    size    int64  //权重总数量\n    cur     int64 //当前权重数量\n    mu      sync.Mutex //全局互斥锁\n    waiters list.List //双向链表,存waiter\n}</code></pre>\n\n<p>waiter 结构</p>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">type waiter struct {\n    n     int64 //需要权重数量\n    ready chan&lt;- struct{} // Closed when semaphore acquired. //通信channel ,无缓冲\n}</code></pre>\n\n<p>Acquire 方法:</p>\n<p>阻塞的获取指定权种的资源，如果没有空闲的资源，会进去休眠等待。</p>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">// Acquire获取权重为n的信号量，阻塞直到资源可用或ctx完成。\n// 成功时，返回nil。失败时返回 ctx.Err（）并保持信号量不变。\n// 如果ctx已经完成，则Acquire仍然可以成功执行而不会阻塞\nfunc (s *Weighted) Acquire(ctx context.Context, n int64) error {\n    s.mu.Lock()\n        // fast path, 如果有足够的资源，都不考虑ctx.Done的状态，将cur加上n就返回\n    if s.size-s.cur &gt;= n &amp;&amp; s.waiters.Len() == 0 {\n      s.cur += n\n      s.mu.Unlock()\n      return nil\n    }\n  \n        // 如果是不可能完成的任务，请求的资源数大于能提供的最大的资源数\n    if n &gt; s.size {\n      s.mu.Unlock()\n            // 依赖ctx的状态返回，否则一直等待\n      &lt;-ctx.Done()\n      return ctx.Err()\n    }\n  \n        // 否则就需要把调用者加入到等待队列中\n        // 创建了一个ready chan,以便被通知唤醒\n    ready := make(chan struct{})\n    \t// 组装waiter\n    w := waiter{n: n, ready: ready}\n    // 插入waiters中\n    elem := s.waiters.PushBack(w)\n    s.mu.Unlock()\n  \n\n        // 等待\n    select {\n    case &lt;-ctx.Done(): // context的Done被关闭\n      err := ctx.Err()\n      s.mu.Lock()\n      select {\n      case &lt;-ready: // 如果被唤醒了，忽略ctx的状态\n        err = nil\n      default: 通知waiter\n        isFront := s.waiters.Front() == elem\n        s.waiters.Remove(elem)\n        // 通知其它的waiters,检查是否有足够的资源\n        if isFront &amp;&amp; s.size &gt; s.cur {\n          s.notifyWaiters()\n        }\n      }\n      s.mu.Unlock()\n      return err\n    case &lt;-ready: // 被唤醒了\n      return nil\n    }\n  }</code></pre>\n\n<h4 id=\"TryAcquire\"><a href=\"#TryAcquire\" class=\"headerlink\" title=\"TryAcquire\"></a>TryAcquire</h4><p>非阻塞地获取指定权重的资源，如果当前没有空闲资源，会直接返回<code>false</code>。</p>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">// TryAcquire获取权重为n的信号量而不阻塞。\n// 成功时返回true。 失败时，返回false并保持信号量不变。\nfunc (s *Weighted) TryAcquire(n int64) bool {\n\ts.mu.Lock()\n\tsuccess := s.size-s.cur &gt;= n &amp;&amp; s.waiters.Len() == 0\n\tif success {\n\t\ts.cur += n\n\t}\n\ts.mu.Unlock()\n\treturn success\n}\n</code></pre>\n\n\n\n<h4 id=\"Release-方法\"><a href=\"#Release-方法\" class=\"headerlink\" title=\"Release 方法\"></a>Release 方法</h4><p>用于释放指定权重的资源，如果有<code>waiters</code>则尝试去一一唤醒<code>waiter</code>。</p>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">// Release释放权值为n的信号量。\nfunc (s *Weighted) Release(n int64) {\n\ts.mu.Lock()\n\ts.cur -= n\n\t// cur的范围在[0 - size]\n\tif s.cur &lt; 0 {\n\t\ts.mu.Unlock()\n\t\tpanic(\"semaphore: bad release\")\n\t}\n\ts.notifyWaiters()\n\ts.mu.Unlock()\n}\n\nfunc (s *Weighted) notifyWaiters() {\n\t// 如果有阻塞的waiters，尝试去进行一一唤醒 \n\t// 唤醒的时候，先进先出，避免被资源比较大的waiter被饿死\n\tfor {\n\t\tnext := s.waiters.Front()\n\t\t// 已经没有waiter了\n\t\tif next == nil {\n\t\t\tbreak\n\t\t}\n\n\t\tw := next.Value.(waiter)\n\t\t// waiter需要的资源不足\n\t\tif s.size-s.cur &lt; w.n {\n\t\t\t// 没有足够的令牌供下一个waiter使用。我们可以继续（尝试\n\t\t\t// 查找请求较小的waiter），但在负载下可能会导致\n\t\t\t// 饥饿的大型请求；相反，我们留下所有剩余的waiter阻塞\n\t\t\t//\n\t\t\t// 考虑一个用作读写锁的信号量，带有N个令牌，N个reader和一位writer\n\t\t\t// 每个reader都可以通过Acquire（1）获取读锁。\n\t\t\t// writer写入可以通过Acquire（N）获得写锁定，但不包括所有的reader。\n\t\t\t// 如果我们允许读者在队列中前进，writer将会饿死-总是有一个令牌可供每个读者。\n\t\t\tbreak\n\t\t}\n\n\t\ts.cur += w.n\n\t\ts.waiters.Remove(next)\n\t\tclose(w.ready)\n\t}\n}</code></pre>\n\n<p>notifyWaiters 方法</p>\n<p>在<code>Acquire</code>和<code>Release</code>方法中都调用了<code>notifyWaiters</code></p>\n<pre class=\"language-go\" data-language=\"go\"><code class=\"language-go\">func (s *Weighted) notifyWaiters() {\n for {\n  // 获取等待调用者队列中的队员\n  next := s.waiters.Front()\n  // 没有要通知的调用者了\n  if next == nil {\n   break // No more waiters blocked.\n  }\n\n  // 断言出waiter信息\n  w := next.Value.(waiter)\n  if s.size-s.cur &lt; w.n {\n   // 没有足够资源为下一个调用者使用时，继续阻塞该调用者，遵循先进先出的原则，\n   // 避免需要资源数比较大的waiter被饿死\n   //\n   // 考虑一个场景，使用信号量作为读写锁，现有N个令牌，N个reader和一个writer\n   // 每个reader都可以通过Acquire（1）获取读锁，writer写入可以通过Acquire（N）获得写锁定\n   // 但不包括所有的reader，如果我们允许reader在队列中前进，writer将会饿死-总是有一个令牌可供每个reader\n   break\n  }\n\n  // 获取资源\n  s.cur += w.n\n  // 从waiter列表中移除\n  s.waiters.Remove(next)\n  // 使用channel的close机制唤醒waiter\n  close(w.ready)\n }\n}</code></pre>\n\n<p>需要注意一个点：唤醒<code>waiter</code>采用先进先出的原则，避免需要资源数比较大的waiter被饿死。</p>\n",
            "tags": [
                "golang",
                "golang",
                "package"
            ]
        },
        {
            "id": "https://costalong.com/2024/07/16/linux/netstat/",
            "url": "https://costalong.com/2024/07/16/linux/netstat/",
            "title": "Linux 网络工具 netstat",
            "date_published": "2024-07-16T02:30:48.000Z",
            "content_html": "<h1 id=\"netstat\"><a href=\"#netstat\" class=\"headerlink\" title=\"netstat\"></a>netstat</h1><h2 id=\"1-netstat-命令\"><a href=\"#1-netstat-命令\" class=\"headerlink\" title=\"1. netstat 命令\"></a>1. netstat 命令</h2><p>netstat 命令用于显示各种网络相关信息，如网络连接，路由表，接口状态 (Interface Statistics)，masquerade 连接，多播成员 (Multicast Memberships) 等等。</p>\n<h3 id=\"语法\"><a href=\"#语法\" class=\"headerlink\" title=\"语法\"></a>语法</h3><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat [-acCeFghilMnNoprstuvVwx][-A&lt;网络类型&gt;][--ip]</code></pre>\n\n<h3 id=\"参数\"><a href=\"#参数\" class=\"headerlink\" title=\"参数\"></a>参数</h3><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">-a或--all 显示所有连线中的Socket。\n-A&lt;网络类型&gt;或--&lt;网络类型&gt; 列出该网络类型连线中的相关地址。\n-c或--continuous 持续列出网络状态。\n-C或--cache 显示路由器配置的快取信息。\n-e或--extend 显示网络其他相关信息。\n-F或--fib 显示路由缓存。\n-g或--groups 显示多重广播功能群组组员名单。\n-h或--help 在线帮助。\n-i或--interfaces 显示网络界面信息表单。\n-l或--listening 显示监控中的服务器的Socket。\n-M或--masquerade 显示伪装的网络连线。\n-n或--numeric 直接使用IP地址，而不通过域名服务器。\n-N或--netlink或--symbolic 显示网络硬件外围设备的符号连接名称。\n-o或--timers 显示计时器。\n-p或--programs 显示正在使用Socket的程序识别码和程序名称。\n-r或--route 显示Routing Table。\n-s或--statistics 显示网络工作信息统计表。\n-t或--tcp 显示TCP传输协议的连线状况。\n-u或--udp 显示UDP传输协议的连线状况。\n-v或--verbose 显示指令执行过程。\n-V或--version 显示版本信息。\n-w或--raw 显示RAW传输协议的连线状况。\n-x或--unix 此参数的效果和指定\"-A unix\"参数相同。\n--ip或--inet 此参数的效果和指定\"-A inet\"参数相同。</code></pre>\n\n<h3 id=\"实例\"><a href=\"#实例\" class=\"headerlink\" title=\"实例\"></a>实例</h3><h4 id=\"1-显示网络状态\"><a href=\"#1-显示网络状态\" class=\"headerlink\" title=\"1. 显示网络状态\"></a>1. 显示网络状态</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -a</code></pre>\n\n<p>结果：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">Proto Recv-Q Send-Q Local Address           Foreign Address         State\ntcp        0      0 192.168.122.1:domain    0.0.0.0:*               LISTEN\ntcp        0      0 localhost:ipp           0.0.0.0:*               LISTEN\ntcp        0      0 localhost:35600         0.0.0.0:*               LISTEN\ntcp        0      0 127.0.0.54:domain       0.0.0.0:*               LISTEN\ntcp        0      0 localhost:31080         0.0.0.0:*               LISTEN\ntcp        0      0 localhost:31055         0.0.0.0:*               LISTEN\ntcp        0      0 hellotalk:domain        0.0.0.0:*               LISTEN\ntcp        0      0 127.0.0.53:domain       0.0.0.0:*               LISTEN\ntcp        0      0 172.16.0.112:50006      113.240.72.111:https    TIME_WAIT\ntcp        0      0 10.100.100.164:56010    10.40.2.11:27017        ESTABLISHED\ntcp        0      0 10.100.100.164:43846    10.40.2.5:27017         ESTABLISHED</code></pre>\n\n<p>字段说明：</p>\n<p>1.Proto：传输层协议 TCP 或 UDP</p>\n<p>2.Recv-Q：接收队列</p>\n<p>3.Send-Q：发送队列</p>\n<p>4.Local Address：本地地址</p>\n<p>5.Foreign Address：远程地址</p>\n<p>6.State：状态 LISTEN：侦听，ESTABLISHED：已建立，TIME_WAIT：关闭</p>\n<h4 id=\"2-显示当前户籍-UDP-连接状况\"><a href=\"#2-显示当前户籍-UDP-连接状况\" class=\"headerlink\" title=\"2. 显示当前户籍 UDP 连接状况\"></a>2. 显示当前户籍 UDP 连接状况</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -nu</code></pre>\n\n<p>结果：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">激活Internet连接 (w/o 服务器)\nProto Recv-Q Send-Q Local Address           Foreign Address         State\nudp        0      0 127.0.0.1:56871         127.0.1.1:53            ESTABLISHED\nudp        0      0 172.16.0.112:42799      106.38.222.156:443      ESTABLISHED\nudp        0      0 172.16.0.112:68         172.16.0.1:67           ESTABLISHED</code></pre>\n\n<h4 id=\"3-显示当前户籍-TCP-连接状况\"><a href=\"#3-显示当前户籍-TCP-连接状况\" class=\"headerlink\" title=\"3. 显示当前户籍 TCP 连接状况\"></a>3. 显示当前户籍 TCP 连接状况</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -nt</code></pre>\n\n<p>结果：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">激活Internet连接 (w/o 服务器)\nProto Recv-Q Send-Q Local Address           Foreign Address         State\ntcp        0      0 10.100.100.164:56010    10.40.2.11:27017        ESTABLISHED\ntcp        0      0 10.100.100.164:43846    10.40.2.5:27017         ESTABLISHED\ntcp        0      0 172.16.0.112:60087      43.159.193.150:8002     TIME_WAIT\ntcp        0      0 172.16.0.112:57712      113.240.72.99:443       TIME_WAIT\ntcp        0      0 10.100.100.164:55986    10.40.2.11:27017        ESTABLISHED\ntcp        0      0 10.100.100.164:43722    10.40.2.5:27017         ESTABLISHED</code></pre>\n\n<h4 id=\"4-显示当前系统中所有正在监听的-TCP-端口、相关的进程以及其状态\"><a href=\"#4-显示当前系统中所有正在监听的-TCP-端口、相关的进程以及其状态\" class=\"headerlink\" title=\"4. 显示当前系统中所有正在监听的 TCP 端口、相关的进程以及其状态\"></a>4. 显示当前系统中所有正在监听的 TCP 端口、相关的进程以及其状态</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -ntlp</code></pre>\n\n<p>结果：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">Active Internet connections (only servers)\nProto Recv-Q Send-Q Local Address           Foreign Address         State       PID/Program name\ntcp        0      0 0.0.0.0:80              0.0.0.0:*               LISTEN      1234/nginx\ntcp        0      0 127.0.0.1:3306          0.0.0.0:*               LISTEN      2345/mysqld\ntcp6       0      0 :::22                   :::*                    LISTEN      3456/sshd</code></pre>\n\n<p>字段解析：</p>\n<ol>\n<li><p>Proto：协议</p>\n<ul>\n<li>tcp：表示 TCP 协议。</li>\n<li>tcp6：表示 IPv6 的 TCP 协议。</li>\n</ul>\n</li>\n<li><p>Recv-Q 和 Send-Q：</p>\n<ul>\n<li>Recv-Q：表示接收队列</li>\n<li>Send-Q：表示发送队列</li>\n</ul>\n</li>\n<li><p>Local Address：本地地址</p>\n<ul>\n<li>0.0.0.0:80：本地地址和端口号，0.0.0.0 表示监听所有接口的 IP 地址，80 是端口号。</li>\n<li>127.0.0.1:3306：本地环回地址（localhost）上的端口 3306。</li>\n<li>:::22：IPv6 地址的端口 22。</li>\n</ul>\n</li>\n<li><p>Foreign Address：远程地址</p>\n<ul>\n<li>0.0.0.0:* 和 :::*：表示监听所有外部地址。</li>\n</ul>\n</li>\n<li><p>State：状态</p>\n<ul>\n<li>LISTEN：表示服务器正在监听 TCP 连接请求。</li>\n<li>ESTABLISHED：表示连接已经建立。</li>\n<li>TIME_WAIT：表示主动关闭连接的一方在关闭连接后，等待 2MSL 后依然没有收到对方的 FIN 报文，处于等待关闭状态。</li>\n<li>CLOSE_WAIT：表示被动关闭连接的一方在收到对方的 FIN 报文后，等待应用进程关闭连接。</li>\n<li>LAST_ACK：表示被动关闭连接的一方在发送完 FIN 报文后，等待对方的 ACK 报文，如果收到 ACK 报文，则处于 CLOSED 状态，否则处于 TIME_WAIT 状态。</li>\n<li>CLOSED：表示连接已经关闭。</li>\n<li>SYN_RECV：表示正在等待处理的请求数目。</li>\n<li>SYN_SENT：表示正在等待远程连接请求。</li>\n<li>CLOSING：表示正在等待远程连接关闭请求。</li>\n<li>UNKNOWN：表示未知状态。</li>\n<li>LISTENING：表示正在等待远程连接请求。</li>\n<li>IDLE：表示连接处于空闲状态。</li>\n<li>BOUND：表示正在等待远程连接请求。</li>\n<li>FIN_WAIT1: 套接字已经发出了一个 FIN 请求，表示它已经完成了发送数据，正在等待对方的 ACK 响应。</li>\n<li>FIN_WAIT2: 套接字收到了对 FIN 请求的 ACK 响应，正在等待对方发出 FIN 请求。</li>\n</ul>\n</li>\n<li><p>PID/Program name：进程 ID 和进程名称</p>\n<ul>\n<li>1234/nginx：进程 ID 为 1234 的 nginx 进程正在监听 80 端口。</li>\n<li>2345/mysqld：进程 ID 为 2345 的 mysqld 进程正在监听 3306 端口。</li>\n<li>3456/sshd：进程 ID 为 3456 的 sshd 进程正在监听 22 端口。</li>\n</ul>\n</li>\n</ol>\n<h4 id=\"5-显示网卡列表\"><a href=\"#5-显示网卡列表\" class=\"headerlink\" title=\"5. 显示网卡列表\"></a>5. 显示网卡列表</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -i</code></pre>\n<p>结果：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">Iface             MTU    RX-OK RX-ERR RX-DRP RX-OVR    TX-OK TX-ERR TX-DRP TX-OVR Flg\nbr-717e070443f8  1500        0      0      0 0             0      0      0      0 BMU\nbr-7f56e5476213  1500        0      0      0 0             0      0      0      0 BMU\ndocker0          1500        0      0      0 0             0      0      0      0 BMU\neno1             1500  3838424      0      0 0       3615697      0      0      0 BMRU\nlo              65536  1115368      0      0 0       1115368      0      0      0 LRU\nutun             1400  2667684      0      0 0       2598307      0      0      0 MOPRU\nvirbr0           1500        0      0      0 0             0      0      0      0 BMU</code></pre>\n<p>字段解析：</p>\n<ol>\n<li>Iface：网卡名称</li>\n<li>MTU：最大传输单元</li>\n<li>RX-OK：接收数据包总数</li>\n<li>RX-ERR：接收数据包错误总数</li>\n<li>RX-DRP：接收数据包丢弃总数</li>\n<li>RX-OVR：接收数据包溢出总数</li>\n<li>TX-OK：发送数据包总数</li>\n<li>TX-ERR：发送数据包错误总数</li>\n<li>TX-DRP：发送数据包丢弃总数</li>\n<li>TX-OVR：发送数据包溢出总数</li>\n<li>Flg：标志</li>\n</ol>\n<ul>\n<li>BMU：广播地址</li>\n<li>BMRU：广播地址和多播地址</li>\n<li>MOPRU：多播地址</li>\n<li>LRU：本地地址</li>\n<li>MPRU：多播地址和本地地址</li>\n<li>OPRU：多播地址和本地地址和广播地址</li>\n<li>ALL：所有地址</li>\n</ul>\n<p>###＃ 6. 显示组播组的关系</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -g</code></pre>\n<p>结果：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">IPv6/IPv4 Group Memberships\nInterface       RefCnt Group\n--------------- ------ ---------------------\nlo              1      mdns.mcast.net\nlo              1      all-systems.mcast.net\n</code></pre>\n<p>字段解析：</p>\n<ol>\n<li>Interface：网卡名称</li>\n<li>RefCnt：引用计数</li>\n<li>Group：组播组</li>\n</ol>\n<p>###＃ 7. 显示网络统计信息</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -s</code></pre>\n<p>结果：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">Ip:\n    Forwarding: 1\n    7269919 total packets received\n    0 forwarded\n    0 incoming packets discarded\n    7265662 incoming packets delivered\n    7078118 requests sent out\n    28 outgoing packets dropped\n    7895 dropped because of missing route\n    8416 reassemblies required\n    4208 packets reassembled ok\n    6 outgoing packets failed fragmentation\n    OutTransmits: 7078115\nIcmp:\n    2022 ICMP messages received\n    6 input ICMP message failed\n    ICMP接收历史\n        destination unreachable: 2020\n        echo requests: 2\n    2176 ICMP messages sent\n    0 ICMP messages failed\n    OutRateLimitHost: 253\n    ICMP发出历史\n        destination unreachable: 2174\n        echo replies: 2\nIcmpMsg:\n        InType3: 2020\n        InType8: 2\n        OutType0: 2\n        OutType3: 2174\nTcp:\n    202192 active connection openings\n    10375 passive connection openings\n    120686 failed connection attempts\n    3047 connection resets received\n    77 connections established\n    6857878 segments received\n    7083293 segments sent out\n    16799 segments retransmitted\n    1119 bad segments received\n    131681 resets sent\nUdp:\n    1456958 packets received\n    2421 packets to unknown port received\n    32 packet receive errors\n    556388 packets sent\n    0 receive buffer errors\n    8 send buffer errors\n    InCsumErrors: 32\n    IgnoredMulti: 12191\nUdpLite:\nTcpExt:\n    3 ICMP packets dropped because they were out-of-window\n    30122 TCP sockets finished time wait in fast timer\n    1 packets rejected in established connections because of timestamp\n    119999 delayed acks sent\n    39 delayed acks further delayed because of locked socket\n    Quick ack mode was activated 34511 times\n    1065795 packet headers predicted\n    687289 acknowledgments not containing data payload received\n    1455607 predicted acknowledgments\n    TCPSackRecovery: 327\n    Detected reordering 676 times using SACK\n    TCPDSACKUndo: 97\n    87 congestion windows recovered without slow start after partial ack\n    TCPLostRetransmit: 8875\n    TCPSackFailures: 8\n    3 timeouts in loss state\n    466 fast retransmits\n    133 retransmits in slow start\n    TCPTimeouts: 11823\n    TCPLossProbes: 5420\n    TCPLossProbeRecovery: 82\n    TCPSackRecoveryFail: 10\n    TCPBacklogCoalesce: 2369\n    TCPDSACKOldSent: 34608\n    TCPDSACKOfoSent: 51\n    TCPDSACKRecv: 1827\n    TCPDSACKOfoRecv: 16\n    1537 connections reset due to unexpected data\n    2171 connections reset due to early user close\n    484 connections aborted due to timeout\n    10 times unable to send RST due to no memory\n    TCPSACKDiscard: 2\n    TCPDSACKIgnoredOld: 7\n    TCPDSACKIgnoredNoUndo: 815\n    TCPSackShifted: 26\n    TCPSackMerged: 342\n    TCPSackShiftFallback: 1984\n    IPReversePathFilter: 22\n    TCPRcvCoalesce: 203651\n    TCPOFOQueue: 50737\n    TCPOFOMerge: 54\n    TCPChallengeACK: 879\n    TCPSYNChallenge: 1140\n    TCPSpuriousRtxHostQueues: 18\n    TCPAutoCorking: 458417\n    TCPFromZeroWindowAdv: 35\n    TCPToZeroWindowAdv: 35\n    TCPWantZeroWindowAdv: 167\n    TCPSynRetrans: 7312\n    TCPOrigDataSent: 3398432\n    TCPHystartTrainDetect: 22\n    TCPHystartTrainCwnd: 1073\n    TCPHystartDelayDetect: 42\n    TCPHystartDelayCwnd: 1831\n    TCPACKSkippedSeq: 286\n    TCPACKSkippedChallenge: 265\n    TCPKeepAlive: 97079\n    TCPDelivered: 3475194\n    TCPAckCompressed: 14499\n    TcpTimeoutRehash: 11471\n    TCPDSACKRecvSegs: 1841\n    TCPDSACKIgnoredDubious: 2\nIpExt:\n    InNoRoutes: 17\n    InMcastPkts: 118643\n    OutMcastPkts: 8410\n    InBcastPkts: 17704\n    OutBcastPkts: 1960\n    InOctets: 3807905209\n    OutOctets: 1523626052\n    InMcastOctets: 53391437\n    OutMcastOctets: 1586713\n    InBcastOctets: 9013152\n    OutBcastOctets: 152880\n    InNoECTPkts: 7512863\n    InECT0Pkts: 18\nMPTcpExt:\n</code></pre>\n\n<h4 id=\"显示监听的套接口\"><a href=\"#显示监听的套接口\" class=\"headerlink\" title=\"显示监听的套接口\"></a>显示监听的套接口</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -l</code></pre>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">激活Internet连接 (仅服务器)\nProto Recv-Q Send-Q Local Address           Foreign Address         State      \ntcp        0      0 hellotalk:domain        0.0.0.0:*               LISTEN     \ntcp6       0      0 [::]:3100               [::]:*                  LISTEN     \nudp        0      0 0.0.0.0:45010           0.0.0.0:*                                            \nudp        0      0 0.0.0.0:mdns            0.0.0.0:*                          \nudp6       0      0 [::]:49287              [::]:*                             \n              7          \n活跃的UNIX域套接字 (仅服务器)\nProto RefCnt Flags       Type       State         I-Node   路径\nunix  2      [ ACC ]     流        LISTENING     1010159  /tmp/.java_pid169241.tmp</code></pre>\n\n<p>字段解析：</p>\n<ul>\n<li>Proto：协议名，如tcp、udp、unix等</li>\n<li>Recv-Q：接收队列，即收到的但未处理的数据包数</li>\n<li>Send-Q：发送队列，即发送但未确认的数据包数</li>\n<li>Local Address：本地地址</li>\n<li>Foreign Address：远程地址</li>\n<li>State：套接字状态，如LISTEN、ESTABLISHED等</li>\n<li>I-Node：套接字inode号</li>\n<li>路径：unix域套接字路径</li>\n<li>RefCnt：引用计数</li>\n<li>Flags：套接字标志，如S表示套接字是被动打开的，即服务器端，而A表示套接字是主动打开的，即客户端</li>\n<li>Type：套接字类型，如流套接字、数据报套接字等</li>\n<li>路径：unix域套接字路径</li>\n</ul>\n<h4 id=\"查看路由表\"><a href=\"#查看路由表\" class=\"headerlink\" title=\"查看路由表\"></a>查看路由表</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -r</code></pre>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">内核 IP 路由表\nDestination     Gateway         Genmask         Flags   MSS Window  irtt Iface\ndefault         172.16.0.1      0.0.0.0         UG        0 0          0 eno1\none.one.one.one 0.0.0.0         255.255.255.255 UH        0 0          0 utun\n10.10.0.0       0.0.0.0         255.255.0.0     U         0 0          0 utun\n10.40.0.0       0.0.0.0         255.255.0.0     U         0 0          0 utun\n10.100.100.0    0.0.0.0         255.255.255.0   U         0 0          0 utun\n30.100.0.0      0.0.0.0         255.252.0.0     U         0 0          0 utun\nfeilian.hellota 172.16.0.1      255.255.255.255 UGH       0 0          0 eno1\nlink-local      0.0.0.0         255.255.0.0     U         0 0          0 utun\n172.16.0.0      0.0.0.0         255.255.254.0   U         0 0          0 eno1\n172.16.6.20     172.16.0.1      255.255.255.255 UGH       0 0          0 eno1\n172.17.0.0      0.0.0.0         255.255.0.0     U         0 0          0 docker0\n192.168.49.0    0.0.0.0         255.255.255.0   U         0 0          0 br-717e070443f8\n192.168.58.0    0.0.0.0         255.255.255.0   U         0 0          0 br-7f56e5476213\n192.168.122.0   0.0.0.0         255.255.255.0   U         0 0          0 virbr0</code></pre>\n\n<p>字段解析：</p>\n<ul>\n<li>Destination：目的网络或目的主机</li>\n<li>Gateway：指定用于转发包的网关。</li>\n<li>Genmask：子网掩码</li>\n<li>Flags：路由标志，如U表示路由是活动的，H表示目的地址是一个主机，G表示使用网关</li>\n<li>MSS：最大分段大小</li>\n<li>Window：TCP窗口大小</li>\n<li>irtt：初始RTT</li>\n<li>Iface：路由接口</li>\n</ul>\n<h4 id=\"只列出监听中的连接\"><a href=\"#只列出监听中的连接\" class=\"headerlink\" title=\"只列出监听中的连接\"></a>只列出监听中的连接</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -tnl</code></pre>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">激活Internet连接 (仅服务器)\nProto Recv-Q Send-Q Local Address           Foreign Address         State      \ntcp        0      0 192.168.122.1:53        0.0.0.0:*               LISTEN     \ntcp        0      0 127.0.0.1:631           0.0.0.0:*               LISTEN     \ntcp        0      0 127.0.0.1:35600         0.0.0.0:*               LISTEN     \ntcp        0      0 127.0.0.54:53           0.0.0.0:*               LISTEN     \ntcp        0      0 127.0.0.1:31080         0.0.0.0:*               LISTEN     \ntcp        0      0 127.0.0.1:31055         0.0.0.0:*               LISTEN     \ntcp        0      0 127.0.1.1:53            0.0.0.0:*               LISTEN     \ntcp        0      0 127.0.0.53:53           0.0.0.0:*               LISTEN     \ntcp6       0      0 :::41499                :::*                    LISTEN     \ntcp6       0      0 127.0.0.1:52829         :::*                    LISTEN     \ntcp6       0      0 127.0.0.1:35955         :::*                    LISTEN     \ntcp6       0      0 127.0.0.1:63343         :::*                    LISTEN     \ntcp6       0      0 127.0.0.1:63342         :::*                    LISTEN     </code></pre>\n\n<h4 id=\"获取进程名、进程号以及用户-ID\"><a href=\"#获取进程名、进程号以及用户-ID\" class=\"headerlink\" title=\"获取进程名、进程号以及用户 ID\"></a>获取进程名、进程号以及用户 ID</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -tunlp</code></pre>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">Active Internet connections (only servers)\nProto Recv-Q Send-Q Local Address           Foreign Address         State       PID/Program name\ntcp        0      0 127.0.1.1:53            0.0.0.0:*               LISTEN      1144/dnsmasq    \ntcp        0      0 127.0.0.1:631           0.0.0.0:*               LISTEN      661/cupsd       \ntcp6       0      0 ::1:631                 :::*                    LISTEN      661/cupsd</code></pre>\n\n<h4 id=\"查看进程的拥有者会更有用。使用-ep-选项可以同时查看进程名和用户名\"><a href=\"#查看进程的拥有者会更有用。使用-ep-选项可以同时查看进程名和用户名\" class=\"headerlink\" title=\"查看进程的拥有者会更有用。使用 -ep 选项可以同时查看进程名和用户名\"></a>查看进程的拥有者会更有用。使用 -ep 选项可以同时查看进程名和用户名</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">netstat -tunlep</code></pre>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">Proto Recv-Q Send-Q Local Address           Foreign Address         State       User       Inode      PID/Program name    \ntcp        0      0 192.168.122.1:domain    0.0.0.0:*               LISTEN      root       14887      1567/dnsmasq        \ntcp        0      0 localhost:ipp           0.0.0.0:*               LISTEN      root       1946474    310640/cupsd     </code></pre>\n\n<p>参考：<br><a href=\"https://docs.oracle.com/cd/E26926_01/html/E25874/ipconfig-142.html\">https://docs.oracle.com/cd/E26926_01/html/E25874/ipconfig-142.html</a></p>\n",
            "tags": [
                "Linux",
                "Network",
                "Linux",
                "Network"
            ]
        },
        {
            "id": "https://costalong.com/2024/07/11/git/no-sensitive/",
            "url": "https://costalong.com/2024/07/11/git/no-sensitive/",
            "title": "如何在 Git 提交中禁止包含敏感信息",
            "date_published": "2024-07-11T10:58:20.000Z",
            "content_html": "<p>为了防止在 Git 提交中包含敏感信息（如数据库密码），可以采取以下几种方法：</p>\n<h2 id=\"1-使用-gitignore\"><a href=\"#1-使用-gitignore\" class=\"headerlink\" title=\"1. 使用 .gitignore\"></a>1. 使用 .gitignore</h2><p>将包含敏感信息的文件添加到 <code>.gitignore</code> 文件中，这样 Git 就不会跟踪这些文件。例如，如果你的敏感信息存储在 <code>config.json</code> 文件中，你可以在 <code>.gitignore</code> 中添加：</p>\n<pre class=\"language-none\"><code class=\"language-none\">config.json</code></pre>\n\n<h2 id=\"2-使用环境变量\"><a href=\"#2-使用环境变量\" class=\"headerlink\" title=\"2. 使用环境变量\"></a>2. 使用环境变量</h2><p>将敏感信息存储在环境变量中，而不是直接在代码中。例如，在代码中读取环境变量：</p>\n<pre class=\"language-python\" data-language=\"python\"><code class=\"language-python\">import os\n\ndb_password = os.getenv('DB_PASSWORD')</code></pre>\n\n<p>在运行应用程序之前，设置环境变量：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">export DB_PASSWORD=\"your_database_password\"</code></pre>\n\n<h2 id=\"3-使用-Git-钩子（pre-commit-钩子）\"><a href=\"#3-使用-Git-钩子（pre-commit-钩子）\" class=\"headerlink\" title=\"3. 使用 Git 钩子（pre-commit 钩子）\"></a>3. 使用 Git 钩子（pre-commit 钩子）</h2><p>可以编写一个 Git pre-commit 钩子，在每次提交之前检查提交内容中是否包含敏感信息。如果包含，则拒绝提交。</p>\n<h3 id=\"方法1：手动编写-pre-commit-钩子\"><a href=\"#方法1：手动编写-pre-commit-钩子\" class=\"headerlink\" title=\"方法1：手动编写 pre-commit 钩子\"></a>方法1：手动编写 pre-commit 钩子</h3><p>首先，创建一个 pre-commit 钩子脚本：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">#!/bin/sh\n# 定义敏感信息模式\nSENSITIVE_PATTERNS=(\"your_database_password\" \"another_sensitive_pattern\")\n\n# 获取已暂存的文件\nSTAGED_FILES=$(git diff --cached --name-only)\n\nfor FILE in $STAGED_FILES; do\n  for PATTERN in \"${SENSITIVE_PATTERNS[@]}\"; do\n    if git grep -q \"$PATTERN\" \"$FILE\"; then\n      echo \"Error: Sensitive information found in $FILE\"\n      exit 1\n    fi\n  done\ndone\n\nexit 0</code></pre>\n\n<p>将这个脚本保存到 <code>.git/hooks/pre-commit</code> 文件中，并确保它具有可执行权限：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">chmod +x .git/hooks/pre-commit</code></pre>\n\n<h3 id=\"方法2：使用-pre-commit-工具-禁止已经提交的文件\"><a href=\"#方法2：使用-pre-commit-工具-禁止已经提交的文件\" class=\"headerlink\" title=\"方法2：使用 pre-commit 工具 禁止已经提交的文件\"></a>方法2：使用 pre-commit 工具 禁止已经提交的文件</h3><ol>\n<li>安装 pre-commit</li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">pip install pre-commit</code></pre>\n\n<p>或者</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">brew install pre-commit   # mac \napt install pre-commit    # linux</code></pre>\n\n<ol start=\"2\">\n<li>添加默认配置文件</li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">pre-commit sample-config &gt; .pre-commit-config.yaml</code></pre>\n\n<p>在项目根目录下生成文件 <code>.pre-commit-config.yaml</code>，内容如下：</p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\"># See https://pre-commit.com for more information\n# See https://pre-commit.com/hooks.html for more hooks\nrepos:\n-   repo: https://github.com/pre-commit/pre-commit-hooks\n    rev: v3.2.0\n    hooks:\n    -   id: trailing-whitespace\n    -   id: end-of-file-fixer\n    -   id: check-yaml\n    -   id: check-added-large-files</code></pre>\n\n<ol start=\"3\">\n<li>配置 pre-commit 钩子：</li>\n</ol>\n<p>在 <code>.pre-commit-config.yaml</code> 文件中添加一个自定义钩子来检查指定文件是否被修改。例如，假设你想保护文件 <code>protected_file.txt</code>，可以这样配置：</p>\n<pre class=\"language-yaml\" data-language=\"yaml\"><code class=\"language-yaml\">repos:\n  - repo: local\n    hooks:\n      - id: protect-protected-file\n        name: Protect protected_file.txt\n        entry: bash protect-protected-file.sh\n        language: system\n        stages: [commit]</code></pre>\n\n<ol start=\"4\">\n<li>创建自定义钩子脚本</li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">#!/bin/sh\n\n# 要保护的文件列表\nPROTECTED_FILES=(\n    \"protected_file.txt\" \n)\n\n# 检查是否有文件被修改\nfor FILE in \"${PROTECTED_FILES[@]}\"; do\n  if git diff --cached --name-only | grep -q \"^$FILE$\"; then\n    echo \"Error: You are trying to modify $FILE, which is protected.\"\n    exit 1\n  fi\ndone\n\n# 允许提交\nexit 0</code></pre>\n\n<ol start=\"5\">\n<li>使脚本可执行：</li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">chmod +x protect-protected-file.sh</code></pre>\n\n<ol start=\"6\">\n<li>安装 pre-commit 钩子：</li>\n</ol>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">pre-commit install</code></pre>\n\n<h2 id=\"4-使用工具（如-git-secrets）\"><a href=\"#4-使用工具（如-git-secrets）\" class=\"headerlink\" title=\"4. 使用工具（如 git-secrets）\"></a>4. 使用工具（如 git-secrets）</h2><p><code>git-secrets</code> 是一个专门用于防止在 Git 仓库中提交敏感信息的工具。它可以在提交之前扫描提交内容，并阻止包含敏感信息的提交。</p>\n<h3 id=\"安装-git-secrets\"><a href=\"#安装-git-secrets\" class=\"headerlink\" title=\"安装 git-secrets\"></a>安装 git-secrets</h3><h4 id=\"对于-macOS\"><a href=\"#对于-macOS\" class=\"headerlink\" title=\"对于 macOS\"></a>对于 macOS</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">brew install git-secrets</code></pre>\n\n<h4 id=\"对于-Linux\"><a href=\"#对于-Linux\" class=\"headerlink\" title=\"对于 Linux\"></a>对于 Linux</h4><pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">git clone https://github.com/awslabs/git-secrets.git\ncd git-secrets\nsudo make install</code></pre>\n\n<p>然后在你的 Git 仓库中初始化 git-secrets 并添加敏感信息模式：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">cd your-repo\ngit secrets --install\ngit secrets --add 'your_database_password'\ngit secrets --add 'another_sensitive_pattern'</code></pre>\n\n<p>这样，每次提交时，git-secrets 都会扫描提交内容并阻止包含敏感信息的提交。</p>\n<h2 id=\"5-使用-Secret-Scanning-服务\"><a href=\"#5-使用-Secret-Scanning-服务\" class=\"headerlink\" title=\"5. 使用 Secret Scanning 服务\"></a>5. 使用 Secret Scanning 服务</h2><p>一些代码托管平台（如 GitHub）提供了 Secret Scanning 服务，可以自动扫描仓库中的敏感信息，并在发现敏感信息时发出警告。</p>\n<p>通过以上方法，可以有效防止在 Git 提交中包含敏感信息，保护你的代码库安全。</p>\n",
            "tags": [
                "Tool",
                "git"
            ]
        }
    ]
}