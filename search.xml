<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>llama 安装</title>
      <link href="/2026/07/01/llama/llama-install/"/>
      <url>/2026/07/01/llama/llama-install/</url>
      
        <content type="html"><![CDATA[<h2 id="安装环境"><a href="#安装环境" class="headerlink" title="安装环境"></a>安装环境</h2><pre class="language-bash" data-language="bash"><code class="language-bash">sudo add-apt-repository ppa:deadsnakes/ppasudo apt updatesudo apt install python3.11 python3.11-venv -y</code></pre><p>安装虚拟环境</p><pre class="language-bash" data-language="bash"><code class="language-bash">/usr/bin/python3.11 -m venv venv</code></pre><p>使用虚拟</p><pre class="language-bash" data-language="bash"><code class="language-bash">source venv/bin/activate# 如果使用的 fishsource venv/bin/activate.fish</code></pre><p>安装 Install PyTorch:</p><pre class="language-bash" data-language="bash"><code class="language-bash">pip install --pre torch torchvision --extra-index-url https://download.pytorch.org/whl/nightly/cpu</code></pre><h2 id="下载"><a href="#下载" class="headerlink" title="下载"></a>下载</h2><p>1. 下载 llama.cp</p><pre class="language-bash" data-language="bash"><code class="language-bash">git clone https://github.com/ggerganov/llama.cpp.git </code></pre><ol><li>下载通义千问1.5-7B模型 或 1.5-32B 模型 </li></ol><pre class="language-bash" data-language="bash"><code class="language-bash"># 使用 huggingface 仓库git clone https://huggingface.co/Qwen/Qwen1.5-7B-Chat# 或git clone https://huggingface.co/qwen/Qwen1.5-32B-Chat.git# 使用 modelscope 仓库git clone https://www.modelscope.cn/Qwen/Qwen1.5-7B-Chat# 或git clone https://www.modelscope.cn/qwen/Qwen1.5-32B-Chat.git</code></pre><h2 id="编译-llama-cp"><a href="#编译-llama-cp" class="headerlink" title="编译 llama.cp"></a>编译 llama.cp</h2><pre class="language-bash" data-language="bash"><code class="language-bash">cd llama.cpp make</code></pre><h3 id="安装llama-依赖"><a href="#安装llama-依赖" class="headerlink" title="安装llama 依赖"></a>安装llama 依赖</h3><pre class="language-bash" data-language="bash"><code class="language-bash">python3 -m  pip install -r requirements.txt</code></pre><h3 id="转换-Qwen-模型为-GGUF"><a href="#转换-Qwen-模型为-GGUF" class="headerlink" title="转换 Qwen 模型为 GGUF"></a>转换 Qwen 模型为 GGUF</h3><p>什么是 GGUF? GGUF是一种用于存储用于GGML推断和基于GGML的执行器的模型的文件格式。GGUF是一种二进制格式，旨在快速加载和保存模型，并易于阅读。传统上，模型是使用PyTorch或其他框架开发的，然后转换为GGUF以在GGML中使用。</p><p>GGUF是GGML、GGMF和GGJT的后继文件格式，旨在通过包含加载模型所需的所有信息来消除歧义。它还设计为可扩展的，因此可以向模型添加新信息而不会破坏兼容性，更多信息访问<a href="https://github.com/ggerganov/ggml/blob/master/docs/gguf.md" title="官方说明文档">官方说明文档</a>。</p><p>查看文件结构</p><pre class="language-bash" data-language="bash"><code class="language-bash">$ tree -L 1.├── llama.cpp  ├── Qwen1.5-32B-Chat└── venv</code></pre><p>执行这转换命令</p><pre class="language-bash" data-language="bash"><code class="language-bash">python3 convert-hf-to-gguf.py  ../Qwen1.5-32B-Chat=&gt; .....INFO:hf-to-gguf:Model successfully exported to '../Qwen1.5-32B-Chat/ggml-model-f16.gguf'</code></pre><p>最后的结果表示已经转为 F16 的 gguf 格式的模型了</p><h3 id="量化模型"><a href="#量化模型" class="headerlink" title="量化模型"></a>量化模型</h3><p>将gguf 的模型量化到INT4 </p><pre class="language-bash" data-language="bash"><code class="language-bash">./llama-quantize  ../Qwen1.5-32B-Chat/ggml-model-f16.gguf  ./models/qwen1.5-chat-ggml-model-Q4_K_M.gguf Q4_K_M=&gt;  ....[ 767/ 771]                   blk.63.attn_q.bias - [ 5120,     1,     1,     1], type =    f32, size =    0.020 MB[ 768/ 771]                 blk.63.attn_q.weight - [ 5120,  5120,     1,     1], type =    f16, converting to q4_K .. size =    50.00 MiB -&gt;    14.06 MiB[ 769/ 771]                   blk.63.attn_v.bias - [ 1024,     1,     1,     1], type =    f32, size =    0.004 MB[ 770/ 771]                 blk.63.attn_v.weight - [ 5120,  1024,     1,     1], type =    f16, converting to q6_K .. size =    10.00 MiB -&gt;     4.10 MiB[ 771/ 771]                   output_norm.weight - [ 5120,     1,     1,     1], type =    f32, size =    0.020 MBllama_model_quantize_internal: model size  = 62014.27 MBllama_model_quantize_internal: quant size  = 18780.70 MBmain: quantize time = 421046.36 msmain:    total time = 421046.36 ms</code></pre><p>../Qwen1.5-32B-Chat/ggml-model-f16.gguf   是转换的源文件路径</p><p>./models/qwen1.5-chat-ggml-model-Q4_K_M.gguf   是转换成功生成的文件路径</p><p>Q4_K_M 是量化方法</p><p>新版量化方法包括：</p><ul><li>Q2_K</li><li>Q3_K_S, Q3_K_M, Q3_K_L</li><li>Q4_K_S, Q4_K_M</li><li>Q5_K_S, Q5_K_M</li><li>Q6_K</li></ul><h3 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h3><h3 id="运行测试"><a href="#运行测试" class="headerlink" title="运行测试"></a>运行测试</h3><pre class="language-bash" data-language="bash"><code class="language-bash">./llama-cli -m ./models/qwen1.5-chat-ggml-model-Q4_K_M.gguf -n 128=&gt; 1) 甲乙丙丁四地中，最可能出现水土流失的是A. 甲B0 乙C0 丙D0 丁答案：C关键点：中国分区地理，水土流失及治理，区域农业生产的条件、布局特点、问题llama_print_timings:        load time =   17027.80 msllama_print_timings:      sample time =      22.98 ms /   128 runs   (    0.18 ms per token,  5571.27 tokens per second)llama_print_timings: prompt eval time =       0.00 ms /     0 tokens (    -nan ms per token,     -nan tokens per second)llama_print_timings:        eval time =  867086.12 ms /   128 runs   ( 6774.11 ms per token,     0.15 tokens per second)llama_print_timings:       total time =  867381.32 ms /   128 tokensLog end</code></pre><p>注意： 这个运行等待的时间比较长</p><p>接下来我们进入对话模型。如何启动呢？我们这里查看 examples/chat.sh的启动方式，来编写启动 Qwen 模型命令。</p><pre class="language-bash" data-language="bash"><code class="language-bash">./llama-cli -m ./models/qwen1.5-chat-ggml-model-Q4_K_M.gguf  -n 256 --grammar-file grammars/json.gbnf -p 'Request: schedule a call at 8pm; Command:'</code></pre><h2 id="错误"><a href="#错误" class="headerlink" title="错误"></a>错误</h2><ol><li>通义千问safetensors_rust.SafetensorError: Error while deserializing header: HeaderTooLarge解决方法</li></ol><p>安装 gi-lfs</p><pre class="language-bash" data-language="bash"><code class="language-bash">sudo apt-get install git-lfs</code></pre><p>进入 Qwen1.5-32B-Chat  执行 git lfs pull</p><pre class="language-bash" data-language="bash"><code class="language-bash">cd Qwen1.5-32B-Chat &amp;&amp; git lfs pull</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> llama </tag>
            
            <tag> ggml </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>5 个插件让 Claude Code 直连数据库:zweite-tools 上手实操</title>
      <link href="/2026/07/01/318/"/>
      <url>/2026/07/01/318/</url>
      
        <content type="html"><![CDATA[<h1 id="5-个插件让-Claude-Code-直连数据库-zweite-tools-上手实操"><a href="#5-个插件让-Claude-Code-直连数据库-zweite-tools-上手实操" class="headerlink" title="5 个插件让 Claude Code 直连数据库:zweite-tools 上手实操"></a>5 个插件让 Claude Code 直连数据库:zweite-tools 上手实操</h1><p><img src="/medias/loading.gif" data-original="https://cdn.jsdelivr.net/gh/costa92/article-images/clawdbot/zweite-tools-plugins_cover_20260701-122743_cbff6bf52c03.jpg" alt="封面图:Claude Code 通过五个插件连向数据库和文档系统"></p><p>「帮我查下生产库今天多少订单」——这话我以前不敢跟 AI 说。它拿不到数据库连接,只能帮我写条 SQL,我还得自己复制到客户端里跑一遍。</p><p>zweite 这个人做了个 Claude Code 插件市场 <a href="https://github.com/zweite/claude-plugins">zweite/claude-plugins</a>,把中间这层给补上了。装完之后,Claude Code 真能自己连库、跑查询,而且带一个「风险刹车」:高危语句先问你一句再动手。</p><p><img src="/medias/loading.gif" data-original="/images/posts/318_2fb8916ca973e8e8682fadf7bc66686b.png" alt="zweite-tools 插件市场仓库首页"></p><p><strong>一句话重点:市场里有五个插件,四个是数据查询(zeppelin / starrocks / mysql / mongodb),共享同一套「执行前分级 + 元数据缓存 + 多环境凭证」设计;第五个 alidocs 负责把钉钉文档拉下来、推上去。</strong> 下面挨个拆,每个都配一个我实际会用的场景。</p><h2 id="先装上-一条命令进市场-一个目录管配置"><a href="#先装上-一条命令进市场-一个目录管配置" class="headerlink" title="先装上:一条命令进市场,一个目录管配置"></a>先装上:一条命令进市场,一个目录管配置</h2><p>安装分两步——先把市场加进来,再挑需要的插件装。这套走的是 Claude Code 官方的 <code>/plugin</code> 机制,不用手动 clone 仓库。</p><pre class="language-bash" data-language="bash"><code class="language-bash">/plugin marketplace add zweite/claude-plugins/plugin install mysql@zweite-tools/plugin install alidocs@zweite-tools</code></pre><p>配置我卡过一次。老版本每个工具各放各的(<code>~/.mysql/config.json</code>),新版统一挪到了 <code>~/.taku/</code> 下面,一个工具一个 JSON。最简单的单库配置长这样:</p><pre class="language-json" data-language="json"><code class="language-json">{  "host": "db-prod",  "port": 3306,  "user": "you",  "password": "…",  "database": "mydb"}</code></pre><p>想换目录用 <code>TAKU_DIR</code> 覆盖就行。旧路径也没废掉——作者保留了对 <code>~/.&lt;tool&gt;/config.json</code> 的回退读取,所以你要是之前配过,升级完不用重弄。</p><pre class="language-bash" data-language="bash"><code class="language-bash">export TAKU_DIR=~/work/taku-config   # 想把配置集中放别处时</code></pre><p>这个设计我挺认的:配置收敛到一个目录,但不强制迁移,老用户零成本。</p><p>整个市场的结构也很规整。仓库根目录一个 <code>marketplace.json</code> 做市场清单,<code>plugins/</code> 下面每个插件一个文件夹,里头是 <code>plugin.json</code> 清单、<code>skills/&lt;tool&gt;/SKILL.md</code> 技能说明、<code>scripts/</code> 脚本外加一份 README。五个插件全是纯 Python,没有二进制、没有编译步骤,脚本就是可读的 <code>.py</code> 文件——想在装之前先看它到底会跑什么,直接翻 <code>scripts/</code> 就行,这点对要连生产库的工具很重要。</p><p>作者还留了个校验命令,自己改了插件或者想确认市场清单没写错,跑一下就知道:</p><pre class="language-bash" data-language="bash"><code class="language-bash">claude plugin validate .</code></pre><p>这里得说清一件事:让 AI「说人话就能查库」,靠的不是魔法。每个插件的 <code>skills/&lt;tool&gt;/SKILL.md</code> 就是一份写给 Claude 的说明书——什么时候该调 <code>mysql.py</code>、参数怎么传、风险档怎么读,都写在里头。装一个插件,等于同时把脚本和这份说明书一起塞给 Claude Code,所以它才知道你说「查订单」时该去跑哪个脚本、按什么姿势跑。这也是插件市场这种形态比裸给你几个 <code>.py</code> 更省事的地方:脚本加技能说明打包在一起,装完即用,不用你再手把手教 AI 怎么调。</p><h2 id="四个数据插件-一套共享设计"><a href="#四个数据插件-一套共享设计" class="headerlink" title="四个数据插件,一套共享设计"></a>四个数据插件,一套共享设计</h2><p>zeppelin、starrocks、mysql、mongodb 这四个,长得像一个模子刻的。把这套共享设计搞懂,四个插件就都会用了。</p><p><img src="/medias/loading.gif" data-original="/images/posts/318_08ee0d01b80601641b2dbff4a7dbb001.jpg" alt="四个数据插件共享的三层设计:风险闸门、元数据缓存、命名凭证"></p><p><strong>第一层是执行前风险分级。</strong> 每条语句先过一遍分类器,分成四档:</p><table><thead><tr><th>风险档</th><th>覆盖的语句</th></tr></thead><tbody><tr><td>safe</td><td>只读查询(SELECT)</td></tr><tr><td>low</td><td>低风险操作</td></tr><tr><td>medium</td><td>中等风险操作</td></tr><tr><td>high</td><td>DDL、不带 WHERE 的删改、覆盖写</td></tr></tbody></table><p>high 档会先弹确认再执行。这套分级不是简单靠关键字匹配就完事——它还会做一层语义判断:识别到语句碰的是生产表、PII 或者财务字段,风险只往上抬,不往下降。也就是说,一条看着人畜无害的 <code>UPDATE</code>,只要落在敏感表上,就可能被抬到需要你确认的档位。这条「只升不降」的规则是我觉得最靠谱的地方,它默认往保守那头站。</p><p>想看某条语句到底判成几档,可以把它直接喂给 <code>risk.py</code> 单独试,不用真连库:</p><pre class="language-bash" data-language="bash"><code class="language-bash">echo 'DROP TABLE foo' | python3 "$ROOT/scripts/risk.py" --magic '%sql'</code></pre><p>这里的 <code>$ROOT</code> 是插件装完后的脚本目录,Claude Code 会告诉你具体路径。日常你几乎碰不到它——正常用法是直接跟 AI 说话,由它在后台调脚本;只有想手动验证某条语句、或者绕开 AI 跑固定查询时,才需要自己拼这种命令。</p><p>自动批准的阈值用环境变量控。默认是 <code>safe</code>,意思是只有只读查询能自己跑,其它都要人点头。这个默认值选得很克制——装完不配任何东西,AI 也只能查、不能改:</p><pre class="language-bash" data-language="bash"><code class="language-bash">export MYSQL_AUTO_APPROVE_LEVEL=safe   # 生产库我只开到这一档</code></pre><p><strong>第二层是元数据缓存。</strong> 每个 profile 会缓存表结构加 10 行样本数据,默认 30 天 TTL。有了它,AI 不用每次都去 <code>DESCRIBE</code> 一遍表,写查询也知道字段长啥样。缓存自己管:</p><pre class="language-bash" data-language="bash"><code class="language-bash">python3 "$ROOT/scripts/mysql.py" cache put --table mydb.orderspython3 "$ROOT/scripts/mysql.py" cache listpython3 "$ROOT/scripts/mysql.py" cache clear --all</code></pre><p>30 天这个默认 TTL 是个平衡:表结构不会天天变,缓存久点省来回;真改了 schema,手动 <code>cache clear</code> 或者等它过期就重新拉。缓存放哪、存多久都能用环境变量调,<code>MYSQL_CACHE_DIR</code> 换目录,<code>MYSQL_CACHE_TTL_DAYS</code> 改新鲜度窗口。</p><p><strong>第三层是命名 profile。</strong> 一份配置里放多套环境,prod、staging 随手切。这层解决的是「同一个库有测试和生产两套连接」的日常麻烦——不用改配置文件,查询时点名用哪个 profile 就行。凭证跟着 profile 走,缓存也按 profile 分开存,测试库的表结构不会污染生产库那份。</p><p>把这三层单独拎出来讲,是因为它们不是某个插件的私货,而是四个数据插件的公共底座。你在 mysql 上学会的「先看风险档、信任缓存、按 profile 切环境」,到 starrocks、mongodb、zeppelin 上原样适用,不用重学。作者把共性抽到一层,新插件只要接上这套约定就能复用同样的安全边界——这也是我判断这个市场不是随手攒几个脚本的关键:它有统一的设计意图,不是四个各写各的散件。</p><h2 id="MySQL-和-StarRocks-一句话查生产库"><a href="#MySQL-和-StarRocks-一句话查生产库" class="headerlink" title="MySQL 和 StarRocks:一句话查生产库"></a>MySQL 和 StarRocks:一句话查生产库</h2><p>先说最常用的 mysql。多环境配置就是在上面那份 JSON 外面套一层 <code>profiles</code>:</p><pre class="language-json" data-language="json"><code class="language-json">{  "default_profile": "prod",  "profiles": {    "prod":    { "host": "db-prod",    "user": "you", "password": "…" },    "staging": { "host": "db-staging", "user": "you", "password": "…", "ssl": true }  },  "cache_ttl_days": 30}</code></pre><p>除了连接三件套,配置项还挺全。每个键都有对应的环境变量,写进 JSON 或者用环境变量覆盖都行,CI 里注入敏感值就靠后者:</p><table><thead><tr><th>环境变量</th><th>配置键</th><th>默认</th><th>作用</th></tr></thead><tbody><tr><td><code>MYSQL_HOST</code></td><td><code>host</code></td><td>必填</td><td>数据库地址</td></tr><tr><td><code>MYSQL_PORT</code></td><td><code>port</code></td><td>3306</td><td>端口</td></tr><tr><td><code>MYSQL_USER</code> / <code>MYSQL_PASSWORD</code></td><td><code>user</code> / <code>password</code></td><td>必填</td><td>账号密码</td></tr><tr><td><code>MYSQL_DATABASE</code></td><td><code>database</code></td><td>空</td><td>默认库</td></tr><tr><td><code>MYSQL_SSL</code></td><td><code>ssl</code></td><td>false</td><td>一开始就强制 TLS</td></tr><tr><td><code>MYSQL_TIMEOUT_SECONDS</code></td><td><code>timeout_seconds</code></td><td>30</td><td>socket 超时</td></tr><tr><td><code>MYSQL_CACHE_TTL_DAYS</code></td><td><code>cache_ttl_days</code></td><td>30</td><td>缓存新鲜度窗口</td></tr><tr><td><code>MYSQL_AUTO_APPROVE_LEVEL</code></td><td>仅环境变量</td><td>safe</td><td>自动批准阈值</td></tr></tbody></table><p>注意最后一行的 <code>MYSQL_AUTO_APPROVE_LEVEL</code> 只认环境变量、不进 JSON——我理解这是故意的:自动批准阈值是台机器的安全策略,不该跟着配置文件一起被拷来拷去。你的笔记本可以宽松点,跳板机上就该锁死在 <code>safe</code>。</p><p>配好之后,日常就是跟 Claude Code 说人话,不用自己拼命令:</p><blockquote><p>「查一下 prod 环境 mydb.orders 表今天下了多少单」</p></blockquote><p>它背后会挑 prod profile、从缓存里读出 orders 的表结构、生成 SELECT、判成 safe 档直接跑,再把结果贴回聊天里。整个过程我不用碰 SQL,也不用记表名。想绕开 AI、手动验证连通性或者跑条固定查询,CLI 口子也留着:</p><pre class="language-bash" data-language="bash"><code class="language-bash">python3 "$ROOT/scripts/mysql.py" test-connpython3 "$ROOT/scripts/mysql.py" --profile staging query --sql 'SELECT 1'</code></pre><p><code>test-conn</code> 我每换一台机器都先跑一遍,连不上先排查网络,别等 AI 生成完查询才发现连不通。</p><p>再举个会触发风险闸的例子,好看清这套刹车到底怎么拦。我让它「把 staging 库里 status 还是 pending 的测试订单清掉」——这是个 DELETE,真按我说的执行会落到一批记录上。它生成语句后判成 high 档,没有直接跑,而是把要执行的那条 SQL 原样贴给我、标上风险等级、停下来等我点头。我扫了眼 WHERE 条件确认没写飞才让它继续。这一趟里,AI 负责把大白话翻成 SQL,真正拍板的还是我——这正是我愿意把它接到 staging、但生产库死守 safe 的原因:让它干翻译的活,别让它干拍板的活。</p><p>starrocks 是同一套逻辑,差别在实现:它用纯 Python 实现了 MySQL 协议,不用装任何数据库驱动。mysql 插件也一样,连 <code>caching_sha2</code> 这种较新的认证方式都自己实现了,没有回落到需要 C 扩展的老库。对我这种懒得在每台机器上装一堆 <code>mysqlclient</code> 编译依赖的人,这点是真省事——一个 Python 环境走天下,离线机器也能跑。StarRocks 面向的是 OLAP 分析场景,让 AI 直接对着数仓问「上周哪个渠道转化最高」这类聚合问题,比自己在 BI 工具里拖拉快得多。</p><h2 id="MongoDB-用-JSON-描述查询-不用背语法"><a href="#MongoDB-用-JSON-描述查询-不用背语法" class="headerlink" title="MongoDB:用 JSON 描述查询,不用背语法"></a>MongoDB:用 JSON 描述查询,不用背语法</h2><p>mongodb 插件的思路跟 SQL 那几个不太一样——Mongo 没有 SQL,它让你用 JSON spec 描述查询,底层走 pymongo,连 Atlas 的 SRV 连接串也支持。</p><p><img src="/medias/loading.gif" data-original="/images/posts/318_21a5ace02f411acafb441bf7bd803b5e.jpg" alt="用 JSON 描述 Mongo 查询,插件带 schema 缓存和采样"></p><p>它的风险分级是「Mongo 专属」的,不是把 SQL 那套硬套过来——<code>drop</code>、<code>deleteMany</code>、<code>updateMany</code> 这类破坏性操作会被单独认出来判档,而 <code>find</code> 这种只读的就放行。用 JSON spec 的好处是,查询意图结构化了,AI 生成起来不容易出语法错,你 review 的时候也一眼能看懂它要干嘛。一个 spec 说清集合和条件:</p><pre class="language-json" data-language="json"><code class="language-json">{  "collection": "orders",  "filter": { "status": "paid" },  "limit": 20}</code></pre><p>缓存这块它做得比 SQL 那几个更关键:按集合(per-collection)缓存 schema 和采样。Mongo 本身没有固定 schema,同一个集合里的文档字段可以千奇百怪,只有靠采样才能让 AI 知道文档大概长啥样、有哪些字段。所以第一次查一个陌生集合,它会先采一批样建立结构认知,之后再问就快了,也更准。</p><pre class="language-bash" data-language="bash"><code class="language-bash">python3 "$ROOT/scripts/mongodb.py" --profile prod query --spec ./find-paid.json</code></pre><p>插件 README 里把这套 JSON spec 用法和风险分类写得很清楚,想照着抄一个查询直接对着看就行:</p><p><img src="/medias/loading.gif" data-original="/images/posts/318_5689ef05c4e10c02653ce49ee58a916f.png" alt="mongodb 插件 README 的 JSON spec 使用示例"></p><p>连接串这块它也照顾到了云上场景,支持 Atlas 的 SRV 连接格式(<code>mongodb+srv://…</code>),托管在 Atlas 的库不用自己拆分片地址。我拿它翻过一个日志集合,字段嵌套三四层,平时用 Compass 一层层点开看眼睛疼,让 AI 按采样出来的结构直接捞我要的那几个字段、顺手做个计数,比手写聚合管道舒服太多。</p><h2 id="Zeppelin-让-AI-在-notebook-里跑-Spark-作业"><a href="#Zeppelin-让-AI-在-notebook-里跑-Spark-作业" class="headerlink" title="Zeppelin:让 AI 在 notebook 里跑 Spark 作业"></a>Zeppelin:让 AI 在 notebook 里跑 Spark 作业</h2><p>zeppelin 插件把面铺得最宽——它能在 Apache Zeppelin 上跑 SQL、Spark、PySpark,甚至 shell 命令,同样带风险控制那套刹车。数据团队如果已经把 Zeppelin 当交互式分析入口,这个插件等于让 AI 直接进到你的 notebook 环境里干活。</p><pre class="language-bash" data-language="bash"><code class="language-bash">python3 "$ROOT/scripts/zeppelin.py" --profile prod run --sql 'SELECT count(*) FROM events'</code></pre><p>一个我会用的场景:让 AI 对着数仓跑一段临时统计。比如「统计上周每天的活跃用户数,按天分组」,它生成一段 PySpark、判档、我确认后在 Zeppelin 里执行,结果回来再顺手问「哪天掉得最狠、大概掉了多少」。以前这套要自己开 notebook、写 Spark、等结果、再手动读数;现在对话里一气呵成,notebook 只是执行后端。</p><pre class="language-bash" data-language="bash"><code class="language-bash">python3 "$ROOT/scripts/zeppelin.py" --profile prod run --pyspark 'df = spark.table("events"); df.groupBy("dt").count().show()'</code></pre><p>因为它连 shell 都能跑,风险刹车在这儿尤其关键——这也是四个插件都先做 risk 分级再执行的原因,口子开得越大,越需要那道闸。Zeppelin 本身是段落式(paragraph)的交互 notebook,一个段落一个解释器,这个插件相当于让 AI 成为你 notebook 里的另一双手:它写 PySpark、你审风险、它跑、结果回来接着分析,人始终在环里。对已经把 Zeppelin 当日常分析入口的数据团队,这个衔接省掉了「AI 给代码 → 我复制进 notebook → 跑 → 再贴结果回去问」的来回。</p><h2 id="Alidocs-把钉钉知识库搬回本地"><a href="#Alidocs-把钉钉知识库搬回本地" class="headerlink" title="Alidocs:把钉钉知识库搬回本地"></a>Alidocs:把钉钉知识库搬回本地</h2><p><img src="/medias/loading.gif" data-original="/images/posts/318_03a3652ed64833d65c9343520e845372.jpg" alt="alidocs 在本地 Markdown 目录和钉钉云端文档树之间双向同步"></p><p>alidocs 是市场里唯一的异类,不碰数据库,专门管钉钉/阿里文档。它认证靠 cookie——从浏览器登录态里把完整 Cookie 头拷出来,配置放 <code>~/.taku/alidocs.json</code>:</p><pre class="language-json" data-language="json"><code class="language-json">{  "cookie": "cna=…; doc_atoken=…; XSRF-TOKEN=…",  "default_out_dir": "~/Downloads/alidocs",  "use_playwright": true}</code></pre><p>依赖是分层按需装的,不用一上来全装:<code>requests</code> 必装,只下载文档有它就够;要往上传/同步得加 <code>oss2</code>(走阿里云 OSS);要把在线文档导出成本地文件还得装 <code>playwright</code> 再 <code>playwright install chromium</code>,靠无头浏览器渲染导出。<code>space_id</code> 不填它会自动探测,<code>use_playwright</code> 开关控制要不要走在线导出。这种「基础功能零重依赖、高级功能才拉重家伙」的分层,让只想下载的人不必背上一个 Chromium。</p><p>我自己最先用起来的就是它——团队的钉钉知识库我一直想整份扒下来做本地检索。它能拉单篇,也能顺着链接把一整个文件夹甚至整个知识库递归拉下来:</p><pre class="language-bash" data-language="bash"><code class="language-bash">python3 "$ROOT/scripts/alidocs.py" fetch --url 'https://alidocs.dingtalk.com/i/nodes/&lt;uuid&gt;' --out ~/docs/team-kb</code></pre><p>插件 README 把 fetch / push / sync 三种用法和 cookie 配置都列全了,照着抄就能跑:</p><p><img src="/medias/loading.gif" data-original="/images/posts/318_76b083aaa711b0901673ee20627d201d.png" alt="alidocs 插件 README 的下载/同步用法示例"></p><p>反过来把本地目录推回云端才是它的另一半价值,尤其适合「本地用 Markdown 写、写完同步回钉钉」的工作方式。推送默认让你先 <code>--dry-run</code> 看一眼要改哪些文件、建哪些节点,确认没问题再 <code>--yes</code> 真跑。同步还能选冲突策略和清理多余文件:</p><pre class="language-bash" data-language="bash"><code class="language-bash">python3 "$ROOT/scripts/alidocs.py" sync --src ./my-project \    --dest '&lt;链接&gt;' --on-conflict overwrite --prune --yes</code></pre><p><code>--on-conflict</code> 控制本地和云端都改过时听谁的,<code>overwrite</code> 就是本地覆盖云端;<code>--prune</code> 会把云端多出来、本地没有的文件删掉,让两边严格对齐。这两个开关配上,alidocs 才算真能当「同步」用,而不只是单向下载。<code>--prune</code> 这种会删远端文件的动作,配 <code>--dry-run</code> 先跑一遍看清要删啥,不亏。</p><p>cookie 会过期是它唯一的日常磕碰——失效了整份重新从浏览器登录态拷一次,把配置里的 <code>cookie</code> 字段换掉就行,已经下载到本地的文件不受影响。对我这种一个月才批量同步一两次的用法,重拷 cookie 的成本可以忽略。</p><h2 id="我会怎么用-谁先别急装"><a href="#我会怎么用-谁先别急装" class="headerlink" title="我会怎么用,谁先别急装"></a>我会怎么用,谁先别急装</h2><p>这套东西适合谁,我心里有杆秤。<strong>如果你已经在用 Claude Code 做日常的数据排查、运维或者文档整理,它把「AI 只能给建议」变成了「AI 能直接动手查」,省掉了复制粘贴那一趟。</strong> 数据团队接 Zeppelin、后端接 MySQL、做知识库的接 alidocs,各取所需。</p><p>但有两种情况我建议先别急。一是别把它当 GUI 客户端的替代品——它是给对话式操作用的,你要肉眼翻一百张表的数据,Navicat 还是更趁手。二是千万别图省事把自动批准直接开到 high:</p><pre class="language-bash" data-language="bash"><code class="language-bash">export MYSQL_AUTO_APPROVE_LEVEL=high   # 别这么干,等于把刹车拆了</code></pre><p>我生产库一律只开 <code>safe</code>。AI 再聪明,<code>DELETE</code> 不带 WHERE 这种事,我也不想给它自动执行的机会——那道风险闸留着,就是为了这一刻。</p><p>还有个安全细节值得先说清:这些凭证——数据库密码、钉钉的 cookie——都是明文躺在 <code>~/.taku/</code> 下的 JSON 里。这跟大多数 CLI 工具一个路数,但既然它连的是生产库和公司文档,我的习惯是给这个目录收紧权限(<code>chmod 600</code>),敏感环境干脆用环境变量注入、不落盘。alidocs 的 cookie 还会过期,失效了重新从浏览器拷一份就行。</p><p>装之前也建议翻一眼对应插件的 <code>scripts/</code> 源码——都是纯 Python,几百行读得完,确认它连库之后到底会跑什么,比盲装一个能碰生产的工具踏实。</p><p>最后放一张速查表,方便按需回来查:</p><table><thead><tr><th>插件</th><th>连什么</th><th>一句话用途</th><th>额外依赖</th></tr></thead><tbody><tr><td>mysql</td><td>MySQL</td><td>一句话查库,纯 Python 协议免驱动</td><td>无</td></tr><tr><td>starrocks</td><td>StarRocks</td><td>对数仓问聚合分析问题</td><td>无</td></tr><tr><td>mongodb</td><td>MongoDB / Atlas</td><td>JSON spec 查询 + 采样建 schema</td><td>pymongo</td></tr><tr><td>zeppelin</td><td>Apache Zeppelin</td><td>让 AI 在 notebook 里跑 SQL/Spark/PySpark</td><td>视环境</td></tr><tr><td>alidocs</td><td>钉钉/阿里文档</td><td>下载、推送、同步整个知识库</td><td>requests(+oss2/playwright)</td></tr></tbody></table><p>想上手就从最贴你工作的那个插件开始装,一个就够,配置十分钟内能跑通:</p><pre class="language-bash" data-language="bash"><code class="language-bash">/plugin install mysql@zweite-tools</code></pre><p>五个插件我建议按需收着,哪天要连哪个库了再回来装对应那个,别一次全铺开。</p><p>真要我排个装机顺序:先 mysql,把 <code>MYSQL_AUTO_APPROVE_LEVEL</code> 锁在 <code>safe</code> 就能安全试水,10 分钟跑通;手上有 MongoDB 就补 mongodb,让它对目标集合先采一轮样;数据团队才需要动 zeppelin 接 notebook;alidocs 完全独立,看你有没有钉钉知识库要搬。五个里我日常真正天天开的就两个——mysql 查库、alidocs 同步文档,剩下三个按项目需要临时装。</p><p>还有个省事的细节:四个数据插件共用 <code>~/.taku/</code> 一个配置目录,<code>chmod 600 ~/.taku/*.json</code> 一条命令就把几份凭证的权限一起收紧了,不用挨个文件改。前面吐槽过的「配置收敛到一处」,到收尾这一步反而成了安全上的便利。</p><p>把 Claude Code 从「帮你写 SQL」升级成「帮你查库」,中间就差这么一个装了风险刹车的插件——四档分级、<code>safe</code> 默认批准、<code>~/.taku/</code> 一处管配置,这三件事记住就够上手。</p><p>值不值得装,说到底取决于你有没有「让 AI 直接动手查数据」的真实需求:有,它省掉你在 AI 和数据库客户端之间来回复制粘贴那一趟;没有,它就只是躺在 <code>~/.taku/</code> 里的几个 JSON,装了也闲置。</p><p>先收藏这篇,上面那张速查表留着——五个插件哪个连什么、缺哪个依赖、跑哪条命令,回来扫一眼就够,不用再翻一遍 README 或者插件源码。</p>]]></content>
      
      
      <categories>
          
          <category> AI-生态 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Claude Code </tag>
            
            <tag> 插件市场 </tag>
            
            <tag> 数据库运维 </tag>
            
            <tag> 钉钉文档 </tag>
            
            <tag> 效率工具 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CodeGraph 进阶：装好之后，这 4 个用法才是真省事</title>
      <link href="/2026/06/29/317/"/>
      <url>/2026/06/29/317/</url>
      
        <content type="html"><![CDATA[<h1 id="CodeGraph-进阶：装好之后，这-4-个用法才是真省事"><a href="#CodeGraph-进阶：装好之后，这-4-个用法才是真省事" class="headerlink" title="CodeGraph 进阶：装好之后，这 4 个用法才是真省事"></a>CodeGraph 进阶：装好之后，这 4 个用法才是真省事</h1><p><img src="/medias/loading.gif" data-original="/images/posts/317_707b9dc1ca6bf6854926a6134bfde6db.jpg" alt="工程师在一张代码知识图谱地图上熟练地圈点查询"></p><p><strong>📚 系列导航</strong>：本文是《CodeGraph 上手到进阶》第 2/2 篇。上一篇：<a href="https://mp.weixin.qq.com/s/64vPfCpwUrTi8gWFZQrkjA">CodeGraph 实战：给 AI agent 装上代码地图</a>。</p><p>上一篇把 CodeGraph 装好、接上了 Claude Code，建完图它就自动生效了。但我观察下来，大多数人就停在这儿——以为”装好就完事了”，结果只用到了它最表层的能力。</p><p>其实装好只是起点。CodeGraph 真正省事的地方，是几个得你主动用起来的进阶玩法。这篇挑 4 个我自己天天在用、官方也明确支持、上一篇没展开的：怎么把 <code>codegraph explore</code> 问到点子上、改代码前怎么用 <code>codegraph impact</code> 和 <code>codegraph affected</code> 看波及范围、怎么把它当库嵌进自己的工具、以及怎么在一个会话里查好几个仓库。讲讲怎么把它榨干。</p><h2 id="把问题问对，explore-才给你想要的"><a href="#把问题问对，explore-才给你想要的" class="headerlink" title="把问题问对，explore 才给你想要的"></a>把问题问对，explore 才给你想要的</h2><p><code>codegraph_explore</code> 是它对外唯一的工具，但同样一个仓库，问得好不好，拿回来的东西差很远。官方在给 agent 的指令里其实写明了三类最适合它的问法。</p><p>第一类是”X 是怎么工作的”。别问”看下这个文件”，直接问机制：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 问机制，它会把相关符号的源码 + 调用链一起给你codegraph explore "登录请求从路由到写库整条调用链是怎么走的"</code></pre><p>第二类是”X 怎么到达 Y”的流程追踪。这是它最能打的场景——因为它能跟住 grep 跟不动的动态分发（回调、接口到实现、框架路由）：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 追一条跨越动态分发的流程codegraph explore "一个 HTTP 请求是怎么最终触发 PaymentService 的"</code></pre><p>还有个细节很多人不知道：你在问题里<strong>直接点名一个文件或符号</strong>，它就当 Read 用，返回那个文件当前带行号的源码。所以”读文件”这件事也能走 explore，不用切回内置工具：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 点名文件 = 读它当前的源码（带行号，和 Read 一个形状）codegraph explore "src/auth/session.ts"</code></pre><p>反过来说，最浪费的问法是把它当搜索框用——“搜一下 login”这种，它能答，但你拿到的是一堆零散命中，跟 grep 没拉开差距。它的价值在”关系”而不是”匹配”：你问的是一条调用链、一个机制、一个符号的上下游，它才把预先建好的调用图用起来。</p><p>举个我自己遇到的例子：一个 React 项目里某个状态莫名其妙被刷新，grep 组件名根本追不到，因为中间隔着一层 context 的回调和重渲染。我前前后后 grep + read 折腾了快 20 分钟没结果，换成 <code>codegraph explore "这个状态是被谁触发更新的"</code>，它把回调那一跳也接上了，一次就指到真正的源头。这种动态分发的调用链，是它和纯文本搜索拉开差距的地方。</p><p>我的习惯是：凡是”这块代码怎么回事”的问题，先 explore 问一句，基本一次就够，省掉了 agent 一堆 grep+read 的探路。</p><h2 id="改代码前，先看一眼会炸到谁"><a href="#改代码前，先看一眼会炸到谁" class="headerlink" title="改代码前，先看一眼会炸到谁"></a>改代码前，先看一眼会炸到谁</h2><p>这是我觉得日常收益最高的一个用法。动一个函数之前，先问清楚它的波及范围，比改完才发现牵一发动全身强太多。</p><p>让 agent 改代码最怕的就是它”只看到眼前那个文件”——把一个被十几处调用的方法签名改了，自己以为改完了，结果一跑全是连锁报错。先用 impact 把影响半径摸清楚，agent 心里有数，要么一起改、要么提前提醒你这个动不得。我把”改公共方法前先 impact 一下”当成了固定动作。</p><p><code>codegraph impact</code> 直接给你某个符号的影响半径，<code>--depth</code> 控制往外追几层：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 改 UserService 之前，看两层以内会牵动什么codegraph impact UserService --depth 2# 也可以单独看谁调它、它调谁codegraph callers handleRequestcodegraph callees processOrder</code></pre><p>impact 给你的不是一个干巴巴的数字，而是顺着调用边一层层展开的受影响符号——谁直接调它、再往上谁调那些调用者。<code>--depth</code> 就是控制这个展开往外走几圈：改一个底层工具函数，往外两三层可能就牵动半个模块；改一个叶子方法，半径基本就它自己。看一眼这个范围，你心里对”这次改动是小手术还是大动干戈”就有数了。</p><p>把这个能力接进测试流程，价值更大。<code>codegraph affected</code> 顺着 import 依赖往上追，找出某次改动会影响哪些测试文件——改完只跑这部分，不用全量。配进 git hook 或 CI 是这样：</p><pre class="language-bash" data-language="bash"><code class="language-bash">#!/usr/bin/env bash# pre-push / CI：只跑被这次改动波及的测试AFFECTED=$(git diff --name-only HEAD | codegraph affected --stdin --quiet)if [ -n "$AFFECTED" ]; then  npx vitest run $AFFECTEDfi</code></pre><p>我在一个全量测试要跑 4 分多钟的项目里接了这个，改一两个文件时只跑被波及的那几个测试，CI 时间直接掉到 30 秒，比原来省了快 5 倍，等待体验完全不一样。它还支持自定义测试文件匹配，比如只挑 e2e：</p><pre class="language-bash" data-language="bash"><code class="language-bash">codegraph affected src/auth.ts --filter "e2e/*"</code></pre><p><code>impact</code> 和 <code>callers/callees</code> 都能带 <code>--json</code>，所以你也可以把它接到自己的脚本里做判断——比如”改动碰到了核心支付模块就强制要求人工 review”，拿 JSON 出来解析一下就能写。这一点在团队流程里比单纯人看更靠得住。</p><p><img src="/medias/loading.gif" data-original="/images/posts/317_5652e62dbd8f17a6dc3b98349fff755f.jpg" alt="改一个符号前用 impact 看波及范围再用 affected 只跑相关测试"></p><h2 id="不只在-agent-里：把-CodeGraph-当库嵌进自己的工具"><a href="#不只在-agent-里：把-CodeGraph-当库嵌进自己的工具" class="headerlink" title="不只在 agent 里：把 CodeGraph 当库嵌进自己的工具"></a>不只在 agent 里：把 CodeGraph 当库嵌进自己的工具</h2><p>很多人没注意到，CodeGraph 的 npm 包直接导出了编程 API，<code>import</code> 和 <code>require</code> 都能拿到那个 <code>CodeGraph</code> 类。也就是说你能把它嵌进自己的脚本或者程序里（比如某个 Electron 应用的主进程），自己驱动这张图。</p><p>什么时候会想这么干？最常见的是你想做一个 CodeGraph 之外的小工具——比如一个代码导航面板、一个”给我这个 bug 相关的所有代码”的内部命令、或者把它接到自己的 agent 工作流里当上下文来源。这些场景不走 MCP，直接调库更顺。其中 <code>buildContext</code> 我觉得最有意思：你丢一句自然语言任务进去，它帮你把相关的符号、代码、关系打包成一段现成的上下文（还能直接出 markdown），省得你自己拼。</p><p>一个最小例子，建图 + 搜符号 + 拿调用者 + 给一段任务组上下文：</p><pre class="language-typescript" data-language="typescript"><code class="language-typescript">import CodeGraph from '@colbymchenry/codegraph';const cg = await CodeGraph.init('/path/to/project');await cg.indexAll({  onProgress: (p) =&gt; console.log(`${p.phase}: ${p.current}/${p.total}`),});const results = cg.searchNodes('UserService');const callers = cg.getCallers(results[0].node.id);// 给一段自然语言任务直接组装出相关代码上下文const context = await cg.buildContext('fix login bug', {  maxNodes: 20, includeCode: true, format: 'markdown',});</code></pre><p>想常驻盯着文件变化、用完收尾，也都有对应方法：</p><pre class="language-typescript" data-language="typescript"><code class="language-typescript">cg.watch();    // 自动同步：文件变了就增量更新cg.unwatch();  // 停止监听cg.close();    // 收尾</code></pre><p>包里还顺带导出了一些更底层的积木，像 <code>DatabaseConnection</code>、<code>QueryBuilder</code> 这些，给那种想自己直接驱动图、不满足于高层方法的场景用；TypeScript 类型也一起带着，接起来不用自己补声明。</p><p>有一点要注意：这条路跑在<strong>你自己的运行时</strong>上，所以需要 Node 22.5+（它用了内置的 <code>node:sqlite</code>）。CLI 和 MCP 那套不受影响，它们跑在自带的 runtime 上，没这个版本要求。</p><h2 id="一个会话，查好几个仓库"><a href="#一个会话，查好几个仓库" class="headerlink" title="一个会话，查好几个仓库"></a>一个会话，查好几个仓库</h2><p>最后一个是 monorepo 和多仓库党会喜欢的。CodeGraph 的索引是按项目走的，每个项目自己 <code>codegraph init</code> 一次，建出各自的 <code>.codegraph/</code>。</p><p>monorepo 最烦的就是改 A 服务时想顺手看一眼 B 服务怎么调它，传统做法要么开两个窗口来回跳，要么 agent 把整个巨型仓库全量扫一遍——又慢又把无关代码塞进上下文。CodeGraph 的处理方式很对路：各服务各建各的图，互不干扰。</p><p>关键在于：查询时只要传一个 <code>projectPath</code>，就能在<strong>同一个 agent 会话里</strong>命中任意一个建过索引的项目——主服务、另一个子服务、甚至旁边那个仓库，来回切都行。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 在某个项目目录里建好它自己的图cd services/payment &amp;&amp; codegraph init# 各服务各自一份索引，互不干扰cd ../user &amp;&amp; codegraph init</code></pre><p>碰到一个还没建索引的路径，它不会报错，而是回一句”这块你用内置工具吧”，干净地把决定权留给你——要不要为某个目录建图，始终是你说了算。对那种”只想给核心几个服务建图、不想全量扫”的大单体仓库，这个设计很省心。</p><p>我自己在一个前后端放一起的仓库里就是这么用的：后端服务建了图，前端那一大坨还在重构的代码暂时没建。问后端的调用链它走图、又快又准；偶尔碰前端，它自动退回 grep/read，不会因为”没索引”就卡住或者报错。这种”建到哪算到哪、没建也不添乱”的渐进式策略，比那种”要么全量索引要么没法用”的工具舒服多了——你可以从最该被理清的那块代码开始，慢慢铺。</p><p>装好只是把地图铺上，真正的省事是从你开始主动查这张图开始的。先挑你最熟的那个项目，下次改代码前先问它一句：</p><pre class="language-bash" data-language="bash"><code class="language-bash">codegraph impact &lt;你要改的那个函数名&gt;</code></pre><p><strong>这几个用法值得收藏</strong>——下次接新项目或者动老代码时，照着试一遍，体感会很不一样。</p>]]></content>
      
      
      <categories>
          
          <category> Coding-Agent </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Claude Code </tag>
            
            <tag> CodeGraph </tag>
            
            <tag> MCP </tag>
            
            <tag> AI编程 </tag>
            
            <tag> 代码知识图谱 </tag>
            
            <tag> 进阶用法 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CodeGraph 实战：给 AI agent 装上代码地图，tool calls 砍掉一半</title>
      <link href="/2026/06/28/315/"/>
      <url>/2026/06/28/315/</url>
      
        <content type="html"><![CDATA[<h1 id="CodeGraph-实战：给-AI-agent-装上代码地图，tool-calls-砍掉一半"><a href="#CodeGraph-实战：给-AI-agent-装上代码地图，tool-calls-砍掉一半" class="headerlink" title="CodeGraph 实战：给 AI agent 装上代码地图，tool calls 砍掉一半"></a>CodeGraph 实战：给 AI agent 装上代码地图，tool calls 砍掉一半</h1><p><img src="/medias/loading.gif" data-original="/images/posts/315_0416b47504384d8f84538c243cd4a844.jpg" alt="AI agent 面对庞大代码库，旁边一张发光的知识图谱地图"></p><p>让 Claude Code 改一个稍微大点的项目，你大概都见过这个画面：它先 <code>ls</code> 一下、再 <code>grep</code> 几个关键词、然后挨个 <code>read_file</code>，把调用链在脑子里拼出来——真正动手改之前，已经烧掉十几个 tool call。</p><p>代码它早晚能找到，问题是这趟”探路”既慢又费 token。我自己在一个几千文件的仓库里盯着 agent 干活，经常一半时间它都在翻文件而不是写代码。</p><p><a href="https://github.com/colbymchenry/codegraph">CodeGraph</a> 想干掉的就是这段浪费：把任意一个文件夹预先解析成一张代码知识图谱，通过 MCP 喂给 agent。下面这篇我按它的<strong>官方最新版本</strong>走一遍——提醒一句，网上不少教程里的 <code>codegraph init -i</code> 之类命令已经过时了，以官方资料为准。</p><h2 id="它解决的是-agent-“找代码”-的浪费"><a href="#它解决的是-agent-“找代码”-的浪费" class="headerlink" title="它解决的是 agent “找代码” 的浪费"></a>它解决的是 agent “找代码” 的浪费</h2><p>先把问题说清楚。没有索引的时候，agent 理解一段陌生代码，靠的是内置工具一遍遍地搜：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># agent 回答"请求是怎么到数据库的"——典型的探路过程grep -r "Database" src/ls src/services/# 然后挨个 read 命中的文件，自己在上下文里重建调用链……</code></pre><p>CodeGraph 的思路是把这件事<strong>提前做掉</strong>：它一次性扫完整个仓库，记下每个符号（函数、类、方法）、每条调用边、每个依赖关系，存成一张图。agent 要查的时候问一句，直接拿回相关源码、调用路径、还有改动的”波及范围”（blast radius）——不用再一个文件一个文件地翻。</p><p><img src="/medias/loading.gif" data-original="/images/posts/315_44a420f5343e8dc3504123ae6a507df5.jpg" alt="无CodeGraph的探路循环对比有CodeGraph的一次查询"></p><p>官方在 7 个真实开源项目上做了对比（Claude Code headless，每组取 4 次中位数），结论挺直接：<strong>tool calls 中位数少 58%、回答快 22%、文件读取从一堆降到接近 0</strong>。</p><table><thead><tr><th>项目</th><th>语言 · 规模</th><th>tool calls</th><th>文件读取</th><th>token</th></tr></thead><tbody><tr><td>VS Code</td><td>TS · ~1万文件</td><td>少 81%</td><td>0 vs 9</td><td>少 64%</td></tr><tr><td>Django</td><td>Python · ~3千</td><td>少 77%</td><td>0 vs 9</td><td>少 60%</td></tr><tr><td>Tokio</td><td>Rust · ~790</td><td>少 57%</td><td>0 vs 8</td><td>少 38%</td></tr><tr><td>Alamofire</td><td>Swift · ~110</td><td>少 58%</td><td>0 vs 9</td><td>少 64%</td></tr></tbody></table><p>官方自己也很诚实地标了一句：<strong>省 token、省钱是”规模相关”的</strong>——小仓库上每次查询省的那点钱又小又飘，得是大到 Google/Microsoft 那种单体仓库、再乘上整个团队每天的用量，才会攒成账单上的一笔。所以小项目用它，图的是<strong>快和准</strong>，不是省钱。这点比那些张口就是”省一半成本”的宣传实在得多。</p><p>你可能会担心一个问题：图是预建的，那我刚改完文件、图还没更新的那一小会儿，agent 会不会拿到旧答案？CodeGraph 在这里做得挺细——改动落盘后监听器很快触发（默认防抖 2 秒），而在这段空窗里，如果 agent 查到一个还没同步的文件，返回结果会顶一个 <code>⚠️</code> 提示，明确告诉它”这个文件刚改过，直接 Read 一下”。实测里 Claude Code 真的会照做，先去读实时内容再回答。不会出现那种”索引过期了还一本正经给你错答案”的情况。</p><p>还有个容易被忽略的场景：你在终端 <code>git pull</code> 了一把、或者用别的编辑器改了几个文件，这期间 MCP server 可能根本没在跑。CodeGraph 在 server 每次重连时会先做一轮 <code>(大小, mtime)</code> + 内容哈希的核对，把这些”背着它发生”的改动在回答第一个查询前补进来。所以不管改动是谁干的、什么时候干的，下次一问都是新的。</p><p><img src="/medias/loading.gif" data-original="/images/posts/315_92d24588aca375e7a3ba9d36fda390c1.png" alt="CodeGraph 官方仓库的 benchmark 对比表"></p><h2 id="三步把-CodeGraph-接上-Claude-Code-Cursor"><a href="#三步把-CodeGraph-接上-Claude-Code-Cursor" class="headerlink" title="三步把 CodeGraph 接上 Claude Code / Cursor"></a>三步把 CodeGraph 接上 Claude Code / Cursor</h2><p>接入分得很清楚：<strong>装 CLI → 连 agent → 给项目建索引</strong>，三件事，别混在一起。</p><p>第一步，装 CLI。它自带运行时，本机没有 Node 也能跑：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># macOS / Linux —— 一行装好，自动挑对应你系统的构建curl -fsSL https://raw.githubusercontent.com/colbymchenry/codegraph/main/install.sh | sh# 已经有 Node？用 npm 也行（任意版本都可以）npm i -g @colbymchenry/codegraph</code></pre><p>装完<strong>记得新开一个终端</strong>——安装脚本把 <code>codegraph</code> 放进了 PATH，但不会改当前这个 shell，不换终端下一步会找不到命令。</p><p>第二步，把 CodeGraph 连到你用的 agent 上。这一步才是”接线”，只装 CLI 是不够的：</p><pre class="language-bash" data-language="bash"><code class="language-bash">codegraph install</code></pre><p>它会自动探测你机器上装了哪些 agent——Claude Code、Cursor、Codex CLI、opencode、Gemini CLI、Antigravity、Kiro、Hermes Agent 都支持——然后把 MCP server 配置写进各自的配置文件。注意这步<strong>只接线、不建索引</strong>，全局跑一次就覆盖所有项目。</p><p>第三步，进到具体项目里建图。这一步才真正扫你的代码：</p><pre class="language-bash" data-language="bash"><code class="language-bash">cd your-projectcodegraph init</code></pre><p><code>codegraph init</code> 一条命令搞定两件事：建出本地 <code>.codegraph/</code> 目录，并把整张图建好。建完之后<strong>自动同步默认就开着</strong>——文件改了它用系统原生事件（FSEvents / inotify）盯着，防抖一下增量更新，索引永远是新的，没有什么 <code>sync</code> 要你手动跑。把 agent 重启一下让 MCP 加载，就能用了。</p><p>如果是配进 CI 或者脚本里，<code>install</code> 支持完全非交互的写法，指定目标和范围就行：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 自动探测 agent、装到全局，全程不问codegraph install --yes# 只接指定的几个，且只对当前项目生效codegraph install --target=cursor,claude --location=local</code></pre><p>想升级直接 <code>codegraph upgrade</code>，它会自己认出你当初是怎么装的（bundle / npm / npx）就地更新；哪天不想用了，<code>codegraph uninstall</code> 会把它写进各个 agent 的那些配置干净地撤掉，项目里的 <code>.codegraph/</code> 索引留着不动。想连某个项目的索引一起清掉，进那个项目跑 <code>codegraph uninit</code> 就行——两件事分开，删 agent 配置和删项目图谱互不牵连。</p><p><img src="/medias/loading.gif" data-original="/images/posts/315_efb010273db477147182afd9e51e9821.jpg" alt="三步接入流程：装CLI、连agent、建索引"></p><h2 id="一次查询，拿到答案而不是一堆文件"><a href="#一次查询，拿到答案而不是一堆文件" class="headerlink" title="一次查询，拿到答案而不是一堆文件"></a>一次查询，拿到答案而不是一堆文件</h2><p>接好之后，agent 在回答结构性问题时就会自动走 CodeGraph，不用你操心。它对外其实<strong>只暴露一个 MCP 工具</strong>——<code>codegraph_explore</code>。</p><p>这是我觉得设计上很克制的一点。官方实测发现，给 agent 一堆细分工具（查调用者、查被调用、查依赖……）反而容易选错，不如一个够强的工具好用。所以 <code>explore</code> 一次调用就把相关符号的源码（按文件分组）、它们之间的调用路径（包括 grep 跟不动的动态分发）、加上波及范围一起返回。</p><p><img src="/medias/loading.gif" data-original="/images/posts/315_29b0c9bdc07691a47b095d84bdc47d02.jpg" alt="codegraph_explore 一次调用返回的三样东西"></p><p>你也可以直接在命令行里手动试，输出和 agent 拿到的一模一样：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 一句话问清楚某个流程codegraph explore "请求是怎么经过中间件链的"# 查一个符号会牵动谁——改之前先看波及范围codegraph impact UserService# 全文搜符号，FTS5 驱动，瞬出codegraph query "login" --kind function</code></pre><p>单体仓库或者多仓库场景也照顾到了：只要某个目录建过 <code>.codegraph/</code> 索引，查询时传一个 <code>projectPath</code> 就能命中它——一个会话里既查主服务、又查另一个子服务都行。碰到没建过索引的路径，它不会报错，而是回一句”这块用你的内置工具吧”，建不建索引始终是你自己决定。</p><p>如果你想手动接 Claude Code，配置也就这么一段——往 <code>~/.claude.json</code> 里加：</p><pre class="language-json" data-language="json"><code class="language-json">{  "mcpServers": {    "codegraph": {      "type": "stdio",      "command": "codegraph",      "args": ["serve", "--mcp"]    }  }}</code></pre><p>“只有一个工具”其实是默认姿态，不是全部。CodeGraph 另外还藏了 7 个更细的工具——<code>codegraph_node</code>、<code>codegraph_search</code>、<code>codegraph_callers</code>、<code>codegraph_callees</code>、<code>codegraph_impact</code>、<code>codegraph_files</code>、<code>codegraph_status</code>。它们默认不挂到 MCP 上，因为这些信息 <code>explore</code> 的返回里基本都带了。真要单独打开，用一个环境变量点名即可：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 按需把额外的工具挂回 MCP surfaceexport CODEGRAPH_MCP_TOOLS=explore,node,search,callers</code></pre><p>手动接的话，顺手在 <code>~/.claude/settings.json</code> 里给它的工具开个 auto-allow，省得每次调用都弹确认（用 <code>codegraph install</code> 接 Claude Code 时这步会自动做掉）：</p><pre class="language-json" data-language="json"><code class="language-json">{  "permissions": {    "allow": ["mcp__codegraph__*"]  }}</code></pre><p>还有个我觉得很实用的命令是 <code>codegraph affected</code>：顺着 import 依赖往上追，找出某次改动会影响哪些测试文件。配进 CI 或者 git hook，改完只跑受影响的那部分测试，不用全量：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 从 git diff 喂进去，只跑被波及的测试git diff --name-only HEAD | codegraph affected --stdin --quiet</code></pre><p>真正让我觉得”这东西比 grep 强一个量级”的，是它能跨语言边界把调用链接上。纯靠 tree-sitter 静态解析，到语言边界就断了——Swift 调一个被自动桥接的 ObjC 方法、JS 通过 React Native 桥调原生模块、Expo Modules 的那套 DSL，grep 全都跟不动。CodeGraph 把这些 hop 显式补进图里，<code>explore</code> 出来的调用路径和波及范围能直接穿过去。它还认得 17 种 Web 框架的路由写法：Django 的 <code>path()</code>、FastAPI 的 <code>@app.get()</code>、Express 的 <code>router.post()</code>……查一个 controller 的调用者，会顺带把绑它的 URL 模式一起带出来。这种”动态分发 + 路由绑定”的关系，正是人肉 grep 最容易漏的部分。</p><h2 id="原理：tree-sitter-解析-SQLite-图-自动同步"><a href="#原理：tree-sitter-解析-SQLite-图-自动同步" class="headerlink" title="原理：tree-sitter 解析 + SQLite 图 + 自动同步"></a>原理：tree-sitter 解析 + SQLite 图 + 自动同步</h2><p>底层其实不神秘，四步：tree-sitter 按语法解析出 AST，抽取节点（函数/类/方法）和边（调用/导入/继承）；全部塞进本地一个 SQLite 库（<code>.codegraph/codegraph.db</code>，带 FTS5 全文搜索）；再做一轮引用解析，把调用对上定义、导入对上源文件；最后文件监听做增量同步。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 想确认索引状态，随时看codegraph status# 关掉匿名遥测（它默认会收集用了哪些命令/语言，但绝不碰你的代码和路径）codegraph telemetry off</code></pre><p>它几乎<strong>零配置</strong>。20 多种语言（TS/JS/Python/Go/Rust/Java/C#/Swift/Kotlin… 一直到 Vue/Svelte/Astro）靠扩展名自动识别，不用你一个个配。<code>node_modules</code>、<code>dist</code>、<code>.venv</code> 这些目录、还有 <code>.gitignore</code> 里的东西、大于 1MB 的文件，默认全部跳过——图里只有你自己的代码，没有第三方噪音。</p><p>真要调，也就一个可选的 <code>codegraph.json</code>，比如把某个提交进仓库的 vendored 主题排除掉，或者给非标准扩展名指个语言：</p><pre class="language-json" data-language="json"><code class="language-json">{  "exclude": ["static/", "**/vendor/**"],  "extensions": { ".tpl": "php" }}</code></pre><p><img src="/medias/loading.gif" data-original="/images/posts/315_9cb4e670d98652c5963c6e4524a81311.jpg" alt="tree-sitter 解析到 SQLite 图再到 MCP 查询的数据流"></p><p>如果你想把它嵌进自己的程序里（比如某个 Electron 应用的主进程），npm 包还直接导出了 <code>CodeGraph</code> 类，<code>import</code> 和 <code>require</code> 都能拿到，自己 <code>init</code>、<code>searchNodes</code>、<code>buildContext</code> 一条龙；只是这条路跑在你自己的运行时上，需要 Node 22.5+（用的是内置的 <code>node:sqlite</code>）。CLI 和 MCP 那套不受影响，它们跑在自带的 runtime 上。</p><p>还有一点对我这种谨慎的人很重要：<strong>全程本地</strong>。源码和那个 SQLite 库都不离开你的机器，没有 API key、不连外部服务。给公司私有仓库建图，心里踏实。</p><p>完整命令和配置可以翻官方文档站，更新很勤，写得也清楚。</p><h2 id="用之前，有几个点值得注意"><a href="#用之前，有几个点值得注意" class="headerlink" title="用之前，有几个点值得注意"></a>用之前，有几个点值得注意</h2><p>最容易踩的一个：CodeGraph 只在 agent <strong>直接查询</strong>它的时候才省事。如果你的 agent 习惯把”先去仓库探一探”这件事外包给一个专门读文件的子 agent（比如 Explore 类 sub-agent），那子 agent 照样一个个 read 文件，CodeGraph 反而成了白挂的一层。官方为此特地在给 agent 的指令里写明”自己直接答，别把探索甩给读文件的子 agent”——用 <code>codegraph install</code> 接 Claude Code 时这段会自动写进去，但你要是手动接、或者套了自定义工作流，记得让主 agent 自己查。</p><p>第二个点跟自动同步有关。前面说”索引永不过期”有个前提：那个文件监听的守护进程得能跑起来。要是你在沙箱环境里、或者显式设了 <code>CODEGRAPH_NO_DAEMON=1</code> 把它关了，watcher 就不工作了，这时候改完代码得自己手动同步一下：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 守护进程没跑时（沙箱 / CI / 受限容器），手动增量同步codegraph sync</code></pre><p>本地正常用基本碰不到，但在 CI 或者受限容器里跑要心里有数。</p><p>最后一点是心态问题：这工具还很年轻、迭代很快（写这篇时最新版才发布没多久），命令和默认行为往后都可能再变。要是你想要个稳定的版本，别一直跟最新，锁一个就好：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 看看有没有新版codegraph upgrade --check# 锁定到指定版本，不自动跟最新codegraph upgrade 1.1.2</code></pre><p>如果你天天用 Claude Code 或者 Cursor 啃大项目，CodeGraph 这种”先建图、再让 agent 查图”的路子，比起一遍遍 grep，体感差距是实打实的。</p><p>下一步很简单，挑一个你最熟的项目，跑这一条命令开始：</p><pre class="language-bash" data-language="bash"><code class="language-bash">codegraph init</code></pre><p><strong>觉得有用就点个收藏吧</strong>——回头接自己项目的时候，照着这三步走一遍就行，省得再翻。</p>]]></content>
      
      
      <categories>
          
          <category> Coding-Agent </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Claude Code </tag>
            
            <tag> 实战教程 </tag>
            
            <tag> CodeGraph </tag>
            
            <tag> MCP </tag>
            
            <tag> AI编程 </tag>
            
            <tag> 代码知识图谱 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>打破 API 围墙：基于 Claude Code 与 Ollama 的混合云 AI 编程助手架构深度解析</title>
      <link href="/2026/06/26/314/"/>
      <url>/2026/06/26/314/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>📄 Abstract：核心摘要</strong><br>本文将从架构师视角深度拆解 <strong>Claude Code</strong> (Anthropic 的下一代 CLI Agent) 与 <strong>Ollama</strong> (本地大模型运行时) 的集成方案。我们将探讨如何通过协议劫持与环境注入，让 Claude Code 运行在本地 <code>Qwen2.5-Coder</code> 或 DeepSeek 模型之上，同时利用 Ollama 的 <code>:cloud</code> 混合云能力实现算力弹性。文章包含完整的通信链路分析、环境配置脚本、模型选型矩阵以及 TCO (总拥有成本) 对比分析。</p></blockquote><p><img src="/medias/loading.gif" data-original="/images/posts/314_fc2c5dff76576c45023c54a00bf95291.jpg" alt="封面图"></p><h2 id="1-引言：Agent-时代的算力主权"><a href="#1-引言：Agent-时代的算力主权" class="headerlink" title="1. 引言：Agent 时代的算力主权"></a>1. 引言：Agent 时代的算力主权</h2><p>Claude Code 的发布标志着 AI 编程助手从 “Copilot (副驾驶)” 向 “Agent (智能体)” 的进化。与简单的代码补全不同，Claude Code 具备阅读文件、执行终端命令、管理 Git 版本控制的<strong>自主权</strong>。然而，默认配置下它强依赖 Anthropic 的 SaaS 服务 (Sonnet 3.5)，这对于拥有严格数据隐私合规要求（如金融、军工、核心研发）的企业架构师来说，是一个无法忽视的阻碍。</p><p>Ollama 的出现以及其近期对 OpenAI/Anthropic 协议的兼容，为我们提供了一个完美的 “B 计划”：<strong>Bring Your Own Model (BYOM)</strong>。</p><p>通过将 Claude Code 强大的 Agent 逻辑与 Ollama 灵活的推理后端结合，我们可以构建一个既拥有顶级交互体验，又完全掌握数据主权的 AI 编程系统。更进一步，Ollama 新推出的 <code>:cloud</code> 混合云模式，允许我们在本地算力不足时，无缝切换到云端高性能模型，形成了完美的 “端云协同” 架构。</p><h2 id="2-架构原理：协议劫持与接口适配"><a href="#2-架构原理：协议劫持与接口适配" class="headerlink" title="2. 架构原理：协议劫持与接口适配"></a>2. 架构原理：协议劫持与接口适配</h2><p>要理解这一方案的可行性，首先需要剖析 Claude Code 的通信机制。</p><h3 id="2-1-通信链路分析"><a href="#2-1-通信链路分析" class="headerlink" title="2.1 通信链路分析"></a>2.1 通信链路分析</h3><p>Claude Code 本质上是一个运行在 Node.js 环境中的 CLI 客户端。它通过标准 HTTP 请求与远端推理 API 进行交互。其核心工作流如下：</p><ol><li><strong>System Prompt 注入</strong>：内置了大量的工具定义（读写文件、Shell 执行等）和安全围栏。</li><li><strong>Context Management</strong>：自动管理会话上下文，截断过长的历史记录。</li><li><strong>Tool Execution</strong>：解析 LLM 返回的 Function Call，在本地执行后将结果回传。</li></ol><p><img src="/medias/loading.gif" data-original="/images/posts/314_f8d265a65ad30cf64e707166ad3e12de.jpg" alt="架构示意图"></p><h3 id="2-2-兼容性层-The-Compatibility-Layer"><a href="#2-2-兼容性层-The-Compatibility-Layer" class="headerlink" title="2.2 兼容性层 (The Compatibility Layer)"></a>2.2 兼容性层 (The Compatibility Layer)</h3><p>Ollama 在 v0.14+ 版本中引入了对 Anthropic Messages API 的部分兼容性支持，或者更准确地说，是社区通过中间件或 Ollama 原生支持实现了接口对齐。</p><p>当我们配置 <code>ANTHROPIC_BASE_URL</code> 时，我们实际上是在告诉 Claude Code：”不要去访问 <code>api.anthropic.com</code>，去访问我指定的地址”。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 标准请求路径https://api.anthropic.com/v1/messages# 重定向后的路径http://localhost:11434/v1/messages  (Ollama 提供的兼容接口)</code></pre><p><strong>架构师视角的挑战：</strong><br>虽然接口通了，但<strong>模型能力</strong>是最大的瓶颈。Claude Code 严重依赖<strong>Function Calling (工具调用)</strong> 能力。如果后端模型（如 Llama 3 8B）无法准确理解 XML 标签或 JSON Schema 的工具定义，Agent 就会陷入 “幻觉循环” 或无法执行任何操作。因此，模型选型至关重要（详见第 4 节）。</p><h2 id="3-实战部署：构建本地-Agent-运行时"><a href="#3-实战部署：构建本地-Agent-运行时" class="headerlink" title="3. 实战部署：构建本地 Agent 运行时"></a>3. 实战部署：构建本地 Agent 运行时</h2><p>本节将详细演示如何在 macOS/Linux 环境下搭建该系统。</p><h3 id="3-1-环境准备"><a href="#3-1-环境准备" class="headerlink" title="3.1 环境准备"></a>3.1 环境准备</h3><p>确保你已安装以下组件：</p><ul><li><strong>Node.js v18+</strong>: 运行 Claude Code。</li><li><strong>Ollama v0.14+</strong>: 必须支持兼容 API。</li><li><strong>Claude Code</strong>: <code>npm install -g @anthropic-ai/claude-code</code></li></ul><h3 id="3-2-核心配置-The-Magic-Variables"><a href="#3-2-核心配置-The-Magic-Variables" class="headerlink" title="3.2 核心配置 (The Magic Variables)"></a>3.2 核心配置 (The Magic Variables)</h3><p>我们需要在 Shell 配置文件（<code>.zshrc</code> 或 <code>.bashrc</code>）中注入环境变量。这里有两种策略：<strong>纯本地模式</strong> 和 <strong>混合云模式</strong>。</p><blockquote><p><strong>💡 Tip：架构师建议</strong><br>建议使用 <code>alias</code> 来区分不同模式，而不是全局污染环境变量，这样你可以灵活切换 “满血版 Claude” (官方 API) 和 “隐私版 Claude” (本地 Ollama)。</p></blockquote><h4 id="策略-A：纯本地模式-Privacy-First"><a href="#策略-A：纯本地模式-Privacy-First" class="headerlink" title="策略 A：纯本地模式 (Privacy First)"></a>策略 A：纯本地模式 (Privacy First)</h4><p>适用于离线环境或极高隐私敏感度场景。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 添加到 ~/.zshrcalias claude-local='env \  ANTHROPIC_API_KEY="ollama" \  ANTHROPIC_BASE_URL="http://localhost:11434/v1" \  claude'</code></pre><ul><li><code>ANTHROPIC_API_KEY="ollama"</code>: 这是一个占位符，Ollama 本地模式不需要真实 Key，但不传会报错。</li><li><code>ANTHROPIC_BASE_URL</code>: 指向 Ollama 的本地服务端口。</li></ul><h4 id="策略-B：Ollama-混合云模式-Hybrid-Power"><a href="#策略-B：Ollama-混合云模式-Hybrid-Power" class="headerlink" title="策略 B：Ollama 混合云模式 (Hybrid Power)"></a>策略 B：Ollama 混合云模式 (Hybrid Power)</h4><p>Ollama 现在支持 <code>:cloud</code> 后缀的模型，这些模型运行在 Ollama 托管的云端（通常是高性能模型，如 Llama 3.1 405B）。</p><blockquote><p><strong>ℹ️ Info：为什么需要混合云？</strong><br>本地运行 70B 参数以上的模型通常需要 48GB+ VRAM（双路 RTX 3090/4090）。对于普通 MacBook Pro 用户，<code>:cloud</code> 提供了按需访问顶级算力的能力，且无需复杂的 AWS/Azure 部署。</p></blockquote><ol><li>注册 Ollama 账号并获取 Public Key。</li><li>在本地登录：<code>ollama login</code></li><li>配置别名：</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">alias claude-cloud='env \  ANTHROPIC_API_KEY="your-ollama-cloud-key" \  ANTHROPIC_BASE_URL="https://api.ollama.com/v1" \  claude'</code></pre><h3 id="3-3-模型启动与验证"><a href="#3-3-模型启动与验证" class="headerlink" title="3.3 模型启动与验证"></a>3.3 模型启动与验证</h3><p>在运行 Claude Code 之前，你必须确保 Ollama 后台已有模型在运行。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 拉取对代码和工具支持较好的模型ollama pull qwen2.5-coder:32bollama pull deepseek-r1:32b# 启动 Claude Code (本地模式)claude --model qwen2.5-coder:32b</code></pre><p><img src="/medias/loading.gif" data-original="/images/posts/314_b5ed60273bfde52e92ec0a7a58c7d8be.jpg" alt="配置流程图"></p><h2 id="4-模型选型矩阵：谁能驾驭-Agent？"><a href="#4-模型选型矩阵：谁能驾驭-Agent？" class="headerlink" title="4. 模型选型矩阵：谁能驾驭 Agent？"></a>4. 模型选型矩阵：谁能驾驭 Agent？</h2><p>这是架构设计的核心难点。Claude Code 的 System Prompt 非常复杂，包含大量的工具定义。大多数小参数模型（&lt;10B）会直接崩溃或输出乱码。</p><p>以下是基于实测的推荐矩阵（截至 2026 年初）：</p><table><thead><tr><th align="left">模型系列</th><th align="left">推荐版本</th><th align="left">最小 VRAM</th><th align="left">Agent 能力评分</th><th align="left">适用场景</th></tr></thead><tbody><tr><td align="left"><strong>Qwen 2.5 Coder</strong></td><td align="left"><strong>32B</strong></td><td align="left">24GB</td><td align="left">⭐⭐⭐⭐⭐</td><td align="left"><strong>最佳本地替代品</strong>。工具调用极其精准，几乎能达到 Sonnet 3.5 80% 的水平。</td></tr><tr><td align="left"><strong>DeepSeek R1</strong></td><td align="left">Distill Llama 70B</td><td align="left">48GB</td><td align="left">⭐⭐⭐⭐</td><td align="left">逻辑推理极强，但输出格式有时难以被 Claude Code 解析（需 Prompt 引导）。</td></tr><tr><td align="left"><strong>Llama 3.1</strong></td><td align="left">70B</td><td align="left">48GB</td><td align="left">⭐⭐⭐⭐</td><td align="left">稳健，通用性强，但代码特化能力不如 Qwen Coder。</td></tr><tr><td align="left"><strong>Mistral Large</strong></td><td align="left">(Cloud Only)</td><td align="left">-</td><td align="left">⭐⭐⭐⭐</td><td align="left">欧洲合规首选，工具调用能力强。</td></tr><tr><td align="left"><strong>Qwen 2.5 Coder</strong></td><td align="left">7B / 14B</td><td align="left">8GB / 16GB</td><td align="left">⭐⭐</td><td align="left"><strong>不推荐</strong>。仅能处理简单对话，执行复杂文件修改任务时极易出错。</td></tr></tbody></table><blockquote><p><strong>⚠️ Warning：警告：小模型的陷阱</strong><br>千万不要尝试用 <code>llama3.2:3b</code> 或 <code>qwen:7b</code> 去驱动 Claude Code 进行复杂工程任务。它们往往会忽略工具定义的 XML 结构，直接把“我要修改文件”这句话打印出来，而不是真正调用 <code>Edit</code> 工具。这会导致 Agent 卡死。</p></blockquote><h2 id="5-性能与-TCO-分析"><a href="#5-性能与-TCO-分析" class="headerlink" title="5. 性能与 TCO 分析"></a>5. 性能与 TCO 分析</h2><p>对于企业架构师，成本和性能是决策的关键。</p><h3 id="5-1-延迟-Latency-对比"><a href="#5-1-延迟-Latency-对比" class="headerlink" title="5.1 延迟 (Latency) 对比"></a>5.1 延迟 (Latency) 对比</h3><ul><li><strong>Claude Sonnet 3.5 (SaaS)</strong>: 平均首字延迟 (TTFT) 约 0.5s - 1.2s。生成速度极快 (~80 tokens/s)。</li><li><strong>Local Qwen 32B (M2 Ultra)</strong>: TTFT 约 2s - 4s（取决于 Prompt 长度）。生成速度 ~20-30 tokens/s。</li><li><strong>Ollama Cloud</strong>: 介于两者之间，取决于网络状况。</li></ul><p><strong>结论</strong>：本地部署的体验明显慢于 SaaS，但对于代码生成这种“思考型”任务，稍长的等待通常可以接受。</p><p><img src="/medias/loading.gif" data-original="/images/posts/314_d097a3cd7c09504ac70b9670dcda58d6.jpg" alt="性能对比图"></p><h3 id="5-2-成本-TCO-计算"><a href="#5-2-成本-TCO-计算" class="headerlink" title="5.2 成本 (TCO) 计算"></a>5.2 成本 (TCO) 计算</h3><p>假设一个 10 人的研发团队，每人每天消耗 500k tokens (输入+输出)。</p><ul><li><p><strong>SaaS 方案 (Anthropic)</strong>:</p><ul><li>Input: $3.00 / M * 0.4 * 10 = $12/day</li><li>Output: $15.00 / M * 0.1 * 10 = $15/day</li><li><strong>年成本</strong>: ~$7,000 (按 260 工作日)</li><li><em>风险</em>: 随使用量线性增长，不可控。</li></ul></li><li><p><strong>本地方案 (Qwen 32B)</strong>:</p><ul><li>硬件: 10 台配备 64GB 统一内存的 Mac mini (M4 Pro) 或 消费级 GPU 工作站。</li><li>一次性投入: ~$20,000。</li><li><strong>年成本</strong>: 电费微乎其微。</li><li><em>优势</em>: 3 年摊销后，成本远低于 SaaS，且资产归公司所有。</li></ul></li></ul><h2 id="6-进阶调优：解决常见问题"><a href="#6-进阶调优：解决常见问题" class="headerlink" title="6. 进阶调优：解决常见问题"></a>6. 进阶调优：解决常见问题</h2><h3 id="6-1-上下文窗口溢出-Context-Window"><a href="#6-1-上下文窗口溢出-Context-Window" class="headerlink" title="6.1 上下文窗口溢出 (Context Window)"></a>6.1 上下文窗口溢出 (Context Window)</h3><p>本地模型通常显存受限。Claude Code 默认假设有 200k context。<br><strong>解决方案</strong>：<br>在启动 Ollama 模型时，显式指定上下文窗口大小（根据你的显存量力而行）：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># Modelfile 配置PARAMETER num_ctx 32768</code></pre><p>如果显存不足，Ollama 会自动 offload 到 CPU，导致速度骤降（从 30 t/s 降至 2 t/s）。请务必监控 <code>ollama ps</code>。</p><h3 id="6-2-“幻觉”-修复"><a href="#6-2-“幻觉”-修复" class="headerlink" title="6.2 “幻觉” 修复"></a>6.2 “幻觉” 修复</h3><p>如果发现 Agent 总是说 “I will read the file” 但不执行动作。<br><strong>解决方案</strong>：<br>通常是因为 System Prompt 对本地模型来说太复杂。尝试在启动 Claude Code 时通过 <code>--prompt</code> 参数注入更明确的指令，或者（更硬核地）修改 Claude Code 源码中的 Prompt 模板，简化 XML 结构要求。</p><h2 id="7-结语：混合-AI-的未来"><a href="#7-结语：混合-AI-的未来" class="headerlink" title="7. 结语：混合 AI 的未来"></a>7. 结语：混合 AI 的未来</h2><p>通过 Claude Code + Ollama，我们不再是被动的 API 消费者，而是 AI 系统的<strong>架构师</strong>。</p><p>这种架构不仅解决了隐私之痛，更为企业内部的 “Domain-Specific Agent”（领域专用智能体）开辟了道路。你完全可以在企业私有代码库上微调一个 <code>CodeLlama</code>，然后通过 Ollama 挂载给 Claude Code 使用，实现既懂公司业务逻辑，又具备 Agent 执行能力的终极编程助手。</p><p>未来已来，它运行在你的 <code>localhost</code> 上。</p><hr><blockquote><p><strong>✅ Success：下一步行动</strong></p><ol><li>检查你的硬件显存（推荐 24GB+）。</li><li>安装 Ollama 并 pull <code>qwen2.5-coder:32b</code>。</li><li>配置 <code>alias</code> 并开始你的第一次本地 Agent 之旅。</li></ol></blockquote><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><strong>Ollama GitHub</strong>: <a href="https://github.com/ollama/ollama">https://github.com/ollama/ollama</a></li><li><strong>Claude Code CLI</strong>: <a href="https://github.com/anthropics/claude-code">https://github.com/anthropics/claude-code</a></li><li><strong>Qwen 2.5 Coder</strong>: <a href="https://huggingface.co/Qwen/Qwen2.5-Coder-32B-Instruct">https://huggingface.co/Qwen/Qwen2.5-Coder-32B-Instruct</a></li><li><strong>Freedium Mirror</strong>: <a href="https://freedium-mirror.cfd/https://medium.com/@joe.njenga/i-tried-new-claude-code-ollama-workflow-its-wild-free-cb7a12b733b5">https://freedium-mirror.cfd/https://medium.com/@joe.njenga/i-tried-new-claude-code-ollama-workflow-its-wild-free-cb7a12b733b5</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> AI Engineering </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Ollama </tag>
            
            <tag> Claude Code </tag>
            
            <tag> AI Agent </tag>
            
            <tag> Local LLM </tag>
            
            <tag> Software Architecture </tag>
            
            <tag> DevOps </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2026/06/26/moment-2026-06-26-5/"/>
      <url>/2026/06/26/moment-2026-06-26-5/</url>
      
        <content type="html"><![CDATA[<p>雨，总是那么突如其来，让人措手不及！</p>]]></content>
      
      
      <categories>
          
          <category> moments </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>vLLM vs Ollama vs llama.cpp：DevOps 怎么选</title>
      <link href="/2026/06/25/313/"/>
      <url>/2026/06/25/313/</url>
      
        <content type="html"><![CDATA[<h1 id="vLLM-vs-Ollama-vs-llama-cpp：DevOps-怎么选"><a href="#vLLM-vs-Ollama-vs-llama-cpp：DevOps-怎么选" class="headerlink" title="vLLM vs Ollama vs llama.cpp：DevOps 怎么选"></a>vLLM vs Ollama vs llama.cpp：DevOps 怎么选</h1><p><img src="/medias/loading.gif" data-original="/images/posts/313_8707ae18a549044a36763c2b108b1877.jpg" alt="三种不同体量的推理引擎并排，象征三种运行时"></p><p>上周又有同事在群里问：自建大模型服务，到底是上 vLLM 还是 Ollama？要不要试试 llama.cpp？</p><p>这个问题我自己踩过坑——一开始拿 Ollama 怼生产，并发一上来延迟直接飙红；后来全栈换 vLLM，结果 CI 机器上根本没 GPU，编译都跑不起来。折腾完我才明白：<strong>这三个东西压根不是竞品，是一条流水线上的三个工位。</strong></p><p>很多选型文章喜欢搞个评分表，最后给个”综合最强”的冠军。我觉得这思路从一开始就错了。它们各自优化的目标根本不在一个维度上：vLLM 优化的是生产吞吐（一个服务能扛 50+ 并发），Ollama 优化的是开发者上手体验（一条 <code>ollama run</code> 就开干），llama.cpp 优化的是 CPU 效率和可移植性（4.5GB 内存跑 7B）。拿吞吐去比 Ollama、拿易用性去比 vLLM，就像拿货车跟跑车比谁省油——问题本身就问歪了。</p><p>先把结论摆这儿，不绕弯子：本地开发拿 Ollama，CI 和边缘设备用 llama.cpp，生产高并发上 vLLM。模型文件三者基本通用，换运行时不是重写架构，更像换个发动机。</p><p>下面我把三个发动机一个个拆开看，顺便纠正两个流传很广的错误说法。</p><h2 id="vLLM：为高并发而生的吞吐怪兽"><a href="#vLLM：为高并发而生的吞吐怪兽" class="headerlink" title="vLLM：为高并发而生的吞吐怪兽"></a>vLLM：为高并发而生的吞吐怪兽</h2><p>如果你的场景是”一个服务，几十上百号人同时调”，那 vLLM 几乎没有对手。</p><p>它快的核心是 <strong>PagedAttention</strong>。官方文档原话是 “Efficient management of attention key and value memory with PagedAttention”——说白了，传统推理给每个请求的 KV 缓存预留一整块连续显存，请求一多显存就碎成渣、白白浪费。PagedAttention 把 KV 缓存当成操作系统的”虚拟内存页”来管，按需分配，碎片几乎归零。</p><p>显存省下来了，就能塞更多请求一起算，这就是第二招 <strong>连续批处理（continuous batching）</strong>。官方功能列表里写的是 “Continuous batching of incoming requests, chunked prefill, prefix caching”。</p><p>这里值得多说两句，因为它是 vLLM 吞吐高的真正原因。传统的”静态批处理”是一批请求必须凑齐、一起进、一起出——其中一个请求生成的 token 特别长，整批都得等它，GPU 大部分时间在干等。连续批处理则是”谁生成完谁就走，空出来的位置立刻补新请求进来”，GPU 利用率几乎拉满。</p><p>配套的还有两个细节：<strong>prefix caching</strong> 会把相同的前缀（比如一段几百 token 的 system prompt）算一次就缓存住，后面命中的请求直接复用；<strong>chunked prefill</strong> 把长输入切块处理，避免一个超长 prompt 把整批的解码都卡死。这三招叠在一起，才是那个夸张吞吐数字的来源——后面 <code>docker run</code> 起服务时这些默认就开着，你不用调。</p><blockquote><p>这里要纠正一个常见说法：很多文章引用 vLLM “吞吐提升 23 倍”。这个 23x 数字其实来自 Anyscale 的一篇外部博客（讲连续批处理的），不是 vLLM 文档自己的实测，引用时最好把出处带上。</p></blockquote><p>部署上 vLLM 走 Docker 最省事，起来就是一个 OpenAI 兼容的 API：</p><pre class="language-bash" data-language="bash"><code class="language-bash">docker run --runtime nvidia --gpus all \  -v ~/.cache/huggingface:/root/.cache/huggingface \  -p 8000:8000 \  vllm/vllm-openai:latest \  --model mistralai/Mistral-7B-Instruct-v0.2</code></pre><p>起来之后，客户端代码跟调 OpenAI 没区别，迁移成本几乎为零：</p><pre class="language-bash" data-language="bash"><code class="language-bash">curl http://localhost:8000/v1/chat/completions \  -H "Content-Type: application/json" \  -d '{    "model": "mistralai/Mistral-7B-Instruct-v0.2",    "messages": [{"role": "user", "content": "你好"}]  }'</code></pre><p>官方文档还提到，除了 OpenAI 接口，它同时支持 “Anthropic Messages API and gRPC support”，接入选择很宽。</p><p><img src="/medias/loading.gif" data-original="/images/posts/313_79b96ffdcb1e9bf9fa3d54c6416eb9b1.png" alt="vLLM 官方文档：PagedAttention 与连续批处理特性"></p><p>性能数字我引一下源文章的实测（注意：<strong>这是第三方博客数据，我没有独立复现</strong>）：A10G 显卡跑 Mistral 7B、batch 16，吞吐约 1800 tokens/秒，p50 延迟 820ms、p99 1450ms，50+ 并发不掉速。数量级可信，具体数字以你自己的硬件为准。</p><p>最后纠正第二个误区——<strong>vLLM 不是只能用 NVIDIA</strong>。这是流传最广的错误。官方明确写支持 “NVIDIA GPUs, AMD GPUs, and x86/ARM/PowerPC CPUs”，还有 TPU、Apple Silicon 等一堆插件。当然生产主流仍是 N 卡，但”必须 N 卡”这话不成立。</p><p>运维上有几个坑我提前说一下。第一，显存要算清楚——模型权重之外，KV 缓存会随并发和上下文长度涨，<code>--max-model-len</code> 设太大很容易 OOM，上线前务必按峰值并发压一遍。第二，单卡放不下的大模型用张量并行 <code>--tensor-parallel-size 2</code> 拆到多卡，但跨卡通信会吃掉一部分收益，不是卡越多越线性。第三，它是常驻显存的重型服务，别指望像 Ollama 那样用完即走——这是生产服务该有的样子，但也意味着你得为它专门留机器。</p><p>我的判断很直接：<strong>只要是面向多人的线上推理，我一律上 vLLM，不会用别的。</strong> 它上手确实比另外两个陡，但生产环境要的就是吞吐和稳定性，这块它做到了。</p><p>反过来，<strong>单人用、低流量的场景别上 vLLM，纯属杀鸡用牛刀。</strong> 它的所有设计——分页显存、连续批处理——都是为”很多请求挤在一起”准备的。你就一个人偶尔调一下，这些机制全是空转，还得伺候 GPU 和一堆启动参数，性价比极低。这种场景 Ollama 一条命令的事。</p><p><img src="/medias/loading.gif" data-original="/images/posts/313_e511dbde5841c2447e8161a63a04d216.jpg" alt="KV 缓存从碎片化到分页管理的抽象示意"></p><h2 id="llama-cpp：没有-GPU-也能把模型扛起来"><a href="#llama-cpp：没有-GPU-也能把模型扛起来" class="headerlink" title="llama.cpp：没有 GPU 也能把模型扛起来"></a>llama.cpp：没有 GPU 也能把模型扛起来</h2><p>vLLM 再强，遇到”机器上没 GPU”就歇菜。这时候 llama.cpp 是我唯一的选择。</p><p>它的定位官方一句话：”LLM inference in C/C++”。纯 C/C++ 写的，没有那堆 Python 依赖，CPU 上就能跑。这让它特别适合两个场景：CI 流水线里跑模型冒烟测试，以及边缘设备/Mac 上做本地推理。</p><p>能在 CPU 上跑大模型，靠的是 <strong>GGUF 量化</strong>。官方支持从 “1.5-bit, 2-bit, 3-bit, 4-bit, 5-bit, 6-bit, and 8-bit” 的整数量化——把原本 FP16 的权重压成低比特整数，体积和内存占用断崖式下降。源文章给的例子是 Mistral 7B 的 Q4_K_M 格式只占 4.5GB 内存（这个具体档位官方页没单列，属 GGUF 常见量化档）。</p><p>量化档位怎么选，是用 llama.cpp 绕不开的决策。规律很简单：<strong>比特越低，体积越小、速度越快，但模型质量损失越大。</strong> 我的经验是 Q4_K_M 是甜点档——7B 模型压到 4-5GB，质量损失肉眼基本看不出，普通笔记本就能跑。要更高质量上 Q5、Q6，机器内存够就 Q8（接近原始精度）；2-bit、3-bit 那种极限压缩除非内存实在紧张，否则别碰，模型会开始说胡话。</p><p>如果机器上有一块不大的显卡，还能用 <code>-ngl</code> 参数把一部分层 offload 到 GPU，剩下的留在 CPU，混合着跑：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 把 20 层放到 GPU，其余在 CPU，适合显存不够装下整个模型的情况./build/bin/llama-server -m model.gguf -ngl 20</code></pre><p>编译和起服务都很直接：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git clone https://github.com/ggml-org/llama.cppcd llama.cppcmake -B build &amp;&amp; cmake --build build --config Release</code></pre><p>llama.cpp 自带一个 OpenAI 兼容的 HTTP 服务，官方描述是 “A lightweight, OpenAI API compatible, HTTP server for serving LLMs”：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 直接从 HuggingFace 拉 GGUF 模型起服务./build/bin/llama-server -hf ggml-org/gemma-3-1b-it-GGUF</code></pre><p>想跑一次性推理不开服务，用 CLI 就够：</p><pre class="language-bash" data-language="bash"><code class="language-bash">./build/bin/llama-cli -m my_model.gguf -p "解释一下什么是量化"</code></pre><p><img src="/medias/loading.gif" data-original="https://cdn.jsdelivr.net/gh/costa92/article-images/clawdbot/shot_llamacpp.png" alt="llama.cpp GitHub 仓库：LLM inference in C/C++"></p><p>性能方面，源文章说现代 CPU 上 15-25 tokens/秒，Apple Silicon（M2 Pro，走 Metal）能到 50 tokens/秒。我得提醒一句：llama.cpp 官方 README 里有 <code>llama-bench</code> 的示例跑分，但<strong>没有针对”现代 CPU / Apple Silicon 通用速度”的官方实测数字</strong>，文中这些 15-25 / 50 tokens/秒 都来自第三方，自己的机器请用 <code>llama-bench</code> 实测。不过 Apple Silicon 的优化是官方实锤的——文档里写 “Apple silicon is a first-class citizen - optimized via ARM NEON, Accelerate and Metal frameworks”。在 Mac 上跑本地模型，它确实是第一梯队。</p><p>放进 CI 的价值我特别想强调。模型上线前最怕”换了个量化版本，结果输出全乱了”这种事。在 CI 里用 llama.cpp 加载目标 GGUF、跑一条固定 prompt、断言输出非空且格式正确，这一步不需要 GPU、几秒钟跑完，能挡住一大半”模型文件本身坏了”的低级事故。我现在的项目都把这步当成模型版本的冒烟测试。</p><p>我的判断：llama.cpp 不追求并发吞吐，它追求的是”在任何破机器上都能把模型跑起来”。CI 没 GPU？边缘盒子只有 ARM？数据敏感不能出本机？这活儿基本只有它和 Ollama 干得了，而要塞进自动化流水线、不带多余依赖，一个 <code>llama-cli -m model.gguf</code> 就能跑的它更纯粹。</p><h2 id="Ollama：本地开发，五分钟跑起来"><a href="#Ollama：本地开发，五分钟跑起来" class="headerlink" title="Ollama：本地开发，五分钟跑起来"></a>Ollama：本地开发，五分钟跑起来</h2><p>如果说 vLLM 是给运维的、llama.cpp 是给嵌入式的，那 Ollama 就是给开发者图省事的。</p><p>我自己日常本地调模型，第一反应永远是 Ollama，因为它把所有麻烦事都藏起来了。一条命令拉模型、起服务、进对话：</p><pre class="language-bash" data-language="bash"><code class="language-bash">ollama run gemma3</code></pre><p>模型从 ollama.com/library 拉，名字一报就行。它现在的明星阵容（官方 About 原话）是 “Kimi-K2.6, GLM-5.1, MiniMax, DeepSeek, gpt-oss, Qwen, Gemma and other models”，主流开源模型基本都在。</p><p>更关键的是它内置 REST API，官方原话 “Ollama has a REST API for running and managing models”，而且带 OpenAI 兼容端点（路径就在 <code>/v1</code>）。本地写的代码，将来切到 vLLM 接口不用动——直接用这个兼容端点，跟前面 vLLM 的请求体长得一模一样：</p><pre class="language-bash" data-language="bash"><code class="language-bash">curl http://localhost:11434/v1/chat/completions \  -H "Content-Type: application/json" \  -d '{    "model": "gemma3",    "messages": [{"role": "user", "content": "写一个快速排序"}]  }'</code></pre><p>想自定义模型行为，用 Modelfile（类似 Dockerfile 的思路）：</p><pre class="language-docker" data-language="docker"><code class="language-docker">FROM gemma3PARAMETER temperature 0.7SYSTEM "你是一个简洁的代码助手"</code></pre><p>顺带说个很多人不知道的点：<strong>Ollama 底层其实就是封装了 llama.cpp</strong>。所以它跑的也是 GGUF 量化模型，能在 CPU 上跑、Apple Silicon 上快，都是继承自 llama.cpp 的能力。Ollama 在它之上加了模型管理、自动显存调度、热加载这些”开发者体验糖”——你只管 <code>ollama run</code>，模型拉取、量化档选择、显存分配它全替你办了。</p><p>它还有几个让本地开发特别舒服的小设计：模型默认会在内存里驻留一段时间（<code>keep_alive</code>），短时间内反复调不用重新加载；多个模型可以并存，按需换出。这些对本地反复试验太友好了。</p><p>还有一点对很多团队是刚需——<strong>全程本地、数据不出门</strong>。代码、内部文档这些敏感东西喂给云端 API 总让人心里没底，用 Ollama 在自己机器上跑，数据一个字节都不外传。对合规要求严的场景，这个特性比性能更重要。175k stars 的社区也意味着新模型出来基本第二天就能 <code>ollama pull</code> 到，跟进速度极快。</p><p>至于它的短板，前面说过了，这里再钉一下：它不是为并发设计的。我那次线上事故的根因就是把”单人爽”误当成”能扛量”。记住它的角色——开发期的加速器，到此为止。</p><p><img src="/medias/loading.gif" data-original="https://cdn.jsdelivr.net/gh/costa92/article-images/clawdbot/shot_ollama.png" alt="Ollama GitHub 仓库：内置 REST 与 OpenAI 兼容 API"></p><p>截至写稿，Ollama 最新版是 v0.30.10（2026-06-17 发布），仓库 175k stars——社区热度它是三者里最高的。</p><p>但别被热度骗了，<strong>我不推荐拿 Ollama 直接上生产</strong>。它优化的是”上手快、单人爽”，不是高并发吞吐。我那次拿它扛线上流量、并发一冲延迟就崩，就是没认清它的定位。它是开发期的瑞士军刀，不是生产期的发动机。</p><h2 id="一张表看清三者差异"><a href="#一张表看清三者差异" class="headerlink" title="一张表看清三者差异"></a>一张表看清三者差异</h2><p>口说无凭，把关键维度拍成表（其中性能数字来自源文章第三方实测，仅供量级参考）：</p><table><thead><tr><th>维度</th><th>vLLM</th><th>llama.cpp</th><th>Ollama</th></tr></thead><tbody><tr><td>定位</td><td>生产高并发</td><td>CPU/边缘/CI</td><td>本地开发</td></tr><tr><td>硬件</td><td>N卡/A卡/CPU 都支持</td><td>CPU 优先，Apple Silicon 一流</td><td>GPU + CPU</td></tr><tr><td>核心技术</td><td>PagedAttention + 连续批处理</td><td>GGUF 量化</td><td>封装 + 一键体验</td></tr><tr><td>API</td><td>OpenAI / Anthropic / gRPC</td><td>OpenAI 兼容</td><td>REST + OpenAI 兼容</td></tr><tr><td>上手难度</td><td>高（要懂 GPU/K8s）</td><td>中（要编译）</td><td>极低（一条命令）</td></tr><tr><td>并发能力</td><td>极强（50+ 不掉速*）</td><td>弱</td><td>弱</td></tr><tr><td>GitHub stars</td><td>——</td><td>118k</td><td>175k</td></tr></tbody></table><blockquote><p>*并发数字来自源文章实测，未独立复现。</p></blockquote><p>表里我特意把 API 那行标出来，因为这是三者能组成流水线的关键——<strong>它们都提供 OpenAI 兼容接口</strong>。这意味着你的业务代码只认一套接口，改个 <code>OPENAI_BASE_URL</code> 就换了底层运行时，代码一行不动。对 DevOps 来说这是天大的好事：不用为换运行时改业务逻辑，也不会被某一家锁死。</p><p>我自己有个真实的演进路线可以参考。最早做一个内部文档问答，本地用 Ollama 把流程跑通，三天就有了能演示的 demo。要进 CI 做回归测试时，CI 机器没 GPU，换 llama.cpp 加载同一个模型的 GGUF 版本，跑个最小推理验证模型没崩。最后上线给全公司用，并发压力上来了，换 vLLM 部署到 K8s。整个过程模型选型没变，业务代码因为都是 OpenAI 接口几乎没动，每一步只是换了个运行时——这就是把它们当流水线用的实际收益。</p><p>三者最妙的地方是模型可以平滑流转。你在 Ollama 上选定的模型，导出成 GGUF 就能喂给 llama.cpp，HuggingFace 上的原始权重又能直接喂给 vLLM。换运行时，配置改几行就行，比如生产的 K8s 里 vLLM 作为主服务、Ollama 作为开发 sidecar：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml"># 开发环境：Ollama 作为轻量 sidecarcontainers:  - name: ollama    image: ollama/ollama:latest    ports:      - containerPort: 11434</code></pre><p>而 CI 里则用 llama.cpp 做一次无 GPU 的推理冒烟测试：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># CI 步骤：无需 GPU，验证模型能正常出 token./build/bin/llama-cli -m model.gguf -p "ping" -n 16 || exit 1</code></pre><p><img src="/medias/loading.gif" data-original="/images/posts/313_fda592b966c431c36869eb0bed3f63e2.jpg" alt="从本地开发到 CI 再到生产的三段式流水线抽象图"></p><h2 id="最后：我会怎么选"><a href="#最后：我会怎么选" class="headerlink" title="最后：我会怎么选"></a>最后：我会怎么选</h2><p>不绕了，直接给场景化推荐：</p><table><thead><tr><th>场景</th><th>我的选择</th><th>原因</th></tr></thead><tbody><tr><td>本地写代码、调 prompt</td><td>Ollama</td><td>一条命令起步，省心</td></tr><tr><td>CI 流水线、边缘设备、无 GPU</td><td>llama.cpp</td><td>纯 C/C++，哪都能跑</td></tr><tr><td>团队共享、生产高并发</td><td>vLLM</td><td>PagedAttention 吞吐碾压</td></tr><tr><td>Mac 上跑本地模型</td><td>llama.cpp</td><td>Apple Silicon 一流优化</td></tr></tbody></table><p>判断逻辑其实可以压成三行：</p><pre class="language-bash" data-language="bash"><code class="language-bash">有没有高并发生产需求？ 有 → vLLM机器上有 GPU 吗？      没有 → llama.cpp就想本地快速试试？     是  → Ollama</code></pre><p>再补一个很多人忽略的成本维度。vLLM 要常驻 GPU，机器成本最高，但摊到每个请求上反而最便宜——因为它把 GPU 榨得最干（高并发时 <code>nvidia-smi</code> 看显存利用率基本贴满），单 token 成本最低。Ollama 和 llama.cpp 单机轻量、几乎零运维成本，但它们不为吞吐设计，硬要扛大流量等于用算力换便利，反而不划算。所以”省钱”在不同规模下答案是反的：小流量用轻量方案省钱，大流量用 vLLM 才省钱。</p><p>还有人会问：能不能只用一个、全场景通吃？技术上当然行，但我不建议。拿 vLLM 在本地开发，每次改代码都要重启一个吃满显存的重服务，体验很差；拿 Ollama 上生产，并发一冲就现原形。每个工具在它不擅长的场景都会让你难受——这正是”三个都装、按阶段用”比”All in 一个”更省心的原因。</p><p>迁移成本低到什么程度？同一个模型，开发期在 Ollama，上线时换 vLLM，客户端代码因为都是 OpenAI 兼容接口，基本一行不用改：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 开发：指向本地 Ollamaexport OPENAI_BASE_URL=http://localhost:11434/v1# 生产：指向 vLLM，代码不变export OPENAI_BASE_URL=http://vllm-svc:8000/v1</code></pre><p>最后说句公道话，这三个工具也有它们都不管的事，别指望换个运行时就万事大吉。<strong>模型本身的质量它们一个都不负责</strong>——选错了基座模型，跑得再快也是快速地输出废话。鉴权、限流（超额了得自己返 <code>429</code>）、可观测性、灰度这些生产必备的东西，三者也都只给你一个裸接口，得你自己在外面套一层。运行时只解决”怎么把模型跑起来、跑多快”，”跑得对不对、稳不稳”还是你的活。</p><p>所以别再纠结”三选一”了。我的建议是三个都装上，按阶段用——这才是把它们当成一个家族的正确姿势。</p><p>把这篇转给那个还在纠结自建大模型用哪个运行时的同事，能省他不少试错时间。</p><p>下一步：先在本机 <code>ollama run</code> 一个小模型跑起来，找回手感，再考虑后面两个。</p>]]></content>
      
      
      <categories>
          
          <category> 技术 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Ollama </tag>
            
            <tag> vLLM </tag>
            
            <tag> llama.cpp </tag>
            
            <tag> 大模型部署 </tag>
            
            <tag> 推理运行时 </tag>
            
            <tag> DevOps实战 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>claude-code-setup：让 Claude 扫描你的项目，自动给出最优配置方案</title>
      <link href="/2026/06/24/311/"/>
      <url>/2026/06/24/311/</url>
      
        <content type="html"><![CDATA[<h1 id="claude-code-setup：让-Claude-扫描你的项目，自动给出最优配置方案"><a href="#claude-code-setup：让-Claude-扫描你的项目，自动给出最优配置方案" class="headerlink" title="claude-code-setup：让 Claude 扫描你的项目，自动给出最优配置方案"></a>claude-code-setup：让 Claude 扫描你的项目，自动给出最优配置方案</h1><p><img src="/medias/loading.gif" data-original="/images/posts/311_0beabc4479f728a520a26c27d32fde09.jpg" alt="Claude 扫描代码库并生成配置卡片的场景"></p><p>用了 Claude Code 一段时间之后，你可能遇到过这个问题：明明知道可以配置很多东西，但不知道从哪开始，也不确定什么配置对当前这个项目最有价值。</p><p>钩子、MCP 服务器、子代理、技能包——这些概念散落在文档的不同角落，每个都要单独去研究。加上不同项目的技术栈不一样，”什么配置对我这个项目有用”这个问题没有一个通用的回答。对于刚接触 Claude Code 的人来说，这段摸索的成本比预期高很多。</p><p><strong>claude-code-setup 做的事情很具体</strong>：扫描你的代码库，根据项目实际用到的语言、框架和依赖，给出针对性的配置建议。不是给你一份通用清单，是真的看过你的项目再说话。</p><p>这是 Anthropic 官方出品的插件（<code>claude-plugins-official</code> 市场中的 <code>claude-code-setup</code>），作者是 Anthropic 内部团队，当前版本 1.0.0。</p><p><img src="/medias/loading.gif" data-original="https://cdn.jsdelivr.net/gh/costa92/article-images/images/screenshot_readme.png" alt="claude-code-setup GitHub README"></p><h2 id="安装只需一条命令"><a href="#安装只需一条命令" class="headerlink" title="安装只需一条命令"></a>安装只需一条命令</h2><p>Claude Code 启动时会自动加载官方市场，不需要额外添加源。在 Claude Code 中运行：</p><pre class="language-bash" data-language="bash"><code class="language-bash">/plugin install claude-code-setup@claude-plugins-official</code></pre><p>安装后激活插件：</p><pre class="language-bash" data-language="bash"><code class="language-bash">/reload-plugins</code></pre><p>如果提示找不到插件，多半是市场没加载或过期了。先刷新一下市场再重试：</p><pre class="language-bash" data-language="bash"><code class="language-bash">/plugin marketplace update claude-plugins-official</code></pre><p>刷新后还找不到，可能是这台机器从没加过这个市场，手动加一次：</p><pre class="language-bash" data-language="bash"><code class="language-bash">/plugin marketplace add anthropics/claude-plugins-official</code></pre><p>要是插件装上了、技能却死活不出现，通常是缓存坏了——清掉缓存目录，重启 Claude Code 再装一遍就好：</p><pre class="language-bash" data-language="bash"><code class="language-bash">rm -rf ~/.claude/plugins/cache</code></pre><p>我在 MacBook 上测试全程不超过 30 秒。安装支持三种范围：<strong>用户级</strong>（跨所有项目生效）、<strong>项目级</strong>（写入 <code>.claude/settings.json</code>，可以共享给团队协作者）、<strong>本地级</strong>（只在当前仓库生效，不共享）。我一般选用户级，让它在所有项目里都能随时调用。</p><p>如果你习惯用图形界面，也可以在 Claude Code 里运行 <code>/plugin</code> 打开插件管理器，切到 <strong>Discover</strong> 标签页，搜索 <code>claude-code-setup</code> 然后点安装。两种方式结果一样，图形界面里还会提前显示这个插件会加载哪些内容和每轮对话大概增加多少上下文成本（Claude Code 2.1.143 以后版本开始显示这个数字），可以提前评估。</p><p>安装完之后不需要做任何额外配置，切到你的项目目录就能直接用。值得一提的是，这个插件是只读模式——它只分析，不修改任何文件，不写 <code>.claude/settings.json</code>，不动 <code>CLAUDE.md</code>，所有建议都以文本形式输出给你看，装不装完全由你决定。这对于还不太熟悉 Claude Code 配置机制的人来说，安全边界很清晰。</p><p>反过来，装了之后想撤也简单：运行 <code>/plugin</code> 切到 <strong>Installed</strong> 标签页就能启用、停用或卸载，命令行直接 <code>/plugin uninstall claude-code-setup@claude-plugins-official</code> 也行。试错成本很低，不合用随时移除。</p><p><img src="/medias/loading.gif" data-original="/images/posts/311_2f2988cf78186628506f9f48b4747f4e.png" alt="Claude Code 插件发现页面"></p><h2 id="它扫了什么、推了什么"><a href="#它扫了什么、推了什么" class="headerlink" title="它扫了什么、推了什么"></a>它扫了什么、推了什么</h2><p>我装好后直接用自然语言触发：</p><pre class="language-text" data-language="text"><code class="language-text">recommend automations for this project</code></pre><p>或者更简短的：</p><pre class="language-text" data-language="text"><code class="language-text">help me set up Claude Code</code></pre><p>插件会先读取项目结构，分析用到了什么语言、有没有前端框架、依赖里有没有数据库 SDK 或外部 API 客户端，然后给出 5 类推荐，每类最多 2 条。</p><p><strong>MCP 服务器</strong>：外部工具集成，把 Claude 连接到数据库、API、文档系统等。比如项目里有大量调用外部文档的需求，会推 context7（实时查官方文档，避免 Claude 凭记忆讲过时 API）；有前端 UI 测试，会推 Playwright MCP（让 Claude 直接操控浏览器做验证）。</p><p><strong>技能包</strong>：预打包的专项能力，一条命令装好就能用。Plan agent 可以让 Claude 在动手之前先输出完整的修改计划，适合改动涉及多个文件的任务；frontend-design 技能提供了一套针对 UI 层的代码约定和审查标准。</p><p><strong>钩子</strong>：绑定到 Claude 工具事件上的自动动作，不需要每次手动触发。比如每次 Claude 编辑完文件后自动跑格式化，或者在 Claude 试图写入某些敏感路径时主动阻断并提示风险——这类保护在多人协作的项目里特别有用。</p><p><strong>子代理</strong>：专项检查的后台代理，可以和主任务并行跑。安全子代理会在每次改动之后扫描常见漏洞类型；性能子代理关注资源占用；无障碍子代理专门检查前端输出是否符合 WCAG 规范，省掉手动审查的时间。</p><p><strong>斜杠命令</strong>：预定义的快捷工作流，比如 <code>/pr-review</code> 自动审查当前改动的质量和风险，<code>/test</code> 快速生成测试用例，<code>/explain</code> 对当前文件生成解释性注释。这些工作流在不同项目里重复用，打包成命令比每次重新描述需求效率高很多。</p><p>每类推荐最多 2 条是有意为之，不想在你第一次用的时候就堆太多选项。如果想深入某一类，可以针对具体类型再追问，插件会在那个类别里给出更多选项。</p><table><thead><tr><th>类型</th><th>解决的问题</th><th>典型例子</th></tr></thead><tbody><tr><td>MCP 服务器</td><td>Claude 需要访问外部资源</td><td>context7、Playwright</td></tr><tr><td>技能包</td><td>特定任务需要专项能力</td><td>Plan agent、frontend-design</td></tr><tr><td>钩子</td><td>重复动作自动执行</td><td>自动格式化、拦截敏感路径</td></tr><tr><td>子代理</td><td>需要专项检查</td><td>安全、性能、无障碍审查</td></tr><tr><td>斜杠命令</td><td>常用工作流一键触发</td><td>/pr-review、/test</td></tr></tbody></table><p><img src="/medias/loading.gif" data-original="/images/posts/311_a90a30ee11de8314660a68a63b5d5287.jpg" alt="五类自动化推荐类型示意图"></p><h2 id="实际效果和一个重要前提"><a href="#实际效果和一个重要前提" class="headerlink" title="实际效果和一个重要前提"></a>实际效果和一个重要前提</h2><p>我在几个不同的项目上试了一遍，推荐结果和项目实际情况的对应关系都挺准的。</p><p>我那个 Next.js 项目里有 Playwright 测试用例，推荐里就真冒出了 Playwright MCP；package.json 里声明了 TypeScript，它就给了 typescript-lsp（代码跳转和类型检查）。我又换到一个 Go 微服务仓库，推荐立马变成了 gopls-lsp 和 git commit 工作流技能。并没有给出什么通用的”适合所有项目”的结果，确实是在看你的代码再说的。</p><p>拿到建议之后，按需安装对应的插件：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 例：安装 context7（官方文档 MCP 服务器）/plugin install context7@claude-plugins-official</code></pre><pre class="language-bash" data-language="bash"><code class="language-bash"># 例：安装 gopls-lsp（Go 语言服务器）/plugin install gopls-lsp@claude-plugins-official</code></pre><p><strong>装 LSP 类插件有个前提</strong>：像 gopls-lsp、typescript-lsp 这类代码智能插件，本身只配置 Claude Code 的 LSP 连接，真正干活的语言服务器二进制得你自己装好——gopls-lsp 依赖 <code>gopls</code>，typescript-lsp 依赖 <code>typescript-language-server</code>。装完插件如果在 <code>/plugin</code> 的 Errors 标签页看到 <code>Executable not found in $PATH</code>，就是少了对应二进制，补上即可。装好之后 Claude 每次改完文件会自动拿到类型错误和缺失 import，不用手动跑编译器（按 Ctrl+O 可内联查看诊断）。</p><p><strong>有一点值得说清楚</strong>：claude-code-setup 只给建议，不帮你执行。扫完出一份清单，装不装、装哪个，全由你自己决定。这个设计让你可以逐条评估，也可以直接追问 Claude “为什么推荐这个”来了解背后的判断依据，比直接帮你全装上要透明很多。我试过追问几次，解释通常都很清楚——比如”你的 package.json 里有 @playwright/test，所以推了 Playwright MCP”，而不是模糊地说”因为你有测试需求”。</p><p>不太适合的情况我也得说清楚：如果你已经系统用过 Claude Code 的配置体系，这个插件提供的信息密度有限——推荐范围主要基于已知的常见模式，不会去搜索市场上所有可用插件，也不会针对特别小众的框架给出定制建议。对于想要”分析完直接帮我全装上”的需求，还是得自己再操作一步。</p><p>适合的场景：刚开始用 Claude Code 不知道从哪里配置；接手了别人的项目或换了新的技术栈，想快速了解有没有现成的插件可以用；团队里来了新人，需要给出配置起点，省掉每个人重新摸索的时间。</p><h2 id="配置之后的下一步"><a href="#配置之后的下一步" class="headerlink" title="配置之后的下一步"></a>配置之后的下一步</h2><p>切到你在用的项目目录，对 Claude 说：</p><pre class="language-text" data-language="text"><code class="language-text">what hooks should I use?</code></pre><p>看看它对这个项目的具体建议。有用的装进来，不相关的跳过，比从零开始猜配置效率高很多。</p><p>如果你想集中了解某一类，也可以直接问得更具体，比如”给我推荐适合这个项目的 MCP 服务器”或者”有没有合适的安全检查子代理”——插件支持这类聚焦请求，会在单一类别里给出更多选项（3-5 条而不是 2 条上限）。</p><p>对某条推荐不确定要不要装，直接追问”为什么推荐这个”。它会给出具体依据，比如”你的 package.json 里有 @playwright/test，所以推了 Playwright MCP”，而不是模糊地说”因为你有测试需求”。根据这个逻辑判断值不值得装，比盲目试要靠谱很多。</p><p>插件官方文档在 Anthropic 的 <a href="https://github.com/anthropics/claude-plugins-official/tree/main/plugins/claude-code-setup">claude-plugins-official 仓库</a>，README 里有每个推荐类型的解释。更多可用的官方插件可以在 Claude Code 的 <a href="https://code.claude.com/docs/en/discover-plugins">discover-plugins 文档</a> 里按类别浏览——代码智能（各语言 LSP）、外部集成（GitHub、Figma、Slack 等）、自动安全审查（<code>security-guidance</code> 插件）都在里面。官方市场由 Anthropic 策展、是否收录由官方把关，来源比第三方市场可靠；不过官方也明确提醒：插件会以你的权限执行代码，Anthropic 并不保证每个插件都按预期工作，所以装前最好在详情页扫一眼它”会装哪些东西”（v2.1.145 起详情页会列出插件包含的命令、技能、MCP/LSP）再决定。</p><p>配置这种事情，实际跑一遍才知道哪些真正有用。claude-code-setup 省掉的是”我都不知道有这些可以配”的那段时间，先让 Claude 给出一份可以执行的候选清单，再按需决策。</p><blockquote><p>收藏起来，下次换项目配置 Claude Code 时按图索骥。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> AI工具 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Claude Code </tag>
            
            <tag> 实战教程 </tag>
            
            <tag> 开发工具 </tag>
            
            <tag> AI插件 </tag>
            
            <tag> 自动化配置 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>循环工程：从写提示词，到设计让 Agent 自己跑的循环</title>
      <link href="/2026/06/24/xun-huan-gong-cheng-cong-xie-ti-shi-ci-dao-she-ji-rang-agent-zi-ji-pao-de-xun-huan/"/>
      <url>/2026/06/24/xun-huan-gong-cheng-cong-xie-ti-shi-ci-dao-she-ji-rang-agent-zi-ji-pao-de-xun-huan/</url>
      
        <content type="html"><![CDATA[<h1 id="循环工程：从写提示词，到设计让-Agent-自己跑的循环"><a href="#循环工程：从写提示词，到设计让-Agent-自己跑的循环" class="headerlink" title="循环工程：从写提示词，到设计让 Agent 自己跑的循环"></a>循环工程：从写提示词，到设计让 Agent 自己跑的循环</h1><p><img src="/medias/loading.gif" data-original="/images/posts/20_9638448bcd3e50d5dbc2aa08585c968d.jpg" alt="循环工程封面：工程师退后一步，循环自动运行"></p><p>Anthropic 负责 Claude Code 的 Boris Cherny 说过一句话：「我不再给 Claude 写提示词了。我让一堆循环替我写、替我判断该干什么。」造出最常用编码 Agent 的人都不再手动敲提示词，这事值得认真看。</p><p><strong>一句话讲清楚：循环工程（Loop Engineering）就是把你自己从「按提示词的人」，换成「设计那套按提示词的系统的人」。</strong> Addy Osmani 在 2026 年 6 月 7 日的<a href="https://addyosmani.com/blog/loop-engineering/">这篇博客</a>里把这个说法系统化了——你写一小段程序，可能是个 shell 循环，可能是个定时任务，由它去跟模型对话，而不是你本人。</p><p>Boris Cherny 的完整说法是：「我不再给 Claude 写提示词了。我有一堆循环跑着，替我给 Claude 写提示词、替我想清楚该干什么。」当造出最常用编码 Agent 的那批人都说自己已经停手不再手动敲了，这件事就从小圈子的玩法变成了值得正经对待的范式。</p><p>下面拆三件事：这套范式到底变了什么、一个循环由哪些零件拼成、以及它有哪些你绕不开的代价。</p><h2 id="提示词工程为什么走到了头"><a href="#提示词工程为什么走到了头" class="headerlink" title="提示词工程为什么走到了头"></a>提示词工程为什么走到了头</h2><p>过去两年，从编码 Agent 里拿东西的方式很固定：写个好提示词，喂够上下文，读它的回复，再写下一句。你打一行，看返回，再打下一行。Addy 的判断是，这部分基本结束了。</p><pre class="language-text" data-language="text"><code class="language-text">你:   实现用户登录接口，用 JWTAgent: [给出代码]你:   加上 refresh tokenAgent: [改]你:   测试没过，错误码 401Agent: [再改]... 你全程在场，一句一句喂</code></pre><p>问题不是 Agent 不够强，而是你成了瓶颈——它每迭代一步都要停下来等你拍板。模型一年比一年能扛长任务，可你的注意力没变多。当一个任务要二十轮来回，你就被钉在椅子上当那根串行的导线：复述约定、贴上下文、确认下一步。我自己最烦的就是每开一个新会话，又要把项目的目录结构、命名习惯、测试怎么跑重新讲一遍。</p><p>Peter Steinberger 把替代方案讲得很直接：你不该再给编码 Agent 写提示词了，你该设计那些给 Agent 写提示词的循环。这句话的重量在于，说它的人正是天天用这些工具的人，而不是旁观者。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 最朴素的循环：把"判断该干什么"也交出去# pick_next_task 是你自己写的挑活函数（从 issue / CI / backlog 里取一件）while true; do  task=$(pick_next_task)  claude -p "完成这件事：$task"      # 让 Agent 干  sleep 300done</code></pre><p>这段循环里，你已经不在每一步出现了。Claude Code 干脆把这件事做成了内建命令。</p><pre class="language-bash" data-language="bash"><code class="language-bash">/loop 30m 检查 CI 失败并尝试修复    # 每 30 分钟跑一轮/goal 把测试覆盖率提到 80%           # 给一个目标，自己迭代到达成</code></pre><p>两个命令的差别值得停一下：<code>/loop</code> 给的是节奏（每隔多久跑一次），<code>/goal</code> 给的是终点（达成某个条件才停）。前者适合”持续盯着某类问题”，后者适合”一件有明确完成标准的事”。</p><p>Addy 把循环定义成「一个递归的目标：你定义一个目的，AI 反复迭代直到完成」。活没变轻松，变的是杠杆点的位置——从”敲提示词”挪到了”设计系统”。提示词工程没有消失，它被降级成了循环内部的一个零件：你仍然要写好那段触发提示词，只是不再亲手把它喂进去。</p><p><img src="/medias/loading.gif" data-original="/images/posts/20_a6a3a6cfbf218d979fd0d6bdf4480c6d.png" alt="Addy Osmani《Loop Engineering》原文（2026-06-07）"></p><h2 id="循环的骨架：自动化、worktree-和-skill"><a href="#循环的骨架：自动化、worktree-和-skill" class="headerlink" title="循环的骨架：自动化、worktree 和 skill"></a>循环的骨架：自动化、worktree 和 skill</h2><p>一个像样的循环，Addy 拆成五个构件外加一层记忆。先看撑起骨架的三个，加上那层缺不得的记忆。</p><p><img src="/medias/loading.gif" data-original="/images/posts/20_fd599dd8d4da4474b656289d9d84280b.jpg" alt="循环五构件加记忆层结构图"></p><p><strong>自动化是循环的心跳。</strong> 它在固定节奏上做发现和分诊——定时触发、git 事件触发、或者 CI 信号触发。Addy 强调过：正是这个会自己醒来的触发器，才让一个循环真的成其为循环，而不是一次手动运行。没有心跳，你写得再漂亮的 prompt 也只是个一次性脚本。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 自动化 = 循环的心跳：定时触发发现和分诊# crontab：每天早上 9 点跑一次0 9 * * * cd /repo &amp;&amp; claude -p "$(cat triage-prompt.md)" &gt;&gt; loop.log</code></pre><p><strong>worktree 解决并行打架。</strong> 同时跑多个 Agent 时，最怕它们改同一批文件互相覆盖。<code>git worktree</code> 给每个任务一份隔离的工作目录。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 每个并行任务一个独立 worktree，互不踩踏git worktree add ../fix-issue-142 -b fix/issue-142git worktree add ../fix-issue-153 -b fix/issue-153git worktree list</code></pre><p><strong>skill 把项目知识固化下来。</strong> Addy 有个说法叫”意图债”——你每次开新会话重新解释一遍约定，就是在反复还这笔债。skill 是一次性还清的方式：一个 <code>SKILL.md</code> 就是一段会被反复调用的说明书，外加可选的脚本和参考文件，让循环每次跑都不必重新教一遍。</p><pre class="language-markdown" data-language="markdown"><code class="language-markdown">---name: triagedescription: 读取昨天的 CI 失败和新开 issue，分类并写入待办---# 分诊流程1. 拉取过去 24h 的 CI 失败记录2. 按「能自动修 / 需人介入」分两类3. 把结论写进 backlog.md</code></pre><p>还有一层不算构件、却缺不得的东西：外部记忆。对话窗口会断、会被压缩，进度不能跟着断——循环把状态写进一个 markdown 文件或 Linear 看板，下一轮醒来先读它，知道昨天干到哪、哪些还没碰。这层记忆活在对话之外，所以换模型、清上下文都不影响它。</p><pre class="language-markdown" data-language="markdown"><code class="language-markdown"># loop-state.md（循环的记忆）### 进行中- [ ] #142 修复登录 401（worktree: fix-issue-142）### 已完成- [x] #138 补全 README 安装步骤</code></pre><h2 id="让循环能动手、会自查：连接器和子-Agent"><a href="#让循环能动手、会自查：连接器和子-Agent" class="headerlink" title="让循环能动手、会自查：连接器和子 Agent"></a>让循环能动手、会自查：连接器和子 Agent</h2><p>剩下两个构件最关键——一个让循环碰得到真实世界，一个让它别自己批改自己的作业。</p><p><img src="/medias/loading.gif" data-original="/images/posts/20_d8e8188b3eee335e3eaf88a67990478f.jpg" alt="写代码的 Agent 与审查的 Agent 互相校验的循环"></p><p><strong>只能读文件系统的循环，只能提建议。</strong> 要让它真正动手——开 PR、更新工单、发 Slack、查数据库——靠的是 MCP（Model Context Protocol）连接器。按 Anthropic 的定义，MCP 是「连接 AI Agent 与外部系统的开放标准」。</p><pre class="language-json" data-language="json"><code class="language-json">{  "mcpServers": {    "github": { "command": "npx", "args": ["-y", "@modelcontextprotocol/server-github"] },    "linear": { "command": "npx", "args": ["-y", "mcp-linear"] }  }}</code></pre><p>MCP 自 2024 年 11 月发布以来铺得很快，按 Anthropic 的说法，社区已经造了上千个 MCP 服务器，主流语言都有 SDK，行业把它当成了事实标准。一个 Agent 实现一次 MCP，就接上了一整个工具生态。</p><p>Anthropic 给的一个数字很能说明问题：用代码方式调用 MCP 工具，能把一次任务的 token 从 15 万压到 2000，省掉 98.7%（见 <a href="https://www.anthropic.com/engineering/code-execution-with-mcp">code execution with MCP</a>）。这对长期跑着的循环不是小数——省下的是真金白银。Codex 和 Claude Code 都讲 MCP，你为一个写的连接器，换到另一个上大多直接能用，不用为换工具重写一遍集成。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># claude mcp 只管配置：list 看已接入的 serverclaude mcp list# github / linear ... 具体工具（open_pr、create_issue）由模型在会话内调用</code></pre><p><strong>子 Agent 把”写”和”查”分开。</strong> 这是循环里最有用的一个结构性动作。Addy 的原话是：写代码的那个模型，给自己的作业打分时太手软了。让同一个 Agent 既写又审，它会本能地替自己辩护。第二个 Agent 换一套指令、有时换一个模型、调高推理档位，专门挑刺，就能抓住第一个说服自己糊弄过去的东西。</p><p>更细一点的分工是按角色拆：一个负责探索（读代码、搞清现状），一个负责动手（写改动），一个负责验证（只找问题）。每个角色给不同的指令，甚至不同的模型，谁也别替谁背书。</p><pre class="language-toml" data-language="toml"><code class="language-toml"># .codex/agents/reviewer.toml —— 一个只负责挑刺的子 Agentname = "reviewer"model = "gpt-5-codex"instructions = """你是代码审查员。只找问题，不写新功能。重点看：边界条件、错误处理、有没有测试。"""</code></pre><p>我自己把审查交给第二个 Agent 之后，第一个 Agent 那些”自我感觉良好”的改动才露馅——它会把一个没处理空值的函数写得理直气壮。社区已经有人把这类现成模式整理成了工具集（<a href="https://github.com/cobusgreyling/loop-engineering">loop-engineering</a>，含 loop-audit / loop-init / loop-cost）。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 让审查子 Agent 过一遍刚写的改动claude --agent reviewer -p "审查 fix/issue-142 分支的 diff"</code></pre><h2 id="一天的循环长什么样"><a href="#一天的循环长什么样" class="headerlink" title="一天的循环长什么样"></a>一天的循环长什么样</h2><p>五个零件单看都不稀奇，拼起来才有意思。Addy 给的样板是一个每天早上在仓库上跑的自动化，我把它顺着走一遍：闹钟一响，自动化醒来，它的 prompt 调一个分诊 skill，读昨天的 CI 失败、新开 issue、最近的 commit，把结论写进一个 markdown 文件或 Linear 看板。</p><p><img src="/medias/loading.gif" data-original="/images/posts/20_6d80896526f99155cc4e5fcb17e9537e.jpg" alt="一天的循环流水线：触发到开PR"></p><p>心跳可以是定时器，也可以挂在 CI 事件上。用 GitHub Actions 当触发器，循环就跟着仓库的节奏走。</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml"># .github/workflows/morning-loop.yml —— 用 CI 事件当心跳on:  schedule:    - cron: "0 1 * * *"   # 每天 UTC 01:00jobs:  loop:    runs-on: ubuntu-latest    steps:      - uses: actions/checkout@v4      - run: claude -p "$(cat .loop/triage.md)"</code></pre><p>这个状态文件就是循环的工作记忆。它读昨天的烂摊子，写下今天要处理的清单，下一轮自己再接上。整套流程里最值得玩味的，是它把”发现工作”也自动化了——你不再需要每天早上自己翻 issue 列表决定先修哪个。</p><p>分诊完，循环对着每件待办开隔离 worktree、让 fixer 子 Agent 起草修复、让 reviewer 子 Agent 对着项目 skill 审一遍，最后连接器开 PR、更新工单。每件事在自己的 worktree 里跑，互不干扰，跑挂了也只脏一个分支。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 分诊 → 隔离 → 修 → 审 → 开 PR，全程无人值守# read_triaged 是你自己的解析函数，从 backlog.md 读出已分诊的 issue 号for issue in $(read_triaged backlog.md); do  git worktree add "../wt-$issue" -b "fix/$issue"  ( cd "../wt-$issue" \      &amp;&amp; claude --agent fixer    -p "修复 $issue" \      &amp;&amp; claude --agent reviewer -p "审查这次改动" \      &amp;&amp; gh pr create --fill --label "loop-generated" --draft )  # 连接器/gh 开 PRdone</code></pre><p>跑完一轮，状态文件被改写成次日早上你能一眼看懂的样子——哪些进了 PR、哪些被标记成需要人介入。</p><pre class="language-markdown" data-language="markdown"><code class="language-markdown"># loop-state.md（次日早上自动更新）### 今早处理- [x] #142 → PR #210 已开，待人审- [ ] #155 改动过大，已标记需人介入</code></pre><p>你早上真正要做的，是看循环昨晚干了什么、决定哪些值得合并。这一步永远在你手上。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 你的活：看循环昨晚干了什么，决定哪些合并gh pr list --label loop-generatedgit log --oneline fix/issue-142</code></pre><h2 id="第一个循环该怎么起步"><a href="#第一个循环该怎么起步" class="headerlink" title="第一个循环该怎么起步"></a>第一个循环该怎么起步</h2><p>看完五个构件，别急着上 GitHub Actions 全自动。循环最稳的起法是先做小、先做得能随时叫停，让它在你看得见的地方把行为暴露出来。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 第一步：只让它分诊和提建议，先别动代码claude -p "读 backlog.md，列出今天该修的 3 件事，但不要改任何文件"</code></pre><p>先看它挑的活靠不靠谱，确认判断力没问题，再给它动手的权限。起步阶段我推荐用 <code>/goal</code> 而不是 <code>/loop</code>，因为前者有明确终点、错了有边界，后者一旦放久最容易在你没盯着的时候跑偏。下一步是把作用域钉死，别让它碰整个仓库。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 用 label 把循环的作用域圈住，它只碰带这个标签的 issuegh issue list --label "loop-safe" --json number,title</code></pre><p>挑一类边界清楚的活先试——补测试、修 lint、更新文档，这些错了也好回滚。等行为稳定，再加一道人审闸门：循环只开 PR，合并永远留给你。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 循环只开草稿 PR，是否合并由人决定claude --agent fixer -p "修复 #142" &amp;&amp; \  gh pr create --label "loop-generated" --draft</code></pre><p>draft 加人审，循环再勤快也越不过你这一关。手动跑顺几天、确认它不闯祸，才值得把它升级成定时循环。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 跑稳几天后，才挂上定时器变成真正的循环crontab -l | grep loop || \  echo "0 9 * * * cd /repo &amp;&amp; ./run-loop.sh" | crontab -</code></pre><p>Addy 那套每天早上自动跑的循环，是终点而不是起点。一上来就全自动，等于把一个你还没摸清脾气的系统放出去替你改代码。</p><h2 id="循环的三个局限，每个都得你扛"><a href="#循环的三个局限，每个都得你扛" class="headerlink" title="循环的三个局限，每个都得你扛"></a>循环的三个局限，每个都得你扛</h2><p>循环替你做不了的事，恰恰是最要紧的那几件。Addy 把它们说得很白。</p><p><strong>验证还是你的活。</strong> 原话是：无人值守跑着的循环，也是无人值守犯错的循环。循环开的 PR 别盲合，至少过一道自动门。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 循环开的 PR 别盲合，至少过一道自动门gh pr checks 210                          # CI 全绿？git diff main...fix/issue-142 | wc -l     # 改了多少行，心里有数</code></pre><p><strong>理解会欠债。</strong> 循环越快地交付你没写的代码，「存在的东西」和「你真正搞懂的东西」之间的缺口就越大。我自己的体会是：连着合并几个循环生成的 PR 后，再去 debug 一个线上问题，会发现有些模块根本没读过，得现学自己仓库里的代码。这笔债平时不响，出事那天利滚利。对付它没有巧办法，只能定期逼自己回读循环写的关键路径。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 对循环生成的代码，强制自己回读关键路径git log --author="loop" --since="1 week" --stat</code></pre><p><strong>判断会交出去，钱也会烧掉。</strong> Addy 说，循环自己跑起来后，很容易就不再有自己的看法，它给什么你就收什么——这是比代码出错更隐蔽的塌方，因为你连”该不该这么做”都不再问了。再加上一句他反复强调的话：你绝对得小心 token 成本。一个无人盯着的循环，半夜可以替你把一个月的预算烧光，所以单轮要设上限，每天要回头看花了多少。</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 给循环装个成本刹车：每轮设美元上限，超了自动停claude -p --max-budget-usd 5.00 "$(cat .loop/triage.md)"# 会话里随时用 /usage 看本轮花了多少；月度封顶在 Claude Console 设 workspace spend limit</code></pre><h2 id="总结：把循环建起来，但人得留在工程师的位置上"><a href="#总结：把循环建起来，但人得留在工程师的位置上" class="headerlink" title="总结：把循环建起来，但人得留在工程师的位置上"></a>总结：把循环建起来，但人得留在工程师的位置上</h2><p>Addy 的收尾很准——把循环建起来，但要像一个打算继续当工程师的人那样去建。循环把杠杆点从”敲提示词”挪到了”设计系统”，可验证、犯错、判断该不该合并这些事，没有一样能交出去。</p><p>循环适合边界清楚、能自动验证的活：修 CI、补测试、跑迁移、清 issue 队列。不适合需求还没想清、或者错一次代价很大的改动——那种你还是得一句一句盯着。想往深里读，Addy 的<a href="https://addyosmani.com/blog/loop-engineering/">原文</a>和 Anthropic 那篇<a href="https://www.anthropic.com/engineering/code-execution-with-mcp">用代码调用 MCP</a>都值得看。</p><blockquote><p>觉得有用，<strong>点个「在看」</strong>，让还在一句句敲提示词的同行也看到。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> AI工程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Claude Code </tag>
            
            <tag> AI编程 </tag>
            
            <tag> Loop Engineering </tag>
            
            <tag> 循环工程 </tag>
            
            <tag> 软件研发 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>16GB 别碰、24GB 凑合、32GB+ 才是生产力：本地编程 LLM 的 2026 真相</title>
      <link href="/2026/06/22/16gb-bie-peng-24gb-cou-he-32gb-cai-shi-sheng-chan-li-ben-di-bian-cheng-llm-de-2026-zhen-xiang/"/>
      <url>/2026/06/22/16gb-bie-peng-24gb-cou-he-32gb-cai-shi-sheng-chan-li-ben-di-bian-cheng-llm-de-2026-zhen-xiang/</url>
      
        <content type="html"><![CDATA[<h1 id="16GB-别碰、24GB-凑合、32GB-才是生产力：本地编程-LLM-的-2026-真相"><a href="#16GB-别碰、24GB-凑合、32GB-才是生产力：本地编程-LLM-的-2026-真相" class="headerlink" title="16GB 别碰、24GB 凑合、32GB+ 才是生产力：本地编程 LLM 的 2026 真相"></a>16GB 别碰、24GB 凑合、32GB+ 才是生产力：本地编程 LLM 的 2026 真相</h1><p><img src="/medias/loading.gif" data-original="/images/posts/19_39102f06a7b410b3a8daafcd40c907e6.jpg" alt="一台笔记本电脑本地运行代码模型，旁边三档内存分级的概念封面"></p><p>先说结论，省得你往下翻：2026 年能在自己电脑上认真写代码的本地模型，只有一个——<strong>Qwen3.6-27B 跑 Q4_K_M 量化</strong>。不是之一，是唯一。</p><p>这话不是我拍脑袋。一位叫 Andrus 的开发者写了篇<a href="https://freedium-mirror.cfd/https://ai.gopubby.com/i-went-looking-for-the-best-local-llm-for-coding-in-2026-7fa97efb34f1">本地编程模型实测</a>，把手头三个真实任务甩给四款模型跑了一遍。我读完后把里面的数据跟我自己这两年折腾本地模型的判断捋了一遍，写成这篇。数据归他，态度归我。</p><p>本地大模型这条路，我看着它坑了所有人好几年。要么是小模型——能装进内存，但写出来的代码是垃圾；要么是大模型——聪明是聪明，可在 MacBook 上每秒吐 4 个字，等它写完一个函数你咖啡都凉了。两头不靠岸，这就是过去几年本地编程的真实处境。直到 2026 年 4 月这一拨模型出来，局面才第一次变了。</p><p><strong>一句话的选型光谱</strong>：16GB 内存别碰本地编程模型，纯属浪费时间；24GB 能跑但要忍；32GB 起步，本地模型才从”玩具”变成”干活的工具”。下面我把怎么测的、为什么是它、你该怎么配，一条条说清楚。</p><h2 id="三个真实任务，四款模型现原形"><a href="#三个真实任务，四款模型现原形" class="headerlink" title="三个真实任务，四款模型现原形"></a>三个真实任务，四款模型现原形</h2><p>跑分榜我一向不太信——刷榜和干活是两码事。Andrus 这次的测法我服：他没用合成基准，直接从自己的待办里拎出三个真实任务，全是会出现在你我 PR 里的活儿。</p><pre class="language-text" data-language="text"><code class="language-text">任务一：跨 4 个 Laravel 文件，重构一个支付通知服务任务二：调一条三表 join、莫名返回重复行的 SQL任务三：给一个完全没测试的模块补单元测试</code></pre><p>复现环境很简单，一条命令拉模型就能跟着测：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 拉 Qwen3.6-27B 的 Q4_K_M 量化版，文件 16.8GB# 占 ~17GB 磁盘，按带宽算下载时间，断了能续传ollama pull qwen3.6:27b# 直接对话跑ollama run qwen3.6:27b</code></pre><p>先说主角 <strong>Qwen3.6-27B</strong> 的真实表现，三个任务三种脸色。重构那道，它第一次漏了一个调用点，我指出来之后第二次才补全——Andrus 的原话是”正是那种让你忍不住全程复查的差一点”。SQL 那道最惊艳，它一眼看穿是 join 条件导致行重复，给的解释比作者自己原来的方案还清楚。单测那道则暴露了短板：代码能编译、能跑，但只覆盖 happy path，明明让它写失败用例，时区边界还是漏了。</p><p>四款模型的真实表现，我整理成一张表：</p><table><thead><tr><th>模型</th><th>参数/架构</th><th>SWE-bench Verified</th><th>真实任务表现</th></tr></thead><tbody><tr><td><strong>Qwen3.6-27B</strong></td><td>27B 稠密</td><td><strong>77.2%</strong></td><td>重构第二次过；SQL 一眼看穿重复行根因；单测只覆盖 happy path</td></tr><tr><td>Qwen3.6-35B-A3B</td><td>35B/3B 激活 MoE</td><td>—</td><td>更快但更莽，语法改对了却踩坏两文件外的类型契约</td></tr><tr><td>Devstral Small 2</td><td>24B agentic</td><td>68.0%</td><td>agent 循环里能读报错改对文件，纯对话就一般</td></tr><tr><td>Codestral</td><td>补全专用</td><td>—</td><td>本地补全能用，但跨文件感知不靠谱，作者两天后关了</td></tr></tbody></table><p>Qwen3.6-27B 的 <a href="https://www.swebench.com/">SWE-bench Verified</a> 拿到 <strong>77.2%</strong>，距 Claude Opus 4.6 的 80.8% 只差 3.6 分。这是什么概念？一个能塞进笔记本、Apache 2.0 授权、没人能涨价没人能封号的开源模型，干到了一线云端模型不久前坐的位置。它原生支持 256K 上下文、能吃文本图像视频，配 vLLM、SGLang、KTransformers 这些框架都顺。我敢说，这是第一个让”本地写代码”不再是行为艺术的模型。</p><p><img src="/medias/loading.gif" data-original="/images/posts/19_1d8a84cfae6ecaf91f2d9fffa8086797.png" alt="Qwen3.6-27B 在 Hugging Face 的模型卡：Apache 2.0 授权、月下载近 600 万"></p><p>剩下三款各有各的定位，别指望一个模型通吃。<strong>Devstral Small 2</strong>（24B、Apache 2.0）是个 agentic 选手，跟 All Hands AI 一起调出来的，在 edit-test-fix 这种 agent 循环里它能可靠地读懂报错、动对的那个文件，而不是动你”刚才提过的”那个文件——这点很关键，但你纯拿它聊天就没那么灵。<strong>Codestral</strong> 是补全专用，native FIM、号称 HumanEval 86.6%，接进 Continue 当本地 Copilot 体验”大概落后云端 Copilot 一年”，跨文件感知不靠谱，Andrus 嫌它费电、有延迟，两天就关了。</p><p>那个 MoE 变体 <strong>Qwen3.6-35B-A3B</strong> 我得单独多说两句。35B 总参但每个 token 只激活 3B。据 Simon Willison 公开实测（二手转引），稠密版在 MacBook 上约 25.57 tokens/s；而 MoE 版社区实测能冲到 35–55 tokens/s——这两个数都不是我亲测的，照搬来给你个量级感。快是真快。但 Andrus 那句吐槽精准：<strong>“错得更快，也改得更快”</strong>——同一个重构任务它输出得漂漂亮亮，却违反了两个文件之外的类型契约；你把报错丢回去，它又秒改。我的判断很简单：MoE 适合探索性瞎试，不适合你打算直接 commit 的活儿。提速的代价是少了那份谨慎，这笔账你自己算。</p><h2 id="瓶颈从来不是算力，是内存带宽"><a href="#瓶颈从来不是算力，是内存带宽" class="headerlink" title="瓶颈从来不是算力，是内存带宽"></a>瓶颈从来不是算力，是内存带宽</h2><p>这是最反直觉的一点，也是为什么过去几年本地大模型一直”能装下但跑不动”。很多人以为本地推理慢是显卡不够猛，错了。</p><p>本地生成 token 的速度，瓶颈不在算力，在内存带宽。Apple Silicon 的统一内存带宽约 270GB/s，而一张 RTX 3090 是 936GB/s——差了三倍多。所以同一个模型，塞进显卡和塞进 Mac，吐字速度完全是两回事。这也解释了为什么以前的大稠密模型在 Mac 上慢成 PPT：每生成一个 token 都要把全部参数从内存里捞一遍，带宽就是天花板。</p><p>MoE 架构这次真正解决的就是这件事——每个 token 只激活一小部分参数（35B 里只动 3B），要搬运的数据量骤降，带宽瓶颈一下就松了。2026 年 4 月这拨模型让大模型在 Apple Silicon 上跑得动，靠的不是芯片变快，是架构绕开了带宽墙。</p><p><img src="/medias/loading.gif" data-original="/images/posts/19_a86af3246d07d8b57916e1d71a935fc6.jpg" alt="内存带宽对比示意，Apple Silicon 与独立显卡的数据吞吐差异"></p><p>还有条铁律得刻进脑子：<strong>模型文件大小要压在总内存的三分之二以内</strong>。macOS 自己先吃掉 3–4GB，上下文缓存还会随 token 数往上涨——你开的上下文越大，缓存越占内存。算一下就懂为什么 16GB 是绝路：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 看模型实际占用，跑起来后查（SIZE 含 KV 缓存，随 num_ctx 浮动）ollama ps# NAME            SIZE     PROCESSOR# qwen3.6:27b     18 GB    100% GPU</code></pre><p>16.8GB 的模型文件，加上系统和上下文缓存，实际吃到 18GB 往上，16GB 内存连塞都塞不下。想知道你这台机器能跑多大的模型，用这个粗算，别凭感觉：</p><pre class="language-python" data-language="python"><code class="language-python"># 可用预算 = 总内存 × 2/3 - 系统占用total_ram = 36        # 你的总内存 GBbudget = total_ram * 2 / 3 - 4   # 减掉 macOS 的 3-4GBprint(f"模型文件别超过 {budget:.0f}GB")   # 20GB，刚好够 27B Q4</code></pre><p>顺带说下 <strong>Q4_K_M</strong> 是什么——4-bit 的 K 系列中档量化。为什么是它而不是更高精度？因为它在体积和质量之间卡得最准：16.8GB 的体积让 24GB 内存也摸得到门槛，质量损失又小到日常写代码基本无感。更高精度的版本质量提升有限，体积却把你直接顶出内存预算。至于 DeepSeek V4 这种动辄要 140GB 的，本地这条路上直接 pass，别惦记，那是给服务器集群准备的。</p><h2 id="num-ctx-默认-2048，这是-Ollama-欠了两年的债"><a href="#num-ctx-默认-2048，这是-Ollama-欠了两年的债" class="headerlink" title="num_ctx 默认 2048，这是 Ollama 欠了两年的债"></a>num_ctx 默认 2048，这是 Ollama 欠了两年的债</h2><p>模型选对了，工具不对照样白搭。这里有个坑，几乎每个用 Ollama 写代码的人都踩过，而且踩了还不一定知道：<a href="https://docs.ollama.com/faq">Ollama 的默认上下文窗口 num_ctx 只有 2048 token</a>。</p><p>2048 是什么水平？你贴一个稍长点的源文件进去就溢出了。最坑的是它不报错——模型不是变笨了，是它压根没看全你的代码，却照样一本正经给你回答。你以为是模型不行，其实是工具默认值在坑你。这个问题被用户吐槽了两年，至今没改默认。写代码至少拉到 32768：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 运行时临时改ollama run qwen3.6:27b&gt;&gt;&gt; /set parameter num_ctx 32768</code></pre><p>想一劳永逸，写进 Modelfile，以后这个模型永远带着大上下文起：</p><pre class="language-docker" data-language="docker"><code class="language-docker">FROM qwen3.6:27bPARAMETER num_ctx 32768</code></pre><p>走 API 的话，就在每个请求里带上 options：</p><pre class="language-json" data-language="json"><code class="language-json">{  "model": "qwen3.6:27b",  "prompt": "重构这个支付服务",  "options": { "num_ctx": 32768 }}</code></pre><p><img src="/medias/loading.gif" data-original="/images/posts/19_5acc81bdb9684257f5c95c19f1d9f5d1.png" alt="Ollama 官方 FAQ 里专门有一条「如何设置上下文窗口大小」"></p><p>还有个白捡的提速，别只盯着 Ollama 一棵树。Ollama 的后端是 llama.cpp，而 <a href="https://lmstudio.ai/">LM Studio</a> 用的是 Apple 的 MLX 框架，同样的模型在 Apple Silicon 上社区实测快 10–30%。同样一块芯片，换个推理后端就白得三成速度，这便宜不占白不占。我的建议很直接：纯对话场景用 LM Studio 吃 MLX 的速度红利，要嵌进脚本和 agent 流程再用 Ollama 的接口和生态。Ollama 截至 2026 年 6 月也出了 MLX 预览支持，但成熟度还差点意思，我暂时不押它。</p><h2 id="光跑起来不算用，得接进编辑器"><a href="#光跑起来不算用，得接进编辑器" class="headerlink" title="光跑起来不算用，得接进编辑器"></a>光跑起来不算用，得接进编辑器</h2><p>终端里 <code>ollama run</code> 复制粘贴算什么生产力？要真拿它写代码，得把本地端点接进你天天用的编辑器。两条路我都走过，分开配反而清爽。</p><p>补全和对话用 <a href="https://continue.dev/">Continue</a> 插件接 Ollama 的本地端点，VS Code 和 JetBrains 都吃，配置就三行：</p><pre class="language-json" data-language="json"><code class="language-json">{  "models": [    { "title": "Qwen3.6-27B", "provider": "ollama", "model": "qwen3.6:27b" }  ]}</code></pre><p>要的是 edit-test-fix 那种 agent 流程，就换 Devstral Small 2 配它自带的 Mistral Vibe CLI，让模型在终端里自己读报错、改对文件、再跑一遍：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 把 Devstral 拉下来，交给 agent 流程驱动ollama pull devstral</code></pre><p>别指望一个插件全搞定——补全归补全，agent 归 agent，混在一起只会互相拖累。</p><h2 id="按你的内存对号入座，别幻想"><a href="#按你的内存对号入座，别幻想" class="headerlink" title="按你的内存对号入座，别幻想"></a>按你的内存对号入座，别幻想</h2><p>讲了半天原理，落到你这台机器上到底能不能用，就看一个数：内存。我把三档直接说死，不跟你打太极。</p><p><img src="/medias/loading.gif" data-original="/images/posts/19_70ec53e8de22e8a072909048046f4618.jpg" alt="三档内存对应的本地编程能力分级示意"></p><table><thead><tr><th>内存</th><th>结论</th><th>我的话</th></tr></thead><tbody><tr><td>16GB</td><td>别碰</td><td>没有一款跑得舒服，装上只会失望，省下这两小时去喝咖啡</td></tr><tr><td>24GB</td><td>凑合</td><td>能跑 27B，但上下文要收紧、生成要忍，算半个工具</td></tr><tr><td>32GB+</td><td>生产力</td><td>从”做实验”变成”真干活”的分水岭，Andrus 那台 36GB 正好踩在中间偏上</td></tr></tbody></table><p>24GB 的机器想跑，得把上下文压回去，拿可用性换余量——别一上来就拉满，缓存会把你顶爆：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 24GB 内存：上下文收到 16K，给系统和缓存留余地ollama run qwen3.6:27b&gt;&gt;&gt; /set parameter num_ctx 16384</code></pre><p>32GB 以上就放开手脚，该多大上下文给多大，还能用 q8 缓存再省点：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 32GB+：直接拉满 32K 上下文写代码，KV 缓存用 q8 压一压OLLAMA_KV_CACHE_TYPE=q8_0 ollama run qwen3.6:27b&gt;&gt;&gt; /set parameter num_ctx 32768</code></pre><p>如果你内存不够又非要本地，那条路不是换更小的模型——小模型能装下，但写出来的代码是垃圾，我宁可不用也不会拿那种东西糊弄自己。要么加内存，要么老老实实掏钱用云端 API。在 16GB 上硬撑本地编程模型，是我见过最典型的”为了省钱反而最费时间”。</p><p>补一句独显党：上面三档说的是 Mac 的统一内存，<strong>独显看的是显存，不是总内存</strong>。一张 24GB 显存的 4090 跑 27B Q4 余量足，带宽还是 Apple Silicon 的三倍多（936 对 270 GB/s），吐字明显更快。代价是显存比内存”硬”——一旦塞不下溢出到内存/CPU，掉速比 Mac 还狠。所以预算够上独显，本地编程体验其实优于同价位的 Mac。Windows / Linux 用户这套显存账和 num_ctx 设置完全照搬；唯独 Intel Mac 别折腾，跟 16GB 同列，直接 pass。</p><p>我自己最认同 Andrus 的一点，是他的诚实：他没吹”本地替代了云端”。<strong>复杂调试、需要跨整个代码库做假设分析的活儿，一线云端模型依然明显更强。</strong> 本地模型不是来取代云端的，是来把 80% 的日常杂活接走的——常规生成、客户代码、补样板，这些根本不值得动用你最贵的那个 API key。</p><p>他实测后的真实变化很说明问题：日常活全交给本地 Qwen3.6-27B，只在真卡死时才升级到付费 API，一个月 API 开销降了 50% 以上。他还顺手在飞行模式下让模型审了自己那篇文章——这才是本地模型最爽的地方：没人能断你网，没人能半夜给你涨价，16.8GB 的权重落在你硬盘上，谁也收不回去。</p><p>所以别问”哪个本地模型最好”，要问”我这个场景配哪个”。我按用途把这四款拆开，对号入座：</p><table><thead><tr><th>场景</th><th>我的推荐</th><th>原因</th></tr></thead><tbody><tr><td>日常写代码 / 重构 / 客户活</td><td><strong>Qwen3.6-27B (Q4_K_M)</strong></td><td>唯一能扛主力的，77.2% SWE-bench 排同体量第一档</td></tr><tr><td>agent 流程（edit-test-fix 循环）</td><td>Devstral Small 2</td><td>读报错、动对文件这件事它做得最稳</td></tr><tr><td>编辑器里的实时补全</td><td>Codestral，但要忍</td><td>能用，跨文件感知弱，费电，按需开</td></tr><tr><td>探索性瞎试、不打算 commit</td><td>Qwen3.6-35B-A3B</td><td>快，但别信它一遍过，错得也快</td></tr><tr><td>16GB 机器</td><td>哪个都别装</td><td>加内存或用云端，这是省时间不是省钱</td></tr></tbody></table><p>说白了，主力位只有 Qwen3.6-27B 一个，其余三款是配角，各补一块短板。我推荐 Qwen3.6-27B 当主力，因为它是唯一一个在重构、SQL、单测三类任务上都没掉链子的（77.2% 的 SWE-bench 也排在同体量第一档）；我不用 35B-A3B 当主力，因为它快归快，提交前那种”两文件外类型契约被它悄悄改坏”的提心吊胆，我受不了。你要是只装一个，别犹豫，就 27B。</p><p>它也不是没有让我皱眉的地方。单测只写 happy path 这事我很介意——明确要了失败用例还漏时区边界，意味着关键路径的测试你还得自己兜底，不能甩手。重构那次的”差一个调用点”也是同理：能用，但你得养成全程复查的习惯，把它当一个聪明但偶尔马虎的初级工程师使，而不是当甩手掌柜。这不是黑它，是用它的正确姿势。</p><p>本地编程模型熬了这么多年，2026 年 4 月这一拨才算真正跨过”能用”那道线。Qwen3.6-27B 不完美，单测还偷懒、复杂活还得求云端，但它是第一个让”在自己机器上写代码”这件事配得上”生产力”三个字的模型。我的判断是：今年年底之前，本地编程会从极客的玩具变成不少人 PR 里的默认起手式。</p><p>把这篇转给那个还在为月底 API 账单肉疼、或者还在 16GB 机器上跟本地模型死磕的同事——先让他把内存这一关搞明白，别的都是后话。</p><p>下一步很简单，一条命令开始：<code>ollama pull qwen3.6:27b</code>，跑起来第一件事，记得把 num_ctx 拉到 32768。</p>]]></content>
      
      
      <categories>
          
          <category> 模型与评测 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 本地LLM </tag>
            
            <tag> Qwen3.6 </tag>
            
            <tag> Ollama </tag>
            
            <tag> 编程模型 </tag>
            
            <tag> 模型选型 </tag>
            
            <tag> 量化部署 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>把 Claude Code 接到 Prometheus + Grafana：14 个变量讲清楚</title>
      <link href="/2026/06/08/292/"/>
      <url>/2026/06/08/292/</url>
      
        <content type="html"><![CDATA[<h1 id="把-Claude-Code-接到-Prometheus-Grafana：14-个变量讲清楚"><a href="#把-Claude-Code-接到-Prometheus-Grafana：14-个变量讲清楚" class="headerlink" title="把 Claude Code 接到 Prometheus + Grafana：14 个变量讲清楚"></a>把 Claude Code 接到 Prometheus + Grafana：14 个变量讲清楚</h1><p><img src="/medias/loading.gif" data-original="/images/posts/292_8ffec82c409c857806865aac71012e58.jpg" alt="Claude Code → OTLP → Prometheus → Grafana 流水线封面"></p><p>我前两天帮一个团队 debug 他们的 Claude Code telemetry，把 alias 拷过去之后 Prometheus 一条数据都没收到。一查发现：默认 protocol 是 gRPC 不是 http/protobuf，端口是 4317 不是 4318，而对方的 Prometheus 只开了 HTTP receiver。一个变量错位，整条数据流哑火。</p><p>Claude Code 的 OpenTelemetry 配置一共有 14 个常用环境变量，分四层：开关、传输、隐私、基数。每一层都有”默认值跟你想要的不一样”的坑。</p><p>这篇分两半。前半把 14 个变量按层讲清楚（含完整对照表）。后半给你一份贴上就能跑的 Prometheus 2.47+ 原生 OTLP receive + Grafana 的 docker-compose——不需要 OTel Collector 中转，最少零件。</p><h2 id="14-个-env-vars-分四层看"><a href="#14-个-env-vars-分四层看" class="headerlink" title="14 个 env vars 分四层看"></a>14 个 env vars 分四层看</h2><p>按功能分组，记住每层只解决一个问题，记起来就简单：</p><table><thead><tr><th>层</th><th>变量</th><th>默认值</th><th>决定</th></tr></thead><tbody><tr><td><strong>开关</strong></td><td><code>CLAUDE_CODE_ENABLE_TELEMETRY</code></td><td>unset (= 关)</td><td>总开关，没有它后面全失效</td></tr><tr><td></td><td><code>CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC</code></td><td>unset</td><td>关 Anthropic 自己的遥测（与你的 OTel 无关）</td></tr><tr><td></td><td><code>OTEL_METRICS_EXPORTER</code></td><td>unset</td><td><code>otlp</code> / <code>prometheus</code> / <code>console</code> / <code>none</code></td></tr><tr><td></td><td><code>OTEL_LOGS_EXPORTER</code></td><td>unset</td><td><code>otlp</code> / <code>console</code> / <code>none</code>（注意没有 prometheus）</td></tr><tr><td><strong>传输</strong></td><td><code>OTEL_EXPORTER_OTLP_PROTOCOL</code></td><td><code>grpc</code> ⚠️</td><td><code>grpc</code> / <code>http/protobuf</code> / <code>http/json</code></td></tr><tr><td></td><td><code>OTEL_EXPORTER_OTLP_ENDPOINT</code></td><td><code>localhost:4317</code></td><td>gRPC 用 4317，HTTP 用 4318</td></tr><tr><td></td><td><code>OTEL_EXPORTER_OTLP_HEADERS</code></td><td>unset</td><td><code>Authorization=Bearer xxx</code></td></tr><tr><td><strong>标签</strong></td><td><code>OTEL_RESOURCE_ATTRIBUTES</code></td><td>unset</td><td><code>team=platform,user.email=x@y.com</code></td></tr><tr><td><strong>隐私</strong></td><td><code>OTEL_LOG_USER_PROMPTS</code></td><td><code>0</code></td><td>prompt 正文上不上报</td></tr><tr><td></td><td><code>OTEL_LOG_TOOL_CONTENT</code></td><td><code>0</code></td><td>tool 输入输出正文</td></tr><tr><td></td><td><code>OTEL_LOG_TOOL_DETAILS</code></td><td><code>0</code></td><td>bash 命令字符串、MCP server 名等元信息</td></tr><tr><td><strong>基数</strong></td><td><code>OTEL_METRICS_INCLUDE_SESSION_ID</code></td><td><code>true</code> ⚠️</td><td>不关掉会导致指标基数爆炸</td></tr><tr><td></td><td><code>OTEL_METRICS_INCLUDE_ACCOUNT_UUID</code></td><td><code>true</code></td><td>多用户场景下也要</td></tr><tr><td></td><td>时间间隔</td><td><code>60000</code> / <code>5000</code> ms</td><td>metrics 60s、logs 5s，debug 期调小</td></tr></tbody></table><p>标⚠️的两个最坑：默认 protocol 是 gRPC，但很多公司 HTTPS 反代只允许 HTTP；默认带 <code>session.id</code>，每次 Claude 重启都是新 series，几天 Prometheus 内存就崩。</p><p>最小启用模板（本地 dev，推到本地 4317）：</p><pre class="language-bash" data-language="bash"><code class="language-bash">export CLAUDE_CODE_ENABLE_TELEMETRY=1export OTEL_METRICS_EXPORTER=otlpexport OTEL_LOGS_EXPORTER=otlp# 默认就是 grpc + localhost:4317，可以不写</code></pre><p>跑一下 <code>claude --version</code> 之后用 <code>tcpdump -i lo port 4317</code> 抓包，如果有流量，说明开关层 OK。</p><p>debug 阶段不想搭后端时，最方便的是 console exporter——直接把 metric 打到 stderr：</p><pre class="language-bash" data-language="bash"><code class="language-bash">export CLAUDE_CODE_ENABLE_TELEMETRY=1export OTEL_METRICS_EXPORTER=consoleexport OTEL_METRIC_EXPORT_INTERVAL=5000   # 5 秒一次，看得见claude# 然后看 stderr，会有 JSON 形式的 metric 流出来</code></pre><h2 id="三种-exporter：gRPC-HTTP-protobuf-内置-Prometheus"><a href="#三种-exporter：gRPC-HTTP-protobuf-内置-Prometheus" class="headerlink" title="三种 exporter：gRPC / HTTP/protobuf / 内置 Prometheus"></a>三种 exporter：gRPC / HTTP/protobuf / 内置 Prometheus</h2><p>我推荐先想清楚走哪条路再配，不然变量改来改去会乱：</p><p><strong>Path A：gRPC OTLP</strong>（默认，最快）</p><pre class="language-bash" data-language="bash"><code class="language-bash">export OTEL_EXPORTER_OTLP_PROTOCOL=grpcexport OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317</code></pre><p>适合本地 / VPC 内部、能直连的场景。性能最好，但很多企业反代不放 gRPC。</p><p><strong>Path B：HTTP/protobuf OTLP</strong>（跨网络、跨反代最稳）</p><pre class="language-bash" data-language="bash"><code class="language-bash">export OTEL_EXPORTER_OTLP_PROTOCOL=http/protobufexport OTEL_EXPORTER_OTLP_ENDPOINT=https://usage.example.com/api/v1/otlp# Prometheus 2.47+ 直接收，无需 OTel Collector</code></pre><p>这是 HelloTalk 那种远程聚合常用的姿势——HTTP/2 走 443，反代不挑食。我自己生产也用这条。</p><p><strong>Path C：内置 Prometheus exporter</strong>（最简单）</p><pre class="language-bash" data-language="bash"><code class="language-bash">export OTEL_METRICS_EXPORTER=prometheus# Claude Code 会在本地起一个 /metrics 端点，Prometheus 主动 scrape</code></pre><p>零依赖，本地用最快。缺点：只能采 metrics，logs 没法走这条；多用户聚合也不方便。</p><p>我自己的选型经验：单机本地用 Path C，团队聚合用 Path B，重度场景（要做 trace 关联）才上 Path A + OTel Collector。</p><h2 id="传输进阶：per-signal-拆分-4-种认证姿势"><a href="#传输进阶：per-signal-拆分-4-种认证姿势" class="headerlink" title="传输进阶：per-signal 拆分 + 4 种认证姿势"></a>传输进阶：per-signal 拆分 + 4 种认证姿势</h2><p>走到生产环境，基本会遇到两个需求：metrics 和 logs 想推到不同后端、跨网络要带认证。Claude Code 都支持，只是参数容易写错。</p><p><strong>Metrics 和 logs 走不同后端</strong>——比如 metrics 进 Prometheus、logs 进 Loki：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 通用 endpoint 不写，用 per-signal overrideexport OTEL_EXPORTER_OTLP_METRICS_PROTOCOL=http/protobufexport OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://prometheus:9090/api/v1/otlpexport OTEL_EXPORTER_OTLP_LOGS_PROTOCOL=grpcexport OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=http://loki:4317</code></pre><p>注意 metrics endpoint 走 HTTP（带 <code>/api/v1/otlp</code> 路径），logs 走 gRPC（只到端口）。两边都各自的协议参数，不会互相覆盖。</p><blockquote><p>⚠️ <strong>Claude 2.1 实测坑</strong>：单独设 <code>OTEL_EXPORTER_OTLP_METRICS_ENDPOINT</code> + <code>OTEL_EXPORTER_OTLP_LOGS_ENDPOINT</code>，<strong>实际只有 metrics endpoint 生效</strong>，logs 信号仍走通用 endpoint。我自己的环境（2.1.153）跑下来就是这个行为——Prom 拿到了、loki 静默落空。最稳的做法是<strong>只设 <code>OTEL_EXPORTER_OTLP_ENDPOINT</code> 指向一个 OTel Collector</strong>,让 Collector 内部按 pipeline 分流给 Prom 和 Loki。Collector 这层配置 12 行就够，下面”Docker 容器”章节会贴完整 yaml。</p></blockquote><p><strong>认证姿势 1：静态 Bearer token</strong>——最简单，token 写在环境变量里：</p><pre class="language-bash" data-language="bash"><code class="language-bash">export OTEL_EXPORTER_OTLP_HEADERS="Authorization=Bearer $(cat /etc/secrets/otel-token)"# 多个 header 用逗号分隔export OTEL_EXPORTER_OTLP_HEADERS="Authorization=Bearer xxx,X-Tenant-ID=team-platform"</code></pre><p><strong>认证姿势 2：mTLS</strong>——企业内 zero-trust 网络常用，OTLP exporter 直接吃 cert 文件路径：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 客户端证书三件套（与 OTLP_PROTOCOL 配合）export OTEL_EXPORTER_OTLP_CLIENT_CERTIFICATE=/etc/ssl/claude-client.crtexport OTEL_EXPORTER_OTLP_CLIENT_KEY=/etc/ssl/claude-client.keyexport OTEL_EXPORTER_OTLP_CERTIFICATE=/etc/ssl/ca-bundle.crt   # 服务端 CA</code></pre><p><strong>认证姿势 3：动态 headers</strong>——token 会过期、要轮换，写个脚本输出 JSON：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 准备一个 helper 脚本，stdout 输出 JSONcat &gt; /usr/local/bin/get-otel-headers.sh &lt;&lt;'EOF'#!/bin/bashTOKEN=$(aws sts assume-role-with-web-identity ... | jq -r '.Credentials.SessionToken')echo "{\"Authorization\": \"Bearer $TOKEN\"}"EOFchmod +x /usr/local/bin/get-otel-headers.sh# 告诉 Claude Code 用这个脚本（仅 http/protobuf 和 http/json 支持，gRPC 不行）export OTEL_EXPORTER_OTLP_HEADERS_HELPER=/usr/local/bin/get-otel-headers.shexport CLAUDE_CODE_OTEL_HEADERS_HELPER_DEBOUNCE_MS=900000   # 15 分钟刷新一次</code></pre><p><strong>认证姿势 4：管理员配置文件</strong>——团队不想让每个开发者自己配 env vars，用 settings 文件集中管理：</p><pre class="language-json" data-language="json"><code class="language-json">// /etc/claude-code/managed-settings.json（管理员级，优先级最高）{  "env": {    "CLAUDE_CODE_ENABLE_TELEMETRY": "1",    "OTEL_METRICS_EXPORTER": "otlp",    "OTEL_EXPORTER_OTLP_PROTOCOL": "http/protobuf",    "OTEL_EXPORTER_OTLP_ENDPOINT": "https://usage.example.com/api/v1/otlp",    "OTEL_EXPORTER_OTLP_HEADERS": "Authorization=Bearer $(cat /etc/secrets/otel)",    "OTEL_RESOURCE_ATTRIBUTES": "deployment.environment=prod"  }}</code></pre><p>放好之后，开发者 <code>claude</code> 一启动就自动拿到这些 env vars，不需要自己 export，也不能覆盖。多团队场景下 <code>OTEL_RESOURCE_ATTRIBUTES</code> 可以叠加打标签：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 用户 shell 里追加 team / user 维度（managed.json 里 prod 不变）export OTEL_RESOURCE_ATTRIBUTES="${OTEL_RESOURCE_ATTRIBUTES},team=infra,user.email=$(git config user.email),cost_center=eng-123"</code></pre><p>后端拿到的 resource attributes 是合并集，Prometheus 那边在 <code>promote_resource_attributes</code> 里挑要 promote 成 label 的字段就行。</p><h2 id="隐私-基数：你不想全推的两类参数"><a href="#隐私-基数：你不想全推的两类参数" class="headerlink" title="隐私 + 基数：你不想全推的两类参数"></a>隐私 + 基数：你不想全推的两类参数</h2><p>这块是踩坑重灾区。默认值在 <em>最保守</em> 一端（什么都不上报），但很多人为了”看清楚一点”把开关全打开，结果一周后日志里塞满了源码片段被合规扫到。</p><p><strong>隐私三件套</strong>（默认全是关，按需开）：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 我个人推荐的"够用不越界"组合export OTEL_LOG_USER_PROMPTS=0     # 用户 prompt 正文：永远关export OTEL_LOG_TOOL_CONTENT=0     # tool 输入输出正文：永远关（会包含文件内容！）export OTEL_LOG_TOOL_DETAILS=1     # tool 元信息：可以开，对审计有用</code></pre><p><code>OTEL_LOG_TOOL_DETAILS=1</code> 会上报 Bash 命令字符串、MCP server / tool 名、skill 名。这部分对审计很有用（知道用户在调哪些工具），但本身不含正文，比 <code>TOOL_CONTENT</code> 安全得多。</p><p>如果你要在 Grafana 的 prompt 二级页里看到 <strong>Tool Calls 的请求参数正文</strong>，还必须额外打开：</p><pre class="language-bash" data-language="bash"><code class="language-bash">export OTEL_LOG_TOOL_CONTENT=1</code></pre><p>注意这只对<strong>新启动的 Claude Code 进程</strong>生效，历史 Loki 数据补不回来。它会把 tool 输入/输出内容一起上报，可能包含文件路径、命令参数、代码片段甚至文件内容，只建议在明确的审计环境里打开。</p><p><strong>还有一个高危隐私变量必须知道</strong>：<code>OTEL_LOG_RAW_API_BODIES</code>。它会上报完整的 Anthropic Messages API 请求 / 响应 JSON，包含整段对话历史和生成内容。开启它等于把所有上面三个开关一次性打开，并且体量翻倍——单次 prompt 体量可以到 60KB。除非你在做 LLM 审计平台，不然永远别开：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 危险，除非你知道自己在做什么# export OTEL_LOG_RAW_API_BODIES=1            # 内联，单条 60KB 截断# export OTEL_LOG_RAW_API_BODIES=file:/var/log/claude-bodies   # 落盘，无截断</code></pre><p><strong>基数控制四件套</strong>（全部默认开的两个一定要关，另外两个按需）：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># 高基数自杀风险：必须关export OTEL_METRICS_INCLUDE_SESSION_ID=false       # 默认 true，每次重启换值export OTEL_METRICS_INCLUDE_ACCOUNT_UUID=false     # 默认 true，多用户场景按 team 聚合时不需要# 默认关的两个：按需开export OTEL_METRICS_INCLUDE_VERSION=true           # 默认 false。Claude Code 升级监控想看版本分布时打开export OTEL_METRICS_INCLUDE_ENTRYPOINT=true        # 默认 false。区分 `claude` / `claude -p` / Agent SDK 调用源</code></pre><p><code>session.id</code> 每次 <code>claude</code> 命令重启都换，加进 metric 标签就是 cardinality 自杀。我之前没关，跑了 4 天 Prometheus 内存从 200MB 涨到 8GB，回查才发现是这个。<code>VERSION</code> 和 <code>ENTRYPOINT</code> 默认是关的——只在你要做”哪些用户还卡在旧版本””脚本调用占比多少”这类分析时才打开。</p><h2 id="Prometheus-原生-OTLP-接收：一条-flag-搞定"><a href="#Prometheus-原生-OTLP-接收：一条-flag-搞定" class="headerlink" title="Prometheus 原生 OTLP 接收：一条 flag 搞定"></a>Prometheus 原生 OTLP 接收：一条 flag 搞定</h2><p>Prometheus 从 2.47 开始原生支持 OTLP HTTP receive，不需要 OTel Collector 当中间件。这是目前最短的接收路径。</p><p>启动时加一个 flag：</p><pre class="language-bash" data-language="bash"><code class="language-bash">prometheus \  --config.file=/etc/prometheus/prometheus.yml \  --web.enable-otlp-receiver</code></pre><p>然后 prometheus.yml 里加一个 <code>otlp:</code> 段，告诉它把哪些 OTel resource attribute 提升成 label：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml"># prometheus.ymlotlp:  # 把这些 OTel 资源属性 promote 成 Prometheus label，方便查询  promote_resource_attributes:    - service.name    - service.instance.id    - team    - user.email  keep_identifying_resource_attributes: true  # translation_strategy 用默认（UnderscoreEscapingWithSuffixes）  #   - OTel 名里 . → _（claude_code.session.count → claude_code_session_count）  #   - counter 自动加 _total 后缀  #   - 带单位的 metric 加单位后缀（详见下文"验证"段）storage:  tsdb:    # OTLP 数据偶尔会乱序到达，给一点容忍    out_of_order_time_window: 30m</code></pre><p>Claude Code 端指过去：</p><pre class="language-bash" data-language="bash"><code class="language-bash">export CLAUDE_CODE_ENABLE_TELEMETRY=1export OTEL_METRICS_EXPORTER=otlpexport OTEL_EXPORTER_OTLP_PROTOCOL=http/protobufexport OTEL_EXPORTER_OTLP_ENDPOINT=http://prometheus:9090/api/v1/otlpexport OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=cumulativeexport OTEL_METRICS_INCLUDE_SESSION_ID=falseclaude</code></pre><p>注意三个细节：endpoint 是 <code>/api/v1/otlp</code>（Prometheus 内部自动补 <code>/v1/metrics</code>）；temporality 一定要 <code>cumulative</code>，Prometheus 不擅长 delta；session_id 关掉。</p><p>验证：</p><pre class="language-bash" data-language="bash"><code class="language-bash">curl -s http://localhost:9090/api/v1/query?query=claude_code_session_count_total | jq .</code></pre><p>如果返回里有 <code>result</code> 数组，数据流就通了。</p><p><strong>实测过的命名规则</strong>（默认 <code>UnderscoreEscapingWithSuffixes</code> 策略）：</p><table><thead><tr><th>OTel 原名</th><th>Prometheus 名</th><th>备注</th></tr></thead><tbody><tr><td><code>claude_code.session.count</code></td><td><code>claude_code_session_count_total</code></td><td>unit=count 时不加单位后缀</td></tr><tr><td><code>claude_code.token.usage</code></td><td><code>claude_code_token_usage_**tokens**_total</code></td><td>⚠️ unit=tokens,加单位</td></tr><tr><td><code>claude_code.cost.usage</code></td><td><code>claude_code_cost_usage_**USD**_total</code></td><td>⚠️ unit=USD,加单位</td></tr><tr><td><code>claude_code.active_time.total</code></td><td><code>claude_code_active_time_**seconds**_total</code></td><td>⚠️ unit=s → seconds</td></tr><tr><td><code>claude_code.lines_of_code.count</code></td><td><code>claude_code_lines_of_code_count_total</code></td><td>unit=count</td></tr><tr><td><code>claude_code.code_edit_tool.decision</code></td><td><code>claude_code_code_edit_tool_decision_total</code></td><td>unit=count</td></tr></tbody></table><p>带单位的那 3 个最容易踩坑——按”原 OTel 名替换点”猜会查不到。我第一次跑就栽在 <code>claude_code_cost_usage_total</code> 上,debug 半天才发现要加 <code>_USD</code>。下面的 PromQL 都用了实测过的正确命名。</p><h2 id="加-Grafana，跑一个-demo-dashboard"><a href="#加-Grafana，跑一个-demo-dashboard" class="headerlink" title="加 Grafana，跑一个 demo dashboard"></a>加 Grafana，跑一个 demo dashboard</h2><p>Grafana 用 provisioning 模式自动加 data source + dashboard，比手点省心。</p><p><code>grafana/provisioning/datasources/prometheus.yml</code>：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">apiVersion: 1datasources:  - name: Prometheus    type: prometheus    access: proxy    url: http://prometheus:9090    isDefault: true    editable: false    jsonData:      httpMethod: POST      timeInterval: 15s</code></pre><p>几条我每天都会看的 PromQL，覆盖”成本 / 产出 / 谁在用”三个维度：</p><pre class="language-promql" data-language="promql"><code class="language-promql"># 1. 各模型每小时 token 消耗（成本归因第一选）sum by (model, type) (increase(claude_code_token_usage_tokens_total[1h]))# 2. 各团队每天的 cost（USD）— 需要前面配了 team resource attributesum by (team) (increase(claude_code_cost_usage_USD_total[1d]))# 3. 每天有多少独立 session 启动（adoption 跟进）sum(increase(claude_code_session_count_total[1d]))# 4. lines_of_code 增减（产出维度）sum(rate(claude_code_lines_of_code_count_total{type="added"}[1h]))# 5. 工具调用决策分布（被拒绝率，看权限模型健康度）sum by (decision) (rate(claude_code_code_edit_tool_decision_total[5m]))</code></pre><p>我自己 dashboard 上最常盯着的是第 2 条 + 第 5 条。前者管钱，后者看 Claude 是不是经常被人按 deny——按 deny 太多说明工具描述写得不好，或者用户对 Claude 不信任，都是要 fix 的信号。</p><blockquote><p>⚠️ <strong>OTLP 累计 counter + <code>increase()</code> 的坑（实测）</strong>：Claude Code 走 OTLP 推的是<strong>累计 counter</strong>，而且只在值<strong>变化时</strong>才推一个样本（稀疏推送）。当某指标在你选的时间窗内只有 1 个样本时，<code>increase(metric[$__range])</code> 因为”两点才能算增量”会<strong>返回空</strong>——dashboard 上就是一片 <code>No data</code>，空闲几分钟后打开尤其明显。两个稳妥替代：</p><ul><li>看”区间累计值”（如总成本、总 token、commit 数）用 <code>sum(last_over_time(metric[$__range]))</code>——取窗口内最后一个样本，顺便绕开 Prometheus 5 分钟 staleness 导致的 instant 查询变空；</li><li>看”窗口增量趋势”，要么把范围拉到足够长让 <code>increase()</code> 有 ≥2 个样本，要么干脆把趋势计算挪到 <strong>Loki 端</strong> <code>sum_over_time({...} | unwrap &lt;字段&gt; [$__interval])</code>——日志侧每条事件都是独立样本，不受稀疏 counter 影响，趋势更准。本文后面 Loki 章节的成本/token 趋势都用后一种。</li></ul></blockquote><p>如果你想直接拿一份现成 dashboard，Anthropic 自己开源了 <code>anthropics/claude-code-monitoring-guide</code> 仓库，里面有 docker-compose + dashboard JSON。我下面这份 docker-compose 是简化版（去掉了它的 Loki 部分，只留 Prometheus + Grafana）：</p><h2 id="Dashboard-自动-provisioning-告警规则"><a href="#Dashboard-自动-provisioning-告警规则" class="headerlink" title="Dashboard 自动 provisioning + 告警规则"></a>Dashboard 自动 provisioning + 告警规则</h2><p>PromQL 写完，下一步是让 Grafana 启动时自动加载 dashboard 和 alert，开发机和生产环境出一份代码就够。</p><p><code>grafana/provisioning/dashboards/dashboards.yml</code>：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">apiVersion: 1providers:  - name: claude-code    type: file    disableDeletion: true    updateIntervalSeconds: 30    allowUiUpdates: false    options:      path: /etc/grafana/provisioning/dashboards/json      foldersFromFilesStructure: true</code></pre><p>然后把 dashboard JSON 放到 <code>grafana/provisioning/dashboards/json/cost.json</code>——下面是一个最小可用的成本归因面板骨架（一个 stat + 一个 timeseries + 两个变量 <code>$team</code> 和 <code>$model</code>）：</p><pre class="language-json" data-language="json"><code class="language-json">{  "title": "Claude Code Cost Attribution",  "templating": {    "list": [      {"name": "team", "type": "query", "datasource": "Prometheus",       "query": "label_values(claude_code_cost_usage_USD_total, team)", "multi": true, "includeAll": true},      {"name": "model", "type": "query", "datasource": "Prometheus",       "query": "label_values(claude_code_cost_usage_USD_total, model)", "multi": true, "includeAll": true}    ]  },  "panels": [    {"id": 1, "type": "stat", "title": "Today's Total Cost (USD)",     "targets": [{"expr": "sum(increase(claude_code_cost_usage_USD_total{team=~\"$team\",model=~\"$model\"}[1d]))"}]},    {"id": 2, "type": "timeseries", "title": "Hourly Cost by Team",     "targets": [{"expr": "sum by (team) (increase(claude_code_cost_usage_USD_total{model=~\"$model\"}[1h]))",                  "legendFormat": "{{team}}"}]}  ]}</code></pre><p><strong>告警规则</strong>——团队成本超阈值时报警，写在 Prometheus 端（也可以用 Grafana 9+ 的 unified alerting）：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml"># prometheus-rules.yml，挂到 prometheus.yml 的 rule_files: 段groups:  - name: claude_code_cost    interval: 1m    rules:      - alert: ClaudeCodeTeamCostOverBudget        expr: |          sum by (team) (increase(claude_code_cost_usage_USD_total[1d])) &gt; 50        for: 5m        labels:          severity: warning        annotations:          summary: "Team {{ $labels.team }} 今日成本 ${{ $value | printf \"%.2f\" }} 超 $50 阈值"      - alert: ClaudeCodeDenyRateHigh        expr: |          sum by (team) (rate(claude_code_code_edit_tool_decision_total{decision="deny"}[15m]))            / sum by (team) (rate(claude_code_code_edit_tool_decision_total[15m])) &gt; 0.3        for: 10m        labels:          severity: info        annotations:          summary: "Team {{ $labels.team }} 工具拒绝率 &gt; 30%，工具描述或权限模型可能要 fix"</code></pre><p>第二条告警是我自己生产里很有价值的一条——deny 率高说明 Claude 推荐的命令经常被否决，要么是 prompt 写得不准，要么是工具描述不清楚，能直接反推 fix 方向。</p><h2 id="Skill-Agent-MCP-使用分布：数据驱动砍-推哪些"><a href="#Skill-Agent-MCP-使用分布：数据驱动砍-推哪些" class="headerlink" title="Skill / Agent / MCP 使用分布：数据驱动砍/推哪些"></a>Skill / Agent / MCP 使用分布：数据驱动砍/推哪些</h2><p>装了 30 个 skill 装了 8 个 MCP server，半年后没人记得到底哪些在跑活，凭印象砍最容易砍错。Claude Code 的 OTel 数据其实把每次调用都打了标签——同一个 <code>claude_code.token.usage</code> metric 上额外带 <code>skill.name</code> / <code>plugin.name</code> / <code>agent.name</code> 维度，按它们 group by 就是一张”谁在烧 token”的天然 dashboard。</p><p><strong>前提是 <code>OTEL_LOG_TOOL_DETAILS=1</code> 必须打开</strong>——不然下面所有 skill/plugin/agent 标签全是空字符串，dashboard 永远看不到：</p><pre class="language-bash" data-language="bash"><code class="language-bash">export OTEL_LOG_TOOL_DETAILS=1# 这会上报：Bash 命令字符串、MCP server/tool 名、skill 名、subagent 类型# 不含：prompt 正文、tool 输入输出 — 见前面"隐私 + 基数"章节的权衡</code></pre><p>打开之后，Prometheus 端会看到 <code>claude_code_token_usage_tokens_total</code> 上多出三个标签：<code>skill_name</code> / <code>plugin_name</code> / <code>agent_name</code>（OTel 名里的 <code>.</code> 在 Prom 端转成 <code>_</code>）。</p><blockquote><p>⚠️ <strong>重要实测发现</strong>：<code>skill_name</code> 和 <code>plugin_name</code> 标签的<strong>值不是 skill 的具体名</strong>(如 <code>using-superpowers</code>)，而是<strong>分类</strong>(<code>'third-party'</code> / 推测还有 <code>'built-in'</code> 等)。Claude Code 应该不想把私有 skill 名上报到 metric。所以下面这条 <code>topk(10, sum by skill_name)</code> 拿到的是”分类型的 token 分布”(third-party vs built-in)而不是”哪 10 个 skill 最烧 token”——别被 PromQL 表达式骗了。<code>agent_name</code> 倒是具体的（如 <code>general-purpose</code>、自定义 subagent 名）。<strong>想要具体 skill 名（全名）别在 Prometheus 这边死磕——走后面 Loki 章节的 <code>skill_activated</code> 事件，那里 <code>skill_name</code> 是真名。</strong></p></blockquote><p><strong>按 skill 分类看 token 分布</strong>——分清楚是 third-party 插件还是内置 skill 在烧钱：</p><pre class="language-promql" data-language="promql"><code class="language-promql"># 实测值通常只有 1-2 类:third-party / built-insum by (skill_name) (claude_code_token_usage_tokens_total{skill_name!=""})# 如果只想看本周新增sum by (skill_name) (increase(claude_code_token_usage_tokens_total{skill_name!=""}[7d]))</code></pre><p><strong>每个 plugin 分类的成本归因（USD）</strong>——类似上,跟 skill_name 通常一致:</p><pre class="language-promql" data-language="promql"><code class="language-promql">sum by (plugin_name) (claude_code_cost_usage_USD_total{plugin_name!=""})# 团队 × plugin 分类的二维视图sum by (team, plugin_name) (claude_code_cost_usage_USD_total{plugin_name!=""})</code></pre><p><strong>Subagent 调用密度</strong>——<code>agent_name</code> 是真正的具体名（<code>general-purpose</code> / <code>code-reviewer</code> / 自定义 subagent 名），这条最有用：</p><pre class="language-promql" data-language="promql"><code class="language-promql"># Top 10 烧 token 的 subagenttopk(10,  sum by (agent_name) (claude_code_token_usage_tokens_total{agent_name!=""}))# 配合 user_email 看是哪个开发者疯狂调 subagenttopk(5,  sum by (user_email, agent_name) (rate(claude_code_token_usage_tokens_total{agent_name!=""}[1h])))</code></pre><p><strong>要看具体 skill / MCP server / 工具名,走 logs 后端</strong>(Loki/Elastic)最直接。这里有一组<strong>第一版踩过坑、必须讲清楚的事实</strong>：Claude Code 走 OTel 上报时，所有 attribute 经 OTel Collector 写成 Loki 3.x 的 <strong>structured metadata</strong>（既不是 stream label，也不是 JSON body），并且<strong>属性名里的 <code>.</code> 全部转成 <code>_</code></strong>——<code>skill.name</code>→<code>skill_name</code>、<code>process.pid</code>→<code>process_pid</code>、<code>event.name</code>→<code>event_name</code>、<code>server.name</code>→<code>server_name</code>。LogQL 查询一律用下划线名。</p><blockquote><p>🔑 <strong>structured metadata 怎么过滤（关键，纠正一个广为流传的误解）</strong>：</p><ul><li>❌ <strong>不能</strong>放进 stream selector 大括号——<code>{service_name="claude-code", tool_name="Skill"}</code> 直接返 <strong>0</strong>（大括号只认真正的 stream label，本环境只有 <code>service_name</code> 一个）；</li><li>✅ <strong>能</strong>用 <strong>pipeline label filter</strong>（管道符）<code>{service_name="claude-code"} | event_name="tool_result" | tool_name="Skill"</code> 精确过滤，实测稳定返回数据，也能 <code>sum by(tool_name)</code> 聚合、<code>| unwrap duration_ms</code> 取数值。</li></ul><p>一句话：<strong>过滤 structured metadata 用管道符 <code>| key="val"</code>，不是大括号 <code>{key="val"}</code></strong>。记牢这条，下面所有按 skill / agent / server 精确过滤的查询都能跑通——不必退回 <code>|=</code> 子串匹配那种容易误伤的写法。</p></blockquote><p>按 <code>event_name</code> 区分事件类型后，最有价值的几类：</p><ul><li><strong><code>skill_activated</code></strong> — 每次 skill 激活打一条，<strong>直接带具体 skill 名</strong>：<code>skill_name</code>（<code>superpowers:using-superpowers</code> / <code>feishu-docs</code> 这种全名）、<code>skill_source</code>（plugin / userSettings / project）、<code>invocation_trigger</code>（user-slash / nested-skill / claude-proactive）。<strong>这是拿”具体哪个 skill 在跑”的正解</strong>——不用像旧版那样绕 <code>tool_result</code> 按行人工翻（详见下面「找具体 skill 名」小节）。</li><li><strong><code>subagent_completed</code></strong> — 每个子代理跑完打一条，带 per-run 明细：<code>agent_type</code>（general-purpose / Explore / 自定义名）、<code>total_tokens</code>、<code>total_tool_uses</code>、<code>duration_ms</code>、<code>model</code>、<code>agent_source</code>（built-in / 自定义）。比 Prometheus 的 <code>agent_name</code> 聚合多了”每一次跑了多久、用了多少工具/token”的粒度。</li><li><strong><code>mcp_server_connection</code></strong> — 每次 MCP server 连接握手打一条。<code>server_name</code> 是具体名（context7 / sequential-thinking / n8n-mcp 等）；<strong><code>status</code> 是连接结果（connected / failed / disconnected）——这是 MCP 健康度的关键信号</strong>（实测 failed 常常 ≥ connected，多发生在 sse/远程 server）；还有 <code>transport_type</code>（stdio 本地 / sse·claudeai-proxy 远程）、<code>server_scope</code>、<code>duration_ms</code>（握手耗时）。</li><li><strong><code>tool_result</code></strong> — 每次工具调用完打一条。<strong>注意（2.1.x 实际行为）</strong>：<code>tool_name</code> 对 MCP 工具<strong>始终是字符串 <code>mcp_tool</code></strong>（不区分 server），真实 server+tool 名在另一个 structured metadata 字段 <code>tool_parameters</code> 里，是 JSON 字符串，例如 <code>{"mcp_server_name":"context7","mcp_tool_name":"resolve-library-id"}</code>。Loki 的 <code>| json</code> 只能解 log body、解不了 structured metadata 里的 JSON value，要拆 server 出来得在客户端（Python / jq）解，或用 <code>line_format</code> 把整串打出来人工看。</li></ul><blockquote><p>📌 <strong>更正一则旧版误解</strong>:本文早期版本写过”Loki 3.3 下用精确 match 偶尔返空、得退回 line filter <code>|=</code> 子串匹配”——复盘下来，那是把 stream selector <code>{key="val"}</code>（structured metadata 放这里确实返 0）和 pipeline filter <code>| key="val"</code>（完全可行）搞混了。实测 <code>| event_name="mcp_server_connection"</code>、<code>| tool_name="Skill"</code>、<code>| status="failed"</code> 这类<strong>管道符</strong>过滤都稳定生效。下面的例子统一用管道符精确过滤；<code>|=</code> 子串匹配只在”搜某个关键词出现在哪”时才用，不再拿来当 label 过滤的替代。</p></blockquote><p><strong>列出本会话连接过的所有 MCP server</strong>(实测 12 个:context7 / chrome-devtools / mixsearch / sequential-thinking-1 / zread / web-search-prime / web-reader / vibe_kanban / playwright / n8n-mcp / plugin:superpowers-chrome:chrome / sequential-thinking):</p><pre class="language-logql" data-language="logql"><code class="language-logql"># 区间内连接过的 MCP server（管道符精确过滤 event_name）sum by (server_name) (  count_over_time({service_name="claude-code"} | event_name="mcp_server_connection" [$__range]))</code></pre><p><strong>MCP 连接健康度（这是最该上 dashboard 的一张）</strong>——<code>status</code> 维度此前完全被”连接次数”掩盖，实测把它拆出来才发现 <code>failed</code> 往往比 <code>connected</code> 还多：</p><pre class="language-logql" data-language="logql"><code class="language-logql"># 连接状态分布（connected / failed / disconnected）sum by (status) (  count_over_time({service_name="claude-code"} | event_name="mcp_server_connection" [$__range]))# 哪些 server 在掉线 —— 直接点名排障入口（实测集中在远程 sse server）sum by (server_name) (  count_over_time({service_name="claude-code"} | event_name="mcp_server_connection" | status="failed" | server_name!="" [$__range]))# 握手延迟 avg by server（unwrap 数值字段，慢连接拖慢启动）avg by (server_name) (  avg_over_time({service_name="claude-code"} | event_name="mcp_server_connection" | server_name!="" | unwrap duration_ms [$__range]))</code></pre><p><strong>每次 MCP 工具调用的延迟、谁调的、参数</strong>(把 <code>tool_parameters</code> 这个 JSON label 解出 <code>mcp_server_name</code> 字段):</p><pre class="language-logql" data-language="logql"><code class="language-logql"># 关键:Loki 3.3 的 | json 只能解 log body,解不了 structured metadata label 里的 JSON value# (tool_parameters 是 OTel Collector 写到 Loki structured metadata 的 label)# 所以 LogQL 只做 line filter 圈定范围,JSON 解在客户端做{service_name="claude-code"} |= "tool_result"  | line_format `{{.user_email}} → {{.tool_parameters}} | ok={{.success}} dur={{.duration_ms}}ms | in={{.tool_input_size_bytes}}B out={{.tool_result_size_bytes}}B`</code></pre><p>实测一行: <code>costa9293@gmail.com → {"mcp_server_name":"context7","mcp_tool_name":"resolve-library-id"} | ok=true dur=4763ms | in=43B out=1679B</code> —— 哪个用户在哪个时刻调了哪个 MCP server 的哪个 tool、参数多大、返回多大,白纸黑字。要按 server 拆开看,客户端用 jq / Python 解 <code>tool_parameters</code> 这串 JSON 即可(Loki WebUI 有自带 JSON inspector)。</p><p><strong>按 MCP server 分组看调用次数</strong>(1h,降序):</p><pre class="language-logql" data-language="logql"><code class="language-logql">topk(20,  sum by (server_name) (    count_over_time({service_name="claude-code"} |= "mcp_server_connection" [1h])  ))</code></pre><p><strong>只看某个 server 的 tool_result</strong>(如 <code>context7</code>):</p><pre class="language-logql" data-language="logql"><code class="language-logql">{service_name="claude-code"} |= "context7" |= "tool_result"</code></pre><p><strong>每次调用的输入参数</strong>(<code>tool_input</code> 也是 JSON label,审计时重要):LogQL 不解它,直接 <code>|= "query":"&lt;keyword&gt;"</code> 做 substring 搜索最实际:</p><pre class="language-logql" data-language="logql"><code class="language-logql"># 找所有传了 query="fastapi" 的 MCP tool 调用{service_name="claude-code"} |= "tool_result" |= "fastapi"</code></pre><p>如果团队关心某个 MCP server 的健康度,直接在 Grafana 里把 <code>server_name</code> 设成变量,配上 <code>rate(count_over_time(...[5m]))</code> 就能看到连接成功率/失败率时间序列。这套组合实测在 Loki 3.3 + OTel Collector 下稳定工作。</p><p><strong>找具体 skill 名：直接查 <code>skill_activated</code>（正解，别再绕 <code>tool_result</code>）</strong></p><p>旧版这里花了很大篇幅教人”用 <code>sum by(tool_name)</code> 拿分布、再到 logs panel 按行人工找 Skill 那一行”——那是当时误以为 structured metadata 不能精确过滤、又没发现有专门事件记这件事。正解是：</p><pre class="language-logql" data-language="logql"><code class="language-logql"># 用了哪些 skill、各多少次（具体全名，不是 third-party/built-in 分类）sum by (skill_name) (  count_over_time({service_name="claude-code"} | event_name="skill_activated" [$__range]))# skill 来源 / 触发方式分布sum by (skill_source)       (count_over_time({service_name="claude-code"} | event_name="skill_activated" [$__range]))sum by (invocation_trigger) (count_over_time({service_name="claude-code"} | event_name="skill_activated" [$__range]))</code></pre><p>实测直接拿到 <code>superpowers:using-superpowers</code> / <code>superpowers:systematic-debugging</code> / <code>feishu-docs</code> 这种具体名，外加 <code>skill_source=plugin/userSettings</code>、<code>invocation_trigger=user-slash/nested-skill/claude-proactive</code>——比在 <code>tool_result</code> 里人工翻精确得多。</p><p><strong>子代理 per-run 明细：查 <code>subagent_completed</code></strong></p><p>Prometheus 的 <code>agent_name</code> 只能给”某 agent 累计烧了多少 token”。要看”每一次跑了多久、用了多少工具”，查事件：</p><pre class="language-logql" data-language="logql"><code class="language-logql"># 各子代理类型运行次数sum by (agent_type) (  count_over_time({service_name="claude-code"} | event_name="subagent_completed" [$__range]))# 每次运行的明细：类型 / 模型 / token / 工具数 / 耗时{service_name="claude-code"} | event_name="subagent_completed"  | line_format `{{.agent_type}} | {{.model}} | tok={{.total_tokens}} tools={{.total_tool_uses}} dur={{.duration_ms}}ms`</code></pre><p><strong>通用范式：structured metadata 一律用管道符精确过滤</strong>。下面这些旧版以为”Loki 返 0”的需求，换成管道符全部能跑：</p><table><thead><tr><th>想做的事</th><th>❌ 旧版（返 0 / 容易误伤）</th><th>✅ 正确写法</th></tr></thead><tbody><tr><td>只看 Skill 工具调用</td><td><code>{service_name="claude-code", tool_name="Skill"}</code></td><td><code>{service_name="claude-code"} | event_name="tool_result" | tool_name="Skill"</code></td></tr><tr><td>只看某 server 连接</td><td><code>|= "context7"</code> 子串匹配</td><td><code>| event_name="mcp_server_connection" | server_name="context7"</code></td></tr><tr><td>工具失败率</td><td>（旧版做不了）</td><td><code>| event_name="tool_result" | success="false"</code> 占总数</td></tr></tbody></table><blockquote><p>旧版列的那张”❌ 多 label match 返 0 / ✅ 只能 sum by”的表，根因就是<strong>没区分 stream selector <code>{}</code> 和 pipeline filter <code>\|</code></strong>。<code>{key="val"}</code> 对 structured metadata 确实返 0，但那不是 Loki 的限制，是写法放错了位置——挪到管道符后就好。</p></blockquote><p><strong>仍然有效的 <code>tool_result</code> logs panel</strong>（看每次调用的工具/参数/耗时，MCP 真实 server 在 <code>tool_parameters</code> JSON 里，客户端解）：</p><pre class="language-logql" data-language="logql"><code class="language-logql">{service_name="claude-code"} | event_name="tool_result"  | line_format `{{.tool_name}} | {{.tool_parameters}} | ok={{.success}} {{.duration_ms}}ms`</code></pre><p>结合 <code>duration_ms</code>、<code>tool_input_size_bytes</code> / <code>tool_result_size_bytes</code> 能快速定位”哪些调用最重”。</p><p>所以”找出 Skill 调用”这种需求,在 Loki 3.3 + OTel Collector 这套组合下,<strong>唯一可靠的方式是 sum by (tool_name) 拿到分布,再用 logs panel 按行人工找 Skill 那一行</strong>。</p><p>也可以简化:只看 metrics 的话,<strong>最实用的是 <code>agent_name</code></strong>——subagent 的具体名能直接 group by 出来,半年下来能看出”哪些自定义 agent 真用了,哪些是装着好看”。skill/plugin 那两个分类标签只能给你”第三方 vs 内置”的粗粒度判断,而且实测在 2.1.153 <strong>永远只有 <code>third-party</code> 一个值</strong>,dashboard 画出来是单色,信息量约等于 0。</p><p>我自己 dashboard 上常驻这三面板(都是确认有真实多样性的 label,不再用 skill/plugin 分类):</p><ul><li><strong>Tokens by Subagent</strong> — <code>sum by (agent_name) (claude_code_token_usage_tokens_total{agent_name!=""})</code>。实测 <code>Explore</code> 和 <code>general-purpose</code> 各 50K+ tokens,两条柱,信息密度高。</li><li><strong>Cost by Model (USD)</strong> — <code>sum by (model) (claude_code_cost_usage_USD_total)</code>。不同模型价格差几倍,这条直接告诉你钱烧在哪类模型上。</li><li><strong>Top 10 Users by Cost (1h)</strong> — <code>topk(10, sum by (user_email) (claude_code_cost_usage_USD_total))</code>。需要 <code>OTEL_METRICS_INCLUDE_USER_EMAIL=true</code> 才能让 <code>user_email</code> 标签有值(默认是 <code>user_id</code> hash)。这条帮我跟 manager 解释钱花在哪——数据胜过 vibe。</li></ul><p>Prometheus 的 <code>skill_name</code> / <code>plugin_name</code> 标签确实只有”第三方 vs 内置”这种粗分类（实测常只有 <code>third-party</code> 一个值，画出来单色、信息量≈0），但<strong>想看具体 skill 名不必退回 <code>tool_result</code> 人工翻——走上面的 <code>skill_activated</code> 事件就好</strong>。MCP 真实 server/tool 仍在 <code>tool_result</code> 的 <code>tool_parameters</code> JSON 里（客户端解）。</p><p><strong>成本归因再进一步：按 <code>query_source</code> 拆”交互 vs 后台自动”</strong></p><p><code>api_request</code> 事件带一个容易被忽略、却极有价值的字段 <code>query_source</code>——标明这次 LLM 调用是谁发起的。实测一拆才发现后台自动调用在悄悄烧钱：</p><pre class="language-logql" data-language="logql"><code class="language-logql"># 成本按来源拆（Loki 端 unwrap cost_usd，精确、不受稀疏 counter 影响）sum by (query_source) (  sum_over_time({service_name="claude-code"} | event_name="api_request" | unwrap cost_usd [$__range]))</code></pre><p>实测分布：<code>repl_main_thread</code>（你手敲的交互）占大头，但 <code>sdk</code>、<code>away_summary</code>（你离开时 Claude 自动写的总结）、<code>agent:*</code>（子代理）、<code>prompt_suggestion</code>、<code>generate_session_title</code> 这些<strong>后台来源加起来能占三到四成</strong>——Prometheus 的 <code>cost_usage</code> 按 model/team 拆完全看不到这一层。想压成本，先看这张图里哪些后台来源能关。</p><h2 id="把-Claude-Code-跑在-Docker-容器里"><a href="#把-Claude-Code-跑在-Docker-容器里" class="headerlink" title="把 Claude Code 跑在 Docker 容器里"></a>把 Claude Code 跑在 Docker 容器里</h2><p>很多人 dev 环境是 host 直跑 Claude Code，但越来越多的团队把它装进容器——一是隔离权限避免一不小心 <code>rm -rf</code> 到宿主机，二是多个项目环境互不污染，三是 OTel 配置可以跟容器走，不污染开发者本地 shell。</p><p><strong>第一步：把 OTel 配置抽到 <code>.env</code> 文件</strong>——容器化的第一收益就是配置和镜像分离：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># otel.env（提交到 git 里也安全，没有 secret）CLAUDE_CODE_ENABLE_TELEMETRY=1OTEL_METRICS_EXPORTER=otlpOTEL_LOGS_EXPORTER=otlpOTEL_EXPORTER_OTLP_PROTOCOL=http/protobufOTEL_EXPORTER_OTLP_ENDPOINT=http://prometheus:9090/api/v1/otlpOTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=cumulativeOTEL_METRICS_INCLUDE_SESSION_ID=falseOTEL_METRICS_INCLUDE_ACCOUNT_UUID=falseOTEL_LOG_TOOL_DETAILS=1OTEL_RESOURCE_ATTRIBUTES=team=infra,deployment.environment=dev</code></pre><p>Secret 单独放，永远别和 <code>.env</code> 混在一起：</p><pre class="language-bash" data-language="bash"><code class="language-bash"># otel.secret.env（gitignore 加上！）OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer xxxxxxxxxxxxx</code></pre><p><strong>第二步：<code>docker run</code> 一次性起一个 Claude Code 容器</strong>——适合临时调一下：</p><pre class="language-bash" data-language="bash"><code class="language-bash">docker run -it --rm \  --name claude-dev \  --network monitoring \  --env-file ./otel.env \  --env-file ./otel.secret.env \  -v "$HOME/.claude:/root/.claude" \  -v "$(pwd):/workspace" \  -w /workspace \  node:24-bookworm \  bash -lc "npm i -g @anthropic-ai/claude-code &gt;/dev/null &amp;&amp; claude"</code></pre><p>三个 <code>-v</code> 挂载点要看清：<code>~/.claude</code> 装登录态和 settings，挂进去之后容器里的 <code>claude</code> 一启动就是登录状态；<code>$(pwd)</code> 是当前项目工作区；<code>--network monitoring</code> 是为了能解析到前面 docker-compose 起的 <code>prometheus</code> 容器名。</p><p><strong>第三步：把 Claude Code 加进 docker-compose</strong>——适合常驻 dev container 模式：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml"># 加到前面 docker-compose.yml 的 services: 段下  claude:    image: node:24-bookworm    container_name: claude-dev    networks: [default]    env_file:      - otel.env      - otel.secret.env    volumes:      - "$HOME/.claude:/root/.claude"      - "$PWD:/workspace"    working_dir: /workspace    tty: true    stdin_open: true    command: ["bash", "-lc", "npm i -g @anthropic-ai/claude-code &amp;&amp; sleep infinity"]</code></pre><p>挂上去之后 <code>docker compose up -d claude</code>，再 <code>docker compose exec claude claude</code> 就进入交互式 Claude，OTel 数据自动推到同 compose 里的 Prometheus 容器。</p><p><strong>Secret 管理</strong>：上面用 <code>--env-file otel.secret.env</code> 是最简单的姿势，但 secret 文件还是落盘的。要更严格可以用 Docker secrets（compose 里 <code>secrets:</code> 段）或者在容器里跑一个 <code>OTEL_EXPORTER_OTLP_HEADERS_HELPER</code> 脚本去调云厂商的 KMS 拿临时 token——参考前面”传输进阶”章节那条动态 headers 的写法，把脚本 COPY 进容器即可。</p><h2 id="完整-docker-compose：贴上就能跑"><a href="#完整-docker-compose：贴上就能跑" class="headerlink" title="完整 docker-compose：贴上就能跑"></a>完整 docker-compose：贴上就能跑</h2><p>把这份 <code>docker-compose.yml</code> + 两个配置文件放一个目录，<code>docker compose up</code> 就能起：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml"># docker-compose.ymlservices:  prometheus:    image: prom/prometheus:v3.5.0    command:      - --config.file=/etc/prometheus/prometheus.yml      - --web.enable-otlp-receiver      - --storage.tsdb.retention.time=30d    ports:      - "9090:9090"    volumes:      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro      - prom_data:/prometheus  grafana:    image: grafana/grafana:12.2.0    ports:      - "3000:3000"    environment:      GF_SECURITY_ADMIN_PASSWORD: admin      GF_AUTH_ANONYMOUS_ENABLED: "true"      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer    volumes:      - ./grafana/provisioning:/etc/grafana/provisioning:ro      - grafana_data:/var/lib/grafana    depends_on:      - prometheusvolumes:  prom_data:  grafana_data:</code></pre><p><code>prometheus.yml</code> 和前一节那份一致，<code>grafana/provisioning/datasources/prometheus.yml</code> 也是前一节那份。目录长这样：</p><pre class="language-bash" data-language="bash"><code class="language-bash">.├── docker-compose.yml├── prometheus.yml└── grafana    └── provisioning        └── datasources            └── prometheus.yml</code></pre><p>起服务 + 在你常用 Claude Code 的 shell 配 env vars 指过去：</p><pre class="language-bash" data-language="bash"><code class="language-bash">docker compose up -dexport CLAUDE_CODE_ENABLE_TELEMETRY=1export OTEL_METRICS_EXPORTER=otlpexport OTEL_LOGS_EXPORTER=otlpexport OTEL_EXPORTER_OTLP_PROTOCOL=http/protobufexport OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:9090/api/v1/otlpexport OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=cumulativeexport OTEL_METRICS_INCLUDE_SESSION_ID=falseexport OTEL_RESOURCE_ATTRIBUTES="team=infra,user.email=$(git config user.email)"claude  # 用一会儿</code></pre><p>5 分钟后打开浏览器访问 <code>localhost:3000</code>（Grafana），data source 已经自动连好 Prometheus，在 Explore 里输入 <code>claude_code_session_count_total</code> 应该能看到至少 1 个 series。</p><p><img src="/medias/loading.gif" data-original="/images/posts/292_78490e829c18d0fde72b98e4b35572a7.png" alt="anthropics/claude-code-monitoring-guide：官方 ROI 测量指南，覆盖 telemetry / 成本 / 产出 / 团队分析四个维度"></p><p><img src="/medias/loading.gif" data-original="/images/posts/292_bc2779b26e950c7e5b19a531d6b7215b.jpg" alt="Claude Code → Prometheus OTLP → Grafana 三阶段架构图"></p><hr><p>启用 telemetry 容易，把数据通起来难。把 14 个变量按”开关 / 传输 / 隐私 / 基数”四层记，知道每层只解决一个问题，配错了也容易定位是哪一层。</p><p>最大的两个默认值坑：protocol 默认是 gRPC（不是 http/protobuf）、<code>session.id</code> 默认带（基数会爆）。剩下的都是常识。</p><blockquote><p>先收藏再看，里面的 docker-compose 是真能直接 <code>docker compose up</code>、跑得起来的最小集——下次起 OTel 接收端就别再现搜配置了。</p></blockquote><h2 id="来源"><a href="#来源" class="headerlink" title="来源"></a>来源</h2><ul><li>Claude Code 监控配置（含全部 env vars 列表 + metric 名）：在 Anthropic docs 搜「monitoring usage」，路径 <code>code.claude.com/docs/en/monitoring-usage</code></li><li>Prometheus OTLP receiver 文档：搜「prometheus opentelemetry」，主页 <code>prometheus.io/docs/guides/opentelemetry/</code>，关键 flag <code>--web.enable-otlp-receiver</code></li><li>官方监控示例仓库（含 Docker Compose + Grafana dashboard）：GitHub 搜「anthropics claude-code-monitoring-guide」</li><li>Prometheus 仓库：GitHub 搜「prometheus prometheus」（64k stars，2026-06-05 push）</li><li>Grafana 仓库：GitHub 搜「grafana grafana」（74k stars，2026-06-05 push）</li><li>OTel Collector contrib（如果你需要 Path A 加中间件）：GitHub 搜「open-telemetry opentelemetry-collector-contrib」</li></ul>]]></content>
      
      
      <categories>
          
          <category> AI-生态 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Claude Code </tag>
            
            <tag> OpenTelemetry </tag>
            
            <tag> Prometheus </tag>
            
            <tag> Grafana </tag>
            
            <tag> 可观测性 </tag>
            
            <tag> 监控运维 </tag>
            
            <tag> 实战教程 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>hermes-web-ui 使用教程</title>
      <link href="/2026/06/04/hermes-web-ui-shi-yong-jiao-cheng/"/>
      <url>/2026/06/04/hermes-web-ui-shi-yong-jiao-cheng/</url>
      
        <content type="html"><![CDATA[<h1 id="hermes-web-ui-使用教程"><a href="#hermes-web-ui-使用教程" class="headerlink" title="hermes-web-ui 使用教程"></a>hermes-web-ui 使用教程</h1><p><img src="/medias/loading.gif" data-original="/images/posts/1_317f5e4a392d2718f6afd27f528610fa.jpg" alt="封面图"></p><p>hermes-web-ui 是 Hermes Agent 的全功能 Web 管理面板，可以通过简洁、响应式的 Web 界面管理 AI 对话会话、监控使用情况与成本、配置平台渠道、安排定时任务、浏览技能——所有操作都在一个干净的界面中完成。</p><p><img src="/medias/loading.gif" data-original="/images/posts/1_12e83cfbabee0f43e534ed629a76d295.png" alt="Hermes Web UI 主界面"></p><p><img src="/medias/loading.gif" data-original="/images/posts/1_f2ec8dde085e879210630292c43c3b65.png" alt="AI 对话界面"></p><h2 id="什么是-Hermes-Agent"><a href="#什么是-Hermes-Agent" class="headerlink" title="什么是 Hermes Agent"></a>什么是 Hermes Agent</h2><p>Hermes Agent 是一个多平台 AI 代理，支持 Telegram、Discord、Slack、WhatsApp 等多个平台。hermes-web-ui 为其提供了可视化的管理界面，无需命令行即可完成大多数配置操作。</p><p><strong>项目信息：</strong></p><ul><li>GitHub: <a href="https://github.com/EKKOLearnAI/hermes-studio">EKKOLearnAI/hermes-studio</a></li><li>许可证: BSL-1.1</li></ul><h2 id="快速开始"><a href="#快速开始" class="headerlink" title="快速开始"></a>快速开始</h2><h3 id="方法一：npm-安装（推荐）"><a href="#方法一：npm-安装（推荐）" class="headerlink" title="方法一：npm 安装（推荐）"></a>方法一：npm 安装（推荐）</h3><pre class="language-bash" data-language="bash"><code class="language-bash"># 安装npm install -g hermes-web-ui# 启动hermes-web-ui start</code></pre><p>启动后打开 <a href="http://localhost:8648/">http://localhost:8648</a></p><h3 id="方法二：一键安装脚本（自动检测系统）"><a href="#方法二：一键安装脚本（自动检测系统）" class="headerlink" title="方法二：一键安装脚本（自动检测系统）"></a>方法二：一键安装脚本（自动检测系统）</h3><p>支持 Debian/Ubuntu/macOS，自动安装 Node.js（如缺少）和 hermes-web-ui：</p><pre class="language-bash" data-language="bash"><code class="language-bash">bash &lt;(curl -fsSL https://cdn.jsdelivr.net/gh/EKKOLearnAI/hermes-web-ui@main/scripts/setup.sh)</code></pre><h3 id="方法三：Docker-Compose"><a href="#方法三：Docker-Compose" class="headerlink" title="方法三：Docker Compose"></a>方法三：Docker Compose</h3><pre class="language-bash" data-language="bash"><code class="language-bash"># 使用预构建镜像（推荐）WEBUI_IMAGE=ekkoye8888/hermes-web-ui:latest docker compose up -d hermes-agent hermes-webui# 或从源码构建docker compose up -d --build hermes-agent hermes-webui# 查看日志docker compose logs -f hermes-webui</code></pre><p>打开 <a href="http://localhost:8648/">http://localhost:8648</a></p><p><strong>Docker 数据持久化：</strong></p><ul><li>Hermes 数据存储在 <code>./hermes_data</code></li><li>Web UI 认证 token 存储在 <code>./hermes_data/hermes-web-ui/.token</code></li><li>首次运行启用认证时，token 会打印在容器日志中</li></ul><h3 id="WSL-环境"><a href="#WSL-环境" class="headerlink" title="WSL 环境"></a>WSL 环境</h3><pre class="language-bash" data-language="bash"><code class="language-bash"># 安装bash &lt;(curl -fsSL https://cdn.jsdelivr.net/gh/EKKOLearnAI/hermes-web-ui@main/scripts/setup.sh)# 启动hermes-web-ui start# WSL 会自动检测并使用 hermes gateway run 后台启动hermes gateway run</code></pre><h2 id="主要功能"><a href="#主要功能" class="headerlink" title="主要功能"></a>主要功能</h2><h3 id="AI-对话"><a href="#AI-对话" class="headerlink" title="AI 对话"></a>AI 对话</h3><ul><li><strong>实时流式响应</strong>：通过 SSE 实现，支持异步运行</li><li><strong>多会话管理</strong>：创建、重命名、删除、切换会话</li><li><strong>自建会话数据库</strong>：本地 SQLite 存储，首次启动时自动从 Hermes state.db 同步会话</li><li><strong>会话分组</strong>：按来源（Telegram、Discord、Slack 等）分组，可折叠</li><li><strong>实时指示器</strong>：活跃会话置顶显示，带旋转图标</li><li><strong>Markdown 渲染</strong>：语法高亮 + 代码复制</li><li><strong>工具调用详情</strong>：展开查看参数和结果</li><li><strong>文件上传/下载</strong>：支持本地、Docker、SSH、Singularity 后端</li><li><strong>全局搜索</strong>：Ctrl+K 跨所有对话搜索</li><li><strong>全局模型选择器</strong>：从 <code>~/.hermes/auth.json</code> 凭据池发现模型</li><li><strong>每会话模型标签</strong>：显示当前使用的模型和上下文 Token 用量</li></ul><pre class="language-bash" data-language="bash"><code class="language-bash"># 凭据池位置~/.hermes/auth.json</code></pre><h3 id="平台渠道配置"><a href="#平台渠道配置" class="headerlink" title="平台渠道配置"></a>平台渠道配置</h3><p>统一管理 8 个平台，在一个页面完成所有配置：</p><ul><li><strong>凭据管理</strong>：写入 <code>~/.hermes/.env</code></li><li><strong>渠道行为设置</strong>：写入 <code>~/.hermes/config.yaml</code></li><li><strong>配置变更自动重启网关</strong></li><li><strong>各平台配置状态检测</strong></li></ul><pre class="language-bash" data-language="bash"><code class="language-bash"># 配置文件位置~/.hermes/.env        # 凭据~/.hermes/config.yaml # 渠道行为配置</code></pre><h3 id="使用分析"><a href="#使用分析" class="headerlink" title="使用分析"></a>使用分析</h3><ul><li><strong>Token 使用量分解</strong>：输入 / 输出分别统计</li><li><strong>会话数量与日均</strong></li><li><strong>预估成本追踪 &amp; 缓存命中率</strong></li><li><strong>模型使用分布图</strong></li><li><strong>30 天每日趋势</strong>：柱状图 + 数据表</li></ul><h3 id="定时任务"><a href="#定时任务" class="headerlink" title="定时任务"></a>定时任务</h3><ul><li><strong>完整的 Cron 管理</strong>：创建、编辑、暂停、恢复、删除</li><li><strong>立即触发执行</strong></li><li><strong>Cron 表达式快捷预设</strong></li></ul><h3 id="模型管理"><a href="#模型管理" class="headerlink" title="模型管理"></a>模型管理</h3><ul><li><strong>自动发现模型</strong>：从凭据池 <code>~/.hermes/auth.json</code> 读取</li><li><strong>获取提供商模型列表</strong>：调用 <code>/v1/models</code> 接口</li><li><strong>添加、更新、删除提供商</strong>：预设 &amp; 自定义 OpenAI 兼容接口</li><li><strong>OpenAI Codex &amp; Nous Portal OAuth 登录</strong></li><li><strong>提供商 URL 自动检测</strong>：支持非 v1 版本（如 <code>/v4</code>）</li><li><strong>提供商级别模型分组</strong>：可切换默认模型</li></ul><pre class="language-bash" data-language="bash"><code class="language-bash"># API 版本示例/v1/models  # 标准/v4/models  # 非标准版本自动检测</code></pre><h3 id="多配置文件-网关"><a href="#多配置文件-网关" class="headerlink" title="多配置文件 &amp; 网关"></a>多配置文件 &amp; 网关</h3><ul><li><strong>配置文件管理</strong>：创建、重命名、删除、切换</li><li><strong>克隆或导入</strong>：从归档文件（<code>.tar.gz</code>）导入</li><li><strong>导出备份</strong>：分享或备份配置</li><li><strong>多网关管理</strong>：每个配置文件独立启动、停止、监控网关</li><li><strong>端口冲突自动解决</strong></li><li><strong>配置文件作用域隔离</strong></li></ul><h3 id="文件浏览器"><a href="#文件浏览器" class="headerlink" title="文件浏览器"></a>文件浏览器</h3><ul><li><strong>远程浏览</strong>：支持本地、Docker、SSH、Singularity 后端</li><li><strong>完整文件操作</strong>：上传、下载、重命名、复制、移动、删除</li><li><strong>创建目录</strong></li><li><strong>文件预览</strong>：语法高亮</li></ul><h3 id="群聊"><a href="#群聊" class="headerlink" title="群聊"></a>群聊</h3><ul><li><strong>多代理聊天室</strong>：通过 Socket.IO 实时消息</li><li><strong>@提及路由</strong>：提及代理触发上下文回复</li><li><strong>上下文压缩</strong>：历史超过 Token 阈值自动摘要</li><li><strong>typing 状态和回复进度指示</strong></li><li><strong>房间管理</strong>：创建、删除、邀请码</li><li><strong>代理管理</strong>：添加/移除代理，带独立配置文件</li><li><strong>SQLite 消息持久化</strong></li><li><strong>移动端响应式</strong>：侧边栏可折叠</li></ul><h3 id="技能与记忆"><a href="#技能与记忆" class="headerlink" title="技能与记忆"></a>技能与记忆</h3><ul><li><strong>浏览和搜索已安装技能</strong></li><li><strong>查看技能详情和附加文件</strong></li><li><strong>用户笔记和配置文件管理</strong></li></ul><h3 id="日志"><a href="#日志" class="headerlink" title="日志"></a>日志</h3><ul><li><strong>查看代理 / 网关 / 错误日志</strong></li><li><strong>多维度过滤</strong>：日志级别、文件、关键词</li><li><strong>结构化日志解析</strong>：HTTP 访问日志高亮</li></ul><h3 id="认证"><a href="#认证" class="headerlink" title="认证"></a>认证</h3><ul><li><strong>Token 认证</strong>：首次运行自动生成，或通过 <code>AUTH_TOKEN</code> 环境变量设置</li><li><strong>可选用户名/密码登录</strong>：首次 Token 认证后通过设置页面设置</li><li><strong>禁用认证</strong>：<code>AUTH_DISABLED=1</code></li></ul><pre class="language-bash" data-language="bash"><code class="language-bash"># 环境变量AUTH_TOKEN=your-token    # 设置认证 tokenAUTH_DISABLED=1         # 禁用认证（不推荐）</code></pre><h3 id="设置"><a href="#设置" class="headerlink" title="设置"></a>设置</h3><ul><li><strong>显示</strong>：流式输出、紧凑模式、推理、成本显示</li><li><strong>代理</strong>：最大轮次、超时、工具强制</li><li><strong>记忆</strong>：启用/禁用、字符限制</li><li><strong>会话重置</strong>：空闲超时、计划重置</li><li><strong>隐私</strong>：PII 去标识化</li><li><strong>模型设置</strong>：默认模型 &amp; 提供商</li><li><strong>API 服务器配置</strong></li></ul><h3 id="Web-Terminal"><a href="#Web-Terminal" class="headerlink" title="Web Terminal"></a>Web Terminal</h3><ul><li><strong>集成终端</strong>：基于 node-pty 和 @xterm/xterm</li><li><strong>多会话支持</strong>：创建、切换、关闭终端会话</li><li><strong>实时键盘输入</strong>：通过 WebSocket 流式传输 PTY 输出</li><li><strong>窗口大小调整支持</strong></li></ul><h2 id="CLI-命令"><a href="#CLI-命令" class="headerlink" title="CLI 命令"></a>CLI 命令</h2><pre class="language-bash" data-language="bash"><code class="language-bash"># 启动hermes-web-ui start# 指定端口hermes-web-ui start --port 9000# 停止hermes-web-ui stop# 重启hermes-web-ui restart# 查看状态hermes-web-ui status# 更新hermes-web-ui update# 版本hermes-web-ui -v# 帮助hermes-web-ui -h</code></pre><h2 id="自动配置"><a href="#自动配置" class="headerlink" title="自动配置"></a>自动配置</h2><p>启动时 BFF 服务器自动执行：</p><ol><li>验证 <code>~/.hermes/config.yaml</code> 并填充缺失的 <code>api_server</code> 字段</li><li>如配置被修改，备份原文件为 <code>config.yaml.bak</code></li><li>检测并启动网关（如需要）</li><li>解决端口冲突（终止占用进程）</li><li>启动成功后自动打开浏览器</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash"># 配置文件位置~/.hermes/config.yaml# 备份文件（如有修改）config.yaml.bak</code></pre><h2 id="开发指南"><a href="#开发指南" class="headerlink" title="开发指南"></a>开发指南</h2><pre class="language-bash" data-language="bash"><code class="language-bash"># 克隆源码git clone https://github.com/EKKOLearnAI/hermes-studio.gitcd hermes-studio# 安装依赖npm install# 开发模式npm run dev</code></pre><ul><li>前端开发服务器：<a href="http://localhost:5173/">http://localhost:5173</a></li><li>BFF 服务器：<a href="http://localhost:8648（代理到">http://localhost:8648（代理到</a> Hermes 的 8642 端口）</li></ul><pre class="language-bash" data-language="bash"><code class="language-bash"># 生产构建npm run build   # 输出到 dist/</code></pre><h2 id="技术架构"><a href="#技术架构" class="headerlink" title="技术架构"></a>技术架构</h2><pre class="language-none"><code class="language-none">Browser → BFF (Koa, :8648) → Hermes Gateway (:8642)                ↓           Hermes CLI (sessions, logs, version)                ↓           ~/.hermes/config.yaml  (渠道行为)           ~/.hermes/auth.json    (凭据池)           Tencent iLink API      (微信扫码登录)</code></pre><p><strong>架构说明：</strong></p><ul><li><strong>前端</strong>：Vue 3 + TypeScript + Vite + Naive UI + Pinia + Vue Router + vue-i18n + SCSS + markdown-it + highlight.js</li><li><strong>BFF 层</strong>：Koa 2，处理 API 代理（路径重写）、SSE 流式响应、文件上传下载（多后端：本地/Docker/SSH/Singularity）、会话 CRUD、配置/凭据管理、微信扫码登录、模型发现、技能/记忆管理、日志读取、静态文件服务</li><li><strong>Web Terminal</strong>：node-pty + @xterm/xterm</li></ul><p>前端采用多代理可扩展设计，所有 Hermes 相关代码都命名空间在 <code>hermes/</code> 目录下（API、组件、视图、存储），便于添加新的代理集成。</p><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><h3 id="首次启动需要认证？"><a href="#首次启动需要认证？" class="headerlink" title="首次启动需要认证？"></a>首次启动需要认证？</h3><p>首次运行时会自动生成认证 token，并在终端打印。按提示访问 <a href="http://localhost:8648/">http://localhost:8648</a> 并输入 token 登录。</p><h3 id="Docker-环境下无法使用-hermes-cli？"><a href="#Docker-环境下无法使用-hermes-cli？" class="headerlink" title="Docker 环境下无法使用 hermes cli？"></a>Docker 环境下无法使用 hermes cli？</h3><p>确保 docker-compose.yml 正确配置了 hermes-agent 服务，且网络连接正常。</p><h3 id="端口被占用？"><a href="#端口被占用？" class="headerlink" title="端口被占用？"></a>端口被占用？</h3><p>hermes-web-ui 会自动检测并解决端口冲突，终止占用端口的旧进程。</p><h3 id="如何查看详细日志？"><a href="#如何查看详细日志？" class="headerlink" title="如何查看详细日志？"></a>如何查看详细日志？</h3><p>在 Web UI 的「日志」页面可查看代理、网关、错误日志，支持按级别和关键词过滤。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>hermes-web-ui 为 Hermes Agent 提供了一个功能完整的 Web 管理界面，覆盖了从日常对话管理到高级配置的所有场景。无论是个人使用还是团队协作，都能显著提升体验。</p><p><strong>推荐安装方式：</strong></p><ul><li>本地开发：npm 安装</li><li>生产环境：Docker Compose</li></ul><p>立即体验：</p><pre class="language-bash" data-language="bash"><code class="language-bash">npm install -g hermes-web-ui &amp;&amp; hermes-web-ui start</code></pre>]]></content>
      
      
      <categories>
          
          <category> AI </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Hermes </tag>
            
            <tag> AI </tag>
            
            <tag> Web UI </tag>
            
            <tag> Vue3 </tag>
            
            <tag> Dashboard </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>周末读了一本好书</title>
      <link href="/2026/03/01/moment-2026-03-01-book/"/>
      <url>/2026/03/01/moment-2026-03-01-book/</url>
      
        <content type="html"><![CDATA[<p>周末花了一下午读完了《设计心理学》，诺曼对日常物品的洞察力太强了。好的设计应该是不需要说明书的。</p>]]></content>
      
      
      <categories>
          
          <category> moments </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 读书 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>尝试了新的咖啡豆</title>
      <link href="/2026/02/25/moment-2026-02-25-coffee/"/>
      <url>/2026/02/25/moment-2026-02-25-coffee/</url>
      
        <content type="html"><![CDATA[<p>今天开了一包来自云南的日晒豆，风味很惊喜 – 有明显的蓝莓和巧克力调。国产精品豆越来越棒了。</p>]]></content>
      
      
      <categories>
          
          <category> moments </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 咖啡 </tag>
            
            <tag> 生活 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>终于搞定了 K8s 网络策略</title>
      <link href="/2026/02/20/moment-2026-02-20-k8s/"/>
      <url>/2026/02/20/moment-2026-02-20-k8s/</url>
      
        <content type="html"><![CDATA[<p>折腾了两天的 NetworkPolicy 终于通了。关键是 <code>namespaceSelector</code> 和 <code>podSelector</code> 要同时匹配，不能只写一个。记个笔记以防下次再踩坑。</p>]]></content>
      
      
      <categories>
          
          <category> moments </category>
          
      </categories>
      
      
        <tags>
            
            <tag> k8s </tag>
            
            <tag> 技术 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Claude Code + CC-Switch + Antigravity Tools 组合使用指南</title>
      <link href="/2026/01/03/claude-code-cc-switch-antigravity-tools-zu-he-shi-yong-zhi-nan/"/>
      <url>/2026/01/03/claude-code-cc-switch-antigravity-tools-zu-he-shi-yong-zhi-nan/</url>
      
        <content type="html"><![CDATA[<p><img src="/medias/loading.gif" data-original="https://file.costalong.com/img/20260103224723137.png" alt="Claude Code + CC-Switch + Antigravity Tools 组合示意图"></p><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>结合了 Claude Code (强大的 AI 编程助手)、CC-Switch (假设为上下文/配置快速切换工具) 和 Antigravity Tools</p><table><thead><tr><th>工具</th><th>功能</th><th>作用</th></tr></thead><tbody><tr><td><strong>Claude Code</strong></td><td>Anthropic 官方 CLI 工具</td><td>在终端中与 Claude 交互，进行代码编写、调试、重构等</td></tr><tr><td><strong>CC-Switch</strong></td><td>账号切换管理器</td><td>管理多个 Claude 账号，一键切换，支持使用量统计</td></tr><tr><td><strong>Antigravity Tools</strong></td><td>API 反代 &amp; 账号管理</td><td>提供本地 API 代理服务，支持 OAuth 授权管理多账号</td></tr></tbody></table><h3 id="工作原理"><a href="#工作原理" class="headerlink" title="工作原理"></a>工作原理</h3><pre class="language-none"><code class="language-none">Claude Code CLI  →  Antigravity Tools (本地反代)  →  Claude API                            ↑                     CC-Switch (账号管理)</code></pre><ul><li><strong>Antigravity Tools</strong> 作为本地代理服务器，转发 Claude Code 的请求</li><li><strong>CC-Switch</strong> 可以在 Antigravity 之上提供更便捷的账号切换功能</li><li>三者配合使用，实现多账号管理和无缝切换</li></ul><hr><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><h3 id="1-Claude-Code"><a href="#1-Claude-Code" class="headerlink" title="1. Claude Code"></a>1. Claude Code</h3><p><a href="https://github.com/anthropics/claude-code">GitHub 仓库</a></p><p><strong>前提条件</strong>：</p><ul><li>Node.js 18+</li><li>npm 或其他包管理器</li></ul><pre class="language-bash" data-language="bash"><code class="language-bash">npm install -g @anthropic-ai/claude-code</code></pre><p>安装后验证：</p><pre class="language-bash" data-language="bash"><code class="language-bash">claude --version</code></pre><h3 id="2-CC-Switch"><a href="#2-CC-Switch" class="headerlink" title="2. CC-Switch"></a>2. CC-Switch</h3><p><a href="https://github.com/farion1231/cc-switch">GitHub 仓库</a></p><p><strong>macOS 安装</strong>：</p><pre class="language-bash" data-language="bash"><code class="language-bash">brew tap farion1231/ccswitchbrew install --cask cc-switch</code></pre><p><strong>Linux 安装</strong>：<br>前往 <a href="https://github.com/farion1231/cc-switch/releases">Releases 页面</a> 下载适合的安装包：</p><ul><li><code>.deb</code> - Debian/Ubuntu 系</li><li><code>.rpm</code> - RedHat/Fedora 系</li><li><code>AppImage</code> - 通用格式</li></ul><h3 id="3-Antigravity-Tools"><a href="#3-Antigravity-Tools" class="headerlink" title="3. Antigravity Tools"></a>3. Antigravity Tools</h3><p><a href="https://github.com/lbjlaq/Antigravity-Manager">GitHub 仓库</a></p><p>前往 <a href="https://github.com/lbjlaq/Antigravity-Manager/releases">GitHub Releases</a> 下载对应系统的包：</p><table><thead><tr><th>系统</th><th>格式</th><th>说明</th></tr></thead><tbody><tr><td><strong>macOS</strong></td><td><code>.dmg</code></td><td>支持 Apple Silicon &amp; Intel</td></tr><tr><td><strong>Windows</strong></td><td><code>.msi</code> 或 <code>.zip</code></td><td>安装版或便携版</td></tr><tr><td><strong>Linux</strong></td><td><code>.deb</code> 或 <code>AppImage</code></td><td>根据发行版选择</td></tr></tbody></table><hr><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><h3 id="CC-Switch-配置"><a href="#CC-Switch-配置" class="headerlink" title="CC-Switch 配置"></a>CC-Switch 配置</h3><p><img src="/medias/loading.gif" data-original="https://file.costalong.com/img/20260103205511054.png" alt="CC-Switch 配置界面"></p><p><strong>主要功能</strong>：</p><ul><li>管理多个 Claude 账号</li><li>快速切换当前使用的账号</li><li>查看各账号的使用量统计</li><li>支持快捷键切换</li></ul><h3 id="Antigravity-Tools-配置"><a href="#Antigravity-Tools-配置" class="headerlink" title="Antigravity Tools 配置"></a>Antigravity Tools 配置</h3><p><img src="/medias/loading.gif" data-original="https://file.costalong.com/img/20260103205426774.png" alt="Antigravity Tools 主界面"></p><p><img src="/medias/loading.gif" data-original="https://file.costalong.com/img/20260103205632292.png" alt="Antigravity Tools API 反代设置"></p><p><strong>关键配置项</strong>：</p><ol><li><strong>API 反代端口</strong>：默认 <code>8045</code>，可自定义</li><li><strong>账号管理</strong>：通过 OAuth 添加多个 Claude 账号</li><li><strong>负载均衡</strong>：可配置多账号轮询或指定账号</li></ol><hr><h2 id="快速接入"><a href="#快速接入" class="headerlink" title="快速接入"></a>快速接入</h2><h3 id="步骤一：添加-Claude-账号（OAuth-授权）"><a href="#步骤一：添加-Claude-账号（OAuth-授权）" class="headerlink" title="步骤一：添加 Claude 账号（OAuth 授权）"></a>步骤一：添加 Claude 账号（OAuth 授权）</h3><ol><li>打开 Antigravity Tools → “账号 / Accounts” → “添加账号” → “OAuth”</li><li>点击生成的授权链接，链接会自动复制到剪贴板</li><li>在浏览器中打开链接，完成 Claude 授权</li><li>授权成功后浏览器显示 “✅ 授权成功!”</li><li>应用自动完成后续流程；如未自动完成，点击”我已授权，继续”</li></ol><blockquote><p><strong>注意</strong>：授权链接包含一次性回调端口，请使用弹窗里生成的最新链接。如果授权时应用未运行，浏览器可能会提示 <code>localhost refused connection</code>。</p></blockquote><h3 id="步骤二：启动-API-反代服务"><a href="#步骤二：启动-API-反代服务" class="headerlink" title="步骤二：启动 API 反代服务"></a>步骤二：启动 API 反代服务</h3><ol><li>在 Antigravity Tools 中打开 “API 反代” 页面</li><li>点击 “开启服务”</li><li>确认服务状态为 “运行中”</li></ol><h3 id="步骤三：配置-Claude-Code-环境变量"><a href="#步骤三：配置-Claude-Code-环境变量" class="headerlink" title="步骤三：配置 Claude Code 环境变量"></a>步骤三：配置 Claude Code 环境变量</h3><p>在终端中设置环境变量：</p><pre class="language-bash" data-language="bash"><code class="language-bash">export ANTHROPIC_API_KEY="sk-antigravity"export ANTHROPIC_BASE_URL="http://127.0.0.1:8045"</code></pre><p><strong>持久化配置</strong>（添加到 shell 配置文件）：</p><p><strong>Zsh</strong> (~/.zshrc)：</p><pre class="language-bash" data-language="bash"><code class="language-bash">echo 'export ANTHROPIC_API_KEY="sk-antigravity"' &gt;&gt; ~/.zshrcecho 'export ANTHROPIC_BASE_URL="http://127.0.0.1:8045"' &gt;&gt; ~/.zshrcsource ~/.zshrc</code></pre><p><strong>Bash</strong> (~/.bashrc)：</p><pre class="language-bash" data-language="bash"><code class="language-bash">echo 'export ANTHROPIC_API_KEY="sk-antigravity"' &gt;&gt; ~/.bashrcecho 'export ANTHROPIC_BASE_URL="http://127.0.0.1:8045"' &gt;&gt; ~/.bashrcsource ~/.bashrc</code></pre><h3 id="步骤四：启动-Claude-Code"><a href="#步骤四：启动-Claude-Code" class="headerlink" title="步骤四：启动 Claude Code"></a>步骤四：启动 Claude Code</h3><pre class="language-bash" data-language="bash"><code class="language-bash">claude</code></pre><p>现在 Claude Code 会通过 Antigravity Tools 的本地代理服务连接到 Claude API。</p><hr><h2 id="工作流程示例"><a href="#工作流程示例" class="headerlink" title="工作流程示例"></a>工作流程示例</h2><h3 id="日常使用流程"><a href="#日常使用流程" class="headerlink" title="日常使用流程"></a>日常使用流程</h3><pre class="language-none"><code class="language-none">1. 启动 Antigravity Tools → 开启 API 反代2. (可选) 使用 CC-Switch 选择要使用的账号3. 打开终端 → 运行 claude4. 开始编程！</code></pre><h3 id="切换账号"><a href="#切换账号" class="headerlink" title="切换账号"></a>切换账号</h3><p><strong>使用 CC-Switch</strong>：</p><ul><li>点击托盘图标，选择要切换的账号</li><li>或使用配置的快捷键快速切换</li></ul><p><strong>使用 Antigravity Tools</strong>：</p><ul><li>在账号管理页面选择默认账号</li><li>或配置负载均衡策略</li></ul><hr><h2 id="常见问题排查"><a href="#常见问题排查" class="headerlink" title="常见问题排查"></a>常见问题排查</h2><h3 id="macOS-提示”应用已损坏，无法打开”"><a href="#macOS-提示”应用已损坏，无法打开”" class="headerlink" title="macOS 提示”应用已损坏，无法打开”"></a>macOS 提示”应用已损坏，无法打开”</h3><p>由于 macOS 的安全机制，非 App Store 下载的应用可能触发此提示。</p><p><strong>解决方法 1 - 命令行修复</strong> (推荐)：</p><pre class="language-bash" data-language="bash"><code class="language-bash">sudo xattr -rd com.apple.quarantine "/Applications/Antigravity Tools.app"</code></pre><p><strong>解决方法 2 - Homebrew 安装时规避</strong>：</p><pre class="language-bash" data-language="bash"><code class="language-bash">brew install --cask --no-quarantine antigravity-tools</code></pre><h3 id="Claude-Code-连接失败"><a href="#Claude-Code-连接失败" class="headerlink" title="Claude Code 连接失败"></a>Claude Code 连接失败</h3><ol><li><strong>检查 Antigravity 服务状态</strong>：确认 API 反代服务已启动</li><li><strong>检查端口</strong>：确认 <code>ANTHROPIC_BASE_URL</code> 端口与 Antigravity 设置一致</li><li><strong>检查环境变量</strong>：运行 <code>echo $ANTHROPIC_BASE_URL</code> 确认已设置</li><li><strong>检查账号</strong>：确认至少有一个有效的 Claude 账号已授权</li></ol><h3 id="环境变量未生效"><a href="#环境变量未生效" class="headerlink" title="环境变量未生效"></a>环境变量未生效</h3><ul><li>确认添加到正确的 shell 配置文件（<code>.zshrc</code> 或 <code>.bashrc</code>）</li><li>运行 <code>source ~/.zshrc</code> 或重新打开终端</li><li>使用 <code>env | grep ANTHROPIC</code> 验证设置</li></ul><hr><h2 id="使用技巧"><a href="#使用技巧" class="headerlink" title="使用技巧"></a>使用技巧</h2><h3 id="1-开机自启"><a href="#1-开机自启" class="headerlink" title="1. 开机自启"></a>1. 开机自启</h3><ul><li><strong>Antigravity Tools</strong>：在设置中开启”开机自启动”</li><li><strong>CC-Switch</strong>：在设置中开启”登录时启动”</li></ul><h3 id="2-多账号负载均衡"><a href="#2-多账号负载均衡" class="headerlink" title="2. 多账号负载均衡"></a>2. 多账号负载均衡</h3><p>在 Antigravity Tools 中添加多个账号后，可配置：</p><ul><li><strong>轮询模式</strong>：请求自动分配到不同账号</li><li><strong>指定模式</strong>：手动选择使用的账号</li></ul><h3 id="3-快捷操作"><a href="#3-快捷操作" class="headerlink" title="3. 快捷操作"></a>3. 快捷操作</h3><ul><li>使用 CC-Switch 的托盘图标快速切换账号</li><li>配置全局快捷键实现一键切换</li></ul><h3 id="4-使用量监控"><a href="#4-使用量监控" class="headerlink" title="4. 使用量监控"></a>4. 使用量监控</h3><p>通过 CC-Switch 或 Antigravity Tools 查看：</p><ul><li>各账号的 API 调用次数</li><li>Token 使用量统计</li><li>剩余配额</li></ul><hr><h2 id="相关链接"><a href="#相关链接" class="headerlink" title="相关链接"></a>相关链接</h2><ul><li><a href="https://docs.anthropic.com/en/docs/claude-code">Claude Code 官方文档</a></li><li><a href="https://github.com/farion1231/cc-switch">CC-Switch GitHub</a></li><li><a href="https://github.com/lbjlaq/Antigravity-Manager">Antigravity Tools GitHub</a></li><li><a href="https://docs.anthropic.com/en/api">Anthropic API 文档</a></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> ai </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>新年第一天</title>
      <link href="/2025/12/31/moment-2026-01-01-newyear/"/>
      <url>/2025/12/31/moment-2026-01-01-newyear/</url>
      
        <content type="html"><![CDATA[<p>2026，新的开始。今年的目标：多写博客、坚持运动、学一门新语言。</p>]]></content>
      
      
      <categories>
          
          <category> moments </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Git Commit</title>
      <link href="/2024/11/19/git/git-commit/"/>
      <url>/2024/11/19/git/git-commit/</url>
      
        <content type="html"><![CDATA[<h2 id="Git-Commit-介绍"><a href="#Git-Commit-介绍" class="headerlink" title="Git Commit 介绍"></a><strong>Git Commit 介绍</strong></h2><p><code>git commit</code> 命令用于将工作区或暂存区的更改提交到版本库，形成可追踪的历史记录点。<br>它是 Git 工作流的核心操作，常用来保存代码的进展或完成阶段性开发。</p><hr><h2 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a><strong>基本用法</strong></h2><pre class="language-bash" data-language="bash"><code class="language-bash"># 将暂存区内容提交到版本库，进入编辑器填写提交信息git commit# 将特定文件提交到版本库（需已被跟踪）git commit [file1] [file2] [...]# 将暂存区内容提交到版本库，同时直接添加提交信息（无需打开编辑器）git commit -m "&lt;message&gt;"# 跳过 git add，将所有已跟踪文件的更改提交到版本库git commit -am "&lt;message&gt;"# 替代上一次提交：# - 若代码无更改，用于修改提交信息# - 若代码有更改，将其合并到上一次提交git commit --amend -m "&lt;message&gt;"</code></pre><hr><h2 id="常用选项"><a href="#常用选项" class="headerlink" title="常用选项"></a><strong>常用选项</strong></h2><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-m &lt;message&gt;</code></td><td>指定提交信息，避免打开编辑器</td></tr><tr><td><code>-a</code></td><td>提交所有已修改或删除的文件（限已被 Git 跟踪的文件）</td></tr><tr><td><code>--amend</code></td><td>修改最近一次提交（包括内容或提交信息）</td></tr><tr><td><code>--no-edit</code></td><td>不修改提交信息，直接沿用上次的提交消息</td></tr></tbody></table><hr><h2 id="修改历史提交"><a href="#修改历史提交" class="headerlink" title="修改历史提交"></a><strong>修改历史提交</strong></h2><h3 id="修改最近一次提交"><a href="#修改最近一次提交" class="headerlink" title="修改最近一次提交"></a><strong>修改最近一次提交</strong></h3><p>如果需要修改最近一次提交的内容或提交信息：</p><ol><li>编辑文件，完成更改后添加到暂存区：<pre class="language-bash" data-language="bash"><code class="language-bash">git add &lt;file-name&gt;</code></pre></li><li>使用 <code>--amend</code> 更新提交：<pre class="language-bash" data-language="bash"><code class="language-bash">git commit --amend</code></pre></li></ol><h3 id="修改更早的提交"><a href="#修改更早的提交" class="headerlink" title="修改更早的提交"></a><strong>修改更早的提交</strong></h3><p>通过交互式变基（<code>git rebase -i</code>）修改历史提交：</p><ol><li><p>启动交互式变基<br>例如，修改最近 3 次提交：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git rebase -i HEAD~3</code></pre></li><li><p>编辑器中显示提交历史：</p><pre class="language-none"><code class="language-none">pick 1234567 feat: 添加新功能pick 89abcde fix: 修复启动问题pick def7890 docs: 更新文档</code></pre></li><li><p>将需要修改的提交改为 <code>edit</code>：</p><pre class="language-none"><code class="language-none">pick 1234567 feat: 添加新功能edit 89abcde fix: 修复启动问题pick def7890 docs: 更新文档</code></pre></li><li><p>保存并退出。</p></li><li><p>修改文件并更新提交：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git add &lt;file-name&gt;git commit --amend</code></pre></li><li><p>完成修改后继续变基：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git rebase --continue</code></pre></li></ol><hr><h2 id="最佳实践"><a href="#最佳实践" class="headerlink" title="最佳实践"></a><strong>最佳实践</strong></h2><ol><li><p><strong>编写清晰的提交信息</strong><br>遵循 <a href="https://www.conventionalcommits.org/">Conventional Commits</a> 格式：</p><pre class="language-none"><code class="language-none">&lt;type&gt;(&lt;scope&gt;): &lt;subject&gt;&lt;空行&gt;&lt;body&gt;&lt;空行&gt;&lt;footer&gt;</code></pre><p><strong>示例：</strong></p><pre class="language-none"><code class="language-none">feat(app): 添加用户登录功能实现用户登录和身份验证。解决了之前登录状态丢失的问题。Closes #123</code></pre></li><li><p><strong>保持提交单一职责</strong><br>每次提交只包含一种逻辑更改（例如，修复一个 bug 或实现一个功能），避免混合提交。</p></li><li><p><strong>修改已推送提交时的注意事项</strong></p><ul><li>若已推送到远程分支，需使用 <code>git push --force</code> 强制覆盖。</li><li>与团队协作时应谨慎，确保其他人未基于该提交工作。</li></ul></li></ol><hr><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a><strong>总结</strong></h2><ul><li><strong><code>git commit</code> 是 Git 的核心命令</strong>，通过合适的参数选项可以更高效地管理提交。</li><li>选择适合的用法（如 <code>--amend</code> 或 <code>rebase</code>）来优化历史提交，同时注意团队协作中的代码一致性。</li></ul>]]></content>
      
      
      <categories>
          
          <category> Tool </category>
          
      </categories>
      
      
        <tags>
            
            <tag> git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>git 回滚</title>
      <link href="/2024/11/19/git/git-rollback/"/>
      <url>/2024/11/19/git/git-rollback/</url>
      
        <content type="html"><![CDATA[<p>在 Git 中，<code>revert</code>、<code>reset</code> 和 <code>rebase</code> 都是用来修改提交记录或状态的，但它们的使用场景和效果各不相同。</p><hr><h2 id="恢复提交：git-revert"><a href="#恢复提交：git-revert" class="headerlink" title="恢复提交：git revert"></a><strong>恢复提交：<code>git revert</code></strong></h2><p><code>git revert</code> 命令的实际结果类似于 <code>reset</code>，但它的方法不同。<code>reset</code> 是通过移动分支指针撤销更改，而 <code>revert</code> 是通过添加一个新的提交取消更改。</p><h3 id="用法"><a href="#用法" class="headerlink" title="用法"></a><strong>用法</strong></h3><pre class="language-bash" data-language="bash"><code class="language-bash">git revert &lt;commit-id&gt;</code></pre><p>例如，撤销最近一次提交：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git revert HEAD</code></pre><h3 id="特点"><a href="#特点" class="headerlink" title="特点"></a><strong>特点</strong></h3><ul><li>安全，不会修改历史记录。</li><li>适合用于团队协作和已推送到远程的分支。</li></ul><hr><h2 id="回退提交：git-reset"><a href="#回退提交：git-reset" class="headerlink" title="回退提交：git reset"></a><strong>回退提交：<code>git reset</code></strong></h2><p><code>git reset</code> 用于改变分支指针，并决定是否保留工作区和暂存区的更改。</p><h3 id="步骤-1：查看提交历史"><a href="#步骤-1：查看提交历史" class="headerlink" title="步骤 1：查看提交历史"></a><strong>步骤 1：查看提交历史</strong></h3><p>使用 <code>git log</code> 查询需要退回的提交 ID：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git log</code></pre><h3 id="步骤-2：选择回退模式"><a href="#步骤-2：选择回退模式" class="headerlink" title="步骤 2：选择回退模式"></a><strong>步骤 2：选择回退模式</strong></h3><ol><li><p><strong>软回退 (<code>--soft</code>)</strong><br>仅回退分支指针，保留暂存区和工作区的更改。</p><pre class="language-bash" data-language="bash"><code class="language-bash">git reset --soft &lt;commit-id&gt;</code></pre></li><li><p><strong>混合回退 (<code>--mixed</code>)</strong><br>回退分支指针，清除暂存区，保留工作区的更改。（这是默认模式）</p><pre class="language-bash" data-language="bash"><code class="language-bash">git reset --mixed &lt;commit-id&gt;</code></pre></li><li><p><strong>硬回退 (<code>--hard</code>)</strong><br>回退分支指针，同时清除暂存区和工作区的所有更改。</p><pre class="language-bash" data-language="bash"><code class="language-bash">git reset --hard &lt;commit-id&gt;</code></pre></li></ol><h3 id="推送到远程"><a href="#推送到远程" class="headerlink" title="推送到远程"></a><strong>推送到远程</strong></h3><p>如果需要将本地的变更覆盖远程分支：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git push --force</code></pre><hr><h2 id="变基：git-rebase"><a href="#变基：git-rebase" class="headerlink" title="变基：git rebase"></a><strong>变基：<code>git rebase</code></strong></h2><p><code>git rebase</code> 用于重新整理提交历史，将当前分支的更改重新应用到另一个基础提交上。</p><h3 id="场景：分支变基"><a href="#场景：分支变基" class="headerlink" title="场景：分支变基"></a><strong>场景：分支变基</strong></h3><p>假设有以下分支：</p><ul><li><strong>master</strong>: <code>C4 -&gt; C2 -&gt; C1 -&gt; C0</code></li><li><strong>feature</strong>: <code>C5 -&gt; C3 -&gt; C2 -&gt; C1 -&gt; C0</code></li></ul><p>要将 <code>feature</code> 分支的提交重新基于 <code>master</code> 分支：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git checkout featuregit rebase master</code></pre><p>变基后的结果：</p><ul><li><strong>master</strong>: <code>C4 -&gt; C2 -&gt; C1 -&gt; C0</code></li><li><strong>feature</strong>: <code>C5' -&gt; C3' -&gt; C4 -&gt; C2 -&gt; C1 -&gt; C0</code></li></ul><h3 id="交互式变基"><a href="#交互式变基" class="headerlink" title="交互式变基"></a><strong>交互式变基</strong></h3><p>可以通过 <code>-i</code> 参数合并、修改或删除提交：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git rebase -i &lt;base-branch&gt;</code></pre><p>编辑器中提供以下选项：</p><ul><li><code>pick</code>: 保留提交。</li><li><code>squash</code>: 合并当前提交到上一个提交。</li><li><code>edit</code>: 修改提交内容。</li></ul><hr><h2 id="恢复历史：git-reflog"><a href="#恢复历史：git-reflog" class="headerlink" title="恢复历史：git reflog"></a><strong>恢复历史：<code>git reflog</code></strong></h2><p><code>git reflog</code> 用于查看分支操作历史。它记录了所有分支指针的变动，帮助找回丢失的提交。</p><h3 id="用法-1"><a href="#用法-1" class="headerlink" title="用法"></a><strong>用法</strong></h3><pre class="language-bash" data-language="bash"><code class="language-bash">git reflog</code></pre><p>示例输出：</p><pre class="language-none"><code class="language-none">e309ee8 (HEAD -&gt; master) HEAD@{0}: pull: Fast-forward30c0e5e HEAD@{1}: commit: fix: 优化代码9429f03 HEAD@{2}: commit: fix: 优化代码1bdcafe HEAD@{3}: commit: fix: 修改配置</code></pre><p>如果误操作（如 <code>git reset --hard</code>），可以通过 <code>git reflog</code> 找到目标提交并恢复：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git reset --hard &lt;commit-id&gt;</code></pre><hr><h2 id="对比总结"><a href="#对比总结" class="headerlink" title="对比总结"></a><strong>对比总结</strong></h2><table><thead><tr><th>功能</th><th><code>git revert</code></th><th><code>git reset</code></th><th><code>git rebase</code></th></tr></thead><tbody><tr><td><strong>作用</strong></td><td>撤销提交，创建新提交</td><td>改变提交历史</td><td>重组提交历史</td></tr><tr><td><strong>安全性</strong></td><td>安全，适合已推送分支</td><td>高风险，慎用于远程分支</td><td>高风险，慎用于远程分支</td></tr><tr><td><strong>适用场景</strong></td><td>团队协作，回滚指定提交</td><td>修改本地提交</td><td>清理历史、线性合并</td></tr></tbody></table><p>选择合适的工具，确保团队协作无缝进行！</p>]]></content>
      
      
      <categories>
          
          <category> Tool </category>
          
      </categories>
      
      
        <tags>
            
            <tag> git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Gomock进行单元测试</title>
      <link href="/2024/07/16/go/tests/gomock/"/>
      <url>/2024/07/16/go/tests/gomock/</url>
      
        <content type="html"><![CDATA[<h2 id="用法"><a href="#用法" class="headerlink" title="用法"></a>用法</h2><p>在 mockgen 命令中,支持两种生成模式：</p><ol><li>source：从源文件生成 mock 接口（通过 -source 启用）</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">mockgen -source=foo.go [other options]</code></pre><ol start="2"><li>reflect：通过使用反射程序来生成 mock 接口.它通过传递两个非标志参数来启用：导入路径和逗号分隔的接口列表</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">mockgen database/sql/driver Conn,Driver</code></pre><p>两种方式生成的 mock 代码并没有什么区别.因此选择合适的就可以了</p><h2 id="写测试用例"><a href="#写测试用例" class="headerlink" title="写测试用例"></a>写测试用例</h2><p><strong>1. 步骤:</strong></p><ol><li>想清楚整体逻辑</li><li>定义想要（模拟）依赖项的 interface（接口）</li><li>使用 mockgen 命令对所需 mock 的 interface 生成 mock 文件</li><li>编写单元测试的逻辑,在测试中使用 mock</li><li>进行单元测试的验证</li></ol><p><strong>2. 目录：</strong></p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">├── mock├── person│   └── male.go└── user    ├── user.go    └── user_test.go</code></pre><p><strong>2. 编写：</strong></p><ol><li><p>定义 interface </p><p> 打开 person/male.go 文件,写入以下内容：</p></li></ol><pre class="language-go" data-language="go"><code class="language-go">package persontype Male interface {Get(id int64) error}</code></pre><ol start="2"><li><p>调用方法</p><p>打开 user/user.go 文件,写入以下内容</p></li></ol><pre class="language-go" data-language="go"><code class="language-go">package userimport "github.com/EDDYCJY/mockd/person"type User struct {Person person.Male}func NewUser(p person.Male) *User {return &amp;User{Person: p}}func (u *User) GetUserInfo(id int64) error {return u.Person.Get(id)}</code></pre><ol start="3"><li>生成 mock 文件</li></ol><p>回到 mockd/ 的根目录下,执行以下命令</p><pre class="language-bash" data-language="bash"><code class="language-bash">$ mockgen -source=./person/male.go -destination=./mock/male_mock.go -package=mock</code></pre><p>在执行完毕后,可以发现 mock/ 目录下多出了 male_mock.go 文件,这就是 mock 文件.那么命令中的指令又分别有什么用呢？如下：</p><pre><code>-source：设置需要模拟（mock）的接口文件-destination：设置 mock 文件输出的地方,若不设置则打印到标准输出中-package：设置 mock 文件的包名,若不设置则为 mock_ 前缀加上文件名（如本文的包名会为 mock_person）</code></pre><p>想了解更多的指令符,可参见 官方文档: <a href="https://github.com/golang/mock#running-mockgen">https://github.com/golang/mock#running-mockgen</a></p><p><strong>输出的 mock 文件</strong></p><pre class="language-go" data-language="go"><code class="language-go">// Code generated by MockGen. DO NOT EDIT.// Source: ./person/male.go// Package mock is a generated GoMock package.package mockimport (gomock "github.com/golang/mock/gomock"reflect "reflect")// MockMale is a mock of Male interfacetype MockMale struct {ctrl     *gomock.Controllerrecorder *MockMaleMockRecorder}// MockMaleMockRecorder is the mock recorder for MockMaletype MockMaleMockRecorder struct {mock *MockMale}// NewMockMale creates a new mock instancefunc NewMockMale(ctrl *gomock.Controller) *MockMale {mock := &amp;MockMale{ctrl: ctrl}mock.recorder = &amp;MockMaleMockRecorder{mock}return mock}// EXPECT returns an object that allows the caller to indicate expected usefunc (m *MockMale) EXPECT() *MockMaleMockRecorder {return m.recorder}// Get mocks base methodfunc (m *MockMale) Get(id int64) error {ret := m.ctrl.Call(m, "Get", id)ret0, _ := ret[0].(error)return ret0}// Get indicates an expected call of Getfunc (mr *MockMaleMockRecorder) Get(id interface{}) *gomock.Call {return mr.mock.ctrl.RecordCallWithMethodType(mr.mock, "Get", reflect.TypeOf((*MockMale)(nil).Get), id)}</code></pre><p><strong>4. 测试用例</strong></p><p>打开 user/user_test.go 文件,写入以下内容：</p><pre class="language-go" data-language="go"><code class="language-go">package userimport ("testing""github.com/EDDYCJY/mockd/mock""github.com/golang/mock/gomock")func TestUser_GetUserInfo(t *testing.T) {ctl := gomock.NewController(t)defer ctl.Finish()var id int64 = 1mockMale := mock.NewMockMale(ctl)gomock.InOrder(mockMale.EXPECT().Get(id).Return(nil),)user := NewUser(mockMale)err := user.GetUserInfo(id)if err != nil {t.Errorf("user.GetUserInfo err: %v", err)}}</code></pre><ol><li><p>gomock.NewController：返回 gomock.Controller,它代表 mock 生态系统中的顶级控件.定义了 mock 对象的范围,生命周期和期待值.另外它在多个 goroutine 中是安全的</p></li><li><p>mock.NewMockMale：创建一个新的 mock 实例</p></li><li><p>gomock.InOrder：声明给定的调用应按顺序进行（是对 gomock.After 的二次封装）</p></li><li><p>mockMale.EXPECT().Get(id).Return(nil)：这里有三个步骤,EXPECT()返回一个允许调用者设置期望和返回值的对象.Get(id) 是设置入参并调用 mock 实例中的方法.Return(nil) 是设置先前调用的方法出参.简单来说,就是设置入参并调用,最后设置返回值</p></li><li><p>NewUser(mockMale)：创建 User 实例,值得注意的是,在这里注入了 mock 对象,因此实际在随后的 user.GetUserInfo(id) 调用（入参：id 为 1）中.它调用的是我们事先模拟好的 mock 方法</p></li><li><p>ctl.Finish()：进行 mock 用例的期望值断言,一般会使用 defer 延迟执行,以防止我们忘记这一操作</p></li></ol><h2 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h2><p>回到 mockd/ 的根目录下,执行以下命令</p><pre class="language-bash" data-language="bash"><code class="language-bash">$ go test ./user</code></pre><p>看到这样的结果,就大功告成啦！您可以自己调整一下 Return() 的返回值,以此得到不一样的测试结果哦 </p><ol><li>测试覆盖率</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">go test -cover ./user</code></pre><p>可通过设置 -cover 标志符来开启覆盖率的统计,展示内容为 coverage: 100.0%.</p><ol start="2"><li>可视化界面</li></ol><p>生成测试覆盖率的 profile 文件</p><pre class="language-bash" data-language="bash"><code class="language-bash">go test ./... -coverprofile=cover.out</code></pre><p>利用 profile 文件生成可视化界面</p><pre class="language-bash" data-language="bash"><code class="language-bash">go tool cover -html=cover.out</code></pre><h2 id="常用-mock-方法"><a href="#常用-mock-方法" class="headerlink" title="常用 mock 方法"></a>常用 mock 方法</h2><ol><li><p>调用方法<br> Call.Do()：声明在匹配时要运行的操作</p><p> Call.DoAndReturn()：声明在匹配调用时要运行的操作,并且模拟返回该函数的返回值</p><p> Call.MaxTimes()：设置最大的调用次数为 n 次</p><p> Call.MinTimes()：设置最小的调用次数为 n 次</p><p> Call.AnyTimes()：允许调用次数为 0 次或更多次</p><p> Call.Times()：设置调用次数为 n 次</p></li><li><p>参数匹配</p><p> gomock.Any()：匹配任意值</p><p> gomock.Eq()：通过反射匹配到指定的类型值,而不需要手动设置<br> gomock.Nil()：返回 nil</p></li></ol><h2 id="生成多个-mock-文件"><a href="#生成多个-mock-文件" class="headerlink" title="生成多个 mock 文件"></a>生成多个 mock 文件</h2><p> 可能会想一条条命令生成 mock 文件,岂不得崩溃？</p><p>当然,官方提供了更方便的方式,我们可以利用 go:generate 来完成批量处理的功能</p><pre class="language-bash" data-language="bash"><code class="language-bash">go generate [-run regexp] [-n] [-v] [-x] [build flags] [file.go... | packages]</code></pre><ol><li>修改 interface 方法</li></ol><p>打开 person/male.go 文件,修改为以下内容：</p><pre class="language-go" data-language="go"><code class="language-go">package person//go:generate mockgen -destination=../mock/male_mock.go -package=mock github.com/EDDYCJY/mockd/person Maletype Male interface {Get(id int64) error}</code></pre><p>关注到 go:generate 这条语句,可分为以下部分：</p><ol><li>声明 //go:generate （注意不要留空格）</li><li>使用 mockgen 命令</li><li>定义 -destination</li><li>定义 -package</li><li>定义 source,此处为 person 的包路径</li><li>定义 interfaces,此处为 Male</li></ol><p> <strong>注意:</strong> 上面注释中的mockgen命令因为在调用时其当前目录是person，所以在-destination参数中我们需要指定../mocks/作为mock生成代码的输出目录。</p><p>有了上面的go:generate注释，我们要生成所有mock只需在项目的根目录下运行下面这条命令即可，是不是非常方便？</p><pre class="language-bash" data-language="bash"><code class="language-bash">go generate ./...</code></pre><p><strong>注意:</strong> 注释中的//和go:generate之间不能有空格，这样go generate才可以把注释当做一条命令来处理。</p><p><strong>总结：</strong><br>在单元测试这一环,gomock 给我们提供了极大的便利.能够 mock 掉许许多多的依赖项,其中还有很多的使用方式和功能.您可以 mark 住后详细阅读下官方文档,记忆会更深刻 </p>]]></content>
      
      
      <categories>
          
          <category> golang </category>
          
      </categories>
      
      
        <tags>
            
            <tag> golang </tag>
            
            <tag> test </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>semaphore 信号量</title>
      <link href="/2024/07/16/go/package/semaphore/"/>
      <url>/2024/07/16/go/package/semaphore/</url>
      
        <content type="html"><![CDATA[<p>源代码:  golang.org/x/sync/semaphore</p><h1 id="semaphore-信号量"><a href="#semaphore-信号量" class="headerlink" title="semaphore  信号量"></a>semaphore  信号量</h1><p>源代码:  golang.org/x/sync/semaphore</p><ol><li>创建</li></ol><pre class="language-go" data-language="go"><code class="language-go">// NewWeighted使用给定的值创建一个新的加权信号量// 并发访问的最大组合权重。func NewWeighted(n int64) *Weighted {   w := &amp;Weighted{size: n}   return w}</code></pre><p>w 结构</p><pre class="language-go" data-language="go"><code class="language-go">// Weighted provides a way to bound concurrent access to a resource.// The callers can request access with a given weight.// NewWeighted使用给定的值创建一个新的加权信号量// 并发访问的最大组合权重。type Weighted struct {    size    int64  //权重总数量    cur     int64 //当前权重数量    mu      sync.Mutex //全局互斥锁    waiters list.List //双向链表,存waiter}</code></pre><p>waiter 结构</p><pre class="language-go" data-language="go"><code class="language-go">type waiter struct {    n     int64 //需要权重数量    ready chan&lt;- struct{} // Closed when semaphore acquired. //通信channel ,无缓冲}</code></pre><p>Acquire 方法:</p><p>阻塞的获取指定权种的资源，如果没有空闲的资源，会进去休眠等待。</p><pre class="language-go" data-language="go"><code class="language-go">// Acquire获取权重为n的信号量，阻塞直到资源可用或ctx完成。// 成功时，返回nil。失败时返回 ctx.Err（）并保持信号量不变。// 如果ctx已经完成，则Acquire仍然可以成功执行而不会阻塞func (s *Weighted) Acquire(ctx context.Context, n int64) error {    s.mu.Lock()        // fast path, 如果有足够的资源，都不考虑ctx.Done的状态，将cur加上n就返回    if s.size-s.cur &gt;= n &amp;&amp; s.waiters.Len() == 0 {      s.cur += n      s.mu.Unlock()      return nil    }          // 如果是不可能完成的任务，请求的资源数大于能提供的最大的资源数    if n &gt; s.size {      s.mu.Unlock()            // 依赖ctx的状态返回，否则一直等待      &lt;-ctx.Done()      return ctx.Err()    }          // 否则就需要把调用者加入到等待队列中        // 创建了一个ready chan,以便被通知唤醒    ready := make(chan struct{})    // 组装waiter    w := waiter{n: n, ready: ready}    // 插入waiters中    elem := s.waiters.PushBack(w)    s.mu.Unlock()          // 等待    select {    case &lt;-ctx.Done(): // context的Done被关闭      err := ctx.Err()      s.mu.Lock()      select {      case &lt;-ready: // 如果被唤醒了，忽略ctx的状态        err = nil      default: 通知waiter        isFront := s.waiters.Front() == elem        s.waiters.Remove(elem)        // 通知其它的waiters,检查是否有足够的资源        if isFront &amp;&amp; s.size &gt; s.cur {          s.notifyWaiters()        }      }      s.mu.Unlock()      return err    case &lt;-ready: // 被唤醒了      return nil    }  }</code></pre><h4 id="TryAcquire"><a href="#TryAcquire" class="headerlink" title="TryAcquire"></a>TryAcquire</h4><p>非阻塞地获取指定权重的资源，如果当前没有空闲资源，会直接返回<code>false</code>。</p><pre class="language-go" data-language="go"><code class="language-go">// TryAcquire获取权重为n的信号量而不阻塞。// 成功时返回true。 失败时，返回false并保持信号量不变。func (s *Weighted) TryAcquire(n int64) bool {s.mu.Lock()success := s.size-s.cur &gt;= n &amp;&amp; s.waiters.Len() == 0if success {s.cur += n}s.mu.Unlock()return success}</code></pre><h4 id="Release-方法"><a href="#Release-方法" class="headerlink" title="Release 方法"></a>Release 方法</h4><p>用于释放指定权重的资源，如果有<code>waiters</code>则尝试去一一唤醒<code>waiter</code>。</p><pre class="language-go" data-language="go"><code class="language-go">// Release释放权值为n的信号量。func (s *Weighted) Release(n int64) {s.mu.Lock()s.cur -= n// cur的范围在[0 - size]if s.cur &lt; 0 {s.mu.Unlock()panic("semaphore: bad release")}s.notifyWaiters()s.mu.Unlock()}func (s *Weighted) notifyWaiters() {// 如果有阻塞的waiters，尝试去进行一一唤醒 // 唤醒的时候，先进先出，避免被资源比较大的waiter被饿死for {next := s.waiters.Front()// 已经没有waiter了if next == nil {break}w := next.Value.(waiter)// waiter需要的资源不足if s.size-s.cur &lt; w.n {// 没有足够的令牌供下一个waiter使用。我们可以继续（尝试// 查找请求较小的waiter），但在负载下可能会导致// 饥饿的大型请求；相反，我们留下所有剩余的waiter阻塞//// 考虑一个用作读写锁的信号量，带有N个令牌，N个reader和一位writer// 每个reader都可以通过Acquire（1）获取读锁。// writer写入可以通过Acquire（N）获得写锁定，但不包括所有的reader。// 如果我们允许读者在队列中前进，writer将会饿死-总是有一个令牌可供每个读者。break}s.cur += w.ns.waiters.Remove(next)close(w.ready)}}</code></pre><p>notifyWaiters 方法</p><p>在<code>Acquire</code>和<code>Release</code>方法中都调用了<code>notifyWaiters</code></p><pre class="language-go" data-language="go"><code class="language-go">func (s *Weighted) notifyWaiters() { for {  // 获取等待调用者队列中的队员  next := s.waiters.Front()  // 没有要通知的调用者了  if next == nil {   break // No more waiters blocked.  }  // 断言出waiter信息  w := next.Value.(waiter)  if s.size-s.cur &lt; w.n {   // 没有足够资源为下一个调用者使用时，继续阻塞该调用者，遵循先进先出的原则，   // 避免需要资源数比较大的waiter被饿死   //   // 考虑一个场景，使用信号量作为读写锁，现有N个令牌，N个reader和一个writer   // 每个reader都可以通过Acquire（1）获取读锁，writer写入可以通过Acquire（N）获得写锁定   // 但不包括所有的reader，如果我们允许reader在队列中前进，writer将会饿死-总是有一个令牌可供每个reader   break  }  // 获取资源  s.cur += w.n  // 从waiter列表中移除  s.waiters.Remove(next)  // 使用channel的close机制唤醒waiter  close(w.ready) }}</code></pre><p>需要注意一个点：唤醒<code>waiter</code>采用先进先出的原则，避免需要资源数比较大的waiter被饿死。</p>]]></content>
      
      
      <categories>
          
          <category> golang </category>
          
      </categories>
      
      
        <tags>
            
            <tag> golang </tag>
            
            <tag> package </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Linux 网络工具 netstat</title>
      <link href="/2024/07/16/linux/netstat/"/>
      <url>/2024/07/16/linux/netstat/</url>
      
        <content type="html"><![CDATA[<h1 id="netstat"><a href="#netstat" class="headerlink" title="netstat"></a>netstat</h1><h2 id="1-netstat-命令"><a href="#1-netstat-命令" class="headerlink" title="1. netstat 命令"></a>1. netstat 命令</h2><p>netstat 命令用于显示各种网络相关信息，如网络连接，路由表，接口状态 (Interface Statistics)，masquerade 连接，多播成员 (Multicast Memberships) 等等。</p><h3 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h3><pre class="language-bash" data-language="bash"><code class="language-bash">netstat [-acCeFghilMnNoprstuvVwx][-A&lt;网络类型&gt;][--ip]</code></pre><h3 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h3><pre class="language-bash" data-language="bash"><code class="language-bash">-a或--all 显示所有连线中的Socket。-A&lt;网络类型&gt;或--&lt;网络类型&gt; 列出该网络类型连线中的相关地址。-c或--continuous 持续列出网络状态。-C或--cache 显示路由器配置的快取信息。-e或--extend 显示网络其他相关信息。-F或--fib 显示路由缓存。-g或--groups 显示多重广播功能群组组员名单。-h或--help 在线帮助。-i或--interfaces 显示网络界面信息表单。-l或--listening 显示监控中的服务器的Socket。-M或--masquerade 显示伪装的网络连线。-n或--numeric 直接使用IP地址，而不通过域名服务器。-N或--netlink或--symbolic 显示网络硬件外围设备的符号连接名称。-o或--timers 显示计时器。-p或--programs 显示正在使用Socket的程序识别码和程序名称。-r或--route 显示Routing Table。-s或--statistics 显示网络工作信息统计表。-t或--tcp 显示TCP传输协议的连线状况。-u或--udp 显示UDP传输协议的连线状况。-v或--verbose 显示指令执行过程。-V或--version 显示版本信息。-w或--raw 显示RAW传输协议的连线状况。-x或--unix 此参数的效果和指定"-A unix"参数相同。--ip或--inet 此参数的效果和指定"-A inet"参数相同。</code></pre><h3 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h3><h4 id="1-显示网络状态"><a href="#1-显示网络状态" class="headerlink" title="1. 显示网络状态"></a>1. 显示网络状态</h4><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -a</code></pre><p>结果：</p><pre class="language-bash" data-language="bash"><code class="language-bash">Proto Recv-Q Send-Q Local Address           Foreign Address         Statetcp        0      0 192.168.122.1:domain    0.0.0.0:*               LISTENtcp        0      0 localhost:ipp           0.0.0.0:*               LISTENtcp        0      0 localhost:35600         0.0.0.0:*               LISTENtcp        0      0 127.0.0.54:domain       0.0.0.0:*               LISTENtcp        0      0 localhost:31080         0.0.0.0:*               LISTENtcp        0      0 localhost:31055         0.0.0.0:*               LISTENtcp        0      0 hellotalk:domain        0.0.0.0:*               LISTENtcp        0      0 127.0.0.53:domain       0.0.0.0:*               LISTENtcp        0      0 172.16.0.112:50006      113.240.72.111:https    TIME_WAITtcp        0      0 10.100.100.164:56010    10.40.2.11:27017        ESTABLISHEDtcp        0      0 10.100.100.164:43846    10.40.2.5:27017         ESTABLISHED</code></pre><p>字段说明：</p><p>1.Proto：传输层协议 TCP 或 UDP</p><p>2.Recv-Q：接收队列</p><p>3.Send-Q：发送队列</p><p>4.Local Address：本地地址</p><p>5.Foreign Address：远程地址</p><p>6.State：状态 LISTEN：侦听，ESTABLISHED：已建立，TIME_WAIT：关闭</p><h4 id="2-显示当前户籍-UDP-连接状况"><a href="#2-显示当前户籍-UDP-连接状况" class="headerlink" title="2. 显示当前户籍 UDP 连接状况"></a>2. 显示当前户籍 UDP 连接状况</h4><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -nu</code></pre><p>结果：</p><pre class="language-bash" data-language="bash"><code class="language-bash">激活Internet连接 (w/o 服务器)Proto Recv-Q Send-Q Local Address           Foreign Address         Stateudp        0      0 127.0.0.1:56871         127.0.1.1:53            ESTABLISHEDudp        0      0 172.16.0.112:42799      106.38.222.156:443      ESTABLISHEDudp        0      0 172.16.0.112:68         172.16.0.1:67           ESTABLISHED</code></pre><h4 id="3-显示当前户籍-TCP-连接状况"><a href="#3-显示当前户籍-TCP-连接状况" class="headerlink" title="3. 显示当前户籍 TCP 连接状况"></a>3. 显示当前户籍 TCP 连接状况</h4><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -nt</code></pre><p>结果：</p><pre class="language-bash" data-language="bash"><code class="language-bash">激活Internet连接 (w/o 服务器)Proto Recv-Q Send-Q Local Address           Foreign Address         Statetcp        0      0 10.100.100.164:56010    10.40.2.11:27017        ESTABLISHEDtcp        0      0 10.100.100.164:43846    10.40.2.5:27017         ESTABLISHEDtcp        0      0 172.16.0.112:60087      43.159.193.150:8002     TIME_WAITtcp        0      0 172.16.0.112:57712      113.240.72.99:443       TIME_WAITtcp        0      0 10.100.100.164:55986    10.40.2.11:27017        ESTABLISHEDtcp        0      0 10.100.100.164:43722    10.40.2.5:27017         ESTABLISHED</code></pre><h4 id="4-显示当前系统中所有正在监听的-TCP-端口、相关的进程以及其状态"><a href="#4-显示当前系统中所有正在监听的-TCP-端口、相关的进程以及其状态" class="headerlink" title="4. 显示当前系统中所有正在监听的 TCP 端口、相关的进程以及其状态"></a>4. 显示当前系统中所有正在监听的 TCP 端口、相关的进程以及其状态</h4><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -ntlp</code></pre><p>结果：</p><pre class="language-bash" data-language="bash"><code class="language-bash">Active Internet connections (only servers)Proto Recv-Q Send-Q Local Address           Foreign Address         State       PID/Program nametcp        0      0 0.0.0.0:80              0.0.0.0:*               LISTEN      1234/nginxtcp        0      0 127.0.0.1:3306          0.0.0.0:*               LISTEN      2345/mysqldtcp6       0      0 :::22                   :::*                    LISTEN      3456/sshd</code></pre><p>字段解析：</p><ol><li><p>Proto：协议</p><ul><li>tcp：表示 TCP 协议。</li><li>tcp6：表示 IPv6 的 TCP 协议。</li></ul></li><li><p>Recv-Q 和 Send-Q：</p><ul><li>Recv-Q：表示接收队列</li><li>Send-Q：表示发送队列</li></ul></li><li><p>Local Address：本地地址</p><ul><li>0.0.0.0:80：本地地址和端口号，0.0.0.0 表示监听所有接口的 IP 地址，80 是端口号。</li><li>127.0.0.1:3306：本地环回地址（localhost）上的端口 3306。</li><li>:::22：IPv6 地址的端口 22。</li></ul></li><li><p>Foreign Address：远程地址</p><ul><li>0.0.0.0:* 和 :::*：表示监听所有外部地址。</li></ul></li><li><p>State：状态</p><ul><li>LISTEN：表示服务器正在监听 TCP 连接请求。</li><li>ESTABLISHED：表示连接已经建立。</li><li>TIME_WAIT：表示主动关闭连接的一方在关闭连接后，等待 2MSL 后依然没有收到对方的 FIN 报文，处于等待关闭状态。</li><li>CLOSE_WAIT：表示被动关闭连接的一方在收到对方的 FIN 报文后，等待应用进程关闭连接。</li><li>LAST_ACK：表示被动关闭连接的一方在发送完 FIN 报文后，等待对方的 ACK 报文，如果收到 ACK 报文，则处于 CLOSED 状态，否则处于 TIME_WAIT 状态。</li><li>CLOSED：表示连接已经关闭。</li><li>SYN_RECV：表示正在等待处理的请求数目。</li><li>SYN_SENT：表示正在等待远程连接请求。</li><li>CLOSING：表示正在等待远程连接关闭请求。</li><li>UNKNOWN：表示未知状态。</li><li>LISTENING：表示正在等待远程连接请求。</li><li>IDLE：表示连接处于空闲状态。</li><li>BOUND：表示正在等待远程连接请求。</li><li>FIN_WAIT1: 套接字已经发出了一个 FIN 请求，表示它已经完成了发送数据，正在等待对方的 ACK 响应。</li><li>FIN_WAIT2: 套接字收到了对 FIN 请求的 ACK 响应，正在等待对方发出 FIN 请求。</li></ul></li><li><p>PID/Program name：进程 ID 和进程名称</p><ul><li>1234/nginx：进程 ID 为 1234 的 nginx 进程正在监听 80 端口。</li><li>2345/mysqld：进程 ID 为 2345 的 mysqld 进程正在监听 3306 端口。</li><li>3456/sshd：进程 ID 为 3456 的 sshd 进程正在监听 22 端口。</li></ul></li></ol><h4 id="5-显示网卡列表"><a href="#5-显示网卡列表" class="headerlink" title="5. 显示网卡列表"></a>5. 显示网卡列表</h4><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -i</code></pre><p>结果：</p><pre class="language-bash" data-language="bash"><code class="language-bash">Iface             MTU    RX-OK RX-ERR RX-DRP RX-OVR    TX-OK TX-ERR TX-DRP TX-OVR Flgbr-717e070443f8  1500        0      0      0 0             0      0      0      0 BMUbr-7f56e5476213  1500        0      0      0 0             0      0      0      0 BMUdocker0          1500        0      0      0 0             0      0      0      0 BMUeno1             1500  3838424      0      0 0       3615697      0      0      0 BMRUlo              65536  1115368      0      0 0       1115368      0      0      0 LRUutun             1400  2667684      0      0 0       2598307      0      0      0 MOPRUvirbr0           1500        0      0      0 0             0      0      0      0 BMU</code></pre><p>字段解析：</p><ol><li>Iface：网卡名称</li><li>MTU：最大传输单元</li><li>RX-OK：接收数据包总数</li><li>RX-ERR：接收数据包错误总数</li><li>RX-DRP：接收数据包丢弃总数</li><li>RX-OVR：接收数据包溢出总数</li><li>TX-OK：发送数据包总数</li><li>TX-ERR：发送数据包错误总数</li><li>TX-DRP：发送数据包丢弃总数</li><li>TX-OVR：发送数据包溢出总数</li><li>Flg：标志</li></ol><ul><li>BMU：广播地址</li><li>BMRU：广播地址和多播地址</li><li>MOPRU：多播地址</li><li>LRU：本地地址</li><li>MPRU：多播地址和本地地址</li><li>OPRU：多播地址和本地地址和广播地址</li><li>ALL：所有地址</li></ul><p>###＃ 6. 显示组播组的关系</p><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -g</code></pre><p>结果：</p><pre class="language-bash" data-language="bash"><code class="language-bash">IPv6/IPv4 Group MembershipsInterface       RefCnt Group--------------- ------ ---------------------lo              1      mdns.mcast.netlo              1      all-systems.mcast.net</code></pre><p>字段解析：</p><ol><li>Interface：网卡名称</li><li>RefCnt：引用计数</li><li>Group：组播组</li></ol><p>###＃ 7. 显示网络统计信息</p><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -s</code></pre><p>结果：</p><pre class="language-bash" data-language="bash"><code class="language-bash">Ip:    Forwarding: 1    7269919 total packets received    0 forwarded    0 incoming packets discarded    7265662 incoming packets delivered    7078118 requests sent out    28 outgoing packets dropped    7895 dropped because of missing route    8416 reassemblies required    4208 packets reassembled ok    6 outgoing packets failed fragmentation    OutTransmits: 7078115Icmp:    2022 ICMP messages received    6 input ICMP message failed    ICMP接收历史        destination unreachable: 2020        echo requests: 2    2176 ICMP messages sent    0 ICMP messages failed    OutRateLimitHost: 253    ICMP发出历史        destination unreachable: 2174        echo replies: 2IcmpMsg:        InType3: 2020        InType8: 2        OutType0: 2        OutType3: 2174Tcp:    202192 active connection openings    10375 passive connection openings    120686 failed connection attempts    3047 connection resets received    77 connections established    6857878 segments received    7083293 segments sent out    16799 segments retransmitted    1119 bad segments received    131681 resets sentUdp:    1456958 packets received    2421 packets to unknown port received    32 packet receive errors    556388 packets sent    0 receive buffer errors    8 send buffer errors    InCsumErrors: 32    IgnoredMulti: 12191UdpLite:TcpExt:    3 ICMP packets dropped because they were out-of-window    30122 TCP sockets finished time wait in fast timer    1 packets rejected in established connections because of timestamp    119999 delayed acks sent    39 delayed acks further delayed because of locked socket    Quick ack mode was activated 34511 times    1065795 packet headers predicted    687289 acknowledgments not containing data payload received    1455607 predicted acknowledgments    TCPSackRecovery: 327    Detected reordering 676 times using SACK    TCPDSACKUndo: 97    87 congestion windows recovered without slow start after partial ack    TCPLostRetransmit: 8875    TCPSackFailures: 8    3 timeouts in loss state    466 fast retransmits    133 retransmits in slow start    TCPTimeouts: 11823    TCPLossProbes: 5420    TCPLossProbeRecovery: 82    TCPSackRecoveryFail: 10    TCPBacklogCoalesce: 2369    TCPDSACKOldSent: 34608    TCPDSACKOfoSent: 51    TCPDSACKRecv: 1827    TCPDSACKOfoRecv: 16    1537 connections reset due to unexpected data    2171 connections reset due to early user close    484 connections aborted due to timeout    10 times unable to send RST due to no memory    TCPSACKDiscard: 2    TCPDSACKIgnoredOld: 7    TCPDSACKIgnoredNoUndo: 815    TCPSackShifted: 26    TCPSackMerged: 342    TCPSackShiftFallback: 1984    IPReversePathFilter: 22    TCPRcvCoalesce: 203651    TCPOFOQueue: 50737    TCPOFOMerge: 54    TCPChallengeACK: 879    TCPSYNChallenge: 1140    TCPSpuriousRtxHostQueues: 18    TCPAutoCorking: 458417    TCPFromZeroWindowAdv: 35    TCPToZeroWindowAdv: 35    TCPWantZeroWindowAdv: 167    TCPSynRetrans: 7312    TCPOrigDataSent: 3398432    TCPHystartTrainDetect: 22    TCPHystartTrainCwnd: 1073    TCPHystartDelayDetect: 42    TCPHystartDelayCwnd: 1831    TCPACKSkippedSeq: 286    TCPACKSkippedChallenge: 265    TCPKeepAlive: 97079    TCPDelivered: 3475194    TCPAckCompressed: 14499    TcpTimeoutRehash: 11471    TCPDSACKRecvSegs: 1841    TCPDSACKIgnoredDubious: 2IpExt:    InNoRoutes: 17    InMcastPkts: 118643    OutMcastPkts: 8410    InBcastPkts: 17704    OutBcastPkts: 1960    InOctets: 3807905209    OutOctets: 1523626052    InMcastOctets: 53391437    OutMcastOctets: 1586713    InBcastOctets: 9013152    OutBcastOctets: 152880    InNoECTPkts: 7512863    InECT0Pkts: 18MPTcpExt:</code></pre><h4 id="显示监听的套接口"><a href="#显示监听的套接口" class="headerlink" title="显示监听的套接口"></a>显示监听的套接口</h4><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -l</code></pre><pre class="language-bash" data-language="bash"><code class="language-bash">激活Internet连接 (仅服务器)Proto Recv-Q Send-Q Local Address           Foreign Address         State      tcp        0      0 hellotalk:domain        0.0.0.0:*               LISTEN     tcp6       0      0 [::]:3100               [::]:*                  LISTEN     udp        0      0 0.0.0.0:45010           0.0.0.0:*                                            udp        0      0 0.0.0.0:mdns            0.0.0.0:*                          udp6       0      0 [::]:49287              [::]:*                                           7          活跃的UNIX域套接字 (仅服务器)Proto RefCnt Flags       Type       State         I-Node   路径unix  2      [ ACC ]     流        LISTENING     1010159  /tmp/.java_pid169241.tmp</code></pre><p>字段解析：</p><ul><li>Proto：协议名，如tcp、udp、unix等</li><li>Recv-Q：接收队列，即收到的但未处理的数据包数</li><li>Send-Q：发送队列，即发送但未确认的数据包数</li><li>Local Address：本地地址</li><li>Foreign Address：远程地址</li><li>State：套接字状态，如LISTEN、ESTABLISHED等</li><li>I-Node：套接字inode号</li><li>路径：unix域套接字路径</li><li>RefCnt：引用计数</li><li>Flags：套接字标志，如S表示套接字是被动打开的，即服务器端，而A表示套接字是主动打开的，即客户端</li><li>Type：套接字类型，如流套接字、数据报套接字等</li><li>路径：unix域套接字路径</li></ul><h4 id="查看路由表"><a href="#查看路由表" class="headerlink" title="查看路由表"></a>查看路由表</h4><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -r</code></pre><pre class="language-bash" data-language="bash"><code class="language-bash">内核 IP 路由表Destination     Gateway         Genmask         Flags   MSS Window  irtt Ifacedefault         172.16.0.1      0.0.0.0         UG        0 0          0 eno1one.one.one.one 0.0.0.0         255.255.255.255 UH        0 0          0 utun10.10.0.0       0.0.0.0         255.255.0.0     U         0 0          0 utun10.40.0.0       0.0.0.0         255.255.0.0     U         0 0          0 utun10.100.100.0    0.0.0.0         255.255.255.0   U         0 0          0 utun30.100.0.0      0.0.0.0         255.252.0.0     U         0 0          0 utunfeilian.hellota 172.16.0.1      255.255.255.255 UGH       0 0          0 eno1link-local      0.0.0.0         255.255.0.0     U         0 0          0 utun172.16.0.0      0.0.0.0         255.255.254.0   U         0 0          0 eno1172.16.6.20     172.16.0.1      255.255.255.255 UGH       0 0          0 eno1172.17.0.0      0.0.0.0         255.255.0.0     U         0 0          0 docker0192.168.49.0    0.0.0.0         255.255.255.0   U         0 0          0 br-717e070443f8192.168.58.0    0.0.0.0         255.255.255.0   U         0 0          0 br-7f56e5476213192.168.122.0   0.0.0.0         255.255.255.0   U         0 0          0 virbr0</code></pre><p>字段解析：</p><ul><li>Destination：目的网络或目的主机</li><li>Gateway：指定用于转发包的网关。</li><li>Genmask：子网掩码</li><li>Flags：路由标志，如U表示路由是活动的，H表示目的地址是一个主机，G表示使用网关</li><li>MSS：最大分段大小</li><li>Window：TCP窗口大小</li><li>irtt：初始RTT</li><li>Iface：路由接口</li></ul><h4 id="只列出监听中的连接"><a href="#只列出监听中的连接" class="headerlink" title="只列出监听中的连接"></a>只列出监听中的连接</h4><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -tnl</code></pre><pre class="language-bash" data-language="bash"><code class="language-bash">激活Internet连接 (仅服务器)Proto Recv-Q Send-Q Local Address           Foreign Address         State      tcp        0      0 192.168.122.1:53        0.0.0.0:*               LISTEN     tcp        0      0 127.0.0.1:631           0.0.0.0:*               LISTEN     tcp        0      0 127.0.0.1:35600         0.0.0.0:*               LISTEN     tcp        0      0 127.0.0.54:53           0.0.0.0:*               LISTEN     tcp        0      0 127.0.0.1:31080         0.0.0.0:*               LISTEN     tcp        0      0 127.0.0.1:31055         0.0.0.0:*               LISTEN     tcp        0      0 127.0.1.1:53            0.0.0.0:*               LISTEN     tcp        0      0 127.0.0.53:53           0.0.0.0:*               LISTEN     tcp6       0      0 :::41499                :::*                    LISTEN     tcp6       0      0 127.0.0.1:52829         :::*                    LISTEN     tcp6       0      0 127.0.0.1:35955         :::*                    LISTEN     tcp6       0      0 127.0.0.1:63343         :::*                    LISTEN     tcp6       0      0 127.0.0.1:63342         :::*                    LISTEN     </code></pre><h4 id="获取进程名、进程号以及用户-ID"><a href="#获取进程名、进程号以及用户-ID" class="headerlink" title="获取进程名、进程号以及用户 ID"></a>获取进程名、进程号以及用户 ID</h4><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -tunlp</code></pre><pre class="language-bash" data-language="bash"><code class="language-bash">Active Internet connections (only servers)Proto Recv-Q Send-Q Local Address           Foreign Address         State       PID/Program nametcp        0      0 127.0.1.1:53            0.0.0.0:*               LISTEN      1144/dnsmasq    tcp        0      0 127.0.0.1:631           0.0.0.0:*               LISTEN      661/cupsd       tcp6       0      0 ::1:631                 :::*                    LISTEN      661/cupsd</code></pre><h4 id="查看进程的拥有者会更有用。使用-ep-选项可以同时查看进程名和用户名"><a href="#查看进程的拥有者会更有用。使用-ep-选项可以同时查看进程名和用户名" class="headerlink" title="查看进程的拥有者会更有用。使用 -ep 选项可以同时查看进程名和用户名"></a>查看进程的拥有者会更有用。使用 -ep 选项可以同时查看进程名和用户名</h4><pre class="language-bash" data-language="bash"><code class="language-bash">netstat -tunlep</code></pre><pre class="language-bash" data-language="bash"><code class="language-bash">Proto Recv-Q Send-Q Local Address           Foreign Address         State       User       Inode      PID/Program name    tcp        0      0 192.168.122.1:domain    0.0.0.0:*               LISTEN      root       14887      1567/dnsmasq        tcp        0      0 localhost:ipp           0.0.0.0:*               LISTEN      root       1946474    310640/cupsd     </code></pre><p>参考：<br><a href="https://docs.oracle.com/cd/E26926_01/html/E25874/ipconfig-142.html">https://docs.oracle.com/cd/E26926_01/html/E25874/ipconfig-142.html</a></p>]]></content>
      
      
      <categories>
          
          <category> Linux </category>
          
          <category> Network </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> Network </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>如何在 Git 提交中禁止包含敏感信息</title>
      <link href="/2024/07/11/git/no-sensitive/"/>
      <url>/2024/07/11/git/no-sensitive/</url>
      
        <content type="html"><![CDATA[<p>为了防止在 Git 提交中包含敏感信息（如数据库密码），可以采取以下几种方法：</p><h2 id="1-使用-gitignore"><a href="#1-使用-gitignore" class="headerlink" title="1. 使用 .gitignore"></a>1. 使用 .gitignore</h2><p>将包含敏感信息的文件添加到 <code>.gitignore</code> 文件中，这样 Git 就不会跟踪这些文件。例如，如果你的敏感信息存储在 <code>config.json</code> 文件中，你可以在 <code>.gitignore</code> 中添加：</p><pre class="language-none"><code class="language-none">config.json</code></pre><h2 id="2-使用环境变量"><a href="#2-使用环境变量" class="headerlink" title="2. 使用环境变量"></a>2. 使用环境变量</h2><p>将敏感信息存储在环境变量中，而不是直接在代码中。例如，在代码中读取环境变量：</p><pre class="language-python" data-language="python"><code class="language-python">import osdb_password = os.getenv('DB_PASSWORD')</code></pre><p>在运行应用程序之前，设置环境变量：</p><pre class="language-bash" data-language="bash"><code class="language-bash">export DB_PASSWORD="your_database_password"</code></pre><h2 id="3-使用-Git-钩子（pre-commit-钩子）"><a href="#3-使用-Git-钩子（pre-commit-钩子）" class="headerlink" title="3. 使用 Git 钩子（pre-commit 钩子）"></a>3. 使用 Git 钩子（pre-commit 钩子）</h2><p>可以编写一个 Git pre-commit 钩子，在每次提交之前检查提交内容中是否包含敏感信息。如果包含，则拒绝提交。</p><h3 id="方法1：手动编写-pre-commit-钩子"><a href="#方法1：手动编写-pre-commit-钩子" class="headerlink" title="方法1：手动编写 pre-commit 钩子"></a>方法1：手动编写 pre-commit 钩子</h3><p>首先，创建一个 pre-commit 钩子脚本：</p><pre class="language-bash" data-language="bash"><code class="language-bash">#!/bin/sh# 定义敏感信息模式SENSITIVE_PATTERNS=("your_database_password" "another_sensitive_pattern")# 获取已暂存的文件STAGED_FILES=$(git diff --cached --name-only)for FILE in $STAGED_FILES; do  for PATTERN in "${SENSITIVE_PATTERNS[@]}"; do    if git grep -q "$PATTERN" "$FILE"; then      echo "Error: Sensitive information found in $FILE"      exit 1    fi  donedoneexit 0</code></pre><p>将这个脚本保存到 <code>.git/hooks/pre-commit</code> 文件中，并确保它具有可执行权限：</p><pre class="language-bash" data-language="bash"><code class="language-bash">chmod +x .git/hooks/pre-commit</code></pre><h3 id="方法2：使用-pre-commit-工具-禁止已经提交的文件"><a href="#方法2：使用-pre-commit-工具-禁止已经提交的文件" class="headerlink" title="方法2：使用 pre-commit 工具 禁止已经提交的文件"></a>方法2：使用 pre-commit 工具 禁止已经提交的文件</h3><ol><li>安装 pre-commit</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">pip install pre-commit</code></pre><p>或者</p><pre class="language-bash" data-language="bash"><code class="language-bash">brew install pre-commit   # mac apt install pre-commit    # linux</code></pre><ol start="2"><li>添加默认配置文件</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">pre-commit sample-config &gt; .pre-commit-config.yaml</code></pre><p>在项目根目录下生成文件 <code>.pre-commit-config.yaml</code>，内容如下：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml"># See https://pre-commit.com for more information# See https://pre-commit.com/hooks.html for more hooksrepos:-   repo: https://github.com/pre-commit/pre-commit-hooks    rev: v3.2.0    hooks:    -   id: trailing-whitespace    -   id: end-of-file-fixer    -   id: check-yaml    -   id: check-added-large-files</code></pre><ol start="3"><li>配置 pre-commit 钩子：</li></ol><p>在 <code>.pre-commit-config.yaml</code> 文件中添加一个自定义钩子来检查指定文件是否被修改。例如，假设你想保护文件 <code>protected_file.txt</code>，可以这样配置：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">repos:  - repo: local    hooks:      - id: protect-protected-file        name: Protect protected_file.txt        entry: bash protect-protected-file.sh        language: system        stages: [commit]</code></pre><ol start="4"><li>创建自定义钩子脚本</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">#!/bin/sh# 要保护的文件列表PROTECTED_FILES=(    "protected_file.txt" )# 检查是否有文件被修改for FILE in "${PROTECTED_FILES[@]}"; do  if git diff --cached --name-only | grep -q "^$FILE$"; then    echo "Error: You are trying to modify $FILE, which is protected."    exit 1  fidone# 允许提交exit 0</code></pre><ol start="5"><li>使脚本可执行：</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">chmod +x protect-protected-file.sh</code></pre><ol start="6"><li>安装 pre-commit 钩子：</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">pre-commit install</code></pre><h2 id="4-使用工具（如-git-secrets）"><a href="#4-使用工具（如-git-secrets）" class="headerlink" title="4. 使用工具（如 git-secrets）"></a>4. 使用工具（如 git-secrets）</h2><p><code>git-secrets</code> 是一个专门用于防止在 Git 仓库中提交敏感信息的工具。它可以在提交之前扫描提交内容，并阻止包含敏感信息的提交。</p><h3 id="安装-git-secrets"><a href="#安装-git-secrets" class="headerlink" title="安装 git-secrets"></a>安装 git-secrets</h3><h4 id="对于-macOS"><a href="#对于-macOS" class="headerlink" title="对于 macOS"></a>对于 macOS</h4><pre class="language-bash" data-language="bash"><code class="language-bash">brew install git-secrets</code></pre><h4 id="对于-Linux"><a href="#对于-Linux" class="headerlink" title="对于 Linux"></a>对于 Linux</h4><pre class="language-bash" data-language="bash"><code class="language-bash">git clone https://github.com/awslabs/git-secrets.gitcd git-secretssudo make install</code></pre><p>然后在你的 Git 仓库中初始化 git-secrets 并添加敏感信息模式：</p><pre class="language-bash" data-language="bash"><code class="language-bash">cd your-repogit secrets --installgit secrets --add 'your_database_password'git secrets --add 'another_sensitive_pattern'</code></pre><p>这样，每次提交时，git-secrets 都会扫描提交内容并阻止包含敏感信息的提交。</p><h2 id="5-使用-Secret-Scanning-服务"><a href="#5-使用-Secret-Scanning-服务" class="headerlink" title="5. 使用 Secret Scanning 服务"></a>5. 使用 Secret Scanning 服务</h2><p>一些代码托管平台（如 GitHub）提供了 Secret Scanning 服务，可以自动扫描仓库中的敏感信息，并在发现敏感信息时发出警告。</p><p>通过以上方法，可以有效防止在 Git 提交中包含敏感信息，保护你的代码库安全。</p>]]></content>
      
      
      <categories>
          
          <category> Tool </category>
          
      </categories>
      
      
        <tags>
            
            <tag> git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ubuntu24.04 微信客户端</title>
      <link href="/2024/07/03/linux/wechat/"/>
      <url>/2024/07/03/linux/wechat/</url>
      
        <content type="html"><![CDATA[<h2 id="使用终端命令行安装铜豌豆软件源。注意需要用到sudo权限"><a href="#使用终端命令行安装铜豌豆软件源。注意需要用到sudo权限" class="headerlink" title="使用终端命令行安装铜豌豆软件源。注意需要用到sudo权限"></a>使用终端命令行安装铜豌豆软件源。注意需要用到sudo权限</h2><pre class="language-bash" data-language="bash"><code class="language-bash">wget -c -O atzlinux-v12-archive-keyring_lastest_all.deb https://www.atzlinux.com/atzlinux/pool/main/a/atzlinux-archive-keyring/atzlinux-v12-archive-keyring_lastest_all.debsudo apt -y install ./atzlinux-v12-archive-keyring_lastest_all.deb</code></pre><h2 id="使用终端命令行安装微信原生版本。注意需要用到sudo权限"><a href="#使用终端命令行安装微信原生版本。注意需要用到sudo权限" class="headerlink" title="使用终端命令行安装微信原生版本。注意需要用到sudo权限"></a>使用终端命令行安装微信原生版本。注意需要用到sudo权限</h2><pre class="language-bash" data-language="bash"><code class="language-bash">sudo apt updatesudo cp /etc/lsb-release /etc/lsb-release.Ubuntusudo apt -y install electronic-wechat-icons-atzlinuxsudo apt -y install com.tencent.wechatsudo cp /etc/lsb-release /etc/lsb-release.wechat</code></pre><h2 id="登录微信并使用"><a href="#登录微信并使用" class="headerlink" title="登录微信并使用"></a>登录微信并使用</h2><pre class="language-bash" data-language="bash"><code class="language-bash">wechat</code></pre><p>登录成功以后，下次就可以直接在终端使用wechat启动微信，或者在显示应用程序里面找到微信linux原生版图标启动了。本次安装的微信版本号：1.0.0.241</p><p><img src="/medias/loading.gif" data-original="/images/linux/wechat/image.png" alt="start wechat"></p><p><a href="https://link.zhihu.com/?target=https://www.atzlinux.com/">铜豌豆 Linux</a>是个开源网站</p>]]></content>
      
      
      <categories>
          
          <category> linux </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ubuntu </tag>
            
            <tag> 微信 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>awk 常用的命令</title>
      <link href="/2024/07/01/linux/awk/"/>
      <url>/2024/07/01/linux/awk/</url>
      
        <content type="html"><![CDATA[<h2 id="处理字符串"><a href="#处理字符串" class="headerlink" title="处理字符串"></a>处理字符串</h2><p><strong>提取字符串第三字符串</strong></p><pre class="language-bash" data-language="bash"><code class="language-bash">echo 'this is a test' |awk '{print $3}'</code></pre><p><strong>替换 字符串中一个</strong></p><pre class="language-bash" data-language="bash"><code class="language-bash">echo "Hello Tom" | awk '{$2="Adam"; print $0}'</code></pre><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">Hello Adam</code></pre><h3 id="利用变量-菜鸟"><a href="#利用变量-菜鸟" class="headerlink" title="利用变量    菜鸟"></a>利用变量    <a href="https://www.runoob.com/w3cnote/8-awesome-awk-built-in-variables.html">菜鸟</a></h3><ol><li><p>$NF  表示当前行有多少个字段，</p><p>因此<code>$NF</code>就代表最后一个字段。</p></li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">echo 'this is a test' | awk '{print $NF}'</code></pre><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">test</code></pre><p>​     <code>$(NF-1)</code>代表倒数第二个字段</p><pre class="language-bash" data-language="bash"><code class="language-bash">echo 'this is a test' | awk '{print $(NF-1)}'</code></pre><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">a</code></pre><p>   2 .变量<code>NR</code>表示当前处理的是第几行。</p><h2 id="处理文件"><a href="#处理文件" class="headerlink" title="处理文件"></a>处理文件</h2><p>处理文件 logs.txt , 文件内容:</p><pre class="language-bash" data-language="bash"><code class="language-bash">07.46.199.184 [28/Sep/2010:04:08:20] "GET /robots.txt HTTP/1.1" 200 0 "msnbot"123.125.71.19 [28/Sep/2010:04:20:11] "GET / HTTP/1.1" 304 - "Baiduspider"</code></pre><p> <strong>获取第一列数据</strong></p><pre class="language-bash" data-language="bash"><code class="language-bash">awk '{print $1}' logs.txt</code></pre><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">07.46.199.184123.125.71.19</code></pre><p><strong>这个文件的字段分隔符是冒号（<code>:</code>），所以要用<code>-F</code>参数指定分隔符为冒号。然后，才能提取到它的第一个字段。</strong></p><pre class="language-bash" data-language="bash"><code class="language-bash">awk -F ':' '{print $1}' logs.txt</code></pre><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">07.46.199.184 [28/Sep/2010123.125.71.19 [28/Sep/2010</code></pre><p><strong>提取log 的时间</strong></p><pre class="language-bash" data-language="bash"><code class="language-bash">awk '{print $2}' logs.txt | awk 'BEGIN{FS=":"}{print $1}' | sed 's/\[//'</code></pre><p>输出:</p><pre class="language-bash" data-language="bash"><code class="language-bash">28/Sep/201028/Sep/2010</code></pre><p>**统计某一个字段的相加 ** </p><p>每次都大于结果</p><pre class="language-bash" data-language="bash"><code class="language-bash">awk '{a+=$(NF-2); print "Total so far:", a}' logs.txt</code></pre><pre class="language-bash" data-language="bash"><code class="language-bash">Total so far: 200Total so far: 504</code></pre><p>执行完后在打印结果</p><pre class="language-bash" data-language="bash"><code class="language-bash">awk '{a+=$(NF-2)}END{print "Total:", a}' logs.txt</code></pre><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">Total: 504</code></pre><h3 id="利用变量"><a href="#利用变量" class="headerlink" title="利用变量"></a>利用变量</h3><ol><li><strong>OFS: 输出字段分隔符变量</strong></li></ol><p><strong>OFS</strong>(Output Field Separator) 相当与输出上的 <strong>FS</strong>, 默认是以一个空格字符作为输出分隔符的，下面是一个 <strong>OFS</strong> 的例子:</p><p>正常空格命令：</p><pre class="language-bash" data-language="bash"><code class="language-bash">awk  '{print $1, $3;}' logs.txt</code></pre><p>输出结果：</p><pre class="language-bash" data-language="bash"><code class="language-bash">07.46.199.184 "GET123.125.71.19 "GET</code></pre><p>注意命令中的 print 语句的, 表示的使用一个空格连接两个参数，也就是默认的OFS的值。因此 <strong>OFS</strong> 可以像下面那样插入到输出的字段之间:</p><pre class="language-bash" data-language="bash"><code class="language-bash">awk 'BEGIN{OFS="=&gt;";}{print $1, $3;}' logs.txt</code></pre><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">07.46.199.184=&gt;"GET123.125.71.19=&gt;"GET</code></pre><p>注意： GET 前面多一个双引号，我们需要去掉</p><pre class="language-bash" data-language="bash"><code class="language-bash">awk  '{print $1, $3;}' logs.txt |sed 's/\"//'awk 'BEGIN{OFS="=&gt;";}{print $1, $3;}' logs.txt |sed 's/\"//'</code></pre><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">07.46.199.184 GET123.125.71.19 GET07.46.199.184=&gt;GET123.125.71.19=&gt;GET</code></pre><ol start="2"><li><strong>变量<code>NR</code>表示当前处理的是第几行。</strong></li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">awk '{print NR") "$1}' logs.txt</code></pre><p>输出:</p><pre class="language-bash" data-language="bash"><code class="language-bash">1) 07.46.199.1842) 123.125.71.19</code></pre><ol start="3"><li><p>变量 NF  表示一列的最后一个字段 </p><p>$(NF-2) 表示倒数第三个字段    </p><p>利用 if 判断倒数第三个字段 是否 等于  200</p><pre class="language-bash" data-language="bash"><code class="language-bash">awk '条件 动作' 文件名</code></pre></li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">awk '{if ($(NF-2) == "200") {print $0}}' logs.txt</code></pre><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">07.46.199.184 [28/Sep/2010:04:08:20] "GET /robots.txt HTTP/1.1" 200 0 "msnbot"</code></pre><h2 id="处理进程"><a href="#处理进程" class="headerlink" title="处理进程"></a>处理进程</h2><ol><li>根据 lsof 命令获取进程 PID</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">lsof -i:3100|awk 'NR&gt;1' |awk '{print $2}' </code></pre><p>  kill  -9 进程</p><pre class="language-bash" data-language="bash"><code class="language-bash">lsof -i:3100|awk 'NR&gt;1' |awk '{print $2}'  | xargs kill -9</code></pre><p>NR  大于  1 表示从第二行开始</p><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">2299757229975722997572299757229975722997572299757229975722997572410946</code></pre><h2 id="统计文件中的数量"><a href="#统计文件中的数量" class="headerlink" title="统计文件中的数量"></a>统计文件中的数量</h2><p>查询 file 文件中 haha 的数量</p><pre class="language-bash" data-language="bash"><code class="language-bash">awk -v RS='haha' 'END {print --NR}' file  </code></pre><p>或者使用 grep </p><pre class="language-bash" data-language="bash"><code class="language-bash">grep -o 'haha' file | wc -l</code></pre><h2 id="查询使用端口"><a href="#查询使用端口" class="headerlink" title="查询使用端口"></a>查询使用端口</h2><pre class="language-bash" data-language="bash"><code class="language-bash">ss -nutlp | awk '{print $1,$5}' | awk -F"[: ]" '{print "协议:"$1, "端口号:"$NF}'|grep "[0-9]"|uniq</code></pre><h2 id="参考链接："><a href="#参考链接：" class="headerlink" title="参考链接："></a>参考链接：</h2><p><a href="https://gregable.com/2010/09/why-you-should-know-just-little-awk.html">Awk Example</a></p><p><a href="https://www.runoob.com/linux/linux-comm-awk.html">菜鸟</a></p><p><a href="https://www.ruanyifeng.com/blog/2018/11/awk.html">awk 入门教程</a></p><p><a href="https://likegeeks.com/awk-command/">30 Examples for Awk Command in Text Processing</a></p><p><a href="https://zhuanlan.zhihu.com/p/34946663">查找grep、提取awk、sed、重定向</a></p>]]></content>
      
      
      
        <tags>
            
            <tag> linux </tag>
            
            <tag> awk </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>tcpdump 常用命令</title>
      <link href="/2024/07/01/linux/tcpdump/"/>
      <url>/2024/07/01/linux/tcpdump/</url>
      
        <content type="html"><![CDATA[<p>tcpdump 支持针对网络层、协议、主机、网络或端口的过滤，并提供and、or、not等逻辑语句来帮助你去掉无用的信息</p><h2 id="命令格式："><a href="#命令格式：" class="headerlink" title="命令格式："></a>命令格式：</h2><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump [ -DenNqvX ] [ -c count ] [ -F file ] [ -i interface ] [ -r file ]        [ -s snaplen ] [ -w file ] [ expression ]</code></pre><p>抓包选项：<br>-c：指定要抓取的包数量。</p><p>-i interface：指定tcpdump需要监听的接口。默认会抓取第一个网络接口</p><p>-n：对地址以数字方式显式，否则显式为主机名，也就是说-n选项不做主机名解析。</p><p>-nn：除了-n的作用外，还把端口显示为数值，否则显示端口服务名。</p><p>-P：指定要抓取的包是流入还是流出的包。可以给定的值为”in”、”out”和”inout”，默认为”inout”。</p><p>-s len：设置tcpdump的数据包抓取长度为len，如果不设置默认将会是65535字节。对于要抓取的数据包较大时，长度设置不够可能会产生包截断，若出现包截断，<br>：输出行中会出现”[|proto]”的标志(proto实际会显示为协议名)。但是抓取len越长，包的处理时间越长，并且会减少tcpdump可缓存的数据包的数量，<br>：从而会导致数据包的丢失，所以在能抓取我们想要的包的前提下，抓取长度越小越好。</p><p>输出选项：<br>-e：输出的每行中都将包括数据链路层头部信息，例如源MAC和目标MAC。</p><p>-q：快速打印输出。即打印很少的协议相关信息，从而输出行都比较简短。</p><p>-X：输出包的头部数据，会以16进制和ASCII两种方式同时输出。</p><p>-XX：输出包的头部数据，会以16进制和ASCII两种方式同时输出，更详细。</p><p>-v：当分析和打印的时候，产生详细的输出。</p><p>-vv：产生比-v更详细的输出。<br>-vvv：产生比-vv更详细的输出。</p><h2 id="tcpdump示例"><a href="#tcpdump示例" class="headerlink" title="tcpdump示例"></a>tcpdump示例</h2><p>==**tcpdump只能抓取流经本机的数据包 **==</p><ol><li>默认启动</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump</code></pre><p>默认情况下，直接启动tcpdump将监视第一个网络接口(非lo口)上所有流通的数据包。这样抓取的结果会非常多，滚动非常快。</p><p>2 . 监视指定网络接口的数据包</p><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -i ens33</code></pre><ol start="3"><li>监视指定主机的数据包，例如所有进入或离开node1的数据包</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -i ens33  host node1</code></pre><ol start="4"><li>打印node1&lt;–&gt;node2或node1&lt;–&gt;node3之间通信的数据包</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -i ens33 host node1 and \(node2 or node3\)</code></pre><ol start="5"><li>打印node1与任何其他主机之间通信的IP数据包,但不包括与node4之间的数据包</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -i ens33 host node1 and not node4</code></pre><ol start="6"><li>截获主机node1 发送的所有数据</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -i ens33 src host node1</code></pre><ol start="7"><li>监视所有发送到主机node1 的数据包</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -i ens33 dst host node1</code></pre><ol start="8"><li>监视指定主机和端口的数据包</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -i ens33 port 8080 and host node1</code></pre><ol start="9"><li>监视指定网络的数据包，如本机与192.168网段通信的数据包，”-c 10”表示只抓取10个包</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -i ens33 -c 10 net 192.168</code></pre><ol start="10"><li>打印所有通过网关snup的ftp数据包</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump 'gateway snup and (port ftp or ftp-data)'</code></pre><p>注意,表达式被单引号括起来了,这可以防止shell对其中的括号进行错误解析</p><ol start="11"><li>抓取ping包</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -c 5 -nn -i ens33 </code></pre><p>==指定主机抓ping包==</p><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -c 5 -nn -i eth0 icmp and src 192.168.100.62</code></pre><ol start="12"><li>抓取到本机22端口包</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -c 10 -nn -i ens33 tcp dst port 22</code></pre><ol start="13"><li>解析包数据</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">tcpdump -c 2 -q -XX -vvv -nn -i ens33 tcp dst port 22</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> linux </tag>
            
            <tag> tcpdump </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>linux date 命令</title>
      <link href="/2024/07/01/linux/date/"/>
      <url>/2024/07/01/linux/date/</url>
      
        <content type="html"><![CDATA[<h2 id="date-命令参数说明"><a href="#date-命令参数说明" class="headerlink" title="date 命令参数说明"></a>date 命令参数说明</h2><!-- 转换表格 --><table><thead><tr><th>字符</th><th>描述</th></tr></thead><tbody><tr><td>%%</td><td>一个文字的 %</td></tr><tr><td>%a</td><td>当前locale 的星期名缩写(例如： 日，代表星期日)</td></tr><tr><td>%A</td><td>当前locale 的星期名全称 (如：星期日)</td></tr><tr><td>%b</td><td>当前locale 的月名缩写 (如：一，代表一月)</td></tr><tr><td>%B</td><td>当前locale 的月名全称 (如：一月)</td></tr><tr><td>%c</td><td>当前locale 的日期和时间 (如：2005年3月3日 星期四 23:05:25)</td></tr><tr><td>%C</td><td>世纪；比如 %Y，通常为省略当前年份的后两位数字(例如：20)</td></tr><tr><td>%Y</td><td>年</td></tr><tr><td>%y</td><td>年份后两位  (00..99)</td></tr><tr><td>%m</td><td>月</td></tr><tr><td>%d</td><td>日</td></tr><tr><td>%D</td><td>按月计的日期；等于%m/%d/%y</td></tr><tr><td>%e</td><td>按月计的日期，添加空格，等于%_d</td></tr><tr><td>%F</td><td>完整日期格式，等价于 %Y-%m-%d</td></tr><tr><td>%H</td><td>小时 24 小时制  hour (00..23)</td></tr><tr><td>%I</td><td>小时 12 小时制   hour (01..12)</td></tr><tr><td>%M</td><td>分钟</td></tr><tr><td>%S</td><td>秒钟</td></tr><tr><td>%s</td><td>当前时间秒数</td></tr><tr><td>%T</td><td>时钟 等于 %H:%M:%S</td></tr><tr><td>%c</td><td>本地时间和日期</td></tr><tr><td>%j</td><td>一年中的第几天</td></tr><tr><td>%W</td><td>一年中的第几周 星期一为一周的第一天</td></tr><tr><td>%w</td><td>星期几  week(0..6) ; 0 是星期天</td></tr><tr><td>%u</td><td>星期几  week(1..7) ; 1 是星期一</td></tr><tr><td>%U</td><td>一年中的第几周  星期日为一周的第一天</td></tr><tr><td>%V</td><td>ISO 周数  星期一为一周的第一天, ISO 周编号</td></tr><tr><td>%x</td><td>日期   (e.g., 12/31/99)</td></tr><tr><td>%X</td><td>时间  (e.g., 23:13:48)</td></tr><tr><td>%z</td><td>时区  数字格式  (e.g., +0800)</td></tr><tr><td>%:z</td><td>时区 +08:00</td></tr><tr><td>%::z</td><td>时区 +08:00:00</td></tr><tr><td>%:::z</td><td>时区 +08</td></tr><tr><td>%Z</td><td>时区缩写  CST</td></tr><tr><td>%n</td><td>换行</td></tr><tr><td>%N</td><td>纳秒(000000000-999999999)</td></tr></tbody></table><h2 id="时间戳与时间互转"><a href="#时间戳与时间互转" class="headerlink" title="时间戳与时间互转"></a>时间戳与时间互转</h2><ol><li>时间戳转换成时间格式</li></ol><pre class="language-Bash" data-language="Bash"><code class="language-Bash">date -d @1718866413  "+%Y-%m-%d %H:%M:%S"=&gt; 2024-06-20 14:53:33</code></pre><ol><li>获取当前时间戳</li></ol><pre class="language-Bash" data-language="Bash"><code class="language-Bash">date +%s=&gt; 1718869999</code></pre><ol><li>获取当天的时钟</li></ol><pre class="language-Bash" data-language="Bash"><code class="language-Bash">date +%T  =&gt; 15:54:22</code></pre><ol><li>获取某个时间节点对应的时间戳</li></ol><pre class="language-Bash" data-language="Bash"><code class="language-Bash">date -d "2021-12-20"  +%s=&gt; 1639929600date -d "2021-12-20 20:20:10"  +%s=&gt; 1640002810</code></pre><ol><li>获取当前时间或指定时间是全年的第几天</li></ol><pre class="language-Bash" data-language="Bash"><code class="language-Bash"># 当前时间date  +%j=&gt; 172 # 指定时间date -d "2021-12-20 20:20:10"  +%j=&gt; 354</code></pre><ol start="2"><li>当前时间是第几周</li></ol><pre class="language-Bash" data-language="Bash"><code class="language-Bash">date +%W=&gt; 25</code></pre><ol><li>查看当前时间日前与时间</li></ol><pre class="language-Bash" data-language="Bash"><code class="language-Bash"># date  "+%x %X"=&gt; 06/20/2024 04:09:04 PM</code></pre><ol><li>查看当前时区</li></ol><pre class="language-Bash" data-language="Bash"><code class="language-Bash">date +%z=&gt; +0800</code></pre><ol><li>设置时区获取时间</li></ol><pre class="language-Bash" data-language="Bash"><code class="language-Bash"># 当前时间戳转换TZ='America/Los_Angeles' date "+%Y-%m-%d %H:%M:%S"=&gt; 2024-06-20 01:22:26TZ="Asia/Shanghai" date "+%Y-%m-%d %H:%M:%S"=&gt; 2024-06-20 16:26:38# 指定时间戳转换TZ='America/Los_Angeles'  date -d @1718866413 "+%Y-%m-%d %H:%M:%S"=&gt; 2024-06-19 23:53:33TZ="Asia/Shanghai" date -d @1718866413 "+%Y-%m-%d %H:%M:%S"=&gt; 2024-06-20 14:53:33</code></pre><p>参考时区表 <a href="https://en.wikipedia.org/wiki/List_of_tz_database_time_zones">https://en.wikipedia.org/wiki/List_of_tz_database_time_zones</a></p><p>通过 <code>timedatectl list-timezones</code> 列出可用的时区</p><pre class="language-Bash" data-language="Bash"><code class="language-Bash">timedatectl list-timezones=&gt;Africa/AbidjanAfrica/AccraAfrica/Addis_AbabaAfrica/AlgiersAfrica/AsmaraAfrica/AsmeraAfrica/BamakoAfrica/BanguiAfrica/BanjulAfrica/BissauAfrica/Blantyre...</code></pre><h2 id="设置系统日期和时间"><a href="#设置系统日期和时间" class="headerlink" title="设置系统日期和时间"></a>设置系统日期和时间</h2><pre class="language-Bash" data-language="Bash"><code class="language-Bash">date –set="20140125 09:17:00"</code></pre><p>通过 <code>man date</code>  查看 date 更多参数</p>]]></content>
      
      
      
        <tags>
            
            <tag> linux </tag>
            
            <tag> date </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Makefile 打印变量</title>
      <link href="/2024/06/18/makefile/print-variable/"/>
      <url>/2024/06/18/makefile/print-variable/</url>
      
        <content type="html"><![CDATA[<p>在编写Makefile时，调试信息的输出对于查找和解决问题至关重要。Makefile提供了几种不同的方式来输出调试信息，包括<code>info</code>、<code>warning</code>、<code>error</code>以及<code>echo</code>命令。下面我们将详细介绍这些工具的使用方法以及它们之间的区别。</p><p>makefile 打印变量方式有两种情况</p><ol><li>使用info/warning/error增加调试信息<ol><li>打印字符串</li></ol></li></ol><pre class="language-Makefile" data-language="Makefile"><code class="language-Makefile">$(info xxx-msg)  # 输出字符串xxxx-msg，不需要加""，info后加空格=&gt;  xxxx-msg </code></pre><pre><code>2. 打印变量</code></pre><pre class="language-Makefile" data-language="Makefile"><code class="language-Makefile">$(info $(GOPATH)) #打印变量GOPATH，变量名用$())=&gt; /home/user/code/go</code></pre><pre><code>3. 字符串、变量混合打印</code></pre><pre class="language-Makefile" data-language="Makefile"><code class="language-Makefile">$(info GOPATH: $(GOPATH))=&gt; GOPATH: /home/hellotalk/code/go</code></pre><pre><code>4. info/warning/error之间区别      info只输出信息：</code></pre><pre class="language-Makefile" data-language="Makefile"><code class="language-Makefile">$(info GOPATH is: $(GOPATH)) =&gt; GOPATH is: /home/hellotalk/code/go</code></pre><pre><code>    warning输出信息和对应的行号：</code></pre><pre class="language-Makefile" data-language="Makefile"><code class="language-Makefile">$(warning ***** $(shell date))=&gt; scripts/make-rules/common.mk:99: ***** Tue Jun 18 05:27:20 PM CST 2024</code></pre><pre><code>    error输出信息和对应的行号，并停止makefile的编译：</code></pre><pre class="language-Makefile" data-language="Makefile"><code class="language-Makefile">$(error GOPATH: $(GOPATH))=&gt; scripts/make-rules/golang.mk:12: *** GOPATH: /home/hellotalk/code/go。 停止。</code></pre><ol start="2"><li>使用echo增加调试信息（echo只能在target：后面的语句中使用，且前面是个TAB）</li></ol><pre class="language-Makefile" data-language="Makefile"><code class="language-Makefile"># 假设我们有一个变量  MY_VARIABLE := Hello, Makefile!    # 定义一个目标（target）  all:    # 注意这里有一个制表符（Tab），而不是空格    @echo "开始构建..."    @echo "MY_VARIABLE 的值是: $(MY_VARIABLE)"    # 假设这里有一些其他的构建命令...    @echo "构建完成!"    # 如果你还有其他的目标（target），也可以在这里定义  clean:    # 同样是制表符（Tab）开头    @echo "开始清理..."    # 这里应该有清理构建产物的命令，比如 rm -f ...    @echo "清理完成!"</code></pre><pre><code>执行测试命令</code></pre><pre class="language-Bash" data-language="Bash"><code class="language-Bash">make -n all</code></pre><pre><code>这将会显示`all`目标下的所有命令，但由于`@`前缀的存在，它们不会显示`echo`命令的内容。如果你想要看到`echo`的内容，你需要移除`@`前缀或者不使用`-n`选项。</code></pre>]]></content>
      
      
      <categories>
          
          <category> makefile </category>
          
      </categories>
      
      
        <tags>
            
            <tag> makefile </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>golang 协程互斥锁</title>
      <link href="/2023/10/14/go/mutex/"/>
      <url>/2023/10/14/go/mutex/</url>
      
        <content type="html"><![CDATA[<p>golang 在多协程的情况下，如果多个协程同时操作一个变量，会出现数据不一致的情况，这个时候就需要使用互斥锁来解决这个问题。</p><h2 id="互斥锁-sync-Mutex"><a href="#互斥锁-sync-Mutex" class="headerlink" title="互斥锁 (sync.Mutex)"></a>互斥锁 (sync.Mutex)</h2><p>互斥即不可同时运行。即使用了互斥锁的两个代码片段互相排斥，只有其中一个代码片段执行完成后，另一个才能执行。</p><p>Go 标准库中提供了 sync.Mutex 互斥锁类型及其两个方法：</p><blockquote><p>Lock 加锁<br>Unlock 释放锁</p></blockquote><h2 id="代码案例"><a href="#代码案例" class="headerlink" title="代码案例"></a>代码案例</h2><p>下面是没有使用锁一个例子情况：</p><pre class="language-go" data-language="go"><code class="language-go">var setMap = make(map[int]bool,0)func printOnce(num int) {  if _, exist := setMap[num]; !exist {  fmt.Println(num) } setMap[num] = true}func main() { for i := 0; i &lt; 10; i++ {  go printOnce(100) } time.Sleep(time.Second)}</code></pre><p>运行 go run main.go 会发生什么情况呢？</p><pre class="language-bash" data-language="bash"><code class="language-bash">go run main.go100100</code></pre><p>多运行几次打印不同的结果, 有时候打印7次，有时候打印10次，有时候还触发 panic，是因为对同一个数据结构的访问冲突了。解决的方法使用 <code>Lock()</code> 与 <code>UnLock()</code> 的方法解决问题</p><pre class="language-go" data-language="go"><code class="language-go">var m sync.Mutexvar setMap = make(map[int]bool, 0)func printOnce(num int) { m.Lock() if _, exist := setMap[num]; !exist {  fmt.Println(num) } setMap[num] = true m.Unlock()}func main() { for i := 0; i &lt; 10; i++ {  go printOnce(100) } time.Sleep(time.Second)}</code></pre><p>运行 go run main.go</p><pre class="language-bash" data-language="bash"><code class="language-bash">go run main.go100</code></pre><p><strong>注意</strong>:</p><blockquote><p>一个互斥锁只能同时被一个 goroutine 锁定，其它 goroutine 就算调用 Lock() 方法也会等待锁的释放。</p></blockquote><pre class="language-go" data-language="go"><code class="language-go">func main() { ch := make(chan struct{}, 2) var m sync.Mutex go func() {  m.Lock()  defer m.Unlock()  fmt.Println("goroutine1: 大概锁定 2s")  time.Sleep(time.Second * 2)  fmt.Println("goroutine1: 锁定结束，准备退出")  ch &lt;- struct{}{} }() go func() {  fmt.Println("goroutine2: 等待解锁")  m.Lock()  defer m.Unlock()  fmt.Println("goroutine2: 锁定结束")  ch &lt;- struct{}{} }() // 等待 goroutine 执行结束 for i := 0; i &lt; 2; i++ {  &lt;-ch }}</code></pre><p>运行 go run main.go</p><pre class="language-bash" data-language="bash"><code class="language-bash"> go run main.gogoroutine1: 大概锁定 2sgoroutine2: 等待解锁goroutine1: 锁定结束，准备退出goroutine2: 锁定结束</code></pre><h2 id="sync-Map-Go-1-9"><a href="#sync-Map-Go-1-9" class="headerlink" title="sync.Map (Go 1.9+)"></a>sync.Map (Go 1.9+)</h2><p>Go 1.9 标准库提供了内置并发安全的 map，可以安全地被多个 goroutine 并发使用，无需额外的加锁或协调。</p><pre class="language-go" data-language="go"><code class="language-go">import "sync"var sm sync.Map  // 直接声明即可使用// 存储sm.Store("key1",100)// 读取if value ,ok := sm.Load("key1");ok {  fmt.PrintLn("Loaded value",value.(int)) // 注意类型断言}// 读取或存储actual, loaded := sm.LoadOrStore("key2",200)fmt.Println("LoadOrStore",actual.(int),"loaded?",loaded)// 删除sm.Delete("key1")// 遍历(注意 Range 的函数签名)sm.Range(func(key , value any) bool{  fmt.Println("Range:",key.(string),value.(int))  return true // 返回 true 继续遍历 ， false 停止})</code></pre>]]></content>
      
      
      <categories>
          
          <category> golang </category>
          
      </categories>
      
      
        <tags>
            
            <tag> golang </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>pods 优雅终止</title>
      <link href="/2023/09/14/k8s/pod-elegant-stop/"/>
      <url>/2023/09/14/k8s/pod-elegant-stop/</url>
      
        <content type="html"><![CDATA[<h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>Pod 销毁时，会停止容器内的进程，通常在停止的过程中我们需要执行一些善后逻辑，比如等待存量请求处理完以避免连接中断，或通知相关依赖进行清理等，从而实现优雅终止目的。本文介绍在 Kubernetes 场景下，实现容器优雅终止的最佳实践。</p><h2 id="当-Kubernetes-杀死一个-pod-时，会发生以下-5-个步骤："><a href="#当-Kubernetes-杀死一个-pod-时，会发生以下-5-个步骤：" class="headerlink" title="当 Kubernetes 杀死一个 pod 时，会发生以下 5 个步骤："></a>当 Kubernetes 杀死一个 pod 时，会发生以下 5 个步骤：</h2><ol><li>Pod 切换到终止状态并停止接收任何新流量，容器仍在 pod 内运行。</li><li>preStop 钩子是一个特殊的命令或 HTTP 请求被执行，并被发送到 pod 内的容器。</li><li>SIGTERM 信号被发送到 pod，容器意识到它将很快关闭。</li><li>Kubernetes 等待宽限期 (terminationGracePeriodSeconds)。此等待与 preStop hook 和 SIGTERM 信号执行并行（默认 30 秒）。因此，Kubernetes 不会等待这些完成。如果这段时间结束，则直接进入下一步。正确设置宽限期的值非常重要。</li><li>向 pod 发送 SIGKILL 信号，然后移除 pod。如果容器在宽限期后仍在运行，则 Pod 被 SIGKILL 强行移除，终止完成。</li></ol><p>总结下大致分为两步，第一步定义 preStop，一般情况下可以休眠 30s，用于处理残余流量；第二步发送 SIGTERM 信号，服务收到信号后进行服务的收尾工作处理。比如：关闭连接、通知第三方注册中心服务关闭…..</p><h2 id="Pods-生命周期的状态"><a href="#Pods-生命周期的状态" class="headerlink" title="Pods 生命周期的状态"></a>Pods 生命周期的状态</h2><p>phase表示一个Pod处于其生命周期的哪个阶段，一共有以下5个可能的取值：</p><ol><li>Pending：Pod已经被k8s系统接受，但Pod中还有容器没有被创建。Pod被调度前和下载容器镜像的时候都处于这个阶段</li><li>Running：Pod已经被调度到Node上，所有的容器都已经被创建，并且至少有一个容器还在运行中（正在启动或重启中的容器也算）</li><li>Succeeded：Pod中的所有容器都成功停止，并且不会再次重启</li><li>Failed：Pod中的所有容器都已经停止，并且至少有一个容器是以失败停止的（以非0状态退出或被系统强制停止）</li><li>Unknown：由于某种原因无法获得Pod的状态，一般是和Pod所在的Host出现通信问题导致</li></ol><p>Pod phase的查看方式：</p><pre class="language-bash" data-language="bash"><code class="language-bash">kubectl get pods whoami-78c854646d-nhgl9 -o yaml |grep 'phase:'</code></pre><p>输出：</p><pre class="language-bash" data-language="bash"><code class="language-bash">phase: Running</code></pre><h2 id="k8s-lifecycle-用法"><a href="#k8s-lifecycle-用法" class="headerlink" title="k8s lifecycle 用法"></a>k8s lifecycle 用法</h2><p>lifecycle 周期有两个hook钩子 postStart 与 preStop  </p><ol><li>PostStart hook是在容器创建(created)之后立马被调用，并且PostStart跟容器的ENTRYPOINT是异步执行的，无法保证它们之间的顺序.</li><li>PreStop hook是容器处于Terminated状态时立马被调用(也就是说要是Job任务的话，执行完之后其状态为completed，所以不会触发PreStop的钩子)，同时PreStop是同步阻塞的，PreStop执行完才会执行删除Pod的操作</li></ol><p>注意:<br>PostStart 会阻塞容器成为Running状态,PreStop 会阻塞容器的删除，但是过了 terminationGracePeriodSeconds时间后，容器会被强制删除,<br>如果PreStop或者PostStart失败的话, 容器会被杀死;</p><h3 id="钩子的回调函数支持三种方式定义动作："><a href="#钩子的回调函数支持三种方式定义动作：" class="headerlink" title="钩子的回调函数支持三种方式定义动作："></a>钩子的回调函数支持三种方式定义动作：</h3><ol><li>exec：在容器内执行命令，如果命令的退出状态码是 0 表示执行成功，否则表示失败</li></ol><pre class="language-yaml" data-language="yaml"><code class="language-yaml">lifecycle：  postStart:    exec:      command:      - cat      - /tmp/healthy</code></pre><ol start="2"><li>httpGet：向指定 URL 发起 GET 请求，如果返回的 HTTP 状态码在 [200, 400) 之间表示请求成功，否则表示失败</li></ol><pre class="language-yaml" data-language="yaml"><code class="language-yaml">lifecycle：    postStart:      httpGet:        path: /login   # URI地址        port: 80  # 端口号        host: 192.168.126.100 # 主机地址        scheme: HTTP   # 支持的协议，http或https# http://192.168.126.100:80/login</code></pre><ol start="3"><li>TCPSocket：在容器尝试访问指定的socket</li></ol><pre class="language-yaml" data-language="yaml"><code class="language-yaml">lifecycle：  postStart:    tcpSocket:      port: 8080</code></pre><p>Example:</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">apiVersion: apps/v1kind: Deploymentmetadata:  creationTimestamp: null  labels:    app: nginx  name: nginxspec:  replicas: 1  selector:    matchLabels:      app: nginx  strategy: {}  template:    metadata:      creationTimestamp: null      labels:        app: nginx    spec:      containers:        - image: nginx          name: nginx          resources: {}          lifecycle:            postStart:              exec:                command: ["/bin/sh","-c","echo 11 &gt;&gt; /usr/share/nginx/html/index.html"]  # 启动容器应用之后执行            preStop:              exec:                command: ["/bin/sh","-c","echo 'Hello from the preStop handler' &gt;&gt; /var/log/nginx/message"] ## 删除pod 完成之前执行status: {}</code></pre>]]></content>
      
      
      <categories>
          
          <category> k8s </category>
          
      </categories>
      
      
        <tags>
            
            <tag> k8s </tag>
            
            <tag> pods </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ubuntu 安装 nfs</title>
      <link href="/2023/09/13/linux/ubuntu-install-nfs-server/"/>
      <url>/2023/09/13/linux/ubuntu-install-nfs-server/</url>
      
        <content type="html"><![CDATA[<ol><li>安装nfs服务</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">sudo apt install nfs-kernel-server</code></pre><ol start="2"><li>编辑配置文件</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">sudo vim /etc/exports# Example for NFSv4:# /srv/nfs4        gss/krb5i(rw,sync,fsid=0,crossmnt,no_subtree_check)# /srv/nfs4/homes  gss/krb5i(rw,sync,no_subtree_check)#/data/nfs-share *(rw,sync,no_subtree_check,no_root_squash) </code></pre><ol start="3"><li>创建共享目录</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">sudo mkdir -p /data/nfs-share</code></pre><ol start="4"><li>重启nfs服务</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">sudo service nfs-kernel-server restart</code></pre><ol start="5"><li><p>常用命令工具</p><p>已经安装的nfs无需安装客户端</p></li></ol><pre class="language-bash" data-language="bash"><code class="language-bash"># 显示已经mount到本机上sudo showmount -e localhost# 将配置文件中的目录全部重新 Export一次，无需重启sudo exportfs -rv# 查看nfs的运行状态sudo nfsstat#查看rpc执行信息，可以用于检测rpc运行情况sudo rpcinfo#查看网络端口，NFS默认是使用111端口。sudo netstat -tu -4</code></pre><ol start="6"><li>客户端的命令</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash"># 安装客户端命令sudo apt install nfs-common</code></pre><ol start="7"><li>显示指定的（192.168.2.167）NFS服务器上export出来的目录</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">sudo showmount -e 192.168.2.167</code></pre><ol start="8"><li>创建本地挂载目录</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">sudo mkdir -p /mnt/data</code></pre><ol start="9"><li>挂载共享目录</li></ol><pre class="language-bash" data-language="bash"><code class="language-bash">sudo mount -t nfs 192.168.3.167:/data/nfs-share /mnt/data</code></pre>]]></content>
      
      
      <categories>
          
          <category> linux </category>
          
      </categories>
      
      
        <tags>
            
            <tag> nfs </tag>
            
            <tag> storage </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>nfs实现k8s持久化</title>
      <link href="/2023/09/13/k8s/k8s-install-nfs/"/>
      <url>/2023/09/13/k8s/k8s-install-nfs/</url>
      
        <content type="html"><![CDATA[<h3 id="1-部署nfs服务端"><a href="#1-部署nfs服务端" class="headerlink" title="1. 部署nfs服务端"></a>1. 部署nfs服务端</h3><p>在任意一台服务器上安装</p><p>（1）安装nfs服务:</p><pre class="language-bash" data-language="bash"><code class="language-bash">yum install -y nfs-utils rpcbindvim /etc/exports/nfsdata *(rw,no_root_squash,no_all_squash,sync)</code></pre><p>（2）保存配置文件后，执行如下操作：<br>在服务端创建对应的目录和赋予权限：</p><pre class="language-bash" data-language="bash"><code class="language-bash">mkdir /nfsdatachmod 777 /nfsdata</code></pre><p>（3） 启动rpcbind和nfs服务：</p><pre class="language-bash" data-language="bash"><code class="language-bash">systemctl start rpcbind &amp;&amp; systemctl enable rpcbindsystemctl start nfs &amp;&amp; systemctl enable nfs</code></pre><p>（4）每个node安装nfs工具</p><pre class="language-bash" data-language="bash"><code class="language-bash">yum install -y nfs-utils </code></pre><p>（5） 每个node查询NFS服务器</p><pre class="language-bash" data-language="bash"><code class="language-bash">showmount -e nfs-ipshowmount -e 192.168.11.101</code></pre><h4 id="NFS-PersistentVolume"><a href="#NFS-PersistentVolume" class="headerlink" title="NFS PersistentVolume"></a>NFS PersistentVolume</h4><p>（1）下面创建一个 PV mypv1，配置文件 nfs-pv1.yml 如下：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">apiVersion: v1kind: PersistentVolumemetadata:  name: mypv1spec:  capacity:    storage: 1Gi  accessModes:    - ReadWriteOnce  persistentVolumeReclaimPolicy: Recycle  storageClassName: nfs  nfs:    path: /data/NFS/k8s/pv1    server: 192.168.11.101</code></pre><p>① capacity 指定 PV 的容量为 1G。</p><p>② accessModes 指定访问模式为 ReadWriteOnce，支持的访问模式有：</p><p>ReadWriteOnce – PV 能以 read-write 模式 mount 到单个节点。</p><p>ReadOnlyMany – PV 能以 read-only 模式 mount 到多个节点。</p><p>ReadWriteMany – PV 能以 read-write 模式 mount 到多个节点。</p><p>③ persistentVolumeReclaimPolicy 指定当 PV 的回收策略为 Recycle，支持的策略有：</p><p>Retain – 需要管理员手工回收。</p><p>Recycle – 清除 PV 中的数据，效果相当于执行 rm -rf /thevolume/*。</p><p>Delete – 删除 Storage Provider 上的对应存储资源，例如 AWS EBS、GCE PD、Azure Disk、OpenStack Cinder Volume 等。</p><p>④ storageClassName 指定 PV 的 class 为 nfs。相当于为 PV 设置了一个分类，PVC 可以指定 class 申请相应 class 的 PV。</p><p>⑤ 指定 PV 在 NFS 服务器上对应的目录。</p><p>注意要做storage-provider段要提前建好文件夹</p><p>注意要做storage-provider段要提前建好文件夹</p><pre class="language-bash" data-language="bash"><code class="language-bash">[dev@rediscom k8s]$ lspv1</code></pre><p>（2）创建 mypv1：</p><pre class="language-bash" data-language="bash"><code class="language-bash">kubectl apply -f nfs-pv1.yaml</code></pre><p>（3）查看 mypv1：</p><pre class="language-bash" data-language="bash"><code class="language-bash">kubectl get pv</code></pre><p><img src="/medias/loading.gif" data-original="/images/k8s/nfs/16373020145856.jpg" alt="pv"></p><p>STATUS 为 Available，表示 mypv1 就绪，可以被 PVC 申请。</p><h4 id="创建-PVC"><a href="#创建-PVC" class="headerlink" title="创建 PVC"></a>创建 PVC</h4><p>(1）接下来创建 PVC mypvc1，配置文件 nfs-pvc1.yml 如下：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">kind: PersistentVolumeClaimapiVersion: v1metadata:  name: mypvc1spec:  accessModes:    - ReadWriteOnce  resources:    requests:      storage: 1Gi  storageClassName: nfs</code></pre><p>PVC 就很简单了，只需要指定 PV 的容量，访问模式和 class。</p><p>（2）创建 mypvc1：</p><pre class="language-bash" data-language="bash"><code class="language-bash">[root@k8s-master pvc]# kubectl apply -f nfs-pvc1.ymlpersistentvolumeclaim/mypvc1 created[root@k8s-master pvc]# kubectl get pvcNAME     STATUS   VOLUME   CAPACITY   ACCESS MODES   STORAGECLASS   AGEmypvc1   Bound    mypv1    1Gi        RWO            nfs            11s</code></pre><p>从 kubectl get pvc 和 kubectl get pv 的输出可以看到 mypvc1 已经 Bound 到 mypv1，申请成功。</p><h3 id="2-Pod使用pvc"><a href="#2-Pod使用pvc" class="headerlink" title="2 Pod使用pvc"></a>2 Pod使用pvc</h3><p>（1）接下来就可以在 Pod 中使用存储了，Pod 配置文件 pod1.yml 如下：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">apiVersion: v1kind: Podmetadata:  name: mypod1spec:  containers:    - name: mypod1      image: busybox      args:      - /bin/sh      - -c      - sleep 30000      volumeMounts:      - mountPath: "mydata"        name: mydata  volumes:    - name: mydata      persistentVolumeClaim:        claimName: mypvc1</code></pre><p>与使用普通 Volume 的格式类似，在 volumes 中过 persistentVolumeClaim 指定使用 mypvc1 申请的 Volume。</p><p>（2）创建 mypod1：</p><pre class="language-bash" data-language="bash"><code class="language-bash">[root@k8s-master pod]# kubectl apply -f pod1.yamlpod/mypod1 created[root@k8s-master pod]# kubectl get pod -o wideNAME                     READY   STATUS              RESTARTS   AGE     IP              NODE        NOMINATED NODE   READINESS GATESmypod1                   0/1     ContainerCreating   0          20s     &lt;none&gt;          k8s-node4   &lt;none&gt;           &lt;none&gt;nginx-58dcb77688-h8c5x   1/1     Running             0          3d23h   172.31.156.68   k8s-node1   &lt;none&gt;           &lt;none&gt;</code></pre><p>（3）验证 PV 是否可用：</p><pre class="language-bash" data-language="bash"><code class="language-bash">kubectl exec mypod1 touch /mydata/hello</code></pre><p>查看生成文件</p><pre class="language-bash" data-language="bash"><code class="language-bash">[root@rediscom ~]# cd /data/NFS/k8s/pv1/[root@rediscom pv1]# lshello</code></pre><p>可见，在 Pod 中创建的文件 /mydata/hello 确实已经保存到了 NFS 服务器目录 /nfsdata/pv1 中。</p><p>如果不再需要使用 PV，可用删除 PVC 回收 PV。</p><h3 id="3-回收-PV"><a href="#3-回收-PV" class="headerlink" title="3. 回收 PV"></a>3. 回收 PV</h3><p>通过pvc回收pv<br>（1）当 PV 不再需要时，可通过删除 PVC 回收。</p><pre class="language-bash" data-language="bash"><code class="language-bash">[root@k8s-master pvc]# kubectl get pvcNAME     STATUS   VOLUME   CAPACITY   ACCESS MODES   STORAGECLASS   AGEmypvc1   Bound    mypv1    1Gi        RWO            nfs            22m# 删除pvc[root@k8s-master pvc]# kubectl delete pvc mypvc1persistentvolumeclaim "mypvc1" deleted</code></pre><p>当 PVC mypvc1 被删除后，发现 Kubernetes 启动了一个新 Pod，这个 Pod 的作用就是清除 PV mypv1 的数据。这个时候再看 NFS 服务器目录 /data/NFS/k8s/pv1/ 中已经没有数据了</p><p><img src="/medias/loading.gif" data-original="/images/k8s/nfs/16373060619523.jpg" alt="data1"></p><p><strong>注意:</strong> 删除 mypvc1 时之前要停止与它相关的 pod ，不然 mypvc1一直处于 Terminating 状态</p><p>（2）当数据清除完毕，mypv1 的状态重新变为 Available，此时则可以被新的 PVC 申请。</p><pre class="language-bash" data-language="bash"><code class="language-bash">[root@k8s-master pvc]# kubectl get pvNAME    CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS      CLAIM   STORAGECLASS   REASON   AGEmypv1   1Gi        RWO            Recycle          Available           nfs                     67m</code></pre><p>（3）因为 PV 的回收策略设置为 Recycle，所以数据会被清除，但这可能不是我们想要的结果。如果我们希望保留数据，可以将策略设置为 Retain。</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">apiVersion: v1kind: PersistentVolumemetadata:  name: mypv1spec:  capacity:    storage: 1Gi  accessModes:    - ReadWriteOnce  persistentVolumeReclaimPolicy: Retain  storageClassName: nfs  nfs:    path: /data/NFS/k8s/pv1    server: 192.168.11.101</code></pre><p>（4）通过 kubectl apply 更新 PV：</p><pre class="language-bash" data-language="bash"><code class="language-bash">[root@k8s-master pv]# kubectl apply -f nfs-pv1.yamlpersistentvolume/mypv1 configured[root@k8s-master pv]# kubectl get pvNAME    CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS      CLAIM   STORAGECLASS   REASON   AGEmypv1   1Gi        RWO            Retain           Available           nfs                     81m[root@k8s-master pv]#</code></pre><p>（5）回收策略已经变为 Retain，通过下面步骤验证其效果：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">[root@k8s-master pvc]# kubectl exec mypod1 touch /mydata/hellokubectl exec [POD] [COMMAND] is DEPRECATED and will be removed in a future version. Use kubectl exec [POD] -- [COMMAND] instead.error: unable to upgrade connection: container not found ("mypod1")[root@k8s-master pvc]# kubectl get podNAME                     READY   STATUS    RESTARTS   AGEmypod1                   1/1     Running   0          27snginx-58dcb77688-h8c5x   1/1     Running   0          4d[root@k8s-master pvc]# kubectl exec mypod1 touch /mydata/hellokubectl exec [POD] [COMMAND] is DEPRECATED and will be removed in a future version. Use kubectl exec [POD] -- [COMMAND] instead.[root@k8s-master pvc]# kubectl delete -f ../pod/pod1.yamlpod "mypod1" deleted[root@k8s-master pvc]# kubectl get pvcNAME     STATUS   VOLUME   CAPACITY   ACCESS MODES   STORAGECLASS   AGEmypvc1   Bound    mypv1    1Gi        RWO            nfs            3m48s[root@k8s-master pvc]# kubectl delete pvc mypvc1persistentvolumeclaim "mypvc1" deleted[root@k8s-master pvc]# kubectl get pvcNo resources found in default namespace.[root@k8s-master pvc]#</code></pre><p>（6）我们再看 NFS 服务器目录 /nfsdata/pv1 中数据还保留。</p><pre class="language-bash" data-language="bash"><code class="language-bash">[root@rediscom pv1]# lshello[root@rediscom pv1]#</code></pre><p>（7）虽然 mypv1 中的数据得到了保留，但其 PV 状态会一直处于 Released，不能被其他 PVC 申请。</p><pre class="language-bash" data-language="bash"><code class="language-bash">[root@k8s-master pvc]# kubectl get pvNAME    CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS     CLAIM            STORAGECLASS   REASON   AGEmypv1   1Gi        RWO            Retain           Released   default/mypvc1   nfs                     89m</code></pre><p>8）为了重新使用存储资源，可以删除并重新创建 mypv1。删除操作只是删除了 PV 对象，存储空间中的数据并不会被删除。</p><p>新建的 mypv1 状态为 Available，已经可以被 PVC 申请。</p><pre class="language-bash" data-language="bash"><code class="language-bash">[root@k8s-master pvc]# kubectl delete pv mypv1[root@k8s-master pvc]# ubectl apply -f nfs-pv1.yaml[root@k8s-master pvc]# kubectl get pvNAME    CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS      CLAIM   STORAGECLASS   REASON   AGEmypv1   1Gi        RWO            Retain           Available           nfs                     2s[root@k8s-master pvc]#</code></pre><p>PV 还支持 Delete 的回收策略，会删除 PV 在 Storage Provider 上对应存储空间。NFS 的 PV 不支持 Delete，支持 Delete 的 Provider 有 AWS EBS、GCE PD、Azure Disk、OpenStack Cinder Volume 等</p><h3 id="4-PV的动态供给"><a href="#4-PV的动态供给" class="headerlink" title="4.  PV的动态供给"></a>4.  PV的动态供给</h3><p>pv的供给方式</p><p>前面的例子中，我们提前创建了 PV，然后通过 PVC 申请 PV 并在 Pod 中使用，这种方式叫做静态供给（Static Provision）。</p><p>与之对应的是动态供给（Dynamical Provision），即如果没有满足 PVC 条件的 PV，会动态创建 PV。相比静态供给，动态供给有明显的优势：不需要提前创建 PV，减少了管理员的工作量，效率高。</p><p>动态供给是通过 StorageClass 实现的，StorageClass 定义了如何创建 PV</p><p>pv动态供给举例<br>StorageClass standard：</p><p><img src="/medias/loading.gif" data-original="/images/k8s/nfs/16373075330215.jpg" alt="standard"></p><p>StorageClass slow：</p><p><img src="/medias/loading.gif" data-original="/images/k8s/nfs/16373075453270.jpg" alt="slow"></p><p>这两个 StorageClass 都会动态创建 AWS EBS，不同在于 standard 创建的是 gp2 类型的 EBS，而 slow 创建的是 io1 类型的 EBS。不同类型的 EBS 支持的参数可参考 AWS 官方文档。</p><p>StorageClass 支持 Delete 和 Retain 两种 reclaimPolicy，默认是 Delete。</p><p>与之前一样，PVC 在申请 PV 时，只需要指定 StorageClass 和容量以及访问模式，比如：</p> <pre class="language-yaml" data-language="yaml"><code class="language-yaml"> kind: PersistentVolumeClaimapiVersion: v1metadata:  name: mypvc1spec:  accessModes:    - ReadWriteOnce  resources:    requests:      storage: 1Gi  storageClassName: standard</code></pre><p>除了 AWS EBS，Kubernetes 支持其他多种动态供给 PV 的 Provisioner，完整列表请参考 <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/#provisioner">https://kubernetes.io/docs/concepts/storage/storage-classes/#provisioner</a></p>]]></content>
      
      
      <categories>
          
          <category> k8s </category>
          
      </categories>
      
      
        <tags>
            
            <tag> k8s </tag>
            
            <tag> nfs </tag>
            
            <tag> storage </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>traefik 简介</title>
      <link href="/2023/09/13/traefik/treafik-introduce/"/>
      <url>/2023/09/13/traefik/treafik-introduce/</url>
      
        <content type="html"><![CDATA[<h3 id="1-官方文档"><a href="#1-官方文档" class="headerlink" title="1. 官方文档"></a>1. 官方文档</h3><p><a href="https://doc.traefik.io/traefik/">https://doc.traefik.io/traefik/</a></p><h3 id="2-简介"><a href="#2-简介" class="headerlink" title="2. 简介"></a>2. 简介</h3><p>traefik是一个使你把微服务暴露出来变的更容易的http反向代理和负载均衡软件。traefik支持K8S、docker swarm、mesos、consul、etcd、zookeeper等基础设施组件，动态的应用它的配置文件设置。</p><h3 id="3-流量示意图"><a href="#3-流量示意图" class="headerlink" title="3. 流量示意图"></a>3. 流量示意图</h3><p><img src="/medias/loading.gif" data-original="http://file.longqiuhong.com/uploads/picgo/treafik-flow.png" alt="treafik-flow"></p><p>代理入口</p><p><img src="/medias/loading.gif" data-original="https://file.longqiuhong.com/uploads/picgo/route-flow.png" alt="route-flow"></p><p>路由：</p><p><img src="/medias/loading.gif" data-original="http://file.longqiuhong.com/uploads/picgo/routers.png" alt="routers"></p><h3 id="4-核心概念"><a href="#4-核心概念" class="headerlink" title="4. 核心概念"></a>4. 核心概念</h3><p>当启动Traefik时，需要定义<code>entrypoints</code>，然后通过entrypoints的路由来分析传入的请求，来查看他们是否是一组规则匹配，如果匹配，则路由可能将请求通过一系列的转换过来在发送到服务上去。</p><p><img src="/medias/loading.gif" data-original="https://file.longqiuhong.com/uploads/picgo/agence.png" alt="agence"></p><ul><li><code>Providers</code> 是基础组件，traefik的配置发现是通过它来实现，它可以是协调器，容器引擎，云提供商或键值存储，通过查询 Providers 的API来查询路由的相关信息，一旦检查变化，就会动态更新路由</li><li><code>Entrypoints</code>监听传入的流量，是网络的入口点，定义了接受请求的端口(HTTP或者TCP)</li><li><code>Routers</code>分析请求(host,path,headers,SSL等)，负责将传入的请求连接到可以处理这些请求的服务上去</li><li><code>Service</code>将请求转发给应用，负责配置如何最终将处理传入请求的实际服务</li><li><code>Middlewares</code>中间件，用来修改请求或者根据请求来做出判断，中间件被附件到路由上，是一种在请求发送到服务之前调整请求的一种方法</li></ul><h3 id="5-路由规则"><a href="#5-路由规则" class="headerlink" title="5. 路由规则"></a>5. 路由规则</h3><p>路由类型分为三种，分别为：<code>http</code>、<code>tcp</code>、<code>udp</code></p><p>路由规则是指，Traefik接收到的请求，根据给定规则路由到不同的服务中。</p><table><thead><tr><th>Rule</th><th>Description</th></tr></thead><tbody><tr><td>Headers(<code>key</code>, <code>value</code>)</td><td>Check if there is a key keydefined in the headers, with the value value</td></tr><tr><td>HeadersRegexp(<code>key</code>, <code>regexp</code>)</td><td>Check if there is a key keydefined in the headers, with a value that matches the regular expression regexp</td></tr><tr><td>Host(<code>example.com</code>, …)</td><td>Check if the request domain (host header value) targets one of the given domains.</td></tr><tr><td>HostHeader(<code>example.com</code>, …)</td><td>Check if the request domain (host header value) targets one of the given domains.</td></tr><tr><td>HostRegexp(<code>example.com</code>, <code>{subdomain:[a-z]+}.example.com</code>, …)</td><td>Check if the request domain matches the given regexp.</td></tr><tr><td>Method(<code>GET</code>, …)</td><td>Check if the request method is one of the given methods (GET, POST, PUT, DELETE, PATCH, HEAD)</td></tr><tr><td>Path(<code>/path</code>, <code>/articles/{cat:[a-z]+}/{id:[0-9]+}</code>, …)</td><td>Match exact request path. It accepts a sequence of literal and regular expression paths.</td></tr><tr><td>PathPrefix(<code>/products/</code>, <code>/articles/{cat:[a-z]+}/{id:[0-9]+}</code>)</td><td>Match request prefix path. It accepts a sequence of literal and regular expression prefix paths.</td></tr><tr><td>Query(<code>foo=bar</code>, <code>bar=baz</code>)</td><td>Match Query String parameters. It accepts a sequence of key=value pairs.</td></tr><tr><td>ClientIP(<code>10.0.0.0/16</code>, <code>::1</code>)</td><td>Match if the request client IP is one of the given IP/CIDR. It accepts IPv4, IPv6 and CIDR formats</td></tr></tbody></table><p>这个正则配起来稍微有点小坑</p><p>为了对<code>Host</code>和<code>Path</code>使用正则表达式，需要声明一个任意命名的变量，然后跟上用冒号分隔的正则表达式，所有这些都用花括号括起来。</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">HostRegexp(`grafana.{domain:.*}`)</code></pre><h3 id="6-服务"><a href="#6-服务" class="headerlink" title="6. 服务"></a>6. 服务</h3><p><img src="/medias/loading.gif" data-original="https://file.longqiuhong.com/uploads/picgo/services.png" alt="services"></p><p>服务负责配置如何到达实际的服务，最终将处理传入的请求。使用<code>service</code>定义：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">http:  services:    traefik:      loadBalancer:        servers:          - url: "http://127.0.0.1:10000"</code></pre><p>​更多配置，可以参考：<a href="https://doc.traefik.io/traefik/routing/services/">官网介绍</a></p><h3 id="7-中间件"><a href="#7-中间件" class="headerlink" title="7. 中间件"></a>7. 中间件</h3><p><img src="/medias/loading.gif" data-original="http://file.longqiuhong.com/uploads/picgo/middleware.png" alt="middleware"></p><p>从图中基本可以明白中间件的作用，也可以理解成拦截器。<br>Traefik中有几种可用的中间件：一些可以修改请求、请求头，一些负责重定向，一些可以添加身份验证等等。</p><p>下面是一个官网给出的示例：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml"># As YAML Configuration Filehttp:  routers:    router1:      service: myService      middlewares:        - "foo-add-prefix"      rule: "Host(`example.com`)"  middlewares:    foo-add-prefix:      addPrefix:        prefix: "/foo"  services:    service1:      loadBalancer:        servers:          - url: "http://127.0.0.1:80"</code></pre><h3 id="参考连接"><a href="#参考连接" class="headerlink" title="参考连接"></a>参考连接</h3><ul><li><a href="https://github.com/traefik/traefik">Traefik源码仓库</a></li><li><a href="https://traefik.io/">Traefik官网</a></li><li><a href="https://doc.traefik.io/traefik/reference/static-configuration/file/">Traefik静态配置项-File provider</a></li><li><a href="https://letsencrypt.org/">Let’s Encrypt</a></li><li><a href="https://www.freedesktop.org/software/systemd/man/systemd.unit.html">Systemd文档</a></li><li><a href="https://ormissia.github.io/posts/deployment/3003-linux-traefik/">ormissia blog</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> traefik </category>
          
      </categories>
      
      
        <tags>
            
            <tag> k8s </tag>
            
            <tag> traefik </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>InitContainers模式</title>
      <link href="/2023/09/09/k8s/initcontainers-mo-shi/"/>
      <url>/2023/09/09/k8s/initcontainers-mo-shi/</url>
      
        <content type="html"><![CDATA[<h1 id="initContainer使用"><a href="#initContainer使用" class="headerlink" title="initContainer使用"></a>initContainer使用</h1><h2 id="介绍："><a href="#介绍：" class="headerlink" title="介绍："></a>介绍：</h2><p>init容器是在同一个Pod中的其他容器之前启动和执行的容器。它的目的是<strong>为Pod上托管的主应用程序执行初始化逻辑</strong>。例如，创建必要的用户帐户、执行数据库迁移、创建数据库模式等等。</p><h2 id="Init-Containers-注意事项"><a href="#Init-Containers-注意事项" class="headerlink" title="Init Containers 注意事项"></a>Init Containers 注意事项</h2><p>在创建 init 容器时，应该考虑一些注意事项：</p><ol><li><p>它们总是在 Pod 中的其他容器之前执行。因此，它们不应包含需要很长时间才能完成的复杂逻辑。启动脚本通常小而简洁。如果您发现向 init 容器添加了太多逻辑，则应考虑将其中的一部分移至应用程序容器本身。</p></li><li><p>init 容器按顺序启动和执行。除非成功完成其前任容器，否则不会调用 init 容器。因此，如果启动任务很长，您可以考虑将其分解为多个步骤，每个步骤由一个 init 容器处理，以便您知道哪些步骤失败。</p></li><li><p>如果任何 init 容器失败，整个 Pod 将重新启动（除非您将 restartPolicy 设置为 Never）。重新启动 Pod 意味着再次重新执行所有容器，包括任何 init 容器。因此，您可能需要确保启动逻辑可以容忍多次执行而不会导致重复。例如，如果数据库迁移已经完成，再次执行迁移命令应该被忽略。</p></li><li><p>init 容器是延迟应用程序初始化直到一个或多个依赖项可用的良好候选者。例如，如果您的应用程序依赖于强加 API 请求速率限制的 API，您可能需要等待特定时间段才能接收来自该 API 的响应。在应用程序容器中实现这个逻辑可能很复杂；因为它需要与健康和准备探测器相结合。一个更简单的方法是创建一个 init 容器，它会等待 API 准备就绪后再成功退出。只有在 init 容器成功完成其工作后，应用程序容器才会启动。</p></li><li><p>Init 容器不能像应用程序容器那样使用健康和就绪探测。原因是它们旨在成功启动和退出，就像 Jobs 和 CronJobs 的行为方式一样。</p></li><li><p>同一个 Pod 上的所有容器共享相同的 Volumes 和网络。您可以利用此功能在应用程序及其初始化容器之间共享数据。</p></li></ol><h2 id="Init容器“Request”和“Limits”行为"><a href="#Init容器“Request”和“Limits”行为" class="headerlink" title="Init容器“Request”和“Limits”行为"></a>Init容器“Request”和“Limits”行为</h2><p>init 容器总是在同一个 Pod 上的其他应用程序容器之前启动。因此，调度程序对 init 容器的资源和限制给予更高的优先级。必须彻底考虑此类行为，因为它可能会导致不希望的结果。例如，如果你有一个 init 容器和一个应用程序容器，并且你将 init 容器的资源和限制设置为高于应用程序容器的资源和限制，那么只有在有一个满足 init 的可用节点时，整个 Pod 才会被调度容器要求。换句话说，即使有一个未使用的节点可以运行应用程序容器，如果 init 容器具有该节点可以处理的更高资源先决条件，Pod 也不会部署到该节点。因此，在定义 init 容器的请求和限制时，您应该尽可能严格。作为最佳实践，除非绝对需要，否则不要将这些参数设置为高于应用程序容器的值</p><h5 id="应用场景01：为数据库做种"><a href="#应用场景01：为数据库做种" class="headerlink" title="应用场景01：为数据库做种"></a>应用场景01：为数据库做种</h5><p>在这个场景中，我们为 MySQL 数据库提供服务。该数据库用于测试应用程序。它不必包含真实数据，但必须填充足够的数据，以便我们可以测试应用程序的查询速度。我们使用 init 容器来处理 SQL 转储文件的下载并将其恢复到托管在另一个容器中的数据库。</p><p>定义文件可能如下所示：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">apiVersion: v1kind: Podmetadata:  name: mydb  labels:    app: dbspec:  initContainers:    - name: fetch      image: mwendler/wget      command: ["wget","--no-check-certificate","https://sample-videos.com/sql/Sample-SQL-File-1000rows.sql","-O","/docker-entrypoint-initdb.d/dump.sql"]      volumeMounts:        - mountPath: /docker-entrypoint-initdb.d          name: dump  containers:    - name: mysql      image: mysql      env:        - name: MYSQL_ROOT_PASSWORD          value: "example"      volumeMounts:        - mountPath: /docker-entrypoint-initdb.d          name: dump  volumes:    - emptyDir: {}      name: dump</code></pre><p>上面的定义创建了一个 Pod，它托管着两个容器：init 容器和应用程序容器。让我们来看看这个定义的有趣方面：</p><ol><li>init 容器负责下载包含数据库转储的 SQL 文件。我们使用 mwendler/wget 镜像，因为我们只需要 wget 命令。</li><li>下载的 SQL 的目标目录是 MySQL 镜像用来执行 SQL 文件的目录（/docker-entrypoint-initdb.d）。此行为内置于我们在应用程序容器中使用的 MySQL 镜像中。</li><li>init 容器将/docker-entrypoint-initdb.d挂载到emptyDir卷。因为两个容器都托管在同一个 Pod 上，所以它们共享相同的卷。因此，数据库容器可以访问放置在 emptyDir 卷上的 SQL 文件。</li></ol><h5 id="如果没有使用-InitContainers会发生什么？"><a href="#如果没有使用-InitContainers会发生什么？" class="headerlink" title="如果没有使用 InitContainers会发生什么？"></a>如果没有使用 InitContainers会发生什么？</h5><p>在这个例子中，我们使用初始化模式来建立关注点分离的最佳实践。如果我们在不使用 init 模式的情况下实现相同的逻辑，我们必须基于 mysql 基础镜像创建一个新镜像，安装 wget，并使用它来下载 SQL 文件。这种方法的缺点是：</p><p>如果我们需要对下载逻辑进行任何更改，我们需要创建一个新镜像，推送它并更改其在定义文件中的引用。这增加了必须维护自定义镜像的负担。</p><p>它在 DB 容器与其启动逻辑之间创建了紧密耦合的关系，这使得应用程序更难管理并增加了引入错误和错误的可能性。</p><h2 id="场景-02：延迟应用程序启动，直到依赖项准备就绪"><a href="#场景-02：延迟应用程序启动，直到依赖项准备就绪" class="headerlink" title="场景 02：延迟应用程序启动，直到依赖项准备就绪"></a>场景 02：延迟应用程序启动，直到依赖项准备就绪</h2><p>init 容器的另一个常见用例是当您需要应用程序等待另一个服务完全运行（响应请求）时。以下定义演示了这种情况：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">apiVersion: v1kind: Podmetadata:  name: myapp-pod  labels:    app: myappspec:  initContainers:  - name: init-myservice    image: busybox:1.28    command: ['sh', '-c', 'until nslookup myservice; do echo waiting for myservice; sleep 2; done;']  containers:  - name: myapp-container    image: busybox:1.28    command: ['sh', '-c', 'echo The app is running! &amp;&amp; sleep 3600']</code></pre><p>因此，假设我们的应用程序在 myapp-container 上运行时无法正常运行，除非 myservice 应用程序正在运行。我们需要延迟 myapp 启动，直到 myservice 准备就绪。为此，我们使用一个简单的 nslookup 命令（第 11 行）不断检查“myservice”的名称解析是否成功。如果 nslookup 能够解析“myservice”，则服务将启动。使用成功退出代码，init 容器终止，让应用程序容器启动。否则，容器在再次尝试之前会休眠两秒钟，从而延迟应用程序容器的启动。</p><p>为了完整起见，这是 myservice 的定义文件：</p><pre class="language-yaml" data-language="yaml"><code class="language-yaml">apiVersion: v1kind: Servicemetadata:  name: myservicespec:  ports:  - protocol: TCP    port: 80    targetPort: 9376</code></pre><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>初始化模式是设计需要启动逻辑的应用程序时要遵循的重要实践。</p><p>Kubernetes 提供 init 容器作为将应用程序逻辑与其启动过程分离的一种手段。</p><p>将应用程序初始化逻辑放在 init 容器中具有许多优点：</p><ol><li><p>您将强加关注点分离原则。一个应用程序可以有它的工程师团队，而它的初始化逻辑是由另一个团队编写的。</p></li><li><p>当涉及到授权和访问控制时，拥有一个单独的团队来处理应用程序的初始化步骤可以使公司更加灵活。例如，如果启动应用程序需要使用需要安全许可的资源（例如，修改防火墙规则），则可以由具有合适凭据的人员完成。应用团队不参与操作。</p></li><li><p>如果涉及的初始化步骤太多，可以将它们分解成多个 init 容器依次执行。如果一个步骤失败，init 容器会报告错误，这可以让您更好地了解逻辑的哪一部分不成功。</p></li></ol><p>使用 init 容器时应考虑以下几点：</p><ol><li><p>初始化容器在失败时重新启动。因此，他们的代码必须是幂等的。</p></li><li><p>初始化容器是请求和限制首先由调度程序检查。不正确的值可能会对调度程序关于放置整个 Pod（包括应用程序容器）的位置的决定产生负面影响。</p></li></ol>]]></content>
      
      
      <categories>
          
          <category> k8s </category>
          
      </categories>
      
      
        <tags>
            
            <tag> k8s </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSI--容器储存接口</title>
      <link href="/2023/09/03/k8s/csi-fen-xi-rong-qi-chu-cun-jie-kou/"/>
      <url>/2023/09/03/k8s/csi-fen-xi-rong-qi-chu-cun-jie-kou/</url>
      
        <content type="html"><![CDATA[<h2 id="1-CSI简介–-储存接口"><a href="#1-CSI简介–-储存接口" class="headerlink" title="1. CSI简介– 储存接口"></a>1. CSI简介– 储存接口</h2><p>  k8s 的设计理念是支持可插拔架构，从而有利用扩展k8s 的功能。在次思想下，k8s 提供了 3 个特定功能得到接口，分别是：容器网络接口（CNI）、容器运行时接口（CRI）、容器储存接口（CSI）。本文主要介绍容器储存接口（CSI）。</p>]]></content>
      
      
      <categories>
          
          <category> k8s </category>
          
      </categories>
      
      
        <tags>
            
            <tag> k8s </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>go-redis 教程</title>
      <link href="/2023/01/30/go-redis-jiao-cheng/"/>
      <url>/2023/01/30/go-redis-jiao-cheng/</url>
      
        <content type="html"><![CDATA[<h2 id="安装go-redis包"><a href="#安装go-redis包" class="headerlink" title="安装go-redis包"></a>安装go-redis包</h2><pre class="language-bash" data-language="bash"><code class="language-bash">go get github.com/redis/go-redis/v9</code></pre><h2 id="连接-redis-服务"><a href="#连接-redis-服务" class="headerlink" title="连接 redis 服务"></a>连接 redis 服务</h2><ol><li>使用 options 方式</li></ol><pre class="language-go" data-language="go"><code class="language-go">package mainimport "github.com/redis/go-redis/v9"func main(){    rdb := redis.NewClient(&amp;redis.Options{    Addr:  "localhost:6379",    Password: "", // no password set    DB:  0,  // use default DB    })    res, err := rdb.Ping(ctx).Result()if err != nil {panic(err)}fmt.Println(res)}</code></pre><ol start="2"><li>使用 dns 字符串方式</li></ol><pre class="language-go" data-language="go"><code class="language-go">package mainimport "github.com/redis/go-redis/v9"func main(){    // 注意 redis 是没有用户的，所以user 设置为空     opt, err := redis.ParseURL("redis://&lt;user&gt;:&lt;pass&gt;@localhost:6379/&lt;db&gt;")    if err != nil {        panic(err)    }    rdb := redis.NewClient(opt)    res, err := rdb.Ping(ctx).Result()if err != nil {panic(err)}fmt.Println(res)}</code></pre><ol start="3"><li>连接 redis 服务设置连接名字</li></ol><pre class="language-go" data-language="go"><code class="language-go">package mainimport "github.com/redis/go-redis/v9"func main(){    // 注意 redis 是没有用户的，所以user 设置为空     opt, err := redis.ParseURL("redis://&lt;user&gt;:&lt;pass&gt;@localhost:6379/&lt;db&gt;")    if err != nil {        panic(err)    }    rdb := redis.NewClient(opt)    res, err := rdb.Ping(ctx).Result()if err != nil {panic(err)}fmt.Println(res)}</code></pre><h2 id="redis-数据类型"><a href="#redis-数据类型" class="headerlink" title="redis 数据类型"></a>redis 数据类型</h2><p>redis 的数据类型有五种，分别为: 字符串(string)、列表(list)、集合(set)、哈希(hash)、有序集合(zset)</p><h3 id="操作字符串-string"><a href="#操作字符串-string" class="headerlink" title="操作字符串 (string)"></a>操作字符串 (string)</h3><ol><li>获取字符串内容执行命令</li></ol><p>redis 命令</p><pre class="language-redis" data-language="redis"><code class="language-redis">get mykey</code></pre><p>直接返回结果</p><pre class="language-go" data-language="go"><code class="language-go">key := "mykey"ctx := context.Background()val, err := rdb.Get(ctx, key).Result()if err != nil {    panic(err)}fmt.Println(val)</code></pre><p>保存命令并稍后分别访问值与错误</p><pre class="language-go" data-language="go"><code class="language-go">key := "mykey"ctx := context.Background()get := rdb.Get(ctx, key)fmt.Println(get.Val())</code></pre><p>执行go-redis包不支持的命令</p><pre class="language-go" data-language="go"><code class="language-go">key := "mykey"ctx := context.Background()val, err := rdb.Do(ctx, "get", key).Result()if err != nil {if err == redis.Nil {fmt.Println("key dose not exists")return}panic(err)}fmt.Println(val)</code></pre><pre class="language-go" data-language="go"><code class="language-go">key := "mykey"ctx := context.Background()val, err := rdb.Do(ctx, "get", key).Text()fmt.Println(val, err)</code></pre><p>帮助函数完整列表:</p><pre class="language-go" data-language="go"><code class="language-go">s, err := cmd.Text()flag, err := cmd.Bool()num, err := cmd.Int()num, err := cmd.Int64()num, err := cmd.Uint64()num, err := cmd.Float32()num, err := cmd.Float64()ss, err := cmd.StringSlice()ns, err := cmd.Int64Slice()ns, err := cmd.Uint64Slice()fs, err := cmd.Float32Slice()fs, err := cmd.Float64Slice()bs, err := cmd.BoolSlice()</code></pre><ol start="2"><li>设置字符串数据</li></ol><p>redis 命令</p><pre class="language-redis" data-language="redis"><code class="language-redis">set mykey "ddd" EX 10 </code></pre><p>go-redis </p><pre class="language-go" data-language="go"><code class="language-go">key := "mykey"ctx := context.Background()cmd := rdb.Set(ctx, key, "ddd", 10)   fmt.Println(cmd.Result())</code></pre><p>设置key到期的时间</p><pre class="language-go" data-language="go"><code class="language-go">expireAt := time.Now().AddDate(1, 1, 1)args := redis.SetArgs{ExpireAt: expireAt}fmt.Println(args)ctx := context.Background()err := rdb.SetArgs(ctx, "key", "hello", args).Err()if err != nil {panic(err)}</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> go </tag>
            
            <tag> redis </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>go-redis 教程</title>
      <link href="/2023/01/30/traefik/go-redis-jiao-cheng/"/>
      <url>/2023/01/30/traefik/go-redis-jiao-cheng/</url>
      
        <content type="html"><![CDATA[<h2 id="安装go-redis包"><a href="#安装go-redis包" class="headerlink" title="安装go-redis包"></a>安装go-redis包</h2><pre class="language-bash" data-language="bash"><code class="language-bash">go get github.com/redis/go-redis/v9</code></pre><h2 id="连接-redis-服务"><a href="#连接-redis-服务" class="headerlink" title="连接 redis 服务"></a>连接 redis 服务</h2><ol><li>使用 options 方式</li></ol><pre class="language-go" data-language="go"><code class="language-go">package mainimport "github.com/redis/go-redis/v9"func main(){    rdb := redis.NewClient(&amp;redis.Options{    Addr:  "localhost:6379",    Password: "", // no password set    DB:  0,  // use default DB    })    res, err := rdb.Ping(ctx).Result()if err != nil {panic(err)}fmt.Println(res)}</code></pre><ol start="2"><li>使用 dns 字符串方式</li></ol><pre class="language-go" data-language="go"><code class="language-go">package mainimport "github.com/redis/go-redis/v9"func main(){    // 注意 redis 是没有用户的，所以user 设置为空     opt, err := redis.ParseURL("redis://&lt;user&gt;:&lt;pass&gt;@localhost:6379/&lt;db&gt;")    if err != nil {        panic(err)    }    rdb := redis.NewClient(opt)    res, err := rdb.Ping(ctx).Result()if err != nil {panic(err)}fmt.Println(res)}</code></pre><ol start="3"><li>连接 redis 服务设置连接名字</li></ol><pre class="language-go" data-language="go"><code class="language-go">package mainimport "github.com/redis/go-redis/v9"func main(){    // 注意 redis 是没有用户的，所以user 设置为空     opt, err := redis.ParseURL("redis://&lt;user&gt;:&lt;pass&gt;@localhost:6379/&lt;db&gt;")    if err != nil {        panic(err)    }    rdb := redis.NewClient(opt)    res, err := rdb.Ping(ctx).Result()if err != nil {panic(err)}fmt.Println(res)}</code></pre><h2 id="redis-数据类型"><a href="#redis-数据类型" class="headerlink" title="redis 数据类型"></a>redis 数据类型</h2><p>redis 的数据类型有五种，分别为: 字符串(string)、列表(list)、集合(set)、哈希(hash)、有序集合(zset)</p><h3 id="操作字符串-string"><a href="#操作字符串-string" class="headerlink" title="操作字符串 (string)"></a>操作字符串 (string)</h3><ol><li>获取字符串内容执行命令</li></ol><p>redis 命令</p><pre class="language-redis" data-language="redis"><code class="language-redis">get mykey</code></pre><p>直接返回结果</p><pre class="language-go" data-language="go"><code class="language-go">key := "mykey"ctx := context.Background()val, err := rdb.Get(ctx, key).Result()if err != nil {    panic(err)}fmt.Println(val)</code></pre><p>保存命令并稍后分别访问值与错误</p><pre class="language-go" data-language="go"><code class="language-go">key := "mykey"ctx := context.Background()get := rdb.Get(ctx, key)fmt.Println(get.Val())</code></pre><p>执行go-redis包不支持的命令</p><pre class="language-go" data-language="go"><code class="language-go">key := "mykey"ctx := context.Background()val, err := rdb.Do(ctx, "get", key).Result()if err != nil {if err == redis.Nil {fmt.Println("key dose not exists")return}panic(err)}fmt.Println(val)</code></pre><pre class="language-go" data-language="go"><code class="language-go">key := "mykey"ctx := context.Background()val, err := rdb.Do(ctx, "get", key).Text()fmt.Println(val, err)</code></pre><p>帮助函数完整列表:</p><pre class="language-go" data-language="go"><code class="language-go">s, err := cmd.Text()flag, err := cmd.Bool()num, err := cmd.Int()num, err := cmd.Int64()num, err := cmd.Uint64()num, err := cmd.Float32()num, err := cmd.Float64()ss, err := cmd.StringSlice()ns, err := cmd.Int64Slice()ns, err := cmd.Uint64Slice()fs, err := cmd.Float32Slice()fs, err := cmd.Float64Slice()bs, err := cmd.BoolSlice()</code></pre><ol start="2"><li>设置字符串数据</li></ol><p>redis 命令</p><pre class="language-redis" data-language="redis"><code class="language-redis">set mykey "ddd" EX 10 </code></pre><p>go-redis </p><pre class="language-go" data-language="go"><code class="language-go">key := "mykey"ctx := context.Background()cmd := rdb.Set(ctx, key, "ddd", 10)   fmt.Println(cmd.Result())</code></pre><p>设置key到期的时间</p><pre class="language-go" data-language="go"><code class="language-go">expireAt := time.Now().AddDate(1, 1, 1)args := redis.SetArgs{ExpireAt: expireAt}fmt.Println(args)ctx := context.Background()err := rdb.SetArgs(ctx, "key", "hello", args).Err()if err != nil {panic(err)}</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> go </tag>
            
            <tag> redis </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2023/01/28/hello-world/"/>
      <url>/2023/01/28/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><pre class="language-bash" data-language="bash"><code class="language-bash">$ hexo new "My New Post"</code></pre><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><pre class="language-bash" data-language="bash"><code class="language-bash">$ hexo server</code></pre><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><pre class="language-bash" data-language="bash"><code class="language-bash">$ hexo generate</code></pre><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><pre class="language-bash" data-language="bash"><code class="language-bash">$ hexo deploy</code></pre><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
      
      
      <categories>
          
          <category> hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>git 安装</title>
      <link href="/2023/01/28/git/git/"/>
      <url>/2023/01/28/git/git/</url>
      
        <content type="html"><![CDATA[<p>最早Git是在Linux上开发的，很长一段时间内，Git也只能在Linux和Unix系统上跑。不过，慢慢地有人把它移植到了Windows上。现在，Git可以在Linux、Unix、Mac和Windows这几大平台上正常运行了。</p><h2 id="Mac-install"><a href="#Mac-install" class="headerlink" title="Mac install"></a>Mac install</h2><pre class="language-bash" data-language="bash"><code class="language-bash">brew install git</code></pre><h2 id="Ubuntu-install"><a href="#Ubuntu-install" class="headerlink" title="Ubuntu install"></a>Ubuntu install</h2><pre class="language-bash" data-language="bash"><code class="language-bash">sudo apt install git</code></pre><h2 id="git-提交标准"><a href="#git-提交标准" class="headerlink" title="git 提交标准"></a>git 提交标准</h2><p><img src="/medias/loading.gif" data-original="http://file.longqiuhong.com/uploads/picgo/16315958305196.jpg" alt="git 提交标准"></p><h2 id="git-常用命令"><a href="#git-常用命令" class="headerlink" title="git 常用命令"></a>git 常用命令</h2><p><img src="/medias/loading.gif" data-original="https://file.longqiuhong.com/uploads/picgo/04453ab5aed44227919c808b392b48d6~tplv-k3u1fbpfcp-zoom-in-crop-mark:1304:0:0:0.awebp" alt="git 常用的命令"></p><p>查看代码版本：</p><pre class="language-bash" data-language="bash"><code class="language-bash">git describe --always --tags --abbrev=0 --match 'v*' --exclude '*/*' | tr -d "[\r\n]"</code></pre><p>删除tag</p><pre class="language-bash" data-language="bash"><code class="language-bash">git tag --delete  tag name  // v本地git push origin :refs/tags/v1.0.0  // 删除远程 v1.0.0</code></pre><p>修改tag 名称</p><pre class="language-bash" data-language="bash"><code class="language-bash">git tag new_tag old_taggit tag -d old_taggit push origin :refs/tags/old_taggit push --tags</code></pre><p>删除远程分支</p><pre class="language-bash" data-language="bash"><code class="language-bash">git push origin --delete &lt;branchName&gt;</code></pre><p>强制切换到 commit_id 这个记录</p><pre class="language-bash" data-language="bash"><code class="language-bash">git reset --hard commit_id</code></pre><p>强制提交到远程</p><pre class="language-bash" data-language="bash"><code class="language-bash">git push -f</code></pre><p>删除本地的远程不存在的分支</p><pre class="language-bash" data-language="bash"><code class="language-bash">git remote prune origin</code></pre><p><code>git commit</code>完之后，还没有执行<code>git push</code>，想修改/撤销这个<code>commit</code>，怎么办？<br>如果只是想修改注释，可以这样操作</p><pre class="language-bash" data-language="bash"><code class="language-bash">git commit --amend这个时候进入`vim`编辑，直接修改即可，修改完注释，退出`vim`编辑`:wq`保存已编辑的注释，重新`git push`即可</code></pre><p>要撤回commit</p><pre class="language-bash" data-language="bash"><code class="language-bash">git reset --soft HEAD^这样就能成功的撤回你刚刚的`commit`操作。HEAD^的意思是上一个版本，也可以写成HEAD~1如果你进行了2次commit，想都撤回，可以使用HEAD~2</code></pre><p><strong>注意，这个命令仅仅是撤回commit操作，写的代码仍然保留</strong></p><p>恢复本地未提交内容</p><pre class="language-bash" data-language="bash"><code class="language-bash">git checkout .</code></pre><pre class="language-bash" data-language="bash"><code class="language-bash">git reset HEAD  *# 回退到上一个版本git reset --hard HEAD^#回退到上上次版本git reset --hard HEAD^^git reset --hard HEAD^^^#回退到指定commitid的版本git reset --hard  commit_id</code></pre><p>恢复本地已提交内容</p><pre class="language-bash" data-language="bash"><code class="language-bash">git reset --hard</code></pre><p>恢复远程未提交内容</p><pre class="language-bash" data-language="bash"><code class="language-bash">git fetch origingit reset --hard origin/master</code></pre><h2 id="git-常见的错误处理"><a href="#git-常见的错误处理" class="headerlink" title="git 常见的错误处理"></a>git 常见的错误处理</h2><p>1、fatal: Not possible to fast-forward, aborting.</p><p>出现的原因：</p><p>两个分之同时改了同样的地方，造成冲突。一般的情况使用 merge  合并分支</p><pre class="language-bash" data-language="bash"><code class="language-bash">git checkout mastergit merge dev</code></pre><p>如果出现代码冲突的时候需要手动解决，但是有时候是无法合并成功的。</p><p>解决:</p><pre class="language-bash" data-language="bash"><code class="language-bash">git checkout mastergit rebase dev</code></pre><p>如果是一个分支，但是代码出现修改，也会出现改错误。</p><p>解决:</p><pre class="language-bash" data-language="bash"><code class="language-bash">git pull origin master --rebasegit pull origin development --rebasegit pull origin test --rebase</code></pre><p><strong>注意</strong> 修改冲突代码</p><p>在提交代码</p><pre class="language-bash" data-language="bash"><code class="language-bash">git push origin HEAD:develop     // develop 是分支</code></pre><p>在冲突解决完毕并且提交代码后，执行下面的命令：</p><pre class="language-bash" data-language="bash"><code class="language-bash">// 在终端也会有需要执行这个命令的提示git rebase --continue</code></pre>]]></content>
      
      
      <categories>
          
          <category> Tool </category>
          
      </categories>
      
      
        <tags>
            
            <tag> git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2023/01/28/traefik/hello-world/"/>
      <url>/2023/01/28/traefik/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><pre class="language-bash" data-language="bash"><code class="language-bash">$ hexo new "My New Post"</code></pre><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><pre class="language-bash" data-language="bash"><code class="language-bash">$ hexo server</code></pre><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><pre class="language-bash" data-language="bash"><code class="language-bash">$ hexo generate</code></pre><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><pre class="language-bash" data-language="bash"><code class="language-bash">$ hexo deploy</code></pre><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
      
      
      <categories>
          
          <category> hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> hexo </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
