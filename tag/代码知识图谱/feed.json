{
    "version": "https://jsonfeed.org/version/1",
    "title": "翼舞成梦",
    "subtitle": "翼舞成梦",
    "icon": "https://costalong.com/images/favicon.ico",
    "description": "翼舞成梦的个人博客，专注于后端开发、K8s 容器化技术、Linux 运维以及人工智能学习笔记分享。",
    "home_page_url": "https://costalong.com",
    "items": [
        {
            "id": "https://costalong.com/2026/06/29/317/",
            "url": "https://costalong.com/2026/06/29/317/",
            "title": "CodeGraph 进阶：装好之后，这 4 个用法才是真省事",
            "date_published": "2026-06-29T14:18:24.000Z",
            "content_html": "<h1 id=\"CodeGraph-进阶：装好之后，这-4-个用法才是真省事\"><a href=\"#CodeGraph-进阶：装好之后，这-4-个用法才是真省事\" class=\"headerlink\" title=\"CodeGraph 进阶：装好之后，这 4 个用法才是真省事\"></a>CodeGraph 进阶：装好之后，这 4 个用法才是真省事</h1><p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/317_707b9dc1ca6bf6854926a6134bfde6db.jpg\" alt=\"工程师在一张代码知识图谱地图上熟练地圈点查询\"></p>\n<p><strong>📚 系列导航</strong>：本文是《CodeGraph 上手到进阶》第 2/2 篇。上一篇：<a href=\"https://mp.weixin.qq.com/s/64vPfCpwUrTi8gWFZQrkjA\">CodeGraph 实战：给 AI agent 装上代码地图</a>。</p>\n<p>上一篇把 CodeGraph 装好、接上了 Claude Code，建完图它就自动生效了。但我观察下来，大多数人就停在这儿——以为”装好就完事了”，结果只用到了它最表层的能力。</p>\n<p>其实装好只是起点。CodeGraph 真正省事的地方，是几个得你主动用起来的进阶玩法。这篇挑 4 个我自己天天在用、官方也明确支持、上一篇没展开的：怎么把 <code>codegraph explore</code> 问到点子上、改代码前怎么用 <code>codegraph impact</code> 和 <code>codegraph affected</code> 看波及范围、怎么把它当库嵌进自己的工具、以及怎么在一个会话里查好几个仓库。讲讲怎么把它榨干。</p>\n<h2 id=\"把问题问对，explore-才给你想要的\"><a href=\"#把问题问对，explore-才给你想要的\" class=\"headerlink\" title=\"把问题问对，explore 才给你想要的\"></a>把问题问对，explore 才给你想要的</h2><p><code>codegraph_explore</code> 是它对外唯一的工具，但同样一个仓库，问得好不好，拿回来的东西差很远。官方在给 agent 的指令里其实写明了三类最适合它的问法。</p>\n<p>第一类是”X 是怎么工作的”。别问”看下这个文件”，直接问机制：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 问机制，它会把相关符号的源码 + 调用链一起给你\ncodegraph explore \"登录请求从路由到写库整条调用链是怎么走的\"</code></pre>\n\n<p>第二类是”X 怎么到达 Y”的流程追踪。这是它最能打的场景——因为它能跟住 grep 跟不动的动态分发（回调、接口到实现、框架路由）：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 追一条跨越动态分发的流程\ncodegraph explore \"一个 HTTP 请求是怎么最终触发 PaymentService 的\"</code></pre>\n\n<p>还有个细节很多人不知道：你在问题里<strong>直接点名一个文件或符号</strong>，它就当 Read 用，返回那个文件当前带行号的源码。所以”读文件”这件事也能走 explore，不用切回内置工具：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 点名文件 = 读它当前的源码（带行号，和 Read 一个形状）\ncodegraph explore \"src/auth/session.ts\"</code></pre>\n\n<p>反过来说，最浪费的问法是把它当搜索框用——“搜一下 login”这种，它能答，但你拿到的是一堆零散命中，跟 grep 没拉开差距。它的价值在”关系”而不是”匹配”：你问的是一条调用链、一个机制、一个符号的上下游，它才把预先建好的调用图用起来。</p>\n<p>举个我自己遇到的例子：一个 React 项目里某个状态莫名其妙被刷新，grep 组件名根本追不到，因为中间隔着一层 context 的回调和重渲染。我前前后后 grep + read 折腾了快 20 分钟没结果，换成 <code>codegraph explore \"这个状态是被谁触发更新的\"</code>，它把回调那一跳也接上了，一次就指到真正的源头。这种动态分发的调用链，是它和纯文本搜索拉开差距的地方。</p>\n<p>我的习惯是：凡是”这块代码怎么回事”的问题，先 explore 问一句，基本一次就够，省掉了 agent 一堆 grep+read 的探路。</p>\n<h2 id=\"改代码前，先看一眼会炸到谁\"><a href=\"#改代码前，先看一眼会炸到谁\" class=\"headerlink\" title=\"改代码前，先看一眼会炸到谁\"></a>改代码前，先看一眼会炸到谁</h2><p>这是我觉得日常收益最高的一个用法。动一个函数之前，先问清楚它的波及范围，比改完才发现牵一发动全身强太多。</p>\n<p>让 agent 改代码最怕的就是它”只看到眼前那个文件”——把一个被十几处调用的方法签名改了，自己以为改完了，结果一跑全是连锁报错。先用 impact 把影响半径摸清楚，agent 心里有数，要么一起改、要么提前提醒你这个动不得。我把”改公共方法前先 impact 一下”当成了固定动作。</p>\n<p><code>codegraph impact</code> 直接给你某个符号的影响半径，<code>--depth</code> 控制往外追几层：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 改 UserService 之前，看两层以内会牵动什么\ncodegraph impact UserService --depth 2\n\n# 也可以单独看谁调它、它调谁\ncodegraph callers handleRequest\ncodegraph callees processOrder</code></pre>\n\n<p>impact 给你的不是一个干巴巴的数字，而是顺着调用边一层层展开的受影响符号——谁直接调它、再往上谁调那些调用者。<code>--depth</code> 就是控制这个展开往外走几圈：改一个底层工具函数，往外两三层可能就牵动半个模块；改一个叶子方法，半径基本就它自己。看一眼这个范围，你心里对”这次改动是小手术还是大动干戈”就有数了。</p>\n<p>把这个能力接进测试流程，价值更大。<code>codegraph affected</code> 顺着 import 依赖往上追，找出某次改动会影响哪些测试文件——改完只跑这部分，不用全量。配进 git hook 或 CI 是这样：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">#!/usr/bin/env bash\n# pre-push / CI：只跑被这次改动波及的测试\nAFFECTED=$(git diff --name-only HEAD | codegraph affected --stdin --quiet)\nif [ -n \"$AFFECTED\" ]; then\n  npx vitest run $AFFECTED\nfi</code></pre>\n\n<p>我在一个全量测试要跑 4 分多钟的项目里接了这个，改一两个文件时只跑被波及的那几个测试，CI 时间直接掉到 30 秒，比原来省了快 5 倍，等待体验完全不一样。它还支持自定义测试文件匹配，比如只挑 e2e：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">codegraph affected src/auth.ts --filter \"e2e/*\"</code></pre>\n\n<p><code>impact</code> 和 <code>callers/callees</code> 都能带 <code>--json</code>，所以你也可以把它接到自己的脚本里做判断——比如”改动碰到了核心支付模块就强制要求人工 review”，拿 JSON 出来解析一下就能写。这一点在团队流程里比单纯人看更靠得住。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/317_5652e62dbd8f17a6dc3b98349fff755f.jpg\" alt=\"改一个符号前用 impact 看波及范围再用 affected 只跑相关测试\"></p>\n<h2 id=\"不只在-agent-里：把-CodeGraph-当库嵌进自己的工具\"><a href=\"#不只在-agent-里：把-CodeGraph-当库嵌进自己的工具\" class=\"headerlink\" title=\"不只在 agent 里：把 CodeGraph 当库嵌进自己的工具\"></a>不只在 agent 里：把 CodeGraph 当库嵌进自己的工具</h2><p>很多人没注意到，CodeGraph 的 npm 包直接导出了编程 API，<code>import</code> 和 <code>require</code> 都能拿到那个 <code>CodeGraph</code> 类。也就是说你能把它嵌进自己的脚本或者程序里（比如某个 Electron 应用的主进程），自己驱动这张图。</p>\n<p>什么时候会想这么干？最常见的是你想做一个 CodeGraph 之外的小工具——比如一个代码导航面板、一个”给我这个 bug 相关的所有代码”的内部命令、或者把它接到自己的 agent 工作流里当上下文来源。这些场景不走 MCP，直接调库更顺。其中 <code>buildContext</code> 我觉得最有意思：你丢一句自然语言任务进去，它帮你把相关的符号、代码、关系打包成一段现成的上下文（还能直接出 markdown），省得你自己拼。</p>\n<p>一个最小例子，建图 + 搜符号 + 拿调用者 + 给一段任务组上下文：</p>\n<pre class=\"language-typescript\" data-language=\"typescript\"><code class=\"language-typescript\">import CodeGraph from '@colbymchenry/codegraph';\n\nconst cg = await CodeGraph.init('/path/to/project');\nawait cg.indexAll({\n  onProgress: (p) =&gt; console.log(`${p.phase}: ${p.current}/${p.total}`),\n});\n\nconst results = cg.searchNodes('UserService');\nconst callers = cg.getCallers(results[0].node.id);\n\n// 给一段自然语言任务直接组装出相关代码上下文\nconst context = await cg.buildContext('fix login bug', {\n  maxNodes: 20, includeCode: true, format: 'markdown',\n});</code></pre>\n\n<p>想常驻盯着文件变化、用完收尾，也都有对应方法：</p>\n<pre class=\"language-typescript\" data-language=\"typescript\"><code class=\"language-typescript\">cg.watch();    // 自动同步：文件变了就增量更新\ncg.unwatch();  // 停止监听\ncg.close();    // 收尾</code></pre>\n\n<p>包里还顺带导出了一些更底层的积木，像 <code>DatabaseConnection</code>、<code>QueryBuilder</code> 这些，给那种想自己直接驱动图、不满足于高层方法的场景用；TypeScript 类型也一起带着，接起来不用自己补声明。</p>\n<p>有一点要注意：这条路跑在<strong>你自己的运行时</strong>上，所以需要 Node 22.5+（它用了内置的 <code>node:sqlite</code>）。CLI 和 MCP 那套不受影响，它们跑在自带的 runtime 上，没这个版本要求。</p>\n<h2 id=\"一个会话，查好几个仓库\"><a href=\"#一个会话，查好几个仓库\" class=\"headerlink\" title=\"一个会话，查好几个仓库\"></a>一个会话，查好几个仓库</h2><p>最后一个是 monorepo 和多仓库党会喜欢的。CodeGraph 的索引是按项目走的，每个项目自己 <code>codegraph init</code> 一次，建出各自的 <code>.codegraph/</code>。</p>\n<p>monorepo 最烦的就是改 A 服务时想顺手看一眼 B 服务怎么调它，传统做法要么开两个窗口来回跳，要么 agent 把整个巨型仓库全量扫一遍——又慢又把无关代码塞进上下文。CodeGraph 的处理方式很对路：各服务各建各的图，互不干扰。</p>\n<p>关键在于：查询时只要传一个 <code>projectPath</code>，就能在<strong>同一个 agent 会话里</strong>命中任意一个建过索引的项目——主服务、另一个子服务、甚至旁边那个仓库，来回切都行。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 在某个项目目录里建好它自己的图\ncd services/payment &amp;&amp; codegraph init\n\n# 各服务各自一份索引，互不干扰\ncd ../user &amp;&amp; codegraph init</code></pre>\n\n<p>碰到一个还没建索引的路径，它不会报错，而是回一句”这块你用内置工具吧”，干净地把决定权留给你——要不要为某个目录建图，始终是你说了算。对那种”只想给核心几个服务建图、不想全量扫”的大单体仓库，这个设计很省心。</p>\n<p>我自己在一个前后端放一起的仓库里就是这么用的：后端服务建了图，前端那一大坨还在重构的代码暂时没建。问后端的调用链它走图、又快又准；偶尔碰前端，它自动退回 grep/read，不会因为”没索引”就卡住或者报错。这种”建到哪算到哪、没建也不添乱”的渐进式策略，比那种”要么全量索引要么没法用”的工具舒服多了——你可以从最该被理清的那块代码开始，慢慢铺。</p>\n<p>装好只是把地图铺上，真正的省事是从你开始主动查这张图开始的。先挑你最熟的那个项目，下次改代码前先问它一句：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">codegraph impact &lt;你要改的那个函数名&gt;</code></pre>\n\n<p><strong>这几个用法值得收藏</strong>——下次接新项目或者动老代码时，照着试一遍，体感会很不一样。</p>\n",
            "tags": [
                "Coding-Agent",
                "Claude Code",
                "CodeGraph",
                "MCP",
                "AI编程",
                "代码知识图谱",
                "进阶用法"
            ]
        },
        {
            "id": "https://costalong.com/2026/06/28/315/",
            "url": "https://costalong.com/2026/06/28/315/",
            "title": "CodeGraph 实战：给 AI agent 装上代码地图，tool calls 砍掉一半",
            "date_published": "2026-06-28T10:34:15.000Z",
            "content_html": "<h1 id=\"CodeGraph-实战：给-AI-agent-装上代码地图，tool-calls-砍掉一半\"><a href=\"#CodeGraph-实战：给-AI-agent-装上代码地图，tool-calls-砍掉一半\" class=\"headerlink\" title=\"CodeGraph 实战：给 AI agent 装上代码地图，tool calls 砍掉一半\"></a>CodeGraph 实战：给 AI agent 装上代码地图，tool calls 砍掉一半</h1><p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/315_0416b47504384d8f84538c243cd4a844.jpg\" alt=\"AI agent 面对庞大代码库，旁边一张发光的知识图谱地图\"></p>\n<p>让 Claude Code 改一个稍微大点的项目，你大概都见过这个画面：它先 <code>ls</code> 一下、再 <code>grep</code> 几个关键词、然后挨个 <code>read_file</code>，把调用链在脑子里拼出来——真正动手改之前，已经烧掉十几个 tool call。</p>\n<p>代码它早晚能找到，问题是这趟”探路”既慢又费 token。我自己在一个几千文件的仓库里盯着 agent 干活，经常一半时间它都在翻文件而不是写代码。</p>\n<p><a href=\"https://github.com/colbymchenry/codegraph\">CodeGraph</a> 想干掉的就是这段浪费：把任意一个文件夹预先解析成一张代码知识图谱，通过 MCP 喂给 agent。下面这篇我按它的<strong>官方最新版本</strong>走一遍——提醒一句，网上不少教程里的 <code>codegraph init -i</code> 之类命令已经过时了，以官方资料为准。</p>\n<h2 id=\"它解决的是-agent-“找代码”-的浪费\"><a href=\"#它解决的是-agent-“找代码”-的浪费\" class=\"headerlink\" title=\"它解决的是 agent “找代码” 的浪费\"></a>它解决的是 agent “找代码” 的浪费</h2><p>先把问题说清楚。没有索引的时候，agent 理解一段陌生代码，靠的是内置工具一遍遍地搜：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># agent 回答\"请求是怎么到数据库的\"——典型的探路过程\ngrep -r \"Database\" src/\nls src/services/\n# 然后挨个 read 命中的文件，自己在上下文里重建调用链……</code></pre>\n\n<p>CodeGraph 的思路是把这件事<strong>提前做掉</strong>：它一次性扫完整个仓库，记下每个符号（函数、类、方法）、每条调用边、每个依赖关系，存成一张图。agent 要查的时候问一句，直接拿回相关源码、调用路径、还有改动的”波及范围”（blast radius）——不用再一个文件一个文件地翻。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/315_44a420f5343e8dc3504123ae6a507df5.jpg\" alt=\"无CodeGraph的探路循环对比有CodeGraph的一次查询\"></p>\n<p>官方在 7 个真实开源项目上做了对比（Claude Code headless，每组取 4 次中位数），结论挺直接：<strong>tool calls 中位数少 58%、回答快 22%、文件读取从一堆降到接近 0</strong>。</p>\n<table>\n<thead>\n<tr>\n<th>项目</th>\n<th>语言 · 规模</th>\n<th>tool calls</th>\n<th>文件读取</th>\n<th>token</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>VS Code</td>\n<td>TS · ~1万文件</td>\n<td>少 81%</td>\n<td>0 vs 9</td>\n<td>少 64%</td>\n</tr>\n<tr>\n<td>Django</td>\n<td>Python · ~3千</td>\n<td>少 77%</td>\n<td>0 vs 9</td>\n<td>少 60%</td>\n</tr>\n<tr>\n<td>Tokio</td>\n<td>Rust · ~790</td>\n<td>少 57%</td>\n<td>0 vs 8</td>\n<td>少 38%</td>\n</tr>\n<tr>\n<td>Alamofire</td>\n<td>Swift · ~110</td>\n<td>少 58%</td>\n<td>0 vs 9</td>\n<td>少 64%</td>\n</tr>\n</tbody></table>\n<p>官方自己也很诚实地标了一句：<strong>省 token、省钱是”规模相关”的</strong>——小仓库上每次查询省的那点钱又小又飘，得是大到 Google/Microsoft 那种单体仓库、再乘上整个团队每天的用量，才会攒成账单上的一笔。所以小项目用它，图的是<strong>快和准</strong>，不是省钱。这点比那些张口就是”省一半成本”的宣传实在得多。</p>\n<p>你可能会担心一个问题：图是预建的，那我刚改完文件、图还没更新的那一小会儿，agent 会不会拿到旧答案？CodeGraph 在这里做得挺细——改动落盘后监听器很快触发（默认防抖 2 秒），而在这段空窗里，如果 agent 查到一个还没同步的文件，返回结果会顶一个 <code>⚠️</code> 提示，明确告诉它”这个文件刚改过，直接 Read 一下”。实测里 Claude Code 真的会照做，先去读实时内容再回答。不会出现那种”索引过期了还一本正经给你错答案”的情况。</p>\n<p>还有个容易被忽略的场景：你在终端 <code>git pull</code> 了一把、或者用别的编辑器改了几个文件，这期间 MCP server 可能根本没在跑。CodeGraph 在 server 每次重连时会先做一轮 <code>(大小, mtime)</code> + 内容哈希的核对，把这些”背着它发生”的改动在回答第一个查询前补进来。所以不管改动是谁干的、什么时候干的，下次一问都是新的。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/315_92d24588aca375e7a3ba9d36fda390c1.png\" alt=\"CodeGraph 官方仓库的 benchmark 对比表\"></p>\n<h2 id=\"三步把-CodeGraph-接上-Claude-Code-Cursor\"><a href=\"#三步把-CodeGraph-接上-Claude-Code-Cursor\" class=\"headerlink\" title=\"三步把 CodeGraph 接上 Claude Code / Cursor\"></a>三步把 CodeGraph 接上 Claude Code / Cursor</h2><p>接入分得很清楚：<strong>装 CLI → 连 agent → 给项目建索引</strong>，三件事，别混在一起。</p>\n<p>第一步，装 CLI。它自带运行时，本机没有 Node 也能跑：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># macOS / Linux —— 一行装好，自动挑对应你系统的构建\ncurl -fsSL https://raw.githubusercontent.com/colbymchenry/codegraph/main/install.sh | sh\n\n# 已经有 Node？用 npm 也行（任意版本都可以）\nnpm i -g @colbymchenry/codegraph</code></pre>\n\n<p>装完<strong>记得新开一个终端</strong>——安装脚本把 <code>codegraph</code> 放进了 PATH，但不会改当前这个 shell，不换终端下一步会找不到命令。</p>\n<p>第二步，把 CodeGraph 连到你用的 agent 上。这一步才是”接线”，只装 CLI 是不够的：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">codegraph install</code></pre>\n\n<p>它会自动探测你机器上装了哪些 agent——Claude Code、Cursor、Codex CLI、opencode、Gemini CLI、Antigravity、Kiro、Hermes Agent 都支持——然后把 MCP server 配置写进各自的配置文件。注意这步<strong>只接线、不建索引</strong>，全局跑一次就覆盖所有项目。</p>\n<p>第三步，进到具体项目里建图。这一步才真正扫你的代码：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">cd your-project\ncodegraph init</code></pre>\n\n<p><code>codegraph init</code> 一条命令搞定两件事：建出本地 <code>.codegraph/</code> 目录，并把整张图建好。建完之后<strong>自动同步默认就开着</strong>——文件改了它用系统原生事件（FSEvents / inotify）盯着，防抖一下增量更新，索引永远是新的，没有什么 <code>sync</code> 要你手动跑。把 agent 重启一下让 MCP 加载，就能用了。</p>\n<p>如果是配进 CI 或者脚本里，<code>install</code> 支持完全非交互的写法，指定目标和范围就行：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 自动探测 agent、装到全局，全程不问\ncodegraph install --yes\n\n# 只接指定的几个，且只对当前项目生效\ncodegraph install --target=cursor,claude --location=local</code></pre>\n\n<p>想升级直接 <code>codegraph upgrade</code>，它会自己认出你当初是怎么装的（bundle / npm / npx）就地更新；哪天不想用了，<code>codegraph uninstall</code> 会把它写进各个 agent 的那些配置干净地撤掉，项目里的 <code>.codegraph/</code> 索引留着不动。想连某个项目的索引一起清掉，进那个项目跑 <code>codegraph uninit</code> 就行——两件事分开，删 agent 配置和删项目图谱互不牵连。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/315_efb010273db477147182afd9e51e9821.jpg\" alt=\"三步接入流程：装CLI、连agent、建索引\"></p>\n<h2 id=\"一次查询，拿到答案而不是一堆文件\"><a href=\"#一次查询，拿到答案而不是一堆文件\" class=\"headerlink\" title=\"一次查询，拿到答案而不是一堆文件\"></a>一次查询，拿到答案而不是一堆文件</h2><p>接好之后，agent 在回答结构性问题时就会自动走 CodeGraph，不用你操心。它对外其实<strong>只暴露一个 MCP 工具</strong>——<code>codegraph_explore</code>。</p>\n<p>这是我觉得设计上很克制的一点。官方实测发现，给 agent 一堆细分工具（查调用者、查被调用、查依赖……）反而容易选错，不如一个够强的工具好用。所以 <code>explore</code> 一次调用就把相关符号的源码（按文件分组）、它们之间的调用路径（包括 grep 跟不动的动态分发）、加上波及范围一起返回。</p>\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/315_29b0c9bdc07691a47b095d84bdc47d02.jpg\" alt=\"codegraph_explore 一次调用返回的三样东西\"></p>\n<p>你也可以直接在命令行里手动试，输出和 agent 拿到的一模一样：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 一句话问清楚某个流程\ncodegraph explore \"请求是怎么经过中间件链的\"\n\n# 查一个符号会牵动谁——改之前先看波及范围\ncodegraph impact UserService\n\n# 全文搜符号，FTS5 驱动，瞬出\ncodegraph query \"login\" --kind function</code></pre>\n\n<p>单体仓库或者多仓库场景也照顾到了：只要某个目录建过 <code>.codegraph/</code> 索引，查询时传一个 <code>projectPath</code> 就能命中它——一个会话里既查主服务、又查另一个子服务都行。碰到没建过索引的路径，它不会报错，而是回一句”这块用你的内置工具吧”，建不建索引始终是你自己决定。</p>\n<p>如果你想手动接 Claude Code，配置也就这么一段——往 <code>~/.claude.json</code> 里加：</p>\n<pre class=\"language-json\" data-language=\"json\"><code class=\"language-json\">{\n  \"mcpServers\": {\n    \"codegraph\": {\n      \"type\": \"stdio\",\n      \"command\": \"codegraph\",\n      \"args\": [\"serve\", \"--mcp\"]\n    }\n  }\n}</code></pre>\n\n<p>“只有一个工具”其实是默认姿态，不是全部。CodeGraph 另外还藏了 7 个更细的工具——<code>codegraph_node</code>、<code>codegraph_search</code>、<code>codegraph_callers</code>、<code>codegraph_callees</code>、<code>codegraph_impact</code>、<code>codegraph_files</code>、<code>codegraph_status</code>。它们默认不挂到 MCP 上，因为这些信息 <code>explore</code> 的返回里基本都带了。真要单独打开，用一个环境变量点名即可：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 按需把额外的工具挂回 MCP surface\nexport CODEGRAPH_MCP_TOOLS=explore,node,search,callers</code></pre>\n\n<p>手动接的话，顺手在 <code>~/.claude/settings.json</code> 里给它的工具开个 auto-allow，省得每次调用都弹确认（用 <code>codegraph install</code> 接 Claude Code 时这步会自动做掉）：</p>\n<pre class=\"language-json\" data-language=\"json\"><code class=\"language-json\">{\n  \"permissions\": {\n    \"allow\": [\"mcp__codegraph__*\"]\n  }\n}</code></pre>\n\n<p>还有个我觉得很实用的命令是 <code>codegraph affected</code>：顺着 import 依赖往上追，找出某次改动会影响哪些测试文件。配进 CI 或者 git hook，改完只跑受影响的那部分测试，不用全量：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 从 git diff 喂进去，只跑被波及的测试\ngit diff --name-only HEAD | codegraph affected --stdin --quiet</code></pre>\n\n<p>真正让我觉得”这东西比 grep 强一个量级”的，是它能跨语言边界把调用链接上。纯靠 tree-sitter 静态解析，到语言边界就断了——Swift 调一个被自动桥接的 ObjC 方法、JS 通过 React Native 桥调原生模块、Expo Modules 的那套 DSL，grep 全都跟不动。CodeGraph 把这些 hop 显式补进图里，<code>explore</code> 出来的调用路径和波及范围能直接穿过去。它还认得 17 种 Web 框架的路由写法：Django 的 <code>path()</code>、FastAPI 的 <code>@app.get()</code>、Express 的 <code>router.post()</code>……查一个 controller 的调用者，会顺带把绑它的 URL 模式一起带出来。这种”动态分发 + 路由绑定”的关系，正是人肉 grep 最容易漏的部分。</p>\n<h2 id=\"原理：tree-sitter-解析-SQLite-图-自动同步\"><a href=\"#原理：tree-sitter-解析-SQLite-图-自动同步\" class=\"headerlink\" title=\"原理：tree-sitter 解析 + SQLite 图 + 自动同步\"></a>原理：tree-sitter 解析 + SQLite 图 + 自动同步</h2><p>底层其实不神秘，四步：tree-sitter 按语法解析出 AST，抽取节点（函数/类/方法）和边（调用/导入/继承）；全部塞进本地一个 SQLite 库（<code>.codegraph/codegraph.db</code>，带 FTS5 全文搜索）；再做一轮引用解析，把调用对上定义、导入对上源文件；最后文件监听做增量同步。</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 想确认索引状态，随时看\ncodegraph status\n\n# 关掉匿名遥测（它默认会收集用了哪些命令/语言，但绝不碰你的代码和路径）\ncodegraph telemetry off</code></pre>\n\n<p>它几乎<strong>零配置</strong>。20 多种语言（TS/JS/Python/Go/Rust/Java/C#/Swift/Kotlin… 一直到 Vue/Svelte/Astro）靠扩展名自动识别，不用你一个个配。<code>node_modules</code>、<code>dist</code>、<code>.venv</code> 这些目录、还有 <code>.gitignore</code> 里的东西、大于 1MB 的文件，默认全部跳过——图里只有你自己的代码，没有第三方噪音。</p>\n<p>真要调，也就一个可选的 <code>codegraph.json</code>，比如把某个提交进仓库的 vendored 主题排除掉，或者给非标准扩展名指个语言：</p>\n<pre class=\"language-json\" data-language=\"json\"><code class=\"language-json\">{\n  \"exclude\": [\"static/\", \"**/vendor/**\"],\n  \"extensions\": { \".tpl\": \"php\" }\n}</code></pre>\n\n<p><img src=\"/medias/loading.gif\" data-original=\"/images/posts/315_9cb4e670d98652c5963c6e4524a81311.jpg\" alt=\"tree-sitter 解析到 SQLite 图再到 MCP 查询的数据流\"></p>\n<p>如果你想把它嵌进自己的程序里（比如某个 Electron 应用的主进程），npm 包还直接导出了 <code>CodeGraph</code> 类，<code>import</code> 和 <code>require</code> 都能拿到，自己 <code>init</code>、<code>searchNodes</code>、<code>buildContext</code> 一条龙；只是这条路跑在你自己的运行时上，需要 Node 22.5+（用的是内置的 <code>node:sqlite</code>）。CLI 和 MCP 那套不受影响，它们跑在自带的 runtime 上。</p>\n<p>还有一点对我这种谨慎的人很重要：<strong>全程本地</strong>。源码和那个 SQLite 库都不离开你的机器，没有 API key、不连外部服务。给公司私有仓库建图，心里踏实。</p>\n<p>完整命令和配置可以翻官方文档站，更新很勤，写得也清楚。</p>\n<h2 id=\"用之前，有几个点值得注意\"><a href=\"#用之前，有几个点值得注意\" class=\"headerlink\" title=\"用之前，有几个点值得注意\"></a>用之前，有几个点值得注意</h2><p>最容易踩的一个：CodeGraph 只在 agent <strong>直接查询</strong>它的时候才省事。如果你的 agent 习惯把”先去仓库探一探”这件事外包给一个专门读文件的子 agent（比如 Explore 类 sub-agent），那子 agent 照样一个个 read 文件，CodeGraph 反而成了白挂的一层。官方为此特地在给 agent 的指令里写明”自己直接答，别把探索甩给读文件的子 agent”——用 <code>codegraph install</code> 接 Claude Code 时这段会自动写进去，但你要是手动接、或者套了自定义工作流，记得让主 agent 自己查。</p>\n<p>第二个点跟自动同步有关。前面说”索引永不过期”有个前提：那个文件监听的守护进程得能跑起来。要是你在沙箱环境里、或者显式设了 <code>CODEGRAPH_NO_DAEMON=1</code> 把它关了，watcher 就不工作了，这时候改完代码得自己手动同步一下：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 守护进程没跑时（沙箱 / CI / 受限容器），手动增量同步\ncodegraph sync</code></pre>\n\n<p>本地正常用基本碰不到，但在 CI 或者受限容器里跑要心里有数。</p>\n<p>最后一点是心态问题：这工具还很年轻、迭代很快（写这篇时最新版才发布没多久），命令和默认行为往后都可能再变。要是你想要个稳定的版本，别一直跟最新，锁一个就好：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\"># 看看有没有新版\ncodegraph upgrade --check\n\n# 锁定到指定版本，不自动跟最新\ncodegraph upgrade 1.1.2</code></pre>\n\n<p>如果你天天用 Claude Code 或者 Cursor 啃大项目，CodeGraph 这种”先建图、再让 agent 查图”的路子，比起一遍遍 grep，体感差距是实打实的。</p>\n<p>下一步很简单，挑一个你最熟的项目，跑这一条命令开始：</p>\n<pre class=\"language-bash\" data-language=\"bash\"><code class=\"language-bash\">codegraph init</code></pre>\n\n<p><strong>觉得有用就点个收藏吧</strong>——回头接自己项目的时候，照着这三步走一遍就行，省得再翻。</p>\n",
            "tags": [
                "Coding-Agent",
                "Claude Code",
                "实战教程",
                "CodeGraph",
                "MCP",
                "AI编程",
                "代码知识图谱"
            ]
        }
    ]
}